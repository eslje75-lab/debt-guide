# Codex 교차감사 인수인계 — 2026-08-17

이 문서는 Claude Code를 포함한 다음 작업자가 현재 로컬 수정의 의도와 출시 제한을 잃지 않게 하는
인수인계 문서다. `CLAUDE.md`의 2026-08-16 우선 규칙, `PROGRESS.md` 최상단,
`LAUNCH-CHECKLIST.md`, `LEGAL-REVIEW-BRIEF.md`와 함께 읽는다.

## 1. 현재 상태

- 작업 경로: `C:\Users\pc\Desktop\클로드코드\법률웹제작\챔로드`
- 운영 사이트와 Worker에는 이번 수정이 반영되지 않았다.
- 이번 작업에서는 **커밋, push, 원격 D1 마이그레이션, Worker/Pages 배포를 하지 않았다.**
- 로컬 작업트리는 원래부터 변경사항이 많았고, Codex가 기존 변경을 초기화하거나 덮어쓰지 않았다.
- 결제 잠금은 `js/main.js`와 `api/src/index.js` 모두 `PAYMENTS_ENABLED = false`로 유지해야 한다.
- 원격 GitHub 저장소 `eslje75-lab/debt-guide`는 확인 당시 **공개 저장소**였다. 과거 커밋에 유료
  본문이 포함되어 있으므로 `dist/`만 배포해도 GitHub 원본·히스토리에서의 열람 문제는 사라지지 않는다.

## 2. 이번 교차감사의 핵심 변경

### 법률 안내를 판정이 아닌 확인 도구로 축소

- 개인회생/파산의 승자, 적합도 백분율, 성공 가능성처럼 읽히는 점수와 그에 따른 유료 CTA를 제거했다.
- 실제 생활비 전액을 자동 공제하지 않고 2026년 기준중위소득 60%를 기계적 참고값으로만 사용한다.
  초과 생계비와 부양가족은 증빙 및 법원의 사건별 판단 대상이라고 표시한다.
- 명목 변제총액만으로 청산가치 보장원칙을 `통과` 처리하지 않는다. 인가 시점 현재가치와 법원 양식을
  확인하도록 `확인 필요` 결과만 낸다.
- 관할, 면책 후 5년, 변제기간, 최근 대출, 납입 중단, 보정기한, 변제계획 변경, 미납 폐지,
  특별면책 요건·효과 문구를 현행 법령과 대법원 판례에 맞춰 보수적으로 교정했다.
- `diagnosis.html`에서 계산에도 쓰지 않던 필수 월 생활비 입력과 중복 저장을 제거했다.

### 사용자 데이터 보호와 데이터 손실 방지

- 비로그인 자료는 현재 탭의 `sessionStorage`에만 두고, 로그인 자료는 이메일 UTF-8 바이트의 16진수로
  만든 충돌 없는 계정별 `localStorage` namespace에 둔다. 로그인 시 익명 자료가 본인 것인지 묻고,
  명시적으로 가져오기를 선택한 경우에만 계정으로 옮긴다.
- 서버 동기화 큐는 요청 성공 전에 비우지 않는다. 재시도 journal 저장·삭제도 성공 여부를 검증하고,
  실패하면 로컬 값을 원복하거나 같은 변경을 남겨 다음 기회에 다시 보낸다.
- 여러 키 저장은 `Storage.saveBatch()`로 묶고 하나라도 실패하면 로컬 값을 원복하며 서버 큐에도 넣지 않는다.
- 로그인 시 서버/게스트 자료를 무조건 덮어쓰지 않는다. 양쪽에 자료가 있으면 이용자가 어느 쪽을
  쓸지 명시적으로 선택하며, 게스트 자료는 계정 cache와 서버 반영이 모두 성공한 뒤에만 지운다.
- 전체 초기화·회원탈퇴·로그아웃은 실제 삭제 성공을 확인하기 전에 성공 메시지를 표시하지 않는다.
- 세션 토큰을 브라우저에 저장하지 못하면 로그인 성공으로 처리하지 않고 새 서버 세션을 폐기한다.
- 세션 만료·직접 계정 전환은 이전 계정 cache와 영속 동기화 큐를 삭제하지 않는다. 로그아웃은 진행 중
  요청 뒤에 생긴 변경까지 `drain()`으로 모두 보낸 뒤 현재 계정과 현재 탭 자료만 지운다.
- 비동기 동기화 작업은 시작 시 계정과 scope 세대를 고정한다. 응답 전에 계정이 바뀌면 옛 성공·실패
  결과가 새 계정 queue나 cache를 건드리지 못한다.
- 구버전 계정 namespace는 정확한 이메일 소유표식이 있을 때만 자동 이전한다. 소유 불명 `cdg_<key>`와
  충돌 가능 namespace는 자동 귀속·삭제하지 않고 격리하거나 복구 배너에서 이용자가 직접 선택한다.
- 주민등록번호 마스킹은 하이픈뿐 아니라 공백·점·줄바꿈 형식도 그럴듯한 생년월일·뒷자리 구분자로 막는다.
  내·외국인 검증식과 OCR 오기까지 보수적으로 다루고, 서버는 JSON 직렬화 전에 문자열 leaf를 먼저
  마스킹한다. 평범한 금액 목록은 훼손하지 않도록 화면·서버·동기화 API에 회귀검사를 둔다.
- BFCache로 이전 계정 화면이 복원되면 계정뿐 아니라 scope 세대 차이를 감지해 민감 화면을 즉시
  비우고 첫 화면으로 보낸다.
- 마이페이지의 전체 초기화는 현재 계정만이 아니라 이 브라우저에 남은 모든 계정·게스트·재시도
  namespace를 지우며, 일부라도 남으면 전체 삭제 성공으로 표시하지 않는다.
- 서버 동기화는 허용된 키·타입·UTF-8 크기만 받고, 깊은 민감필드와 prototype 오염 키를 제거한다.

### 유료 콘텐츠·API 보안

- 유료 본문은 정적 HTML이 아니라 Worker 모듈로 옮겼다. 무료 첫 단계만 공개 산출물에 남는다.
- 명시적 버튼 클릭 뒤에만 `POST /api/content/open`과
  `{type, consent:true, consentVersion:'content-open-v1'}`를 보낸다. 서버가 제공개시 기록 저장에 성공한
  뒤에만 본문을 반환한다. 페이지 진입이나 gate 이벤트에서 자동으로 열지 않는다.
- 옛 `GET /api/content/steps`는 본문 없이 409를 반환한다. 옛 `/api/content/access`는 기록을 만들지 않는
  조회 호환 경로뿐이다.
- 인증·OTP·재설정 토큰 입력 제한, 시도 횟수·메일 발송 제한, 재설정 토큰 원자적 claim, 단일 세션,
  API `no-store`와 기본 보안 헤더를 보강했다.
- 공개 배포는 `node tools/build-public.js`의 허용목록으로 만든 `dist/`만 사용한다. API·도구·운영 문서가
  포함되면 빌드가 실패한다.
- `.github/workflows/deploy-pages.yml`을 추가했지만, GitHub Pages 설정을 **GitHub Actions** 소스로 바꾸기
  전에는 기존 branch 배포를 대체하지 않는다. `_headers`는 Cloudflare Pages에서는 적용되지만 GitHub
  Pages에서는 같은 방식으로 적용되지 않는다.

### 결제·이용권 원장

- 주문 완료의 중복·병렬 요청이 이용권을 두 번 부여하지 않게 주문 단위 원자적 claim과 grant 원장을
  도입하는 보완 작업이 포함되어 있다.
- 같은 상품 재구매의 기간·AI 회수를 주문별 grant로 추적하고, 한 주문을 환불할 때 그 주문이 실제로
  남긴 이용기간·미사용 회수만 회수하는 방향이다.
- 관련 스키마는 `api/schema.sql`과
  `api/migrations/2026-08-16-order-entitlement-ledger.sql`을 함께 본다. **원격 적용 전 반드시 D1을 백업하고,
  SQL과 코드·테스트의 최종 diff를 사람이 확인한다.**
- 신규 분리동의 가입은 `users.consent_form_version='signup-consent-v1'`로 남긴다. 기존 가입은
  `signup-consent-legacy`로 구분하며, Worker 배포 전에
  `api/migrations/2026-08-16-signup-consent-form-version.sql`을 적용해야 한다.

## 3. 되돌리면 안 되는 불변조건

1. 결과 화면에 절차 추천·승자·적합도 %·면책 예상액을 되살리지 않는다.
2. 실제 지출 생활비를 자동 전액 인정하지 않는다. 단순 가구원 수를 법원 인정 부양가족 수로 단정하지 않는다.
3. 명목 월변제액×횟수만으로 청산가치 `통과`를 표시하지 않는다.
4. 유료 본문을 HTML, 공개 JS, 공개 저장소의 새 커밋에 다시 넣지 않는다.
5. 콘텐츠를 자동 fetch해서 제공개시 기록을 만들지 않는다.
6. 동기화 큐를 서버 성공 전에 지우거나, 저장·삭제 실패를 성공으로 표시하지 않는다.
7. 계정이 바뀌어도 같은 localStorage 키를 공유하는 구조로 되돌리지 않는다.
8. 약관·개인정보처리방침의 시행일과 동의 버전은 법률 검토·사전 공지 없이 즉시 바꾸지 않는다.
9. 판매 잠금은 아래 출시 차단사항을 모두 닫기 전 해제하지 않는다.

## 4. 판매 개시 전 남은 차단사항

### 사람의 판단이 필요한 항목

- 국내 변호사에게 `LEGAL-REVIEW-BRIEF.md`를 전달해 유상 AI 질문형 서류점검, 개인별 요건 표시,
  보정 대응 콘텐츠가 변호사법·법무사법상 허용되는 운영구조인지 서면 검토받는다.
- 단계별 콘텐츠와 AI 회차가 가분적인지, 제공개시 단위를 주문·단계·AI 1회 중 무엇으로 볼지,
  부분환불 산식과 사전고지를 확정한다. 현재 약관의 “이용하지 않은 부분 환불” 약속과 패키지 단위
  `content_access`만으로는 구현·입증 단위가 완전히 맞지 않는다.
- 가입의 만 14세 확인·약관·개인정보 수집이용 동의는 화면/API에서 분리했지만 그 문구와 증빙 수준,
  Anthropic ZDR 적용 여부·실제 보유기간, Cloudflare·Resend 등 국외이전 고지를 변호사 또는 개인정보 전문가에게 확인한다.
- 화면과 방침은 현재 “챔로드 DB에는 AI 원문을 저장하지 않지만 ZDR이 없으면 Anthropic에 원칙적으로
  최대 30일 보관될 수 있음”으로 일치시켰다. 실제 계정의 ZDR/DPA가 확인되기 전에는 즉시 폐기라고 되돌리지 않는다.
- 사업자등록, 사업용 전화번호, 통신판매업 신고 판단, 사업자 표시, 포트원 라이브 계약을 완료한다.

### 배포 구조

권장안은 **소스 저장소를 비공개로 전환하고 Cloudflare Pages가 그 비공개 저장소를 빌드**하게 하는 것이다.
빌드 명령은 `node tools/build-public.js`, 출력 디렉터리는 `dist`로 고정한다. 이 방식은 `_headers`도
적용할 수 있고 Worker와 운영 위치를 한 사업자 쪽에 모으기 쉽다.

대안은 비공개 소스 저장소와 `dist/`만 담는 공개 배포 저장소를 분리하는 것이다. 현재 공개 저장소를
그대로 둔 채 새 커밋에서 파일만 지우는 것으로는 과거 히스토리 노출을 해결할 수 없다. 이미 공개된
유료 원문은 유출 가능성이 있다고 보고, 필요하면 내용을 개정한 뒤 비공개 원본에서만 운영한다.

현재 GitHub Pages를 계속 쓸 경우 Repository Settings → Pages → Source를 **GitHub Actions**로 바꾸고
`.github/workflows/deploy-pages.yml`이 올린 `dist` artifact만 게시되는지 확인한다. 다만 공개 Git 원본 문제와
GitHub Pages에서 `_headers`가 적용되지 않는 제한은 별개로 남는다.

## 5. 검증 명령

루트에서 아래를 모두 실행한다.

```powershell
node tools/check-project.js
node tools/test-storage-regressions.js
node tools/test-legal-regressions.js
node --test api/tests/security-regression.test.mjs api/tests/paid-content-static.test.mjs
node tools/build-public.js
git diff --check
```

그 다음 `node tools/serve.js --public`로 `dist/`를 열고 최소한 다음을 직접 확인한다.

- 비로그인 진단 → 결과: 두 절차를 중립적으로 표시하고 절차 승자·백분율이 없어야 한다.
- 진단 결과: 입력한 실제 생활비가 아니라 2026년 기준중위소득 60% 참고값을 사용해야 한다.
- 계정 A 로그아웃 → 비로그인/계정 B: A의 결과·체크리스트·AI 이용기록이 보이지 않아야 한다.
- 오프라인 저장/동기화 후 재접속: 실패한 변경이 사라지거나 서버의 옛 값으로 조용히 덮이지 않아야 한다.
- 전체 초기화·탈퇴: 서버 실패 시 성공 문구가 나오지 않고, 재로그인해도 삭제 데이터가 되살아나지 않아야 한다.
- 회생·파산·유지관리·보정 페이지: 첫 단계만 무료이고, 버튼을 누르기 전 유료 본문 요청이 없어야 한다.
- 유료 계정으로 명시적 열기: 제공개시 안내 → 기록 저장 → 본문 순서이며 새로고침 후 중복 소비가 없어야 한다.
- 동일 결제 완료 요청 병렬 2회: 주문 1건의 이용권만 생겨야 한다.
- 같은 상품 2회 구매 후 한 주문 환불: 다른 주문의 기간·회수는 남고 환불 주문 몫만 회수되어야 한다.
- `dist/api`, `dist/tools`, 운영 `.md`가 없고 배포 URL의 `/api/src/**`가 404여야 한다.

## 6. 실제 배포 순서

1. 위 법률·환불 정책과 비공개 소스/호스팅 구조를 먼저 확정한다.
2. 판매 잠금을 유지하고 운영 D1 백업과 복구 리허설을 한다.
3. 전체 테스트를 통과한 한 커밋으로 프런트와 Worker를 묶고, push 후 Quality Gate 성공을 확인한다.
4. Actions에서 Pages 수동 배포를 먼저 실행한다. 새 가입 요청은 분리동의 항목과 구 Worker 호환 필드를 함께 보낸다.
5. D1을 다시 백업한 뒤 `2026-08-16-phone-send-log.sql` → `2026-08-16-order-entitlement-ledger.sql` →
   `2026-08-16-signup-consent-form-version.sql` 순으로 적용하고 테이블·컬럼·이관 건수를 읽기 전용 조회로 확인한다.
6. Actions의 Worker 수동 배포에서 두 확인란을 체크해 실행한다. 성공 뒤 deprecated score 재저장이 막히는지 확인하고
   `2026-08-16-remove-diagnosis-scores.sql`을 적용해 0건을 확인한다.
7. 운영에서 가입, 콘텐츠 열기, AI 1회, 동기화, 로그아웃, 삭제, 결제·환불 테스트를 완료한다.
8. 사업자 정보·약관·방침 시행일·동의 버전을 사전공지 절차에 맞춰 반영한 뒤 마지막에만 결제를 연다.

## 7. Claude Code와 Codex를 번갈아 쓸 때

모델 자체를 번갈아 쓰는 것은 문제없다. 문제는 두 모델이 서로 다른 과거 문맥을 믿거나 동시에 같은 파일을
고치는 경우다. 다음 규칙을 지키면 된다.

1. 한 시점에는 한 모델만 쓰기 작업을 한다. 다른 모델은 읽기 전용 리뷰만 맡긴다.
2. 시작할 때 `git status`, `git diff --stat`, 이 문서와 `PROGRESS.md` 최상단을 읽는다.
3. 작은 목적별 브랜치와 커밋을 만들고, 모델 이름보다 변경 목적을 커밋 메시지에 적는다.
4. 작업 전후 전체 테스트를 실행하고, 실패한 상태를 다음 모델에게 넘기지 않는다.
5. 약관·법률 계산·API 계약·D1 스키마를 바꿀 때는 코드, 테스트, 문서, 마이그레이션을 한 묶음으로 본다.
6. 다음 모델에게 “전체를 개선해 달라”보다 허용 파일, 금지사항, 완료 조건, 실행할 테스트를 명시한다.
7. 모델이 작성한 `PROGRESS.md`의 완료 표시는 실제 diff와 테스트 결과로 다시 확인한다.

다음 Claude Code 작업의 첫 요청에는 아래처럼 붙이면 안전하다.

> `CLAUDE.md`, `CODEX-HANDOFF.md`, `PROGRESS.md` 최상단, `LAUNCH-CHECKLIST.md`를 먼저 읽고
> 현재 git diff를 보존해 주세요. 커밋·push·배포·원격 D1 변경은 하지 마세요. 판매 잠금을 유지하고,
> CODEX-HANDOFF의 불변조건을 어기지 마세요. 작업 후 명시된 전체 회귀 테스트와 git diff --check를
> 실행하고, 수정 파일·테스트 결과·남은 수동 작업을 보고해 주세요.`

## 8. Git·Cloudflare 연결에 관하여

이번 환경은 로컬 파일과 읽기 전용 Git 메타데이터는 다룰 수 있었지만, Git 커밋·push와 Cloudflare 계정의
원격 D1·배포 권한을 사용하지 않았다. 따라서 코드 보완과 로컬 검증은 가능하지만 실제 반영은 별도 단계다.
GitHub 또는 Cloudflare 연결 도구를 나중에 붙이더라도 첫 작업은 읽기 전용 상태 확인으로 제한하고,
백업·마이그레이션·배포·DNS 변경은 각각 대상과 순서를 확인한 뒤 실행한다.

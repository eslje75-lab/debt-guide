# 작업 진행 기록

`/clear` 이후 새 세션에서 바로 이어서 작업하기 위한 기록. Claude는 세션 시작 시 이 파일을 먼저 읽고, 의미 있는 작업 단위(기능 추가/수정 완료, 또는 멈추는 시점)마다 갱신한다.

## 🔴 다음 세션 시작 지점 (2026-07-29 기준)

`/clear` 직후 이 블록부터 읽을 것. 아래 2개가 **합의는 끝났고 구현만 남은** 작업이다.
(종전 1번 "청약철회 기산점 교체"는 **2026-07-29 구현 완료** — 아래 "현재 상태" 최상단 참조.)

### 1. 이메일 발송 — 현재 상태는 "전무" (2026-07-29 전수 확인)

| 기능 | 상태 |
|---|---|
| 회원가입 이메일 인증 | ❌ `users`에 `email_verified` 컬럼 자체가 없음 |
| 비밀번호 재설정 | ❌ `find-account.html`이 "준비 중" 안내 페이지 + 운영자 메일 수동 처리 |
| 결제 완료 알림(계약내용 서면) | ❌ 없음 |
| 이용기간 만료 예정 안내 | ❌ 없음 — **약관에서도 뺐다**(2026-07-29). 마이페이지 만료일 표시로 대체했으므로 메일 의무 없음 |

API 라우트 20개 중 메일 관련 0건. Resend/SendGrid/SMTP 등 발송 코드·바인딩 없음. 발송 도메인도 없음(도메인 미구매). → LAUNCH-CHECKLIST 4단계에서 일괄 처리.
주문 확인 메일(계약내용 서면, 제13조②)은 **교부 의무 자체는 남지만** 청약철회 기산점이 결제일로 옮겨져 우선순위가 '공개 전 필수'→'일반 준수'로 내려갔다.

### 2. 텍스트량 감량 — 방향 미정 (사용자 결정 필요)

한글 음절 수: rehabilitation 9,853 / bankruptcy 8,519 / result 5,853. 용어 툴팁으로 "모르는 말" 문제는 풀었으나 **분량 자체는 그대로**. 제안한 3안 중 미선택 상태:
①단계별 접기(현재 단계만 펼침 — 정보 손실 0, 추천) ②상단 "지금 할 일 3가지" 요약 ③상세를 더보기로 이전.

### 그 외 미착수(설계는 확정)
- 숫자 검산을 AI에서 **코드로 분리** — 채권자목록 합계·안분율 100%, 변제계획안 가용소득×기간 ≥ 청산가치, 수입지출 정합성. `js/diagnosis.js` 계산 로직 재사용. AI는 서술형(진술서·보정서)만.
- 진행센터 체크리스트 항목 안에 [작성예시][검토받기] 배치(현재는 `ai-review.html` 별도 페이지)
- 재검토 이력 — ⚠️**초안 원문을 저장하지 말 것**(주민번호 포함 가능). 지난 검토의 `questions[].topic` 문자열만 저장하고, 다음 검토 때 새 원문과 함께 보내 반영 여부를 판정시킬 것.
- ~~`entitlements`·`ai_trial` 테이블 D1 마이그레이션 적용~~ → **2026-07-29 완료**(`content_access`까지 3개. 아래 "현재 상태" 참조)

---

## 현재 상태 (최신이 위로)

### 2026-08-08 (2차) — 고객 연락용 휴대폰 번호(선택) 수집

배경: 운영자가 "문제 발생 시 이메일 외 연락 수단"의 필요를 제기. **본인확인(SMS 인증)과는 별개** — SMS 인증은 여전히 미도입(2012 헌재 위헌·최소수집 방침 유지), **단순 연락처만** 선택 수집. 수집 지점 = **결제 단계(사용자 결정)**.

- **백엔드**: `users.phone` 컬럼(프로덕션 ALTER 적용) + `POST /api/user/phone`(저장/삭제, `^01\d{7,9}$` 느슨한 검증, 빈값이면 NULL) + `getSessionUser`/`handleMe`에 phone + `handleAdminOverview` 회원조회에 phone.
- **결제(pricing.html startPayment)**: 거래조건 confirm 후 **선택 prompt** → 입력 시 포트원 `customer.phoneNumber`로 전달 + `/api/user/phone` 저장(빈값이면 미수집, 마이페이지 기존 번호 유지). **마이페이지 계정설정**에 연락처 필드(조회·수정·삭제, /me로 로드).
- **관리자**: 회원 목록에 '연락처' 열 추가(운영자가 직접 연락용).
- **방침**: 제2항 수집항목에 '연락처(선택) — 휴대폰 번호, 고객지원·연락 목적, SMS인증 아님, 미입력 가능·수정삭제 가능' 행. 시행일 **2026-08-08** + 변경이력. **CONSENT_VERSION → privacy-2026-08-08**(재배포 완료).
- **legal T1(인라인 검증)**: 선택 항목이라 최소수집 원칙 부합, 목적 명시(법 제15조①), 본인확인 아님을 명시. 에이전트 미스폰(경미·비스폰 원칙).
- **검증**: 전 파일 구문·마이그레이션·무인증 401(라우트 확인, 롤아웃 ~23초). **마이페이지 조회/저장/삭제는 로그인해서 실브라우저 테스트 가능**. 결제 단계 prompt는 판매 오픈 후.
- ⚠️문자 **자동발송은 없음** — 저장만 하며 운영자가 직접 전화/문자하는 용도. 자동 SMS 필요 시 국내 SMS 서비스(알리고 등) 별도 연동 필요(Resend는 메일 전용).

### 2026-08-08 — 주문확인 서면 메일 + 청약철회 신청 폼(LAUNCH 4단계 잔여) ⚠️실결제 E2E 미확인(판매 잠금)

사업자등록 대기 중 미리 구축. 둘 다 **결제와 묶여** 있어 판매 잠금(PAYMENTS_ENABLED=false) 상태에선 실발화되지 않음 — 판매 오픈 시 자동 작동.

- **주문확인 서면 메일**(전상법 제13조② 교부의무): `handlePaymentComplete`가 결제 확정 후 `sendOrderConfirmation` 호출. 담는 항목 = 사업자 신원(`BUSINESS_INFO` 상수)·상품/가격/지급수단·공급 방법/시기·청약철회 기한(결제일+14일)·방법·효과+지연배상금 연15%·분쟁처리·약관 링크. 메일 실패해도 결제 확정엔 영향 없음(.catch). 날짜는 `fmtDate`(KST +9h 보정).
- **⚠️`BUSINESS_INFO`는 현재 플레이스홀더** — 사업자등록 후 상호·등록번호·통신판매신고번호·주소·연락처를 실값으로 교체할 것(index.js 상단 상수, terms 제11조·privacy 12항과 일치). 대표자만 '최은식' 기재됨.
- **청약철회 신청**: `POST /api/payment/withdraw`(본인 결제) + 마이페이지 결제내역에 **[청약철회 신청]** 버튼(paid + 결제일 14일 이내에만 노출). 서버 로직 = **14일 이내 + 미개시(content_access 없음) → 자동 전액환불**(portoneCancel+status refunded+revokePackage+완료메일), 그 외(개시분 있음/기간 경과/PG미설정/PG실패) → 접수 후 **운영자 알림 메일(ADMIN_EMAIL) + 이용자 접수 안내** 후 관리자 수동 환불. `withdrawal_requests` 테이블(pending|auto_refunded|resolved)에 접수 기록(전상법 제13조②5호 서식·제5조④ 전자문서 충족). 프론트는 사유 prompt→전송, 환불 시 1.6초 후 reload.
- **DB**: `withdrawal_requests` 테이블+인덱스 프로덕션 적용. ⚠️gotcha: `wrangler d1 execute --remote **--file**`이 import 엔드포인트에서 **Authentication error 10000**로 실패 → **`--command`(쿼리 API)로는 정상**. 앞으로 원격 마이그레이션은 --command로 문장 나눠 실행할 것.
- **검증**: 전 파일 구문 통과, withdraw 무인증 401(라우트·게이트 확인). ⚠️배포 직후 첫 호출은 404(롤아웃 레이스) — 수초 후 401. **실결제→주문메일 수신·청약철회 자동환불 E2E는 판매 오픈(라이브 채널) 후에만 가능**.
- **다음(판매 오픈 시)**: BUSINESS_INFO 실값 + 포트원 라이브 시크릿 교체 + 라이브 결제 1건으로 [주문메일 수신]·[청약철회 자동환불]·[개시 후 운영자검토] 3케이스 확인. (선택)admin에 withdrawal_requests pending 목록 노출.

### 2026-08-06 — 이메일 인프라(Resend) 도입: 비밀번호 재설정·가입 이메일 인증 ⚠️실메일 수신 E2E 미확인

LAUNCH-CHECKLIST 4단계 착수. 발송 서비스 = **Resend**(운영사 Plus Five Five, Inc.), 발송 도메인 `chamroad.com` 인증 완료(Cloudflare Authorize 자동 레코드 3종: MX `send`, TXT `send` SPF, TXT `resend._domainkey` DKIM), 리전 도쿄(ap-northeast-1). 발신주소 `no-reply@chamroad.com`. `RESEND_API_KEY` 시크릿 등록됨.

- **인증 강도(사용자 결정): 소프트** — 가입·로그인은 인증과 무관하게 자유, 미인증이면 안내 배너만. 비밀번호 재설정은 인증 여부와 무관하게 작동.
- **DB 마이그레이션 적용(프로덕션 D1)**: `users.email_verified INTEGER DEFAULT 0` 추가 + `email_tokens` 테이블(token_hash PK=SHA-256, purpose 'reset'|'verify', expires_at, used_at) + 인덱스. 파일 `api/migrations/2026-08-06-email-infra.sql`. ⚠️ALTER는 멱등 아님(1회만).
- **Worker**(`api/src/index.js`): Resend 발송 모듈(`sendEmail`, 실패해도 가입 안 막음) + 메일 템플릿(인라인 스타일, escHtml). 엔드포인트 4개 — `request-reset`(항상 200·계정열거 방지·60초 쿨다운), `reset-password`(토큰검증→비번교체→토큰소진→전세션종료→로그인잠금해제), `verify-email`(멱등), `resend-verification`(Bearer). 가입 시 인증메일 자동발송, 로그인/가입/me 응답에 `emailVerified`. 토큰은 세션처럼 **해시만 저장**.
- **프론트**: `find-account.html` 재작성(재설정 요청), `reset-password.html`·`verify-email.html` 신규(URL 토큰). `js/main.js` Auth에 me/requestReset/resetPassword/verifyEmail/resendVerification + `_saveSession`에 emailVerified + `initPage`에 미인증 슬림 배너(세션당 닫기, 옛 세션은 /me로 확정).
- **개인정보처리방침 개정**(privacy.html): 제5조 위탁표에 Plus Five Five, Inc.(Resend) 행, 제6조 국외이전 ④(미국·일본, 이메일·이름, support@resend.com, 제28조의8 제2항 5개 호 충족), 거부방법 문단 반영, 시행일 2026-08-06 + 변경이력. **CONSENT_VERSION → `terms-2026-07-29/privacy-2026-08-06`**(bump, 재배포 완료).
- **legal-verify T2(인라인)**: 조문 원문(CaseNote) 대조로 제1항 3호 가목·제2항 5개 호 충족 확인, 위탁(제26조) 분류 정확(제3자제공 아님), 국가 표기를 '미국 및 일본'으로 조임. Resend 법인명·연락처·처리국가는 resend.com 방침에서 확인.
- **검증 완료**: 마이그레이션 반영 확인, 구조 스모크(verify/reset 잘못된토큰 400, resend 무인증 401), 전 파일 구문 통과. **⚠️미확인 = 실제 메일 수신·전체 플로우 E2E**(분류기가 PII조회·발송POST 차단 → 사용자가 실브라우저로 가입/재설정 흐름을 돌려 메일 수신을 직접 확인해야 함). 기존 회원 1명 존재(email_verified=0).
- ⚠️**주의**: 백엔드가 프론트 push보다 먼저 배포돼, push 전 가입이 있었다면 인증메일 링크가 아직 없는 verify-email.html을 가리켰을 수 있음(가입 0건이라 실피해 없음). push로 해소.

### 2026-08-05 — 커스텀 도메인 chamroad.com 연결 ✅배포·E2E 검증

도메인 `chamroad.com` **구매 완료(Cloudflare Registrar, `eslje75` 계정, 만료 2027-08-05)** → 실서비스 도메인으로 연결. LAUNCH-CHECKLIST 3단계 완료.

- **API 서브도메인**: `api.chamroad.com` → Worker `chamroad-api` 커스텀 도메인 연결. `api/wrangler.jsonc`에 `routes:[{pattern:"api.chamroad.com", custom_domain:true}]` + `workers_dev:true`(workers.dev 병행 유지) 추가 후 `wrangler deploy`. Cloudflare가 DNS(프록시)·SSL 자동 프로비저닝. **wrangler OAuth 토큰은 `workers_routes:write`는 있으나 `zone`은 read만** — 그래서 Worker 커스텀 도메인은 CLI로 됐지만 아래 본체 A레코드는 대시보드에서 사용자가 직접 넣음.
- **본체 도메인**: `chamroad.com`(apex) A레코드 4개 → GitHub Pages IP(185.199.108~111.153), **전부 회색 구름(DNS only)**. 🚨프록시(주황) 두면 GitHub Pages SSL이 깨짐 — 반드시 DNS only. `CNAME` 파일(`chamroad.com`) 신규 커밋으로 GitHub이 커스텀 도메인 자동 인식.
- **코드 URL 일괄 교체**(sed, 바이트단위라 한글 무손상): 전 페이지 OG·canonical + `sitemap.xml` + `robots.txt` → `chamroad.com`, `js/main.js` `API_BASE` → `https://api.chamroad.com`. **🐛404.html 절대경로 `/debt-guide/`(5곳) → 루트 `/`** — 커스텀 도메인은 프로젝트 경로(`/debt-guide/`)가 아니라 **루트에서 서빙**되므로 안 고치면 404 페이지의 파비콘·홈링크가 깨짐(전환 함정).
- **CORS**: `api/src/index.js` `ALLOWED_ORIGINS`에 `https://chamroad.com`·`https://www.chamroad.com` 추가(github.io도 전환기 병행 유지).
- **E2E 검증**: `https://chamroad.com` 200+타이틀/콘텐츠, 진단·요금제·회생 페이지 200, 커스텀 404 동작, `api.chamroad.com` 무인증 401+`Access-Control-Allow-Origin: https://chamroad.com`+한글메시지. DNS는 1.1.1.1 DoH로 apex 4개 IP·api 프록시 IP 확인.
- ⚠️**남은 마무리(대시보드, 사용자)**: ①GitHub Settings→Pages **Enforce HTTPS 토글**(인증서 발급 후 활성화됨 — 현재 http는 리다이렉트 안 됨) ②`www.chamroad.com` 원하면 CNAME→`eslje75-lab.github.io`(현재 미설정, apex만). 
- ⚠️**판매는 계속 잠금**(`PAYMENTS_ENABLED=false`) — 사업자등록 전. 도메인·이메일(4단계)까지는 진행 가능하나 실제 오픈은 사업자등록 후.
- **다음**: LAUNCH-CHECKLIST 4단계(이메일 인증·비밀번호 재설정·주문확인 서면) — 이제 발송 도메인(chamroad.com)이 생겨 착수 가능.

### 2026-07-29 (5차) — 가입 동의를 서버에서 강제·기록 + 약관·방침 시행일 정정 ✅E2E·jsdom 7종

사용자 질문("가입 시 본인인증이 없다")에서 출발해 점검한 결과, **본인인증은 필요 없고 다른 곳이 비어 있었다.**

- **본인확인(휴대폰·실명)은 도입하지 않는다**(결정): 정보통신망법 제44조의5 본인확인제는 **2012.8.23. 헌재 전원일치 위헌**으로 폐지돼 회원가입 본인확인은 의무가 아니다. 이 서비스는 채무·건강 등 민감정보를 다루므로 **최소수집**이 맞고, 결제 단계에서 카드사 인증이 실질적 확인 역할을 한다. 주민번호·휴대폰 수집은 방침(제3자 미전송·최소수집)과도 어긋난다.
- 🐛**진짜 결함: 동의가 화면에서만 검사되고 서버로 가지 않았다.** `login.html`이 체크박스를 확인만 하고 `Auth.signup`은 name/email/password만 보냈다 → ①동의 사실이 어디에도 남지 않고 ②**API 직접 호출로 동의 없이 가입 가능**(스모크 테스트 계정이 실제로 그렇게 만들어졌다).
  - 근거: 「개인정보 보호법」 **제22조 제3항**(동의 없이 처리할 수 있는 개인정보라는 **입증책임은 개인정보처리자 부담**), **제22조의2 제1항**(만 14세 미만 아동은 법정대리인 동의). 만 14세 이상 확인 기록이 없으면 다툴 근거가 없다.
  - 수정: 서버 `validateSignup`이 **`body.agree === true`를 필수**로 요구(엄격 비교 — 문자열 `"true"`도 거부), `users.agreed_at`·`consent_version`에 시각과 판본 저장. `CONSENT_VERSION = 'terms-2026-07-29/privacy-2026-07-29'` 상수로 관리 — **약관·방침 개정 시 이 값을 함께 올릴 것**(재동의 대상 회원을 가려낼 근거).
  - D1: `users`에 `agreed_at INTEGER`·`consent_version TEXT` ALTER 적용(프로덕션 반영 완료).
- **약관·방침 시행일 정정**: 약관은 7/23 표기인데 제5조·제6조를 7/28~29에 실질 개정했고, 방침도 7/21 이후 결제내역 열람·익명통계가 추가됐다. 공개일에 맞춰 **둘 다 2026년 7월 29일**로 통일하고 방침 변경이력에 한 줄 추가. (공개 전이라 이용자 0명 → 불이익 변경 통지 문제는 없었음)
- **검증**: 프로덕션 E2E — `agree` 누락 400(5회 연속) / `agree:false` 400 / `agree:"true"` 400 / `agree:true` 200 + DB에 `agreed_at`·`consent_version` 기록 확인. jsdom 7종 — 체크 시 `agree:true` 전송, 미체크 시 요청 자체 미발생·오류 노출. 테스트 계정 3개 정리 완료(현재 users는 관리자 1명).
- ⚠️**배포 롤아웃 레이스 재확인**: 배포 직후 `agree` 누락 요청이 한 번 **200으로 통과**했다(옛 버전 인스턴스). 수 초 뒤 5회 재시도는 전부 400. **배포 직후의 단발 결과를 믿지 말 것** — 반복 호출로 확인해야 한다.
- ⚠️**관리자 계정(id 15)은 `agreed_at`이 NULL**이다. 가입 시점에 서버가 기록하지 않았고, 화면 체크만으로는 서버측 증거가 없어 **소급 기입하지 않았다**. 운영자 본인 계정이라 실무상 문제는 없다.
- ⚠️**남은 것**: 이메일 인증·비밀번호 재설정은 여전히 없음(발송 인프라 필요 — 4단계). 가입 rate limit도 없음 — IP 기준은 방침상 미도입이므로 **Cloudflare 인프라 레벨 Rate Limiting**으로 거는 것이 방침과 일관된다.

### 2026-07-29 (4차) — 판매 잠금(`PAYMENTS_ENABLED=false`) + 사이트 공개 ✅jsdom 13종

**방침 변경**(사용자 결정): 종전 "1~5단계 다 끝내고 마지막에 push"에서 **"사이트는 공개하되 판매만 닫는다"**로 전환. 공개 자체가 문제가 아니라 **사업자 신원 표시(전상법 제10조) 없이 테스트 채널로 판매를 개시하는 것**이 문제였기 때문. 결제를 닫으면 판매 개시가 아니다.

- 공개하는 실익: `API_BASE`가 localhost에서는 로컬 워커를 보므로 실브라우저 검증에 `wrangler dev --remote`가 필요했는데, 공개하면 `https://eslje75-lab.github.io`가 프로덕션 API로 바로 붙는다(CORS 기허용).
- `js/main.js` **`PAYMENTS_ENABLED = false`** 한 곳으로 제어. `pricing.html`은 ①상단 '정식 오픈 준비 중' 안내 표시 ②결제 버튼 5개 라벨을 전부 교체(+`aria-disabled`) ③`startPayment` 진입 자체 차단. **버튼 라벨까지 바꾸는 이유는 표시·광고와 이행의 일치**(제13조⑤) — 살 수 없는데 '구매하기'로 두면 그 자체가 어긋난다.
- **해제 조건**(순서 고정): 1단계(사업자등록 + 약관 제11조·방침 12항 실값) **및** 2단계(포트원 라이브 + 라이브 결제 1건 검증) 완료 후 플래그만 `true`.
- **jsdom 13종 PASS**: false일 때 안내 노출·버튼 5개 전부 교체·`/api/payment/prepare` 미호출·포트원 결제창 미호출·결제 전 confirm도 안 뜸 / **true로 되돌리면 원상 복구되고 주문 생성이 호출됨**(원복 가능성까지 검사).
- 🔎**참고**: 관리자 이메일 선점 위험은 push와 무관하다 — Worker가 이미 공개돼 있어 CORS와 무관한 서버 호출(curl)로 누구나 가입을 시도할 수 있다. 그래도 **`ADMIN_EMAIL` 계정 선점 가입은 빨리 할 것**(현재 users 0건).

### 2026-07-29 (3차) — 프로덕션 서버 동기화(D1 마이그레이션 + Worker 배포) ✅스모크 검증

**발견**: 프로덕션이 7/28 2차 이전 상태로 밀려 있었다. 배포된 Worker에 `POST /api/content/access`가 **404**(라우트 자체 없음)였고, D1에는 `content_access`·`entitlements`·`ai_trial` **3개 테이블이 모두 없었다**. PROGRESS에는 `entitlements`·`ai_trial`만 적혀 있었고 `content_access` 누락과 **Worker 미배포**는 기록에 없었다. 즉 미리보기 게이트 서버측·이용권·AI 총량제가 프로덕션에 하나도 반영돼 있지 않았다.

- **D1 마이그레이션 적용**(순서 중요 — 테이블 없이 새 Worker를 올리면 `activeEntitlements`·`grantEntitlement`가 깨진다): `content_access` / `entitlements` + `idx_entitlements_user` / `ai_trial`. 적용 전 `sqlite_master`를 `schema.sql`과 전수 대조해 기존 8개 테이블은 일치함을 확인(payments.refunded_at ALTER 포함), 추가 전용 DDL만 실행.
- **Worker 배포**: `npx wrangler deploy` → Version `5d3cc8cc-de42-4fa9-985d-94c4f795014f`. 새로 필요한 시크릿 없음(참조 env 7종 전부 기설정).
- **스모크 검증**(테스트 계정 → 확인 → 탈퇴 API로 정리, 전 테이블 0건 확인): `/api/content/access` 404→**401**(비로그인)·**200 `recorded:false`**(로그인·미보유 = 미리보기는 제공 개시가 아님) / `GET /api/data`가 **`entitlements: []`**를 정상 반환(새 테이블 조회 경로 살아 있음) / 탈퇴 후 세션 401·고아 레코드 0.
- ⚠️**gotcha 재확인**: 배포 직후 같은 경로가 한 번은 401, 한 번은 404를 냈다 — Cloudflare 버전 롤아웃 중 옛 버전이 섞여 응답한다. **배포 직후 즉시 E2E를 치지 말 것**(수 초 후 재시도하면 안정).
- ⚠️**미검증(권한 차단)**: 이용권 부여 → 만료일 표시 E2E. `payments`·`entitlements` 직접 INSERT가 분류기에 차단됐고, 관리자 테스트 지급은 `ADMIN_EMAIL` 계정 선점 가입이 아직 안 된 상태라 못 했다. **관리자 계정 가입 후 admin '🧪 테스트 지급' → 마이페이지 만료일 표시 확인**이 남은 절차.
- push는 **보류**(사용자 결정) — 로컬 main이 origin보다 4커밋 앞선 상태 유지. 이유는 LAUNCH-CHECKLIST 6단계 방침 + 포트원 테스트 채널·사업자 정보 미기재.

### 2026-07-29 (2차) — 청약철회 기산점을 "결제일부터 14일"로 교체 + 만료일 안내를 메일→마이페이지로

발송 기능이 없는 "주문 확인 메일"에 환불 기간이 걸려 있어 **공개 시 약관이 거짓**이 되는 상태를 해소. 근거: 법 제17조 제1항 괄호가 *"거래당사자가 다음 각 호의 기간보다 긴 기간으로 약정한 경우에는 그 기간"*이라 **더 긴 약정을 허용**한다. 결제일+7일로 하면 기산점(서면 수령일·공급 시작일·주소를 안 날)이 결제일보다 늦어질 때 **기간 축소**가 되어 제21조①1호에 걸리므로(공정위 제재 사례: 위버스컴퍼니 등 4개사) **결제일+14일**이어야 어떤 기산점보다도 항상 길다.

- `terms.html` 제6조 ① — "서면 수령일부터 7일 / 주소를 안 날부터 7일"(제17조①1·2호) 전부를 **"결제일부터 14일"** + 법정 기간보다 긴 약정임을 명시하는 문구로 교체. ②(제공 개시분 제한)·③(하자 3개월/30일)·④·⑤는 그대로.
- `pricing.html` 결제·환불 안내 박스 + 결제 전 `confirm()` — 같은 문구로 통일("법정 7일보다 긴 기간을 약정").
- `terms.html` 제5조 **"만료 30일 전 이메일 안내" 삭제** — 발송 수단이 없어 그대로 두면 약관 위반. 대신 **"만료일은 마이페이지 결제 상태에서 확인"**으로 바꾸고, 약속이 참이 되도록 `mypage.html`에 **이용기간 만료일 표시를 실제 구현**(`renderEntitlementExpiry`/`paintExpiry` — `GET /api/data`의 `entitlements[].expiresAt`. 서버가 만료 안 된 것만 내려주므로 접근 판정과 동일 근거. 로그인 캐시로 즉시 표시 후 서버 값으로 갱신, 오프라인이면 캐시 유지).
- 코드 주석 정합: `js/main.js` `noteContentOpened`, `api/src/index.js` `handleAdminRefund` 의 "7일" → "결제일부터 14일".
- `LAUNCH-CHECKLIST.md` '확정된 방침'에 기산점 결정과 **되돌리지 말 것** 경고 추가.
- 검증: `mypage`·`pricing`·`terms` 인라인 스크립트 구문 파싱 PASS(`new Function`).
- **jsdom 12종 PASS**(실제 `mypage.html` + `js/main.js` 실행, `GET /api/data` 목킹): 패키지별 만료일 렌더 / 서버가 만료 이용권을 빼면 그 칸은 **빈칸**(날짜를 지어내지 않음) / 캐시로 즉시 표시 후 **서버 값이 캐시를 덮어씀** / 오프라인이면 캐시 유지 / 비로그인은 `/api/data`를 부르지 않고 만료일 칸 자체를 만들지 않음 / `entitlements`가 배열이 아니거나 `expiresAt`이 null이어도 예외 없이 빈칸. 스크립트는 스크래치패드 `test-expiry.js`(미커밋).
- ⚠️**실데이터·브라우저 픽셀은 여전히 미확인** — 관리자 테스트 지급이 선행돼야 함(`ADMIN_EMAIL` 계정 미가입).
- 🐛**로그아웃 시 이용권 캐시 잔존 — 수정 완료**: `Auth.logout()`이 세션 키만 지워 로그아웃 후에도 마이페이지가 "○○ 패키지 이용 중"으로 보였다(공용 PC에서 다음 사용자에게 구매 사실 노출. 콘텐츠 접근 자체는 서버 게이트가 막으므로 표시만의 문제였다). `Auth._PLAN_CACHE` 6개 키를 **raw `localStorage.removeItem`으로** 제거하도록 수정.
  - ⚠️**`Storage.remove()`를 쓰면 안 된다** — `DataSync.queueDel`이 서버 `user_data`에서도 지우는데, `handleGetData`가 `plan_packages`를 user_data에서 읽으므로 **정상 구매자가 잠긴다**. 코드 주석에 근거를 남겨 뒀다. jsdom 테스트가 `/api/data/sync` 미호출을 검사한다.
- 🔎**미수정·서버측 관련 결함**: 같은 이유로 **마이페이지 '저장된 데이터 전체 초기화'(`confirmReset`)가 유료 접근을 날린다** — `Storage.remove('plan_packages')` + `DataSync.clearServer()`(clearAll)로 서버 user_data가 비워지고, `handleGetData`가 `owned`(=user_data.plan_packages)를 기준으로 필터링하므로 **`entitlements` 행이 멀쩡히 살아 있어도 `plan_packages: []`가 되어 잠긴다**. 현재는 확인창이 "이용권 정보가 사라지니 문의하라"고 안내할 뿐. 근본 수정안: `handleGetData`가 `owned`를 필터링하는 대신 **활성 `entitlements`와 합집합**을 만들도록 바꾸면(레거시 키는 그대로 통과) 초기화·동기화 사고에도 유료 접근이 자동 복구된다. `handleSyncData`의 **del 경로에 `PLAN_KEYS` 가드가 없는 것**(put에는 있음)도 함께 볼 것. → **Worker 재배포 필요**.

### 2026-07-27 — 진단 단순화(퇴직금 제거) + 법률 용어 자동 툴팁 ✅jsdom 검증

사용자 문제 제기: ① 무료진단에서 예상 퇴직금 금액을 물으니 "얼마로 써야 하지" 부담으로 피로감, ② 사이트 전반 텍스트가 많고 모르는 용어가 자주 나옴. 플랫폼 목적은 **정확한 변제금 계산기가 아니라 셀프 준비 안내**임을 재확인.

- **무료진단에서 예상 퇴직금 입력 제거**: `diagnosis.html` 4단계 재산 필드 6→5개, `diagnosis.js`의 `severanceValue`/`severanceHalf` 삭제(totalAssets에서 빠짐). 근거 = 사이트 표준 예시(가용 1,061,457×36=38,212,452 vs 청산가치 10,260,000)에서 퇴직금 유무가 판정을 바꾸지 않음. 판정이 뒤집히려면 저소득+장기근속이라는 좁은 조합이어야 함. 정확한 반영은 유료 단계 `setup.html`(q-employed)에서 계속 수행.
- `setup.html` 프리필: `severanceValue > 0` → 재직 판정하던 줄 삭제(무료진단이 더는 안 물음). setup에서 직접 답하게 둠.
- `result.html` 변제금 3종 요약 아래 **추정치 안내 문구 상시 노출** — "재직 중이면 예상 퇴직금 절반이 재산에 더해지고, 부양가족·법원 인정 생계비 범위에 따라 달라진다".
- **법률 용어 자동 툴팁**(`main.js` GLOSSARY 48개 + `tagTerms`/`initGlossary`): 페이지마다 수동으로 `<span class="term-tip">`을 심는 대신 본문 텍스트 노드를 훑어 자동 태깅. **용어당 화면에서 1회만**(점선 도배 방지), 긴 용어 우선 매칭(`면책결정`이 `면책`보다 먼저), 링크·버튼·입력창·헤더·푸터·제목(h1~h3) 제외. `taggedTerms()`가 매 회차 DOM에서 다시 읽어 탭 전환으로 본문이 다시 그려져도 자가 복구. MutationObserver(200ms 디바운스)로 JS 렌더 콘텐츠도 커버 — 남은 용어 없으면 즉시 반환해 순환 없음.
- **터치 대응**: 기존 term-tip은 `:hover`만 있어 모바일에서 안 열렸음. `.tip-open` 토글(탭/바깥탭/ESC) + `:focus-visible` + `tabindex/role/aria-label` 추가(`styles.css`, `main.js`).
- **jsdom 검증 PASS**: 중복 태깅 없음 / 링크·버튼·헤더·푸터 안 태깅 안 됨 / `면책결정`·`면책불허가`·`보정권고` 통째로 매칭 / 본문 텍스트 무손상 / 재실행 시 증가 없음(멱등).
### 2026-07-29 — 면책신청 무료 분리 + 이용권(기간·회수) 체계 도입 ✅legal-verifier 검증·회귀 5종

**출발점**: 회생 완주 패키지 9단계가 "변제 완료 후 면책신청"이라 상품 수명이 5년+였고, 이용기간을 정할 수 없었다. 조문을 확인해 보니 개인회생 면책은 **제624조 제1항이 "변제를 완료한 때에는 … 직권으로 면책의 결정을 하여야 한다"는 기속행위**이고 심문·이의기간·관재인 조사가 없다. 반면 개인파산은 심문(제558조)·관재인 조사(제560조)·이의신청(제562조)·불허가사유 7가지(제564조①)로 전혀 다른 난이도. → **회생 면책만 무료로 분리**.

- **`discharge.html` 신규(무료)**: 개인회생 면책신청 안내. `rehabilitation.html` 9단계 제거(8단계까지 = 인가까지). pricing은 이미 "서류 준비 → **인가 완료**"로 광고 중이었으므로 오히려 광고와 실제가 일치하게 됨. NAV_LINKS·푸터 연결.
  - ⚠️ **특별면책(제624조②, 3요건)은 무료로 빼지 않았다** — 회생위원 조사·면담·의견서·이해관계인 의견청취가 붙는 완전히 다른 절차. `maintenance.html`(변제기간 관리)에 그대로 유지.
- **이용권 체계**(`entitlements` 테이블): 패키지별 **이용기간 + AI 회수**를 한곳에서 관리. `grantEntitlement()`는 재구매 시 **남은 기간 끝에서 연장 + 회수 합산**(덮어쓰기 아님 — 보정 추가 대응은 여러 번 사는 상품). `GET /api/data`가 `plan_packages`를 **만료되지 않은 것만**으로 덮어써 `requirePackage`가 만료를 자동 반영. 환불·회수(`revokePackage`) 시 이용권도 삭제.
  - 기간: 회생 완주 24 / 파산 완주 24 / 변제기간 관리 84 / 보정 추가 대응 12개월. `addMonths()`는 말일 보정 포함(1/31+1개월=2/28).
- **AI 검토 총량제**: 완주 12회 / 관리·보정 각 8회 / **미구매 1회 무료 체험**(`ai_trial`). 일일 상한은 30→**10**으로 낮춰 남용 방지용으로만 남김. 실패한 호출은 미차감(성공 시에만 batch로 차감). 소진 시 403 + 요금제 유도.
  - 체험 1회는 **시행령 제21조의2 제3호(체험용 디지털콘텐츠)**도 충족 → 환불 제한 근거가 미리보기(제1호)와 **이중**이 됨.
- **동시 세션 1개**: 로그인 시 `DELETE FROM sessions WHERE user_id = ?`. IP·기기 미수집이라 개인정보 방침 유지. ⚠️**시간을 나눠 쓰는 순차 양도는 못 막는다** — 본인확인 없이는 불가능하며, 공개 후 실제 남용이 관측되면 그때 도입하기로 보류.
- **결제 전 고지 3곳**(전상법 제13조② + 시행령 제20조2호 '판매조건 제한'): pricing 안내 박스 / 결제 전 confirm(`PACKAGE_TERM_MONTHS`·`PACKAGE_AI_QUOTA` 상수) / terms 제5조. ⚠️**프론트 상수는 서버 `PACKAGE_TERMS`와 반드시 일치해야 하며, 테스트가 이 일치를 검사한다.**
- **legal-verifier 검증 반영**(조문은 직접 재대조함):
  - 🐛 **비면책채권 제7호(근로자의 임치금·신원보증금) 누락** — 제625조②는 **8개 호**인데 7개만 적었음. 추가.
  - 🐛 **"조세 등의 청구권"이 과대** — 제583조①2호 한정(원천징수 조세·부가세·개별소비세·주세·특별징수 지방세)임을 명시. 안 그러면 "모든 세금은 안 없어진다"로 읽힘.
  - **채권자 즉시항고 존재**(제627조) — "이의신청 절차도 없다"는 서술 수정. 공고 효력일부터 14일(제13조②) 경과로 확정됨을 4단계에 추가.
  - **회생위원 조사·보고 있음**(서울회생법원 준칙 제451호) — "관재인 조사 없음"은 맞으나 회생위원은 있음.
  - 제625조③에 **담보** 포함(보증인만 쓰고 담보 누락했었음).
  - 면책신청서 실제 양식 확인 → **환급계좌란**이 있어 남은 적립금이 그 계좌로 환급됨. 누락 시 불이익이라 강조 박스 추가.
  - **신용정보 해제는 법원이 직권 통보**(준칙 제452호) / 결정문은 **제증명 신청**으로 받아야 함(송달 생략 가능) / 면책신청 종기 = 절차 종료 전(대법원 2012마811) / 서울회생법원은 **6개월 미신청 시 직권면책** 안전망.
  - 전자소송 링크는 **양식 배포처가 아니라 제출 경로** → 라벨 분리.
  - 약관 보완: 잔여 회수 **소멸·환불 불가 명시**(약관규제법 제5조 작성자 불이익 원칙 대비), **만료 30일 전 안내** 조항 신설, 일일 제한이 회수를 없애지 않음을 명시.
- **회귀 5종 PASS**: 이용권(기간계산·부여·재구매누적·만료경계·고지일치) / AI 스키마·설계불변식 / 미리보기 7종 / 게이트 7종(위조차단) / 용어 툴팁.
- ⚠️**공개 전 필수(미구현)**: ~~①주문 확인 메일 ②만료 30일 전 안내 메일~~ → **2026-07-29 (2차)에서 기산점을 결제일+14일로 옮기고 만료 안내를 마이페이지 표시로 대체해 둘 다 해소**(위 항목 참조). 남은 것은 ③`ai_trial`·`entitlements` 테이블 **D1 마이그레이션 적용**.

### 2026-07-28 (3차) — 서류검토 AI를 '판정자'에서 '질문자'로 전환 ✅검증 7종

배경: "N건 발견" 식 판정 출력이 실용성도 낮고 변호사법·법무사법 경계에도 걸린다는 사용자 지적. 조문을 확인해 보니 금지되는 것은 **작성·감정·법률상담** 세 가지이고, **점검과 질문은 어디에도 없다**(법무사법 제2조①1 "법원에 제출하는 서류의 작성" + 제3조① / 변호사법 제109조1 "감정·대리·법률상담 또는 법률관계 문서 작성"). 따라서 결과를 흐리게 만들 필요가 없고, **판정만 안 하면 구체적으로 말해도 된다**는 결론.

- **핵심 전환**: AI가 판정하지 않고 **질문한다**. "채무 발생 경위가 불충분합니다"(감정) → "2020~2022년에 채무가 늘어난 사정이 적혀 있지 않습니다. 이 기간에 어떤 일이 있었나요?"(정보 제공). 문장은 언제나 이용자가 쓴다 = 대필 회피 + 셀프 진행 플랫폼 목적과 일치.
- **금지를 프롬프트가 아니라 스키마로 강제**(`api/src/index.js` `AI_REVIEW_SCHEMA`): 출력 형식을 `output_config.format`(json_schema)으로 고정하고, **대체 문장·판정을 담을 필드를 아예 만들지 않았다**. `{ present[], questions[{topic,question}], lawNotes[{excerpt,statute,note,question}] }` 뿐. 프롬프트로만 막으면 모델이 넘어가므로 구조로 막는 것. ⚠️**필드 추가 시 이 원칙을 깨지 말 것** — 코드 주석에 근거 조문과 함께 남겨둠.
- **`severity`(심각도 등급) 제거**: 등급을 매기는 것 자체가 '감정'. `ai-review.html`의 `SEV` 상수 삭제, 결과 화면에서 등급 뱃지 제거.
- **"이상 없음"을 절대 출력하지 않는다**: 질문이 0건일 때 "이번 점검에서 추가로 여쭐 항목을 찾지 못했습니다 + 법원의 판단을 예측하지 않습니다"로 사실만 진술. "제출해도 된다"는 판단은 이용자 몫.
- **🐛 잠재 버그 수정**: `claude-sonnet-5`는 `thinking`을 생략하면 **adaptive thinking이 기본 동작**인데(Opus 4.8/4.7과 반대) `max_tokens`가 사고+응답을 함께 제한한다. 기존 1024로는 구조화 출력이 중간에 잘려 JSON 파싱이 실패한다 → **4096으로 상향** + `stop_reason === 'max_tokens'` 감지해 502로 올림(잘린 JSON을 조용히 통과시키지 않음). `output_config.effort: 'medium'` 추가.
- **진술서 전용 보강**(`AI_NARRATIVE_HINT`, `isNarrativeDoc()`): 진술서·경위서·생활상황 계열에만 붙는 점검 항목(채무 시작 계기 / 증가 기간의 사정 / 사용처 / 상환 노력 / 현재 상황 / 시기·금액 불일치). 채권자목록 등에는 붙지 않음.
- **`lawNotes` 설계**: 초안에 도박·투자·사치성 소비 등 면책불허가 사유로 검토될 표현이 있을 때만 생성. **"이 표현은 위험하다"(판단)가 아니라 "이 조항은 …을 면책불허가 사유로 정합니다"(사실) + "관련 사정을 함께 적으셨나요?"(질문)**. 없는 위험을 만들어내지 않도록 빈 배열 허용을 명시.
- **검증 7종 PASS**(`node`, 실제 API 미호출·fetch 목킹): 스키마가 structured outputs 제약 준수(모든 object에 `additionalProperties:false`+`required`, 미지원 키워드 `minLength`/`maxItems` 등 없음) / **설계 불변식**(스키마에 `suggestion`·`severity`·`score`·`rewrite`·`example`·`verdict`·`summary` 필드 부재) / 요청 본문에 `json_schema`·`effort`·`max_tokens≥4096` 실림, sonnet-5가 거부하는 샘플링 파라미터 없음, messages가 user로 끝남(프리필 미지원) / 진술서엔 보강 지침 붙고 채권자목록엔 안 붙음 / `max_tokens` 절단 감지 / `refusal` 감지 / 정규화 상한·불량항목 제거.
- ⚠️**아직 안 한 것**(설계는 확정, 구현 미착수): ①숫자 검산은 코드로 분리(채권자목록 합계·안분율 100%, 변제계획안 가용소득×기간≥청산가치, 수입지출 정합성 — `diagnosis.js` 계산 로직 재사용) ②진행센터 체크리스트 항목 안에 [작성예시][검토받기] 배치 ③`drafts` 테이블로 재검토 이력("지난번 지적 3건 중 2건 반영") ④회수 정책을 문서별→패키지 총량제.

### 2026-07-28 (2차) — 환불 정책 전환(이용 개시 후 제한) + 미리보기 게이트 ✅T2 검증·jsdom 17케이스

사용자 결정: "이미 이용했으면 환불해 주면 안 된다 — 쓰고 환불하는 악용이 늘 것". **LAUNCH-CHECKLIST '확정된 방침(뒤집지 말 것)'의 종전 환불 정책을 의도적으로 뒤집은 것**이므로 체크리스트도 함께 갱신함.

- **법적 제약**: 전자상거래법 제17조 제2항 제5호는 디지털콘텐츠 '제공 개시' 후 청약철회 제한을 허용하지만, **같은 항 단서 + 제6항 단서**에 따라 ①청약철회 불가 사실의 표시 ②시행령 제21조의2의 시험사용 제공을 **모두** 해야 제한이 유효하다. 표시만 하고 미리보기가 없으면 **제한 자체가 무효**가 되어 종전보다 나빠진다.
- **미리보기 게이트로 전환**(`main.js`): 전체 화면 `showPaywall` 오버레이 → `enterPreview`/`lockSection` 방식. 유료 4종에서 **1단계 체크리스트 전체 + 항목별 상세 가이드**를 미구매자에게 열고, 2단계 이후·서식 예시 라이브러리는 잠금. 시행령 제21조의2 **제1호(일부 이용의 허용)** 이행. `chamroad:gate` 이벤트로 서버 판정 확정 시 재렌더. **`showPaywall`/`hidePaywall` 삭제됨**.
- **`content_access` 테이블 신설**(`api/schema.sql`) + `POST /api/content/access`(`handleContentAccess`) — 보유자가 콘텐츠를 처음 연 시각을 최초 1회 기록. `GET /api/payment/history`에 `first_access_at` LEFT JOIN으로 함께 내려줌(환불 판단 근거). 미구매자의 미리보기 열람은 기록하지 않음.
- **🐛 반박 검증이 잡아낸 자체 결함**: 최초 구현이 `unlockContent()`에서 바로 이용 개시를 기록했는데, 결제 후 1.2초 뒤 진행센터로 **자동 이동**하므로 소비자가 아무것도 열지 않았는데 기록이 찍혀 **약관 ①(7일 전액 환불)이 한 번도 작동할 수 없는 상태**였음(제21조①1 기만적 청약철회 방해 소지). → `noteContentOpened()`로 분리해 **2단계 이상 이동·서식 예시 열람** 등 명시적 열람 행위에만 바인딩.
- **문구 개정**: `terms.html` 제6조 ①~⑤ 전면 개정, `pricing.html` 안내 박스·결제 전 고지. 반박 통과분만 반영 — "제공이 **완료**"→"제공이 **개시**"(법문 일치), 기산점을 "결제일"→"**계약내용 서면(주문 확인 메일) 수령일**"(제17조①1·2), 지연배상금 **연 15%**(시행령 제21조의3), 디지털콘텐츠 **반환의무 없음**(제18조①단서), 서면 **발신주의**(제17조④), 위약금 청구 없음(제18조⑨).
- **반박으로 기각한 수정안**: ①"일할 또는 단위별 계산 환불" — 단계별 대금을 표시한 적이 없어 **산정 불가능한 약속**이라 오히려 채무불이행 근거가 됨 ②"미리보기 화면 자체에 환불 제한 표시가 법정 의무" — 제6항 단서의 "표시와 함께"는 두 의무의 접속(and)이지 동일 화면 요구가 아님 ③"1단계 공개는 시험사용으로 부족" — 시행령 제21조의2는 "하나 이상"만 요구하고 분량 기준 없음 ④"제17조② 단서(철회권 부활)를 약관에 적을 의무" — 제13조② 어느 호에도 없는 제재규범 ⑤대법원 2024다221042 인용 — **개인회생 청구이의 사건으로 전자상거래법과 무관**(오인용) ⑥공정위 지침 "Ⅱ.10.나.(2)" 단락 번호 — 원문 확인 실패.
- **약관 ② 가분성 열거 삭제**: "회차·기간 단위 패키지(보정·변제관리)만 가분적"이라고 열거한 것이 **완주 패키지 구매자의 법정 철회권을 박탈**(약관규제법 제9조1호 무효 소지)해, 법문 그대로 "가분적 디지털콘텐츠로 구성된 계약의 제공이 개시되지 아니한 부분"으로 대체. 가분성 판단을 약관이 선점하지 않게 함.
- **jsdom 검증 17케이스 PASS**: 미리보기 7종(유출 없음·DOM 복구 가능·CTA 분기·멱등) + 게이트 회귀 7종(**로컬 위조 차단 유지 확인**) + 용어 툴팁 회귀.
- ⚠️**공개 전 필수(LAUNCH-CHECKLIST 4단계에 추가함)**: **결제 시 계약내용 서면(주문 확인 메일) 발송**. 이걸 안 보내면 제17조①2호로 기산점이 안 잡혀 환불 제한 방어가 무너지고, 이미 "주문 확인 메일을 받은 날부터 7일"로 써 둔 약관·안내가 거짓이 된다. 청약철회 서식(제13조②5호 괄호)·사업자 신원 실값 기재도 함께.
- ⚠️**별건 발견(미수정)**: `pricing.html`의 "서류검토AI 지원 2회"가 실제 구현과 다름 — `ai-review.html`에 `requirePackage` 호출이 없고 `handleAiReview`는 세션만 확인하며 `DAILY_AI_LIMIT=30`. 즉 **미구매자도 하루 30회 사용 가능**. 광고와 이행의 불일치(제13조⑤)이자, "완주 패키지에 회수 단위 급부가 있다"는 가분성 논거를 스스로 만드는 자충수. 표시를 실제에 맞추거나 구현에 게이트를 넣어야 함 — **사용자 결정 필요**.

### 2026-07-28 — GLOSSARY 법률 검증(T2: legal-verifier → adversarial-refuter + consistency-checker) ✅반영 완료

- **에이전트 판정 충돌을 반박 검증으로 확정**: 채권자목록 누락 채권의 비면책에 '악의' 요건이 필요한지를 두고 legal-verifier(불필요)와 consistency-checker(필요)가 정반대 판정. 반박 결과 **legal-verifier 승** — 제625조 제2항 제1호(개인회생)는 '악의' 없이 "기재되지 아니한 청구권", 제566조 제7호(파산)는 "**악의로** 기재하지 아니한 청구권". consistency-checker가 제624조 제3항 제1호(면책**불허가** 사유의 악의)를 제625조 제2항으로 오독한 것. **→ 사이트 5곳은 이미 절차별로 정확해 수정 0곳.** `js/main.js` 채권자목록 툴팁도 현행 "면책되지 않을 **수 있습니다**"가 회생·파산 공용으로 유일하게 양쪽 참이라 **유지**(강화 금지). 대법원 2025.10.16. 2024다221042가 "원칙적 소극"으로 정식화해 "무조건" 표현도 금지.
- **반영한 수정(반박 통과분)**: 회생위원 지정→**선임**(제601조) / 가용소득에 **세금·보험료** 공제 추가(제579조4호 나~라목+시행령17조, 4대보험료 전부) / 표준생계비 60%는 **원칙이며 증감 가능**(재민 2004-4 제7조②) / 기준중위소득 = **가구**소득 중간값 / 원천징수 3.3%는 '프리랜서'가 아니라 **사업(영업)소득** / 면책·면책결정에 **비면책채권·보증인 책임 잔존** 명시(제566조·제625조②③) / 인가결정 "**납입은 보통 인가 전부터**"(재민 2004-4 제7조③, 서울준칙 제432호 — hedge 필수, 서울 전용 준칙이라 단정 금지) / 특별면책 3요건(제624조②) / 파산재단에서 압류금지·면제재산 제외(제383조) / 임차보증금 **면제재산 신청 가능**(제383조②1호, 기한=파산선고 후 14일) / 압류에 **월 250만원 압류금지** 추가(민사집행법 시행령 제36056호, 시행 2026.2.1, 종전 185만원) / 청산가치 문구를 result.html과 동일하게 통일('전부' 과장 제거) / 가처분소득을 result.html 기존 툴팁 문구로 통일.
- **반박으로 기각한 수정안**: ①채권자목록 강화(위) ②특별면책 "5년간 파산면책 제한" — 제564조①4호는 "제624조에 의하여 면책"이라 **정상 완주 면책에도 동일 적용**되므로 특별면책만의 불이익처럼 쓰면 오독 ③가처분소득 "일반 경제 용어입니다" — 경제학 disposable income은 생계비를 안 빼므로 오히려 악화 ④포괄적금지명령 "법인회생 전용" — 제593조⑤가 개인회생에 준용하므로 틀림.
- **🐛 실제 버그 발견·수정**: 자동 태깅이 합성어 앞부분만 잘라 **정반대 뜻**을 붙이고 있었음 — `비면책` 채무 → `비[면책]`("빚 책임이 없어짐" 툴팁), `압류금지`재산 → `[압류]금지`. GLOSSARY에 `비면책`·`압류금지`·`보정명령`·`송달료`·`수임료` 키를 추가해 길이 우선 정렬로 해결. jsdom 회귀 테스트 추가(합성어 5쌍) — 전 항목 PASS.
- 부수 일관성 수정: `rehabilitation.html:214` "최저생계비"(2015년 폐지 용어) → "표준생계비" / `resources.html:76` 동시폐지 서술을 `result.html:642`("관재인 선임이 대부분, 동시폐지는 예외적")와 정합.
- ⚠️**미반영(확정 보류)**: 표준생계비 가산 3항목(주거비·의료비·미성년자녀 교육비)은 **서울회생법원 준칙 제405호 전용** — 본문에 쓸 때 "서울회생법원 기준" 병기 필요, 60자 툴팁에는 미포함. `포괄적금지명령`·`별제권`·`재량면책`은 사이트 본문 등장 0회라 툴팁이 렌더링되지 않음(정의는 고쳐 둠).

- ⚠️**남은 것**: 텍스트량 과다 문제(rehabilitation 9,853자·bankruptcy 8,519자)는 편집 방향 미정 — 아래 "다음에 할 일" 참조.

### 2026-07-24 (3차) — 유료 콘텐츠 서버 게이팅(잠금 오버레이) ✅로직 검증

배경: 유료 센터 4종이 결제 게이트 없이 URL로 열려 있었음(정적 사이트 시절부터 미구현). 공개 전 필수 작업.
- **잠금 대상 4종**(사용자 결정): rehabilitation(rehab-full), bankruptcy(bankrupt-full), maintenance(maintain), supplement(type별 rehab/bankrupt 완주+보정). 방식=잠금 오버레이.
- `main.js requirePackage(allowed, pkgName)` + showPaywall/hidePaywall. 각 페이지 initPage 뒤 호출(supplement은 TYPE 정해진 뒤).
- **서버 검증 기반**: 로컬 plan은 위조 가능하므로 GET /api/data의 plan_packages로 최종 판정. 로컬 캐시로 즉시 표시(구매자 UX) 후 서버 재확인해 미보유면 차단. 네트워크 실패 시 로컬 신뢰(정상 구매자 보호). **테스트 지급·결제 모두 서버 반영되어 통과**.
- 접근 매핑: 완주=해당 진행센터, 완주+보정추가=보정센터(완주에 보정1회 포함), 변제관리=별도(완주로 접근 불가). 매핑 로직 12케이스 node 검증 전부 PASS.
- privacy 제8항 이전에 결제내역 열람 갱신 완료(2차).
- ⚠️**한계**: GitHub Pages 정적 호스팅이라 HTML 소스는 공개됨 → 완벽한 DRM 아님(기술적 우회 가능). 정상 경로·일반 사용자에겐 게이트로 작동하며 "결제 안 하면 정상적으로는 못 봄" 목적 달성. 완전 차단은 콘텐츠를 Worker에서 동적 서빙하는 아키텍처 변경 필요(과함, 미채택).
- ⚠️**배포**: 정적 파일 변경(Worker 무관) → git push가 배포인데 **미공개라 보류**. 프로덕션 반영은 공개 시점. 지금은 로컬(live server)에서 동작.
- **실코드 E2E 검증 완료(2026-07-24, jsdom으로 실제 main.js 실행)**: 비로그인→잠금, 서버보유→통과, 서버미보유→잠금, **로컬 위조(서버엔 없음)→서버확인 후 잠금(위조 차단 실증)**, 정상구매→통과, 네트워크실패+로컬보유→관용통과, 네트워크실패+로컬없음→잠금. 오버레이 CTA 분기(비로그인=로그인+next, 로그인미구매=구매하기)까지 7/7 PASS. 실제 픽셀 화면만 미확인(브라우저).

### 2026-07-24 (2차) — 마이페이지 결제 내역 열람 ✅배포·E2E 검증

- `GET /api/payment/history` (Bearer) — 본인 결제 내역. status IN ('paid','refunded','test')만, 최신순 50건(미완료 pending/failed 제외).
- mypage.html: 결제 상태 카드 아래 '🧾 결제 내역' 섹션(내역 있을 때만 표시). 패키지명·결제일·주문번호·금액·상태뱃지(결제완료/환불됨/무료지급). 환불건은 취소선.
- privacy 제8항 열람권: "결제 내역은 이메일 요청" → "마이페이지에서 직접 확인"으로 갱신(정합).
- **E2E 검증**: 비로그인 401, 지급 전 빈 배열, 테스트지급 후 test 1건 정확 조회. 테스트 계정 정리(전 테이블 0건).

### 2026-07-24 — 로그인 무차별 대입(brute-force) 방어 ✅배포·E2E 검증

- `login_attempts(email, fail_count, window_start, locked_until)` 테이블(프로덕션 D1 반영).
- 정책: 최근 15분 윈도우 내 실패 5회 → 15분 잠금(상수 LOGIN_MAX_FAILS/WINDOW/LOCK). 성공 시 실패기록 삭제(리셋).
- handleLogin: 잠금 확인(429) → 인증 → 실패 시 recordLoginFail → 성공 시 DELETE. **존재하지 않는 이메일도 카운트**해 잠금 여부로 계정 존재가 새지 않게 함. user 없어도 `valid = user && verifyPassword`로 같은 실패 경로.
- **E2E 검증**: 5회 실패 후 6회째 429 잠금, 잠긴 동안 올바른 비번도 429(방어 정상), 3회 실패→성공 시 리셋되어 카운트 초기화 확인. 테스트 계정·잠금기록 정리(전 테이블 0건).
- 참고: IP 기준 제한은 미도입(개인정보·방침 고려) — 필요 시 Cloudflare 인프라 레벨 Rate Limiting으로 보완 가능. change-password·delete-account는 세션 필요라 브루트포스 표면 작아 미적용.

### 2026-07-23 (3차) — 자체 익명 분석(데이터 개선 도구) ✅배포·검증

목적: 이탈 구간·전환율 파악. GA4/Clarity 대신 자체 익명 집계 선택(사용자 결정) — 민감정보 사이트라 제3자·세션리플레이 리스크 회피, 방침 "제3자 안 씀" 유지.
- **완전 익명**: 개인·세션·IP 저장 안 함. `analytics(day, event, label, count)` 테이블에 날짜별 카운트만 UPSERT.
- `POST /api/analytics {event, label}` — 인증 불필요(익명). 이벤트는 화이트리스트(`ANALYTICS_EVENTS`)만 허용, label은 `[\w\-./]`만 남기고 40자 컷(오염·XSS 방지). 실패해도 조용히 무시.
- 수집 이벤트: pageview(페이지별), diag_step(진단 2·3·4단계 전진), diag_complete, pay_start, pay_complete.
- 프론트: `main.js track()` + initPage에서 pageview 자동. diagnosis.js nextStep/submit, pricing startPayment에 삽입.
- admin: '📈 이용 분석'(최근 30일) — 페이지뷰 상위8 + 진단 퍼널(시작→2·3·4단계→완료 이탈 막대) + 결제 전환율. overview 응답에 analytics 배열 추가.
- privacy 제9항: "익명 통계만 집계, 개인·IP 미포함, 제3자 미전송" 한 줄 추가.
- **검증**: 익명 전송 E2E — 정상 이벤트 정확 집계, 미허용 이벤트(evil_hack) 거부, injection 라벨 정제 확인. 테스트 데이터 정리(analytics 0건).
- ⚠️미결(별개): 유료 콘텐츠 페이지 결제 게이트 부재 — 공개 시 서버 게이팅 도입 여부 결정 필요.

### 2026-07-23 (2차) — 관리자 테스트 지급 기능 ✅배포·E2E 검증

목적: 개발 중/지인 테스트 시 실제 결제 없이 유료 패키지를 회원에게 부여.
- `POST /api/admin/grant {email, package}` — 회원 이메일로 패키지 지급. payments에 `status='test'` 기록(매출 SUM은 'paid'만이라 제외됨) + grantPackage 호출.
- `POST /api/admin/revoke-test {paymentId}` — 테스트 지급 회수. 포트원 취소 없이(실결제 아님) status='revoked' + 같은 패키지 다른 활성('paid'|'test') 없을 때만 revokePackage.
- admin.html: '🧪 테스트 지급' 카드(이메일+패키지 select+지급) + 결제내역에 test='테스트'/revoked='회수됨' 뱃지 + test건 '회수' 버튼.
- **E2E 검증**(admin 임시가입→grant→target서버 plan=premium 확인→overview에 test행 확인→revoke→plan 사라짐 확인→전 계정 정리, DB 0건): 전 경로 통과.
- ⚠️지급 후 대상 회원은 **다시 로그인해야** 반영(syncOnLogin이 서버 plan을 받아옴). admin UI 안내문에 명시.
- 참고: 유료 콘텐츠 페이지(rehabilitation/maintenance/supplement 등)는 **결제 진입 게이트가 없어** URL 직접 접근으로도 열림 — 테스트엔 편하나 공개 시점엔 서버 게이팅 도입 여부 별도 결정 필요(미결).

### 2026-07-23 — 이용약관·환불정책 실제 결제구조 정합 + 환불 백엔드 + 결제위조 차단 ✅배포·검증

배경: terms.html 제5조가 "실제 결제·판매가 이루어지지 않습니다"라는 낡은 거짓(포트원 실결제 구현·검증 완료 상태). 청약철회·환불 조항 자체가 없었음. 전자상거래법 정합성 점검에서 시작해 환불 백엔드까지 구현.

**1) 환불 정책 = "결제일 7일 무조건 전액 환불"로 확정(사용자 결정 2026-07-23)**
- 처음엔 "이용 개시 후 제한"으로 설계했으나 교차검증 2종이 뒤집음:
  - (법률) 전자상거래법 제17조②단서+제6항 후단: 디지털콘텐츠 철회제한을 유효화하려면 "표시"만으론 부족, **시험사용 상품(미리보기/체험판) 제공까지 함께** 필요. 없으면 이용개시 후에도 전액환불 의무.
  - (코드) "AI검토=이용개시" 판정이 서버 데이터로 불가(ai_usage는 날짜별 카운트뿐, 결제 연결·시각 없음). 게다가 AI검토는 plan 게이트 없는 **무료 기능**이라 유료 제공개시 지표로 부적절.
  - → 제한을 아예 주장하지 않는 7일 무조건 환불이 이 서비스 규모(1인·소량·AI무료)에 최적. 시험판·이용개시추적 불필요, 공정위 리스크 최소.

**2) terms.html 전면 개정(11개 조)** — 제4조 회원가입·계정, 제5조 유료서비스·결제(공급시기=결제 즉시), **제6조 청약철회·환불**(①7일 무조건 전액 ②하자·불이행 3개월/30일 제17조③ ③이메일 요청→청약철회일 3영업일 환급+지연배상 제18조 ④미성년자 취소권), 제10조 분쟁해결(소비자상담 1372·한국소비자원·전자거래분쟁조정), 제11조 운영자 정보(최은식, 사업자등록 후 상호·신고번호 기재 유보).

**3) 환불 백엔드(신규)** — `POST /api/admin/refund`(admin 전용): 포트원 V2 결제취소(`POST /payments/{id}/cancel`, 전액) → `payments.status='refunded'`+`refunded_at`(스키마 ALTER, 프로덕션 D1 반영) → `revokePackage`로 이용권 회수(plan_packages에서 제거, 대표 재설정 or 남은 것 없으면 plan* 전체 삭제). admin.html 결제목록에 환불 버튼 + refunded 뱃지. 매출 SUM은 status='paid'만이라 환불건 자동 제외.

**4) 결제 위조 차단(보안, 검증 중 발견)** — `Storage.save`가 모든 cdg_* 키를 서버로 올리므로, 콘솔에서 `cdg_plan='premium'` 심고 로그인하면 **결제 없이 서버에 프리미엄 부여**되는 취약점이 있었음. 서버 `handleSyncData` put에서 `PLAN_KEYS`(plan/plan_packages/plan_type/plan_package/plan_package_name) 거부 → 이용권은 결제 검증한 grantPackage/revokePackage만 쓰기 가능. 환불 후 로컬 plan이 재로그인 시 부활하던 문제도 이 방어로 함께 해결. ✅E2E로 위조 차단 확인.

**5) pricing.html** — 결제 전 confirm 고지 + 상시 환불안내 박스를 "7일 무조건 환불"로 단순화. 낡은 "정식 출시 준비 중" 메타 문구 제거.

**6) 재검증(adversarial) 반영** — 개정본을 다시 반박검증해 5개 신규 이슈 발견·처리:
- (A, 高) 포트원 취소 성공 후 DB UPDATE 실패 시 영구 불일치 → `portoneCancel` 헬퍼가 '이미취소'를 멱등 성공으로 처리(재클릭 502 루프 해소), UPDATE에 `WHERE status='paid'` 가드.
- (C, 中) 회당 상품(correction-*) 중복구매 시 1건 환불이 전체 회수 → 환불 후 "같은 user+package의 다른 paid 결제 수"를 세어 0일 때만 revokePackage.
- (#5) 탈퇴자 환불 경로 없음(payments CASCADE 삭제, archive 미조회) → handleAdminRefund가 payments에 없으면 `payments_archive` fallback 조회 후 포트원 취소 + archive.status='refunded'. ⚠️단 admin 대시보드는 아직 탈퇴자 결제를 목록에 안 띄움 → API는 payment_id로 호출 가능하나 UI 버튼은 없음(운영자가 D1에서 payment_id 찾아 호출, 또는 포트원 콘솔 수동). 후속 UI 과제.
- (B, 高) 환불 후 localStorage plan이 재로그인 시 부활 → **4)의 PLAN_KEYS 서버 가드로 이미 해결**(로컬 plan 올려도 서버 거부).
- (D, 低) 메인 환불 시 부가옵션이 대표로 승격 → 드문 엣지, 미수정(문서화).

**검증**: revokePackage 단위검증(단일→전체회수, 대표환불→재설정) + 권한차단 E2E(비로그인·비관리자 403) + **plan 위조차단 E2E 통과**(diagnosis_data는 저장, plan 계열 전부 거부 확인). **미검증(실카드 필요)**: 실제 포트원 결제→환불 버튼 전체 경로, archive fallback은 사용자가 실카드 1건으로 최종 확인 필요.

**gotcha**: Cloudflare Workers `wrangler deploy` 직후 몇 초 전파 지연 — 배포 직후 즉시 E2E 치면 옛 버전이 응답할 수 있음(재시도로 확인). 포트원 V2 취소 = `POST https://api.portone.io/payments/{id}/cancel` + `Authorization: PortOne {API_SECRET}`, body `{reason}`, amount 생략 시 전액.

**남은 것**: 실카드 결제→환불 최종 E2E(사용자), 로그인 시도 횟수 제한(미구현), 결제내역 열람 API(현재 이메일 요청 안내), 사업자등록 후 terms 제11조·통신판매 신고번호 기재.

### 2026-07-21 — 개인정보처리방침 전부 개정 + 회원탈퇴 기능 ✅배포·E2E 검증 완료

배경: 방침이 "서버를 운영하지 않는 정적 사이트, 서버로 전송·수집·보관되지 않음" 기준이라 백엔드 도입 후 **내용 전체가 사실과 반대**였음. 법 제30조·시행령 제31조 기준으로 재작성하고, 방침이 약속하는 권리(삭제권)를 실제로 행사할 수 있도록 탈퇴 기능을 함께 구현.

**1) 회원탈퇴 (신규)**
- `POST /api/auth/delete-account` (Bearer + `{password}` 재확인) — `DELETE FROM users` → FK CASCADE로 sessions·user_data·ai_usage·payments 동시 삭제.
- ⚠️ 삭제 **전에** 완료 결제(`status='paid'`)를 신설 테이블 `payments_archive`(users FK 없음)로 이관 — 전자상거래법 시행령 제6조 5년 보존 의무. CASCADE가 결제기록까지 지우는 문제를 막는 핵심.
- `mypage.html` 계정 설정에 탈퇴 UI(경고 + 비밀번호 + confirm 2단), `main.js` `Auth.deleteAccount()` + `DataSync.cancelPending()`(대기 중 동기화가 지운 데이터를 되올리는 것 방지).
- **프로덕션 E2E 검증**: 가입→데이터저장→오답비번 401→탈퇴 200→세션 무효 401→같은 이메일 재가입 성공. D1 직접 조회로 고아 레코드 0건 확인.

**2) 민감정보 서버 저장 중단 (중요)**
- 진단의 `hasHealthIssues`(건강상태)·`debtCauses`(의료비·도박 포함)가 서버 저장되고 있었음 = 법 제23조 민감정보 무단 처리.
- `DataSync.SENSITIVE_FIELDS` + `_strip()`로 **업로드 경로에서만 제거**(정기 flush·로그인 시 게스트 이관 양쪽). 계산은 브라우저에서 하므로 진단 결과는 그대로. 부작용: 다른 기기 로그인 시 이 두 항목은 옮겨지지 않음(방침에 명시).

**3) 서버 수정 2건** — 새 세션 생성 시 그 계정의 만료 세션 삭제(만료행 무기한 잔존 문제), `clearAll` 시 `ai_usage`도 삭제(삭제 창구 부재 문제).

**4) 방침(privacy.html) 14개 항 전면 재작성** — 처리목적/항목/보유기간/제3자제공/위탁/국외이전 3건 상세/파기/권리행사/자동수집장치/안전성조치/자동화된 결정/보호책임자/구제방법/변경이력. 개인정보 보호책임자 = **최은식**(법 제31조②상 개인 운영자는 본인이 당연히 CPO).

**5) login.html** — 가입 동의의 "이용약관" 버튼이 `alert('준비 중')` 스텁이었음 → 실제 terms.html·privacy.html 링크 + "만 14세 이상" 확인 문구로 교체.

**교차검증(에이전트 2종 상호반박)에서 잡아 고친 것**
- 코드 대조: "지정 도메인 외 요청 차단"(→CORS일 뿐), "마이페이지에서 결제내역 열람·설정에서 정정"(→그런 API 없음, 이메일 요청으로 정정), 진단 항목 과소열거, `profile`·게스트 소급이관·Tailwind CDN 국외전송 누락, 탈퇴해도 타 기기 localStorage 사본은 남음.
- 법령 대조: **위임장 근거 오류**(시행규칙 별지 제11호 ✗ → 시행령 제45조②+「개인정보 처리 방법에 관한 고시」 별지 제11호서식 ○), 열람 10일 근거(시행령 제41조④⑤) 명시하고 정정·삭제·처리정지는 "지체 없이"로 분리, **행정심판 문구 삭제**(공공기관 전용 조항), 카드사를 수탁자에서 제외(독자 처리자), 회원정보 "탈퇴 즉시 파기" ↔ 결제기록 이메일 5년 보관 모순에 단서 추가.
- 법 제30조①·시행령 제31조① 필수 기재사항 **전 항목 충족** 판정(민감정보 공개가능성·가명정보·국외직접수집은 해당없음으로 생략 적법).

**gotcha**: D1은 FK CASCADE가 켜져 있어 `users` 삭제 시 결제기록까지 사라진다 — 법정 보존 대상은 반드시 별도 테이블로 먼저 옮길 것. D1 실제 저장 리전은 API 응답의 `served_by_region`으로 확인 가능(현재 APAC/SIN) — 방침의 국외이전 국가 기재 근거.

**후속 과제(미착수)**: 결제내역 열람 API(현재 이메일 요청으로 안내 중), 로그인 시도 횟수 제한, terms.html 환불·청약철회 조항이 실제 결제 구조와 맞는지 점검.

### 2026-07-20 (3차) — 비밀번호 정책 강화

- **정책**: 8자 이상 + **영문·숫자·특수문자 모두 포함**. 서버(`passwordError` — validateSignup·change-password 공용)와 클라(`js/main.js` 동일 `passwordError` — login 가입·mypage 비번변경)에 이중 적용. 위반 시 구체적 사유 표시.
- **UI**: 가입·비번변경 화면 라벨/플레이스홀더에 "(8자 이상, 영문·숫자·특수문자 포함)" 안내.
- **검증**: 프로덕션에서 8자미만/영문없음/숫자없음/특수문자없음 각각 400 + 정책충족 200 확인.
- **이메일 중복체크**: 이미 있음(가입 시 409 "이미 사용 중인 이메일" + DB email UNIQUE). 추가 작업 불필요.
- **이메일 인증**: 미구현. 이메일 발송 서비스(Resend 등) 필요 → 도메인·실서비스 전환 시 password reset과 함께 도입 검토(find-account.html이 "이메일 인증 준비 중"으로 안내 중).

### 2026-07-20 (2차) — 관리자 페이지(운영 대시보드) ✅배포·검증

- **`admin.html` 신설**(noindex·nofollow, 네비 미노출 — 운영자가 URL 직접 접근): 통계카드(전체회원·오늘가입·결제완료건·누적매출·AI검토누적) + 결제내역 표(최근100, 일시·이메일·패키지·금액·상태badge) + 회원목록 표(최근100). 미로그인/비관리자는 "접근 권한 없음" + 로그인 링크.
- **서버**: `requireAdmin`(세션 이메일 === `ADMIN_EMAIL`) + `GET /api/admin/overview`(통계+회원100+결제100 한 번에). 프로덕션 시크릿 `ADMIN_EMAIL=eslje75@gmail.com` 설정.
- **검증**: 관리자 200(통계·목록 정상)/비관리자 403/무인증 403 — 로컬 통과 + 프로덕션 게이팅(403) 확인.
- ⚠️ **운영자는 `eslje75@gmail.com` 계정을 반드시 (강한 비번으로) 가입해야 관리자 접근 가능.** 그 이메일로 로그인한 세션만 admin API 통과. 공개 전 선점 가입 권장.
- **미커밋**(직전 커밋 9807271 이후 admin.html·index.js·PROGRESS 변경분).

### 2026-07-20 — 백엔드 Phase 4: 결제(포트원 PortOne V2) ✅실결제 E2E 검증 완료

- **자격증명 설정 완료(프로덕션 시크릿)**: `PORTONE_STORE_ID`(store-…), `PORTONE_CHANNEL_KEY`(channel-key-…), `PORTONE_API_SECRET`(값은 포트원 콘솔에서만 확인). 테스트 채널 = 토스페이먼츠 테스트. 설정 중 삽질 교훈: ①V1 아닌 **V2** 채널이어야 store-/channel-key- 형식 ②**채널 키**(포트원 생성)와 PG **클라이언트 키**(test_ck_) 혼동 주의 ③API Secret은 **연동정보→식별코드·API Keys→V2**에 있고, **복사 버튼**으로 전체 복사(드래그하면 …로 잘림→401).
- **실결제 E2E 성공(토스 테스트모드, 실제 무출금)**: 결제창 호출→카드결제→서버 complete가 포트원 조회로 status=PAID+금액 149000/29000 일치 검증→`maintain` 패키지 부여(plan=premium, plan_packages=["maintain"])→마이페이지 "이용 중" 표시까지 전 과정 확인. 최초 시도는 "해지된 카드" PG거부로 FAILED(정상 동작, 카드 문제)—토스 테스트는 별도 테스트카드 없이 실제 유효카드 입력(무출금).
- **프론트 버그 수정**: 결제 실패 시 조용히 삼키던 것 → `payRes.message` 그대로 표시.
- **정리 완료**: 임시 디버그 엔드포인트·PAY_DEBUG 제거, main.js API_BASE 원복(localhost 분기 복원), 로컬 정적서버 종료, 테스트 데이터 전량 삭제(users/payments/user_data=0).
- **⚠️ 실서비스 전환 시**: 사업자등록 후 포트원 콘솔에서 **라이브 채널** 연결→라이브 store/channel/secret로 시크릿 교체. 현재는 테스트채널이라 실결제 안 됨.

### 2026-07-19 (5차) — 백엔드 Phase 4: 결제(포트원 PortOne V2) 코드 완성·구조검증 (완료, 위 5차→최종은 7/20 항목)

- **PG 결정(사용자)**: 포트원(PortOne) — 여러 PG·간편결제(카카오/네이버페이)를 하나의 연동으로 확장 가능. V2 SDK 사용.
- **흐름**: 브라우저 `PortOne.requestPayment()` → paymentId → 서버가 `GET api.portone.io/payments/{id}`(API Secret)로 **status=PAID + amount.total 일치 검증** → 패키지 부여. 금액은 서버 `PACKAGES` 표가 소스(클라 금액 불신, 위변조 방지).
- **DB**: `payments(payment_id PK, user_id, package, amount, status pending|paid|failed, created_at, paid_at)`.
- **API**: `POST /api/payment/prepare`(Bearer — 주문 생성, 서버가 paymentId·금액 확정, storeId·channelKey 반환) / `POST /api/payment/complete`(Bearer — 포트원 조회·검증·확정, 멱등 처리, `grantPackage`가 user_data의 plan·plan_packages·plan_package(_name)·plan_type에 부여 — 부가옵션 correction-*는 대표 패키지 안 덮음).
- **프론트(`pricing.html`)**: 목업 모달 제거. `mockPayment/mockComplete` → `passesPurchaseGuards`(자격·오구매 가드 전량 보존) + `startPayment`(로그인 게이트 → prepare → PortOne 결제창 → complete → 서버가 준 plan 로컬 반영 → 패키지 페이지 이동). SDK `https://cdn.portone.io/v2/browser-sdk.js`.
- **가격**: rehab-full 149,000 / maintain 29,000 / bankrupt-full 49,000 / correction-rehab·correction-bankrupt 각 19,000 (기존 pricing.html과 동일, 서버 PACKAGES에 확정).
- **구조검증 통과**: 무인증401 / 알수없는상품400 / prepare200(paymentId·서버확정금액149000·storeId 반환) / complete 더미시크릿502 / **남의 주문 404(소유권)** / 서버 금액권위 확인. 전부 로컬. 프로덕션 DB 청결(users/payments/user_data=0).
- **활성화 남은 작업(자격증명 3개 필요)**: 포트원 콘솔에서 ① 테스트 채널 연결(토스/이니시스 등 테스트 PG) ② Store ID·Channel Key·V2 API Secret 확보 → 프로덕션에 `wrangler secret put PORTONE_STORE_ID / PORTONE_CHANNEL_KEY / PORTONE_API_SECRET` 설정. 설정 전엔 prepare/complete가 503으로 정상 실패. 실결제는 사업자등록 후 라이브 채널로 교체.

### 2026-07-19 (4차) — 백엔드 Phase 3: AI 서류검토 실제 Claude API ✅배포·실검증 완료

- **목업 제거**: `ai-review.html`의 `runReview()`가 규칙 기반 키워드 검사 → **실제 Claude API 호출**로 전환. 목업 흉내 금지 방침대로, 실패 시 옛 키워드 검사로 위장하지 않고 정직한 오류만 표시.
- **법적 안전 설계(변호사법 리스크 대응)**: Worker 시스템 프롬프트가 AI를 '서류 완성도 점검 보조자'로 엄격 제약 — 법률자문·결과예측·법적결론 금지, 누락·불명확·수치불일치·형식·개인정보노출만 지적. 응답은 {summary, issues[], confirmed[], suggestions[]} JSON.
- **엔드포인트**: `POST /api/ai/review`(Bearer 필수). 입력 30~6000자. `ai_usage(user_id, day, count)` 테이블로 **일 30회 제한**(비용·남용 방지, 성공 시에만 증가). 프론트는 로그인 게이트(게스트는 로그인 유도) + 주민번호 패턴 사전 경고 + 정적 작성팁·세트 진행률 유지.
- **모델**: `AI_MODEL = 'claude-sonnet-5'` — **검증 완료**. ⚠️ 이 계열은 **assistant 프리필 미지원**(400 "does not support assistant message prefill"). 그래서 JSON 프리필 기법 제거하고 messages는 user로 끝냄 + 응답에서 text 블록만 뽑아 JSON 파싱(코드펜스 대비 {…} 정규식 폴백). `ANTHROPIC_VERSION='2023-06-01'`.
- **API 키**: 사용자가 발급해 `wrangler secret put ANTHROPIC_API_KEY`로 프로덕션 시크릿 설정 완료(Cloudflare 암호화 보관, 코드·깃 미노출). 만료 없음 + 월 지출 한도 권장.
- **실검증(프로덕션, 실제 과금)**: 진술서 초안(일부러 모호 요소 포함)으로 호출 → HTTP 200, ~12.6초. AI가 "대략 6천만원→정확금액 필요"·모호한 날짜·채권자명 누락 정확히 지적, 마스킹 인식, **결과예측·단정 표현 0건(법적안전 통과)**. 구조 테스트도 전부 통과(401/503/400/413/502). 검증 계정·디버그 시크릿 전부 삭제(users/ai_usage/sessions=0).
- **관찰**: 응답 ~12초로 다소 김(버튼에 "AI가 검토 중입니다..." 표시로 대응). 필요 시 모델을 Haiku로 낮추면 비용↓·속도↑(정확도 트레이드오프).

### 2026-07-19 (3차) — 백엔드 Phase 2: 사용자 데이터 서버 저장 ✅배포·검증 완료

- **설계 원칙**: 기존 동기 localStorage를 **캐시로 유지**하고 로그인 시에만 서버 미러링 — 수십 개 `Storage.load` 호출부를 async로 안 바꿔도 되고 게스트도 그대로 작동.
- **DB**: `user_data(user_id, key, value TEXT=JSON, updated_at, PK(user_id,key))` 신설(프로덕션+로컬 적용).
- **API 추가**(`api/src/index.js`): `GET /api/data`(전체 조회 {key:value}), `POST /api/data/sync`({put:{k:v}, del:[k], clearAll} 배치 upsert/삭제, D1 batch=트랜잭션). 한도: 키 64자·값 100KB·요청당 60키·본문 512KB. 전부 Bearer 필수.
- **프론트(`js/main.js`)**: `Storage.save/remove`가 로컬 즉시 반영 + `DataSync`로 700ms 디바운스 백그라운드 푸시(로그인 시만). `pagehide`에 keepalive 플러시로 이탈 시 유실 방지. **`DataSync.syncOnLogin()`**(로그인/가입 직후 자동): 서버에 데이터 있으면 **서버 우선**(로컬 앱데이터 교체 — 다른 기기·이전 게스트 잔여 제거), 서버 비어있으면 **게스트 로컬데이터를 계정으로 이관**. auth 키(`cdg_auth*`)는 동기화 제외. `mypage` 초기화는 `DataSync.clearServer()`로 서버까지 삭제.
- **검증**: 로컬 통합 10/10(게스트→가입 이관, 기기A↔B pull, 서버 우선 재로그인, 로그아웃 중 게스트저장 서버격리, clearServer) + 프로덕션 스모크 8/8(한글값 보존·put/del/clearAll·무인증401). 검증 계정 전부 삭제(users/data/sessions=0).
- **미완**: 페이지 로드마다 실시간 pull은 미구현(로그인 시점 동기화로 커버 — 같은 세션 유지 중 타기기 변경은 재로그인 때 반영). Phase 3(AI검토)·4(결제) 남음.

### 2026-07-19 (2차) — 파비콘 자산 보강 + 도메인 방침

- **파비콘 완비**: 기존 `favicon.svg`(전 페이지 연결됨)에 더해 `apple-touch-icon.png`(180, iOS 홈화면·full-bleed), `icon-192.png`·`icon-512.png`(PWA), `favicon.ico`(16+32 멀티, 구버전·크롤러) 생성. `manifest.json` 신설(테마색 #533afd). **20개 전 HTML head에 apple-touch-icon·manifest·favicon.ico·theme-color 링크 삽입**(404.html은 `/debt-guide/` 절대경로, 나머지는 상대). sharp+png-to-ico로 SVG에서 렌더(도구는 scratchpad, 프로젝트 미포함).
- **도메인 방침(사용자 결정 2026-07-19)**: 브랜드 romanization = **`chamroad`**(API·D1과 일치). 사이트 비공개(push 보류) 중이라 **구매는 공개 시점에**. 이름만 확정, 연 비용 절감. 공개 시 `chamroad.co.kr`(또는 .kr/.com) 구매 → GitHub Pages(CNAME) + `api.chamroad.*`를 Cloudflare Worker 커스텀 도메인으로 연결(Worker가 이미 Cloudflare라 간단). 구매 시 main.js `API_BASE`도 커스텀 도메인으로 교체.

### 2026-07-19 — 백엔드 착수: Cloudflare Workers + D1 인증 API (Phase 1) ✅로컬 검증 완료

- **결정(사용자)**: 스택 = Cloudflare Workers + D1 / 결제 = 토스페이먼츠 테스트 모드로 개발(사업자등록 후 라이브 키 교체) / 순서 = 회원·인증 → (다음) 데이터 저장 → AI 검토 → 결제.
- **인프라**: D1 `chamroad` 생성(id `a3de34d7-6d87-4f7e-abc0-8a1a3c5d6827`, APAC). 프로덕션 DB에 스키마 적용 완료 — `users`(PBKDF2 해시), `sessions`(토큰 SHA-256 해시만 저장).
- **`api/` 신규(Worker 프로젝트)**: `wrangler.jsonc`(D1 바인딩) / `schema.sql` / `src/index.js` — 엔드포인트: signup·login·logout·me·change-password. 보안: PBKDF2-SHA256 10만회 + PEPPER(서버 시크릿, HMAC 프리해시), 세션토큰 32바이트(DB엔 해시만), 로그인 실패 메시지 통일(이메일 존재 노출 방지), CORS 화이트리스트(github.io + localhost 3456/5500), timingSafeEqual, 비번 변경 시 타 세션 전체 무효화. `.dev.vars`(로컬 PEPPER)는 gitignore.
- **프론트 교체**: `js/main.js` Auth 객체 → API 연동(login/signup/changePassword는 async fetch, 세션 캐시는 localStorage `cdg_auth_session`에 유지해 getSession/isLoggedIn 동기 조회 보존, logout은 로컬 즉시+서버 백그라운드). base64 비밀번호 저장(`cdg_auth_users`) 폐기. `login.html` doLogin/doSignup async 전환+중복제출 방지. **`API_BASE` 프로덕션 URL은 배포 후 교체 필요(`CHAMROAD_API_URL_TODO` 플레이스홀더)**.
- **find-account.html 전면 교체**: 이름+이메일만으로 비밀번호 재설정은 서버에선 계정탈취 경로라 제공 안 함(목업 금지 원칙) → "이메일 인증 방식 준비 중 + 문의(eslje75@gmail.com) + 로그인 상태면 마이페이지에서 변경" 정직 안내로 재작성. **mypage.html에 계정 설정 카드(비밀번호 변경 UI) 신규** — change-password API 실연동.
- **검증**: wrangler dev 로컬에서 curl 11개 시나리오(가입/중복409/오입력401/로그인/me/로그아웃/비번변경/옛비번 거부/검증400/CORS 허용·차단) + UTF-8 한글 저장 + **실제 main.js Auth 코드를 node로 로드해 로컬 API 상대 통합 테스트 7항목 전부 통과**.
- **✅ 배포 완료(2026-07-19)**: `npx wrangler login`(인증됨) → 프로덕션 PEPPER 시크릿 설정(로컬과 다른 새 값) → `npx wrangler deploy`. **엔드포인트: `https://chamroad-api.eslje75.workers.dev`**. main.js `API_BASE`를 이 URL로 교체 완료(localhost는 8787 dev로 자동 분기). 프로덕션 D1에 스키마 반영 확인(users/sessions/idx). **실배포 검증 통과**: 가입/로그인/me/틀린비번401/CORS 허용헤더/HTTPS + node fetch로 한글(홍길동) 왕복 정상 확인. 검증용 테스트 계정은 프로덕션 DB에서 삭제 정리함(현재 users 0건).
- **배포 계정**: Cloudflare `eslje75` 계정. Worker 이름 `chamroad-api`. 재배포는 `cd api && npx wrangler deploy`. 시크릿 변경은 `npx wrangler secret put PEPPER`(변경 시 기존 비밀번호 검증이 전부 깨지므로 주의 — 초기 운영 중엔 건드리지 말 것).

### 2026-07-18 (4차) — 결핍 감사 반영: 생활·불안 안내 대폭 보강 (gap-auditor 결과)

- **배경**: 결핍 감사 결과 "절차 안내는 탄탄한데 신청서 바깥의 삶(비용·노출·생계·진행 중 제약)이 비어 있음" → 우선순위 G2→G1→G3→G4 + G5~G9 FAQ 묶음으로 반영. **모든 수치·법리 공식 출처 검증**(easylaw·scourt·law.go.kr·서울회생법원 실무).
- **검증한 핵심 사실**: 회생 인지대 30,000원(전자 27,000)·송달료 1회 5,500원×채권자비례·외부 회생위원 예납금 15만(서울 등 내부위원 없음), 파산 인지대 소액·관재인 예납금 30만·소송구조 가능 / 급여압류 1/2·최저 250만 보호(민집 제246조)+압류금지채권 범위변경 신청 / 편파변제·신규대출·재산은닉 = 면책불허가·파산취소 사유 / 법원은 회사에 통지 안 함·등본/가족관계증명서에 회생파산 기록 안 남음·면책정보 신용정보원 7년 / 파산 자격제한 직업(변호사·회계사·법무사·공인중개사·보험설계사·경비원 등, 일반직원·공무원은 원칙 결격 아님)·면책 시 당연복권(제574조) / 기각 시 즉시항고 1주 불변기간·재신청·파산전환 / 해외여행 금지규정 없음(파산은 관재인 면담·기일 출석 협조) / 신용회복 시점(회생 인가후 1년 성실변제 공공정보 조기삭제, 파산 면책 5년 후 삭제).
- **G2 준비 중 금지행동 경고 박스**: rehabilitation·bankruptcy 상단(0단계 카드 다음)에 빨강 박스 — 편파변제(특히 가족·지인 먼저 갚기)·신규대출/카드·재산은닉/가족통장 급여수령 금지 + "모든 상환 멈추고 목록부터" 원칙.
- **G3 비용**: r7-4·b7-4 가이드에 검증 금액 반영(수임료 없이 법원비용만·소송구조 안내). FAQ에 상세 비용 항목.
- **G1 압류 대응**: setup 맞춤결과(압류 페르소나)에 급여 1/2·250만 보호+범위변경 신청 추가. FAQ 전용 항목.
- **resources.html FAQ 14→23개·4개 카테고리 개편**(제도 이해3/자격·비용6/진행 중 생활·상황8/결과·이후6): 신규 9문항 — 비용, 회사·가족 노출·등본 기록, 급여/통장 압류 생계, 이사, 해외여행, 이직/사업자, 파산 직업제한, 상속·재산 증가 신고, 기각 대처, 신용 회복 시점, 전자소송 접수 방법. 렌더를 카테고리 그룹으로 변경.
- 검증: 4파일 구문 통과, FAQ 23개·카테고리 4·필드 누락 0·toggle 인덱스 0~22 고유, 진행센터 항목수 37/31 유지(회귀 없음). gap-auditor 에이전트는 .claude/agents에 상주.

### 2026-07-18 (3차) — 관할법원 안내·찾기 기능 (사용자 요청)

- **질문(사용자)**: 관할법원은 등본상 주소지 기준? 이용자가 자기 관할법원을 알 수 있는 안내 필요.
- **법률 검증(공식 출처)**: 관할 = 채무자의 보통재판적(=주소, 실무상 주민등록상 주소) 소재지 관할 **지방법원 본원·회생법원 전속**(채무자회생법 제3조, easylaw 확인). 지원(支院)은 접수 안 함 — **춘천지법 강릉지원만 예외**(강릉·동해·삼척·속초·양양·고성). **2026. 3. 1. 대전·대구·광주 회생법원 개원 → 회생법원 6곳**(법률신문·각급법원설치법 개정 확인). 선택 특례: 울산·경남→부산회생, 충북→대전회생, 전북·제주→광주회생에도 신청 가능. 수원회생법원 관할 = 경기 남부 19개 시·군, **부천·김포는 인천지법**(공식 관할구역 확인).
- **구현**: `js/main.js`에 `COURT_FINDER_DATA`(17개 시·도, 경기/강원/경남은 시·군 2단계 선택) + `initCourtFinder()` 공용 컴포넌트 — 지역 선택 → 관할법원 + 선택 특례 표시 + 대법원 관할법원찾기 링크 + "주민등록상 주소 기준·전입신고 먼저" 주의. **rehabilitation·bankruptcy·setup 3개 페이지에 마운트.** r7-5·b1-2 가이드도 본원 원칙·강릉지원 예외·회생법원 6곳 기준으로 갱신.
- 검증: 매핑 데이터 node 실행 검증(17개 시·도, 신설 회생법원 3곳, 부천·김포=인천, 강릉지원, 특례 4종, 양산=울산) + 구문 검사 통과.

### 2026-07-18 (2차) — setup 질문지 모순 조합 방지 (사용자 지적)

- **지적(사용자)**: "소득 없음"인데 "재직 중 예"처럼 논리적으로 불가능한 답 조합이 가능했음.
- **수정(`setup.html`)**: 소득 형태에 따라 무관한 질문 자동 숨김 — 재직 여부(퇴직금)는 **급여소득일 때만**, 부수입은 **'소득 없음'이 아닐 때만** 표시(`syncConditionalQuestions`). 숨겨진 질문은 값도 '아니요'로 강제(collectAnswers에서 이중 방어), 필수 검증도 보이는 질문만 요구. 보이는 질문만 1부터 동적 번호 재부여(.q-card/.q-num). 프리필(프로필·무료진단) 후에도 동기화 실행 — 진단에서 퇴직금을 입력했어도 소득 없음이면 재직=아니요로 정리됨.

### 2026-07-18 — 진행센터 온보딩 개편: 맞춤 준비 진단(setup.html) + 단계 로드맵 (사용자 요청)

- **요청(사용자)**: 처음 하는 사람은 "뭐부터 어떻게?"가 안 보임 → ① 단계 안내를 버튼 형태로 만들어 단계별로 안내 ② 진단 없이 결제한 사용자도 자기 상황을 입력해 필요한 서류를 확실히 알게 하는 사전 질문지. 상호 논의로 3가지 확정: 로드맵+한 단계씩 표시 / 별도 페이지 공용 / 명칭 "맞춤 준비 진단"(AI 표현 안 씀 — 규칙 기반이라 정확성 보장, 목업 흉내 금지 방침 유지).
- **`setup.html` 신규(맞춤 준비 진단, ?type=rehab|bankrupt)**: 질문 7개(+파산 전용 2개: 폐업·2년내 처분) — 소득형태(5종)/재직(퇴직금)/보유재산(멀티)/개인간채무/담보채무/법적조치/부수입. **무료진단 데이터로 프리필**(incomeType·severanceValue·재산값·hasLegalAction·securedDebt), 기존 프로필 있으면 그것 우선. 저장 시:
  - `computeNA()` 규칙 매핑 → 서류센터(d-i1~4·d-a2·a3·a5·d-d3)와 체크리스트(r2-3·r3-2·r7-6·b3-3)에 **'해당없음' 자동 반영**(`applyNA`: 미처리 항목만 na, 이전 자동처리분 중 이번에 해당하게 된 것은 해제, 사용자가 직접 체크/처리한 것은 불가침)
  - 맞춤 결과: 요약 칩 + "준비 안 해도 되는 항목 N건" 목록 + "특히 챙길 것"(압류→중지·금지명령, 재직→퇴직금 1/2, 폐업→폐업사실증명원 등) + 진행센터/서류센터 CTA
  - `cdg_profile` 저장(autoNA 기록 포함). noindex.
- **진행센터 2종 개편(rehabilitation·bankruptcy)**: 레이아웃 = 히어로 → **0단계 카드**(프로필 없으면 "내 상황 설정하고 시작" 강조 CTA / 있으면 요약 칩+수정 링크) → 진행률 바 → **진행 단계 지도**(9/8단계 버튼 그리드: 단계명·n/m·완료✓ 초록·현재 단계 강조) → **선택된 한 단계만 체크리스트 표시**(이전/다음 단계 버튼, 진입 시 첫 미완료 단계 자동 선택) → 도구 바·작성예시(하단 이동). 체크/해당없음 시 로드맵 뱃지 실시간 갱신.
- **mypage**: 초기화 목록에 'profile' 추가. CLAUDE.md 페이지 표에 setup.html 추가.
- **검증(DOM 실행)**: setup NA 매핑·재적용 시나리오(압류 없음→있음 변경 시 r7-6 자동 해제, 사용자 체크 r2-1 유지), rehab 로드맵 9버튼·첫 미완료 단계 자동 선택·gotoStep 이동·체크 저장·진행률 4/37·전 항목 처리 시 100%, bankrupt 8버튼·CTA·이동 — 전부 통과. 구문 검사 4파일 통과.

### 2026-07-17 (5차) — 진행센터 ↔ 서류 체크센터 역할 분리 (중복 축소, 사용자 지적)

- **지적(사용자)**: 셀프 진행센터의 1~4단계(기본정보·채권자·소득·재산 준비)와 서류 체크센터가 같은 서류를 두 번 체크하는 것처럼 중복돼 보임. 왜 두 화면인지·구분이 필요한지 질문 → 상호 검토 후 "역할 분리 + 중복 축소" 방향 채택.
- **정리 원칙**: **서류 발급은 서류 체크센터가 전담**(발급처·공식 양식·발급 방법), **진행센터는 전체 절차 순서 + 서류센터에 없는 진행 고유 작업**(채권자 목록 작성, 소득 산정, 청산가치 계산, 진술서 경위, 서명 준비 등)만 담당. 진행센터의 순수 서류-발급 항목은 "서류 체크센터에서 발급·체크" 위임 항목으로 대체.
- **`rehabilitation.html`(회생 45→37항목)**: 1단계=기본정보&서류준비(발급 위임+주소최신화+서명, 3), 2단계=채권자 목록 작성(목록·금액발생일·개인간채무, 3), 3단계=소득 정리(평균산정+근로외소득, 2), 4단계=재산·청산가치 정리(재산목록화+청산가치계산, 2). 5~9단계(수입지출/작성/최종체크/보정/면책)는 그대로. 각 위임 항목에 documents.html 링크.
- **`bankruptcy.html`(파산 36→31항목)**: 1단계=기본정보&서류준비(발급 위임+관할, 2), 2단계=채무 발생 경위(진술서, 4 — 진행 고유라 그대로), 3단계=채권자 목록 작성(4, b3-2만 발급 위임으로 문구 조정), 4단계=재산·처분이력(재산목록화+처분이력, 2). 5~8단계 그대로.
- **`mypage.html`**: 진행률 분모 회생 45→37, 파산 36→31로 갱신(REHAB_STEPS/BANKRUPT_STEPS 실제 항목 수와 일치). 진행센터 상단 도구 안내 문구를 "이 체크리스트=진행 순서 / 서류 발급=서류 체크센터"로 역할 명시(회생·파산 양쪽).
- **검증**: node로 항목 수 실측(회생 37=3+3+2+2+4+6+6+7+4, 파산 31=2+4+4+2+4+5+5+5, mypage 공식과 일치·id 중복 0), 회생 진행센터 인라인 스크립트 DOM 실행(필수 29 체크 78% → 선택 8 해당없음 100% 도달 → 해제 시 복귀, 정상), 3개 파일 구문 검사 통과, #example-library 크로스링크 유지, 삭제 항목 id 외부 참조 0건. 김○민 청산가치 예시(10,260,000)·박○수 합계(87,400,000) 일관성 유지.

### 2026-07-17 (4차) — '전체 서류/유형 보기' 토글 제거 (사용자 지적)

- **지적(사용자)**: 서류 체크센터에서 회생 패키지 이용 중인데 '전체 서류 보기'로 파산 서류까지 볼 수 있는 건 의미 없고 혼란만 줌 → 구매한 절차 서류만 보이면 됨.
- **`documents.html`**: '전체 서류 보기' 토글 버튼·`showAll` 상태·`toggleShowAll()` 제거. `visibleDocs()`는 구매 절차 기준으로만 필터, `applyPkgUI()`는 반대 절차 탭·양식 링크를 항상 숨김. 안내 문구(💡 ○○ 패키지 이용 중)는 유지.
- **`ai-review.html`**: 동일한 '전체 유형 보기' 토글도 같은 이유로 제거(`showAllGroups`·`toggleShowAllGroups()`), `visibleGroups()`/`applyPkgNotice()` 단순화. 보정 패키지(correction-*)는 기존대로 보정·소명 + 해당 절차 유형 표시.
- **미구매 사용자**: 기존과 동일하게 전체 표시(안내 배너 없음) — PROC_FILTER null 분기 유지.
- 구문 검사 통과, 잔존 참조 0건.

### 2026-07-17 (3차) — 플로팅 '임시저장' 버튼 추가 (사용자 요청)

- **요청(사용자)**: 셀프 진행 중 스크롤 어느 위치에서든 임시저장 버튼이 보였으면 함(준비할 게 많아 수시 저장 필요).
- **구현**: `js/main.js`에 `initTempSave(getStatus)` 공통 함수 신설 — 화면 **좌하단 고정** 플로팅 pill 버튼(스크롤탑=우하단·토스트=우하단과 비겹침). 클릭 시 저장 확인 → 초록 "저장됨"으로 1.8초 전환 + 토스트 `진행 상황을 저장했습니다 · N/M 처리 완료` + 마지막 저장 시각(HH:MM) 버튼에 표시. `css/styles.css`에 `#temp-save-btn` 스타일(+모바일).
- **연결**: 체크리스트 5개 페이지(`rehabilitation`·`bankruptcy`·`maintenance`·`supplement`·`documents`)가 초기 렌더 직후 `initTempSave(() => countProgress(...))` 호출(documents는 visibleDocs/docHandled 기준). 비체크리스트 페이지에는 미노출.
- **참고**: 체크리스트는 이미 항목 변경 시 자동 저장되므로 이 버튼은 **저장 확인·안심 + 저장 시각 표시** 용도(데이터 유실 없음). DOM 실행 테스트로 버튼 생성·클릭·토스트·시각 기록 확인, 6개 파일 구문 검사 통과.

### 2026-07-17 (2차) — 진행률 '해당없음' 처리 도입 (조건부 항목 100% 도달 불가 버그, 사용자 지적)

- **문제(사용자)**: 셀프 진행센터 체크리스트에서 근로소득자는 '자영업자 소득증명', 무주택자는 '부동산 등기' 같은 **'해당 시 필수' 항목을 체크할 수 없는데** 진행률 분모(전체 항목)에는 포함돼 **아무리 준비해도 100%에 도달 불가**. 재산자료 등 해당 없는 항목도 동일.
- **해결**: 필수가 아닌 항목('해당 시 필수/권장', '권장')에 **"해당없음" 버튼** 추가 → 저장값을 `true`(체크)/`'na'`(해당없음) **3-state**로 확장. 체크 또는 해당없음이면 '처리 완료'로 진행률에 카운트. 기존 데이터(`true`)와 호환. '해당없음' 처리 시 체크박스 비활성+뱃지 표시.
- **적용 파일**: 체크리스트 4종(`rehabilitation`·`bankruptcy`·`maintenance`·`supplement`) — `isOptional`/`stateOf`/`countProgress`/`toggleNA` 공통 패턴. `documents.html` — 조건부 서류 10종(부동산 등기·차량·임대차·자영업 소득증명·근로 소득자료·면책신청서 별도·대출약정서·압류서류)에 `opt: true` 부여 후 동일 처리, 카운트 라벨 '준비 완료'→'처리 완료'. `styles.css`에 `.checklist-item.na` 스타일. `mypage.html`: `calcProgress`가 `true||'na'` 카운트(진행률 카드 100% 도달 가능), `calcChecks`(서류 '준비 완료' 수)는 `=== true`만(해당없음 제외). 각 페이지 안내문에 "해당없음 버튼으로 넘기면 진행률에 반영" 문구.
- **조건부 항목 수**: rehabilitation 12(선택)/45, bankruptcy 5/36, maintenance 3/16, supplement 5/13, documents 10(opt)/30.
- **검증**: rehabilitation 인라인 스크립트를 실제 DOM 스텁으로 구동 — 필수 33개만 체크 시 **73%에서 멈춤(버그 재현)** → 선택 12개 해당없음 처리 시 **100%(45/45) 도달** 확인, 해당없음 해제 시 98%로 복귀·저장값 삭제 정상. 전 페이지 100% 도달 시뮬(node)·6개 파일 구문 검사 통과.

### 2026-07-17 — 2차 검증(일관성·법률 전수·UI 무결성) 완주 + 확정 14건 수정

- **2차 워크플로우 완주(에이전트 11개, 오류 0)**: 탐색 3종(일관성/법률 전수/UI 무결성) → P0/P1 8건 반박 검증. 결과: CONFIRMED 6(중복 제외 5) / **REFUTED 2** / P2 10. 법조문 20여 개·수치·링크·핸들러·Storage 키 등 40+ 항목 "이상 없음" 전수 확인(2026 기준중위소득 고시값, 압류금지 250만원 시행령, 예납금 30만원 실무준칙 등 공식 출처 대조 포함).
- **반박 검증이 걸러낸 오탐 2건(수정 안 함)**: ① 제619조 변제계획 변경에 사정변경 요구는 '실무 관행'이라는 지적 → 대법원 2018마6364 결정상 법적 요건으로 확립 — 현재 문구가 정확 ② 신복위 "최대 10년" 지적 → 프리워크아웃·신속채무조정은 일반 채무자도 10년이라 기관 카드 기준 현행 표기가 맞음. **반박 없이 고쳤다면 오히려 부정확해졌을 사례.**
- **P1 수정 5건**: ① result.html 조문표기 — "제579조(변제계획 인가 요건)" → 제579조 4호(가용소득)+제614조(변제계획의 인부) 2줄로 정정 ② rehabilitation r8-2 및 8단계 예시 제목 '보정명령'→'보정권고'(회생 트랙 용어 통일) ③ **about.html footer-placeholder 누락** — 법률대리 아님 고지·약관 링크가 이 페이지만 미렌더였음(+toast div 추가) ④ pricing 오구매 confirm이 원점수만 봐서 하드블록(신청불가) 사용자를 놓침 → rehab-full/bankrupt-full에 한해 diagnosis_data 기반 하드블록 전용 confirm 우선 표시(maintain·correction은 진행 중 고객이므로 제외 — 반박자 지적 반영) ⑤ mypage 소액 채무(1,000만 미만) 분기 — result와 동일하게 배지·변제액 숨기고 신복위 안내로 대체.
- **P2 수정 9건**: plan_type도 plan_package와 동일 보호(추가옵션 구매 시 미덮어쓰기), mypage 진행률 카드를 plan_packages 트랙 기준으로(양쪽 보유 시 둘 다 표시), compare 회생 기간 "신청~인가 통상 5~9개월"로 pricing 산식과 정합, 신복위 해제조건 "완제 시 즉시·1년 이상 성실상환 시"(ccrs 확인), index 수임료 비단정화, submitDiagnosis 전 단계 재검증(goStep 우회 차단), 죽은 코드 제거(updateProgress, toggleAllSections/expand-all 참조), mypage initPage('home')→('mypage') 네비 하이라이트 수정, maintenance 김○민 사례 날짜 플레이스홀더화(시간선 모순 해소).
- **P2 잔여(선택)**: maintenance 제619조에 판례 병기, compare 신복위 10년에 제도별 주석 — 반박자 의견상 현행도 정확해 보류.
- 검증: 페르소나 계산 재현·전 수정 파일 인라인 스크립트 구문검사·45/36 항목 대조 통과. **커밋 완료, push 보류 유지.**

### 2026-07-13 — 상호반박 검증 에이전트 구축 + E2E 감사 발견 17건 수정

- **검증 에이전트 4종 신설(`.claude/agents/`)**: `legal-verifier`(법률 공식출처 검증), `adversarial-refuter`(반박 교차검증), `journey-auditor`(페르소나 E2E 감사), `consistency-checker`(페이지 간 일관성). 법률 콘텐츠 수정 후 워크플로우: legal-verifier → adversarial-refuter 순.
- **E2E 감사(회생·파산 신청자 페르소나 2종, 워크플로우 + 인라인 반박검증)** — 19건 발견, 17건 수정:
  - **P0 법률 문구(`result.html` 3곳 + 1곳)**: "회생 인가 후 추심·압류 자동 중지"(130행) / "신청 즉시 보전처분으로 강제집행 중단"(525·877행) → **중지·금지명령(제593조) + 개시결정 시 자동 중지(제600조)** 기준으로 정정 (법령 재검증 완료. 보전처분(제592조)은 채무자 재산처분 금지라 오용이었음). "이자 신청 시점부터 동결"→개시결정 후 이자 후순위(기검증 제581조→제446조 준용)로 정정.
  - **파산관재인 안내(`result.html` buildTrusteeWarning)**: "동시폐지가 가장 일반적" 삭제 — **현행 실무는 관재인 선임이 원칙**(easylaw·서울회생법원 실무준칙 361호 확인, 예납금 원칙 30만원 명시). 재산 500만 미만 숨김 조건 제거 → 파산 우세면 항상 표시(무재산자도 예납금 대비 필요).
  - **진단 재산 입력에 '예상 퇴직금' 필드 신설**: 절반만 청산가치 산입(`diagnosis.js` severanceHalf) — 재산목록 예시(퇴직금 1/2)와 진단 계산 일치. `debtTypes` 데드코드 제거. 연체기간 선택지 경계 명확화("1개월 이상~3개월 미만" 식). 생활비 "별도 처리" 오해 문구 수정.
  - **pricing 패키지 저장 개선**: `plan_packages` 배열 누적 저장 + 완주 패키지 보유 중 보정 추가옵션 구매 시 `plan_package` 덮어쓰기 방지. **진단 결과와 반대 절차 패키지 구매 시 confirm 경고**(오구매 방지). mypage는 보유 패키지 전체 뱃지+바로가기 표시, 초기화 목록에 `ai_reviewed`·`plan_packages` 추가.
  - **rehabilitation 3항목 신설(42→45)**: r7-6 중지·금지명령 신청(제593·600조), r8-6 회생위원 면담 준비, r8-7 개인회생채권자집회 — pricing 패키지 구성 약속("회생위원 면담 준비 체크리스트"·"채권자집회 대응")과 실내용 일치화. mypage 진행률 공식 45로 갱신.
  - **bankruptcy 3단계 채권자목록 예시**: 박○수 사례 누락 2건(◇◇카드 700만·김○○ 1,000만) 추가, 합계 87,400,000원 명시.
  - **mypage**: 파산 우세 사용자에게 "개인회생 월 변제액 0원" 노출 차단(scores 비교 분기, 파산용 요약으로 대체), AI 기록 라벨 우선순위 `r.label` 우선으로 수정.
  - **ai-review**: 필수 키워드 동의어 지원(`이름|성명|신청인` 등 9개 규칙 확장) — "성명" 표기 정상 문서 오탐 제거.
  - **기간 통일**: 파산 "신청~면책 통상 6개월~1년"으로 pricing(2곳)·compare(2곳) 통일 (기존 6~8개월 vs 선고~면책 6개월~1년 모순).
  - **result.html**: 점수 30 미만 카드의 "유리한 점" 제목을 "여건이 갖춰지면 유리해질 수 있는 점"으로 조건부화(무소득자 자기모순 해소), diagnosis_repay 부분 누락 시 재진단 안내 가드.
- **검증**: node로 페르소나 2종 계산 재현(회생 84점·1,061,457원 = 김○민 예시와 일치 / 파산 100점·0원), 수정 8개 파일 인라인 스크립트 구문 검사 통과, 체크리스트 45/36 유일 id 확인, 가격·패키지 키·Storage 키 쓰기/읽기 짝 전수 대조 이상 없음.
- **미수정(보류·로드맵行)**: 유료 페이지 접근 게이트(체험 단계 의도 여부 사용자 결정 필요), "서류검토AI 2회" 횟수 제한(백엔드에서 구현), 파산 우세→회생 패키지 구매 UI 차단 강화.
- **미커밋분 포함 스냅샷 커밋**(push는 계속 보류 — 백엔드 완성 후 일괄 공개 방침 유지).

### 2026-07-07 (5차) — 사이트 전역 가독성 개선 (사용자 지적: 회색 글씨 작고 연함)

- **원인 3가지를 `css/styles.css`에서 전역 처리** (개별 HTML 수정 없이 기존 Tailwind 오버라이드 패턴 사용):
  - ① body `font-weight` 300(Light)→**400** — 작은 한글이 얇게 렌더되던 근본 원인.
  - ② 회색 한 단계씩 진하게: `.text-slate-400` #94a3b8(대비 2.9:1 WCAG 미달)→**#64748b**(4.8:1), `.text-slate-500`→**#475569**(7.1:1), `--c-ink-mute`(푸터 링크·badge-low 등)→#475569. **다크 배경 예외**: `.bg-slate-700/800`, `.hero-gradient` 내부의 slate-400/500은 #b6c2d4로 밝게 유지(mypage 다크 히어로 확인).
  - ③ 최소 글자 상향: `.text-xs` 12→**13px**(LH 1.25rem, `leading-relaxed` 등 명시 행간은 재선언으로 유지), `.text-[11px]` 11→**12px**, 모바일 compare-table 12→13px.
- `result.html` 흰 배경 위 `text-slate-300` 문구 1곳(정성 비교 안내)은 slate-400/500으로 개별 수정 — slate-300 자체는 다크 히어로 전용이라 전역 미변경.
- 브라우저 검증(computed style 실측): documents(11px 안내문 12px·#64748b), mypage 다크 히어로 밝기 유지, rehabilitation 작성예시 모달 표 13px·오버플로 없음, index 저대비 잔존 스캔 0건(감지 1건은 인디고 그라디언트 위 blue-200 = 정상), 푸터 링크 #475569, 콘솔 에러 0. preview_screenshot은 여전히 타임아웃 플레이크.
- **미커밋·미배포 유지.**

### 2026-07-07 (4차) — 작성예시 내용 검증 Q&A + 도구 모음 중복 버튼 제거

- **사용자 질문 3건(작성예시 정확성) 공식 출처 재검증 — 콘텐츠 수정 불요 확인**: ① 채권자목록 원금/이자 구분 기재는 실제 양식 그대로(법률구조공단 "채권현재액(원금, 이자)" 기재요령 + 개시결정 후 이자는 후순위개인회생채권이라 구분 필요 — 제581조2항→제446조 준용) ② 수입 목록 "세후 월평균" = 최근 1년 세후 소득(상여 포함)÷12가 원칙, 직장 변동 시 변동 후 소득만(가용소득 공제 항목은 제579조4호) ③ 대출금 사용내역서는 법정 제출 서류가 아니라 보정권고 대응용 실무 문서 — 서류 체크센터(공식 발급 서류 목록)에 없는 것이 의도된 구성.
- **도구 모음 "실전 (서류) 작성예시" 버튼 제거(사용자 지적 — 바로 아래 섹션과 중복)**: `rehabilitation/bankruptcy/maintenance/supplement.html` 4개 파일 모두 `#example-library` 앵커 버튼 삭제, 그리드 `sm:grid-cols-3`→`2`. maintenance/supplement는 도구 설명 문구도 2종 기준으로 조정("예시는 바로 아래" 안내 추가). `#example-library` 섹션·id는 유지 — ai-review.html의 크로스 페이지 링크(`rehabilitation.html#example-library` 등) 정상 동작.
- 브라우저 검증: 4페이지 도구 카드 2개·grid-cols-2·작성예시 섹션과 버튼 6종 전부 유지, 콘솔 에러 0. (preview_screenshot은 이번에도 타임아웃 플레이크 — snapshot/eval로 확인)
- **미커밋·미배포 유지.**

### 2026-07-07 (3차) — 구매 패키지 기준 공용 페이지 필터링 (사용자 지적 반영)

- **문제(사용자)**: 회생 패키지를 샀는데 서류 체크센터·AI 검토에 파산 내용까지 다 보여서 혼란 — 결제한 서비스에 맞는 내용만 나오게.
- **방식**: 구매 시 저장되는 `plan_package`(rehab-full/maintain/correction-rehab → 회생, bankrupt-full/correction-bankrupt → 파산)를 읽어 필터. **미구매면 기존처럼 전체 표시(안내 배너 없음)**. 필터 중에는 상단에 "💡 ○○ 패키지 이용 중 — △△ 서류만 표시" 배너 + **"전체 서류 보기" 토글**(막다른 화면 방지, 토글 왕복 가능).
- **`documents.html`**: 반대 절차의 탭 버튼·서류(회생 필터 시 파산 8종 제외 → 22개 / 파산 필터 시 회생 6종 제외 → 24개)·상단 법원 양식 링크(`data-proc` 속성으로 회생/파산 구분) 숨김. `visibleDocs()`가 renderDocs/toggleDoc의 기준 목록. 숨긴 탭이 현재 탭이면 '전체'로 복귀.
- **`ai-review.html`**: `visibleGroups()`로 서류 유형 그룹 필터 — 완주 패키지는 해당 절차 세트 + 보정·소명(8종), **보정 추가 대응은 보정·소명 그룹을 맨 앞 + 기본 선택 '보정서'**. 기본 선택 유형도 패키지 기준(`defaultType()`).
- **`supplement.html`**: `?type` 파라미터 없이 열리면 구매 패키지 기준으로 회생/파산 기본 유형 결정 (기존: 무조건 rehab).
- 브라우저 검증(패키지 시드 주입→정리): rehab-full(문서 22개·파산 탭/링크 숨김·토글 30↔22, AI 검토 8종·기본 회생신청서), bankrupt-full(24개·회생 숨김·파산 링크만), correction-bankrupt(AI 검토 보정 그룹 첫번째·기본 보정서, supplement 무파라미터→보정명령·박○수 예시), 미구매(30개·14종·배너 없음·탭 4링크 전부) — **콘솔 에러 0**.
- **미커밋·미배포 유지.**

### 2026-07-07 (2차) — 보정·변제기간 센터 상세화 + AI 검토 서류 14종 확장 (사용자 지적 반영)

- **`supplement.html`(보정 대응 센터)**: 13개 전 항목에 "자세히" 패널(필수 뱃지+구체 방법+링크+항목별 예시), 도구 3종(서류센터/실전예시/AI검토) 상단 배치, **실전 작성예시 3종 신설(보정서/소명서/사용내역표)** — `?type`별로 김○민(회생·개회 사건번호)/박○수(파산·부호 미표기) 사례 분기, 수치는 각 진행센터 페르소나와 동일(회생 대출 700만 소명 / 파산 인출 300만 소명). 기한 문구 "통상 2~4주" → **"보정서 기재 기한 기준(통상 1~2주, 더 길게 주어지기도)"**로 rehabilitation.html과 통일. 모달 2xl+HTML 표 지원. 항목 수·저장 키 불변.
- **`maintenance.html`(변제기간 관리 센터)**: 16개 전 항목 "자세히" 패널, 도구 상단 배치, **실전 작성예시 3종 신설** — ① 변제계획 변경신청(이직 소득 260→190만, 월 변제 1,061,457→361,457 변경 전후 표) ② 소득 변동 증빙 정리표(상황별 서류·발급처) ③ 특별면책 신청 서면(제624조2항 3요건 구조 — 기납입 25,474,968원(24회) ≥ 청산가치 10,260,000원, 회생 페르소나와 수치 일관). 7/6 기검증 법조문 문구(619조·611조5항·624조2항)는 그대로 유지.
- **`ai-review.html`(사용자 질문: "6종이면 충분한가, 전체 업로드가 낫지 않나")**: 검토 서류 **6종 → 14종** (회생 세트 6 + 파산 세트 6 + 보정서·소명서 2) — 셀프 진행센터의 제출 서류 세트와 1:1 대응. **그룹별 "N/6 검토함" 추적**(`ai_reviewed` 키, ✓뱃지·미검토 서류 목록 표시), 유형별 실전 작성예시 링크(입력창 상단 + 결과 팁), "다른 서류 이어서 검토" 버튼. 검토 기록에 label 저장 + `mypage.html` 라벨 폴백(`DOC_LABELS[k]||r.label||k`) 1줄 수정. **파일 업로드(HWP/PDF)+실제 AI 피드백은 백엔드 도입 시 구현으로 명시**(목업으로 흉내 내지 않음 — 7/2 점검 19번 이슈 방침 유지). 유형 키는 기존 6종 값 보존(기록 호환), 신규만 추가.
- 브라우저 검증: supplement rehab/bankrupt 분기(문구·예시·저장 키 분리·토글 양방향), maintenance 수치·모달, ai-review 검토 실행→키워드 확인·팁·세트 현황 1/6·뱃지·선택 유지·기록 라벨, mypage 라벨 표시 — **콘솔 에러 0**. (supplement 체크 테스트에서 "저장 안 됨"처럼 보인 것은 7/6 테스트 잔존 체크가 남아 있던 것 — 실제 토글·저장 정상)
- **미커밋·미배포 유지.**

### 2026-07-07 — 회생·파산 체크리스트 "체크만 하는 서비스" 문제 개선 (사용자 지적 반영)

- **문제(사용자)**: 유료 진행 페이지가 체크박스+한 줄 힌트뿐이라 처음 하는 사람이 못 따라감. 작성예시가 단계당 1개뿐, AI검토·서류센터 링크는 맨 아래, 법원 양식 다운로드 연결 없음, 진짜 제출물처럼 보이는 예시 부재.
- **`rehabilitation.html` / `bankruptcy.html` 전면 개편**:
  - 도구 바로가기(서류 체크센터·실전 작성예시·AI 검토)를 **최상단**으로 이동 (기존 하단 박스 제거)
  - **42/36개 전 항목에 "자세히" 접이식 패널**: 필수 여부 뱃지(필수/해당 시 필수/권장) + 구체적 발급·작성 방법 + 공식 사이트 링크(크레딧포유·어카운트인포·온크레딧·내보험찾아줌·정부24·efamily·홈택스·등기소·고용24·법원 양식) + 일부 항목별 예시 모달. **항목 수·id 불변 → mypage 진행률 42/36 그대로(브라우저 확인)**
  - **"실전 서류 작성예시" 라이브러리 신설**: 가상 인물 완성형 문서 각 6종. 회생(김○민 38세 1인가구): 채권자목록/재산목록/수입지출목록/진술서/변제계획안/대출금 사용내역서(보정 대비). 파산(박○수 52세 코로나 폐업): 동시신청서/진술서/채권자목록/재산목록/현재의 생활상황/수입지출목록. HTML 표 렌더용 `showDocExample`(innerHTML, 정적 콘텐츠) 추가, 모달 max-w-lg→2xl. 모든 예시에 "가상 사례·법원 최신 양식 기준" 경고 배너.
  - **수치 일관성**: 회생 예시 실수령 260만 − 1인 표준생계비 1,538,543(diagnosis.js `getStandardLiving`과 동일) = 가용 1,061,457 × 36회 = 38,212,452 ≥ 청산가치 10,260,000(퇴직금 1/2 포함), 채권 4건 합 46,700,000·안분율 합 100%. 파산 예시 채무 6건 합 87,400,000.
- **`documents.html`**: 상단 "법원 공식 양식 내려받기" 카드(서울회생법원 양식모음 회생 gubun=25/파산 gubun=24 — 페이지 실존 확인, 전자소송포털 PSPA17M01/02). 서류 20→**30개**(회생 양식 6종 세트 완성, 파산 동시신청 세트+주민등록초본, 혼인관계증명서 추가). 각 서류에 발급 단계(issue)·유의사항(note)·공식 링크 → "발급 방법" 모달 + 행 내 "공식 사이트 ↗" 버튼. **id는 의미 기준 유지**(d-r2=변제계획안·d-r3=수입지출 예전 체크 호환 — 시드 테스트로 확인, 신규는 d-r4~6/d-b4~8/d-c4). 부정확했던 '보험다모아'(비교공시 사이트) → **'내보험찾아줌'(cont.insure.or.kr)** 정정. 기존 등본 "3개월 이내" → 회생은 "신청일 기준 2개월 이내 원칙"으로 정정(easylaw).
- **법률 검증(공식 출처, 작성 전 전부 확인)**: easylaw(회생 신청서 기재사항·첨부서류·관공서 서류 2개월, 파산 진술서 구성: 학력경력→생활상황→파산에 이르게 된 사정→과거 면책 이력), 대한법률구조공단 resu.klac.or.kr(채권자목록 원금/이자 구분·재산목록 담보공제 실가치·변제계획안 4원칙), 제610조(변제계획안 신청일부터 14일, 실무 동시제출)·제611조(3년 원칙, 제614조1항4호 청산가치 등 특별사정 시 5년), 전자민원센터(파산 동시신청 = 신청서+진술서+4서면, 주민등록**초본**(주소변동 포함)·혼인관계증명서(이혼 포함), 부본 1부 / 회생 채권자목록 부본 = 채권자 수+2), 제566조 악의 누락 채권 비면책(7/6 기검증 재사용), 크레딧포유·어카운트인포·온크레딧·내보험찾아줌 실존·용도 확인. **대출금 사용내역서는 법정 양식이 아니라 보정 실무 관행으로 소개**(민간 출처 복수 일치, 단정 표현 회피 — "요구하는 경우가 많음").
- **브라우저 검증**: 3페이지 렌더·자세히 토글·모달(텍스트/HTML 표 양쪽)·체크 저장/복구·파산탭 신규 서류·문서 id 중복 0·mypage 42/36·진단/결과 스모크 — **콘솔 에러 0**. preview_screenshot은 이번에도 백지/타임아웃 플레이크(페이지 문제 아님 — inspect로 모달 렌더 실측 확인).
- **미커밋·미배포 유지** (백엔드 완성 후 일괄 공개 방침).

### 2026-07-06 (2차) — 패키지별 구매 후 전용 페이지 분리 (사용자 지적 반영)

- **문제**: 어떤 패키지를 사도 `plan='premium'`만 저장되고 회생/파산 전체 안내 페이지로 이동 → 변제기간 관리(29,000)·보정 추가 대응(19,000) 구매자가 회생 완주 패키지와 동일한 화면을 봄.
- **신규 페이지 2개**:
  - `maintenance.html` (변제기간 관리 센터) — 패키지 구성 그대로 가이드 3개: ① 변제계획 변경 신청(제619조, 변제기간 한도 3년·특별사정 시 5년은 제611조 5항) ② 실직·소득 변동 대처 ③ 특별면책 신청(제624조 **2항** — 3요건, 개인회생절차 종료 전 서면 신청). 체크리스트 저장 키 `maintain_checks`. **법률 근거 전부 공식 출처 검증**: easylaw.go.kr 면책결정 페이지(특별면책은 2항, 3항은 불허사유 — 첫 검색 요약이 3항이라 했으나 easylaw로 정정), law.go.kr 검색(619조·611조5항·2017 개정 경과), 서울회생법원 변제계획변경·특별면책 소개자료.
  - `supplement.html` (보정 대응 센터) — `?type=rehab`(보정권고)/`?type=bankrupt`(보정명령)로 용어·문구 분기. 3단계(내용 확인→자료 준비→보정서 작성·제출) 체크리스트, 저장 키 `supplement_checks_rehab`/`_bankrupt` 분리.
- **`pricing.html`**: `mockPayment`에 패키지 키 3번째 인자 추가(rehab-full/maintain/correction-rehab/bankrupt-full/correction-bankrupt). `mockComplete`가 `plan_package`·`plan_package_name` 추가 저장 + `PACKAGE_DEST` 맵으로 패키지별 이동.
- **`mypage.html`**: 결제 상태에 "프리미엄" 대신 **구매한 패키지명 + 전용 안내 바로가기 링크** 표시. 데이터 초기화 목록에 신규 키 5개 추가.
- 브라우저 검증: 변제기간 패키지 구매→maintenance.html 이동, 보정권고 구매→supplement.html?type=rehab 이동, type별 용어 분기(보정권고를/보정명령을 조사 포함), 체크리스트 저장·진행률, 마이페이지 패키지명·링크 — 전부 확인, 콘솔 에러 0건. (preview_screenshot 도구만 타임아웃 — 페이지 문제 아님, eval/snapshot으로 검증)
- **미커밋·미배포** (백엔드 완성 후 일괄 공개 방침 유지).

### 2026-07-06 — compare 장단점 정확성 보완 + FAQ 4건 추가 (전부 공식 출처 검증)

- **compare.html 장단점 문구 수정(사용자 지적 반영)**: ① 개인회생 단점 "소득 대부분을 변제에 사용"(과장) → "생계비를 제외한 소득(가용소득)을 변제에 사용". ② 신복위 장점 "재산 처분·자격 제한 없음" → "재산 처분 없음, 공무원 등 **법적** 자격 제한 없음"으로 범위 명확화. ③ 신용정보 등록에 따른 카드·대출 제한을 **3개 카드 모두** 단점에 추가(한쪽만 쓰면 오독): 신복위(완제 또는 1년 경과 시 해제·ccrs 확인), 개인회생(변제 중 카드 발급 제한, 1년 이상 성실상환 시 조기 해제 가능 — 2025-07-18 제도), 파산(면책 후 5년 경과 시 해제·ccrs 비교표).
- **resources.html FAQ 10→14개**: 신규 4건 모두 공식 출처 검증 후 작성 — ① 개인회생 채무 한도(담보 15억/무담보 10억, 제579조 — easylaw 신청자격 페이지) ② 비면책채권(세금·벌금·고의 불법행위 손배·양육비 등 + 회생은 목록 미기재/파산은 악의 누락 채권 비면책, 제566조·제625조 — easylaw 면책의 효력) ③ 가족·보증인 영향(면책은 본인만, 보증인 의무 존속, 제567조·제625조) ④ 재신청 제한(파산 면책 후 7년·제564조1항4호 / 회생 신청 전 5년 내 면책 시 기각·제595조5호 — 7/2 기검증). 브라우저 검증: 14개 렌더·토글 정상·콘솔 에러 0.
- 여전히 **미커밋·미배포** (백엔드 완성 후 일괄 공개 방침 유지).

### 2026-07-04 (추가) — compare.html 카드에 장단점 섹션 추가

각 제도 카드(신용회복위원회/개인회생/개인파산·면책)의 기존 사실 나열(채무처리·기간·신청기관·추심중단)만으로는 지식 없는 사용자가 유불리를 판단하기 어렵다는 지적(사용자) → 카드마다 CTA 버튼 위에 **👍 장점 / 👎 단점** 박스(초록/빨강) 2~3개 불릿씩 추가.

**법률 근거 확인(공식 출처):**
- 신복위 원금감면 폭 "0~70%"(협약 가입 금융회사 한정, 채권사 동의 필요) — ccrs.or.kr 채무조정제도 비교 페이지(MENU_ID=490) 및 개인워크아웃 안내(MENU_ID=1930) 확인
- 개인회생 청산가치 보장(재산 가치만큼만 변제 반영, 재산 유지 가능)은 기존 diagnosis.js 로직과 일치
- 개인파산 면책 전 공무원 임용 등 자격 제한(복권 전까지) — easylaw.go.kr 확인. 개인회생은 파산선고 자체가 없어 해당 없음

**구현:** 각 카드의 마지막 사실 블록(추심·독촉 중단) 다음, CTA `<a>` 앞에 `border-t` 구분선 + `bg-green-50`/`bg-red-50` 박스 삽입. 전역 CSS(`styles.css:35`)에 이미 `word-break:keep-all` 적용돼 있어 개별 클래스 생략. 데스크톱 3열·모바일 1열 모두 브라우저 검증(스크린샷), 콘솔 에러 0건.

### 2026-07-04 — 배포 전 정비 + 전략 결정(백엔드 먼저 완성 후 일괄 공개)

배포 전 정비를 진행하다가 **전략을 "백엔드까지 완성해 한 번에 공개"로 결정**(사용자). 이에 따라 공개 배포는 **보류**하고, 로컬에서 백엔드 개발·전체 플로우 테스트를 이어감. **관리자 페이지는 백엔드 도입 시 함께**(지금 불필요 — 정적 사이트라 관리할 서버 데이터 없음, GitHub Pages 공개 노출·보안 무의미). 참고 결정: 문의는 이메일(eslje75@gmail.com) / **개인 운영(사업자등록 전)**.

**결제 버튼 — 비활성화했다가 원복(현재 mock 결제 유지):**
- 처음엔 공개 배포 대비로 구매 버튼 5개를 "출시 예정"으로 비활성화하고 `mockPayment/closePay/mockComplete`·`#pay-modal`을 제거했으나, **백엔드 먼저 완성 전략으로 바뀌면서 `git checkout`으로 원복**. 이유: 결제 후 premium 플로우(서류검토 등)를 로컬에서 계속 테스트해야 함. premium은 `localStorage cdg_plan='premium'` 플래그 하나로 열리므로, mock 결제 또는 콘솔에서 `localStorage.setItem('cdg_plan','premium')`로 재현 가능. 원복 후 SEO 메타 블록만 다시 얹음.
- **미배포 전제라 약관/개인정보처리방침·SEO는 "초안"으로 보유** — 공개 시점에 백엔드 반영해 확정.

**법률 표현 정정(`compare.html`) — 유지(정확성 수정이라 롤백 안 함):**
- "파산선고와 동시에 **모든** 추심 즉시 중단"(과장) → "파산선고 시 재산에 대한 강제집행 효력 상실, **면책결정으로 채무 최종 소멸**". 근거: easylaw.go.kr, 채무자회생법 제348조. 신복위 "신청 다음 날 추심중단"(line 60)·개인회생 금지명령 셀(line 104)은 공식 확인 결과 정확 → 유지.

**작성해 둔 초안·자산(미배포, 백엔드 완성 시 재검토):**
- **이용약관·개인정보처리방침 신설(`terms.html`, `privacy.html`)**: 개인 운영 기준 작성. 핵심 — 백엔드 없는 정적 사이트라 회원가입·진단 입력값은 **이용자 브라우저(localStorage)에만 저장, 운영자 서버 수집 없음**을 명시(실제 수집 개인정보는 이메일 문의분에 한정). 푸터(`js/main.js renderFooter`)에 약관·방침·소개·문의 링크 줄 추가 → 전 페이지 자동 노출. **⚠️ 백엔드로 회원·진단 데이터를 서버 저장하게 되면 개인정보처리방침 수집·저장 조항은 반드시 개정 필요(현재 문구는 "서버 저장 안 함" 기준).**

**SEO·기본기 자산 (백엔드와 무관하게 유효 — 유지):**
- **meta description·OG·favicon**: 전 16개 HTML `<head>`에 삽입. 색인 대상 공개 12개(index/diagnosis/compare/rehabilitation/bankruptcy/documents/ai-review/resources/pricing/about/terms/privacy)는 description+og(type/site_name/title/description/url)+favicon. 인증·동적 4개(mypage/login/result/find-account)는 `robots noindex`+favicon.
- **신규 자산**: `favicon.svg`(로고 마크, 인디고 배경), `robots.txt`(auth 페이지 Disallow + Sitemap), `sitemap.xml`(공개 12페이지), `404.html`(자체완결 브랜드 404, 자산은 `/debt-guide/` 절대경로).
- **OG 이미지 완료(`og-image.png`, 1200×630)**: 카톡·SNS 공유 썸네일. 브랜드 카드(HTML 디자인)를 **Chrome 구 headless**(`chrome --headless --screenshot`, Edge headless=new는 --screenshot 미출력이라 실패)로 렌더. 12개 색인 페이지 og 블록에 `og:image`(+width/height)·`twitter:card=summary_large_image` 추가. **주의: og:image URL은 배포 도메인(github.io/debt-guide/) 절대경로라, 실제 배포 전에는 카톡 디버거에서 404 — 배포 후 정상.** 재생성 시: `scratch-og-image.html`류 디자인을 로컬서버로 띄우고 Chrome headless 스크린샷.
- 검증: 로컬 프리뷰(포트 3456)에서 privacy/terms 헤더·푸터 렌더, index meta·og·favicon, favicon/robots/sitemap/404 전부 HTTP 200, pricing 원복 후 구매버튼 5·mockPayment·모달 복구, 콘솔 에러 0건.

**상태:** 이번 변경 전체 **미커밋·미배포**(백엔드 완성 후 일괄 공개 예정). 유지 항목 = compare 법률정정 + 약관/방침 초안 + 푸터 링크 + SEO 자산 전체. 원복 항목 = pricing 결제 버튼(mock 유지).

**다음 큰 작업 = 백엔드.** 교체 지점은 격리돼 있음: `js/main.js`의 `Auth` 객체(로그인/가입/찾기/재설정)와 `Storage` 객체(진단·plan 저장) 본문을 `localStorage` → `fetch(API)`로 교체하면 되고, 이를 호출하는 login/mypage/diagnosis/result 페이지는 거의 안 바뀜. 결제는 새로 붙이고, 개인정보처리방침 개정 동반.

### 2026-07-03 — 소액 채무 안내 + 홈 버튼 입체화 등 UI 손질

- **소액 채무 안내(`result.html`)**: 총채무 **1,000만원 미만**이면 적합도 배너·위험 스트립·다음 단계 CTA·상세 아코디언을 모두 숨기고, "채무 규모가 크지 않은 편입니다 → 신용회복위원회 채무조정 우선 안내"(안 A) 카드 + **총 채무 숫자 1칸만** 표시. 신복위는 자격요건까지 다루지 않고 **사이트(www.ccrs.or.kr) + 대표번호 1600-5500(평일 09:00~18:00)** 만 안내(전화번호는 ccrs.or.kr 공식 확인). 구현: `LOW_DEBT_THRESHOLD`(=10,000,000) 상수 + `isLowDebt()`/`buildLowDebtGuidance()`/`buildLowDebtKeyNumber()`. `renderResults()` 날짜 로드 직후 조기 분기, 하단 `renderCta()` IIFE에도 `if(isLowDebt) return` 가드. 무데이터 분기는 그 앞이라 회귀 없음. **기준 1,000만원은 법정 최소액이 아니라 "법원 절차 비용·부담 대비 실익" 근거의 heuristic — 사용자 승인.**
- **홈 진행 버튼 입체화(`index.html`+`css/styles.css`)**: "무료 채무진단"/"챔로드 셀프진행" 버튼에 3D 눌림 효과. Tailwind arbitrary value가 CDN에서 안 먹어 `.btn-3d-white`/`.btn-3d-primary` 클래스를 styles.css에 직접 추가. 흰 버튼엔 pulse 애니메이션.
- **잔손질(`result.html`)**: 중복이던 "상세 정보 전체 펼치기" 버튼 삭제, mini-disclaimer 가독성 상향(text-sm/slate-500/medium), 하단 고지 여백 mt-8→mt-16.
- **커밋(`b3faf78`) + `git push origin main` 완료** — 이전 2~3차 미배포분 포함 전체 배포됨. GitHub Pages 반영 대기.

### 2026-07-02 (3차) — 진단결과 페이지 "간단 결과 우선" 구조로 개편

`result.html` 정보 과다 문제 해결 — "결론 먼저, 상세는 접기(무료)" 구조로 재구성. **진단결과 유료화 아이디어는 사용자 결정으로 폐기** (신뢰 훼손·퍼널 역효과·변호사법 109조 리스크 검토 결과).

**새 페이지 구조 (첫 화면 1~1.5스크롤):**
1. 추천 절차 배너 → 2. 한 줄 참고 고지(`#mini-disclaimer`) → 3. 핵심 숫자 3칸(`buildKeyNumbers` — 회생 우세: 월변제·총변제·면제예상 + 0원/청산가치 경고 노트, 파산 우세: 총채무·재산·채권자 수) → 4. 위험 요약 스트립(`buildRiskStrip` — "위험 N건" 접힌 빨간 띠, 펼치면 상세) → 5. 다음 단계 CTA → 6. 접힌 아코디언 4종(적합도 카드 / 두 절차 비교 / 변제금 계산 근거 / 파산관재인 주의) → 7. 전체 펼치기·재진단 → 8. 하단 고지

**구현 메모:**
- 공통 셸: `accordionShell(id, title, html)` / `toggleSection(id)` / `toggleAllSections()` — 본문 `.acc-body`, 화살표 `.acc-chev` 클래스 기준
- 기존 계산·문구 빌더 함수는 그대로 재사용. `buildDischargeRiskWarning` → `getDischargeRisks`(수집) + `buildRiskStrip`(요약 띠) + `buildRiskDetail`(본문)로 분리
- 삭제된 요소: 상단 disclaimer 박스(하단으로 통합), `#discharge-risk-warning`·`#result-cards`·`#tossup-advice`·`#trustee-warning`·`#detail-sections-container`·`#cards-heading` 고정 컨테이너, `makeAccordion` 데드코드
- 회귀 테스트 통과: 회생 우세+청산가치(첫 화면 799px) / 파산 우세+위험 2건 스트립 / 근접 점수(아코디언 4종) / 무데이터 가드 / 개별·전체 토글, 콘솔 에러 0건
- **아직 커밋/푸시 안 함** (2차 수정분 포함 전체 미배포 상태)

### 2026-07-02 (2차) — 점검 발견 이슈 일괄 수정 완료

아래 "1차 점검" 목록의 **1~18, 20~23번을 모두 수정**하고 브라우저 회귀 테스트(페르소나 4종 + 무데이터 + 마이페이지 진행률 + login next 차단)까지 통과. 콘솔 에러 0건. **아직 커밋/푸시 안 함.**

**수정 내용 요약:**
- `result.html` — hasRecentLoan 버그 수정(3개월/1년 분리 경고), 조문 정정(도박·투기 6호, 신용거래 2호, 재신청 기각 595조 5호), "불허가 후 7년 대기" 문구 삭제→즉시항고 안내, 사기죄는 형법 347조로 표기, "이자 3회" 속설 삭제, 배너·카드·상세의 변제 여력을 diagnosis_repay 기준으로 통일, 청산가치 경고 박스 신설, 무데이터 접근 시 진단 안내 표시, 미사용 변수 제거
- `js/diagnosis.js` — calcScores도 표준생계비(effLiving) 기준 사용, 파산 면책 이력(bankrupt-done-recent) 시 rehab -15 반영
- `diagnosis.html` — uncheckLegalNone 추가(해당없음 자동 해제), 데드코드 제거, 나이 라벨 * 추가
- `mypage.html` — rehab 42항목으로 수정(111% 버그), 미사용 상수 제거
- `pricing.html` — 당연복권·심문기일·금지명령 표현 정정 / `bankruptcy.html` — 압류금지 250만원(2026.2. 시행령)·심문기일 표현 / `resources.html` — 회생위원·압류금지 250만원 / `rehabilitation.html` — 회생위원 용어·직권면책 병기
- `login.html` — next 파라미터 same-origin 검증 / `ai-review.html` — 베타(규칙 기반) 안내 문구 / `index.html` — 인라인 hex 제거·?v=4 제거
- `app.html`·`build.ps1` **삭제** / `CLAUDE.md` 최신화(localStorage, 요금제 4종, launch.json)

**남은 항목 (별도 결정 필요):** 19·21번 — 실서비스 전환 시 백엔드(회원·결제) + 실제 AI 검토 API 연동. compare.html "파산선고와 동시에 추심 중단" 표현도 추후 검증 권장.

### 2026-07-02 (1차) — 전체 사이트 정밀 점검 완료 (수정은 아직 안 함)

전 페이지 코드 분석 + 실제 브라우저 진단 플로우 테스트(페르소나 3종) + 법률 수치 공식 출처 검증 완료. **발견 이슈만 기록하고 코드는 건드리지 않았음.** 다음 세션에서 P0부터 수정.

**P0 — 확정 버그 (동작 오류, 런타임 재현됨):**
1. `result.html` `buildReasons()`가 존재하지 않는 `data.hasRecentLoan` 필드 참조 → 최근 신규대출 경고가 적합도 카드에 절대 표시 안 됨. `data.recentLoan === 'within-3m'` 등으로 수정 필요
2. `diagnosis.html` `toggleLegalNone()` — '해당없음' 체크 상태에서 압류 등 다른 항목 체크 시 해당없음이 해제 안 됨 → 둘 다 체크되면 `hasLegalAction=false`로 계산 (압류 입력이 무시됨)
3. `mypage.html` 개인회생 진행률 분모 38 하드코딩 — 실제 `rehabilitation.html`은 9단계 42항목 (4+5+4+5+4+6+5+5+**4**) → 전부 체크 시 111% 표시. 하드코딩 대신 실데이터 기준으로 계산 권장
4. `app.html` + `build.ps1` — 예전 폴더("클로드코드 작업") 시절 단일파일 빌드 산출물. 어디서도 링크 안 되지만 GitHub Pages에 공개 배포 중 (옛 사이트명·옛 요금 10만원·신복위 포함 옛 진단). 저장소에서 삭제 필요

**P1 — 법률 정확성 (공식 출처로 검증 완료):**
5. 도박 면책불허가 근거: 사이트는 "제564조 제1항 **제4호**" → 실제는 **제6호** (`result.html` buildDischargeRiskWarning)
6. 1년 내 신용거래: 사이트는 "**제5호**" → 실제는 **제2호** (`result.html` 같은 함수)
7. "면책불허가 확정 후 **7년 대기 후 재신청**" 문구 → 법적 근거 없음. 제564조 1항 4호의 7년은 '면책을 **받은** 후' 기준 (`result.html` 면책불허가 안내 박스)
8. `pricing.html` 파산 절차 "복권 **신청**으로 자격 제한 해제" → 면책결정 확정 시 **당연복권**(제574조), 신청 불필요. 신청복권(제575조)은 면책 못 받고 5년 경과 시
9. "월 **150만원** 이하 급여 보호" → 민사집행법 시행령 개정으로 2026-02-01부터 압류금지 최저 **250만원** (`bankruptcy.html` 4단계 예시, `resources.html` 마지막 FAQ)
10. '변제**관리**인' 용어 → 개인회생은 '**회생위원**' (`rehabilitation.html` 8·9단계, `resources.html` FAQ)
11. 제595조 5호: **신청 전 5년 이내 면책(파산 면책 포함)** 받은 경우 개인회생 기각 → 현재 diagnosis.js는 '회생 면책 5년'만 반영, 파산 면책 이력의 회생 영향 미반영
12. 심문기일 출석: `pricing.html` "강제 아님, 권장" vs `bankruptcy.html` "반드시 출석" **상호 모순** — 공식 출처 확인 후 통일 필요
13. "사기죄(채무자회생법 **제643조**)로 형사고소" (`result.html` 2곳) — 643조는 사기회생죄, 파산 쪽은 사기파산죄(제650조)·형법 347조 영역. 조문 재확인 후 수정 필요
14. "이자 3회 이상 납부 이력이 있으면 사기 혐의 기각 가능성 높음" — 법적 근거 불명확한 속설. 단정 표현 완화 필요

**P2 — 로직/UX/구조 개선:**
15. `calcScores`는 입력 생활비, `calcRepayment`는 표준생계비 기준 → 같은 결과 페이지에 "변제 여력 월 100만원"(배너·카드)과 "월 변제액 0원"(상세)이 동시 표시되는 모순 (런타임 재현: 소득 250만·부양 1명 케이스)
16. 청산가치 보장 미반영: 총재산 > 3년 변제총액이면 변제금 상향·기간 연장(최대 5년) 필요한데 계산·경고 없음
17. `result.html` 진단 없이 직접 접근 시 "소득 없음" 등 허위 카드 표시 → 데이터 없으면 diagnosis.html 안내/리다이렉트
18. `login.html` `?next=` 무검증 `location.replace()` → `javascript:` URL 실행 가능. 상대경로만 허용하도록
19. AI 서류검토가 실제로는 키워드 포함 여부 검사 목업 — 유료 패키지 핵심 기능으로 판매 중이므로 실제 AI 연동(백엔드 필요) 또는 안내 문구 조정 필요
20. `index.html` 서비스 카드 인라인 `style="box-shadow: 0 0 0 2px #1d4ed8"` — CLAUDE.md 인라인 hex 금지 규칙 위반 (옛 파랑)
21. Auth/결제 전부 localStorage 목업 — 실서비스 전 백엔드 필수 (비밀번호 base64 저장, 결제 시 아무 패키지나 'premium' 부여)
22. CLAUDE.md 자체가 낡음: "sessionStorage 저장"(실제 localStorage), "프리미엄 10만원 단일 플랜"(실제 149,000/49,000/29,000/19,000 4종)
23. 잔손질: diagnosis.html 하단에 삭제된 드롭다운(`prior-adj-container`) 참조 데드코드, mypage `REHAB_TOTAL` 미사용 상수, result.html `hasFinance`·`hasNonFinanceDebt` 미사용 변수, index.html만 `styles.css?v=4` 캐시버스팅, 나이 필드 필수인데 라벨에 * 없음

**로컬 테스트 환경:** `.claude/launch.json` 추가됨 (`chamroad-static`, node 정적 서버, 포트 3456) — Claude가 브라우저 자동 테스트에 사용

### 2026-06-28 — 전체 반응형 정비 완료

모바일·태블릿에서 레이아웃이 깨지지 않도록 전체 사이트 반응형 정비.

**변경된 파일:**
- `css/styles.css` — `@media (max-width: 640px)` 확장: `.section`/`.section-sm` 패딩 축소, `#toast` 모바일 위치(좌우 여백 16px·전체 너비), `#scroll-top-btn` 위치 조정, `term-tip` 툴팁 너비·위치 모바일 최적화
- `diagnosis.html` — 단계 라벨에 `step-label` 클래스 추가(모바일 자동 숨김), 최근 대출 선택 그리드 `grid-cols-3` → `grid-cols-1 sm:grid-cols-3`, 재산 입력 라벨 `w-36` → `w-24`
- `pricing.html` — 요금제 카드 5개 모두 `flex-col sm:flex-row` 레이아웃으로 변경(모바일 세로 적층)
- `js/main.js` — 푸터 패딩 `py-16 px-6` → `py-10 md:py-16 px-4 md:px-6`, 그리드 gap 모바일 축소

### 2026-06-28 — Stripe 디자인 시스템 적용 완료

DESIGN-stripe.md 기준으로 전체 사이트 디자인 시스템 적용.

**변경된 파일:**
- `css/styles.css` — 완전 재작성. Stripe 디자인 토큰(CSS 변수), Inter 폰트, blue→indigo 색상 매핑, 필 버튼 CSS 규칙, Stripe 그림자, 라이트 푸터 클래스
- `js/main.js` — 내비게이션 로고 색상(그라디언트→인디고 단색), 푸터 재작성(다크→화이트)
- `index.html` — 히어로 CTA 버튼 `rounded-xl` → `rounded-full`

**CSS가 자동 처리하는 항목 (HTML 변경 불필요):**
- 모든 `bg-blue-700` 요소 → `#533afd` (인디고)
- `a.bg-blue-700`, `button.bg-blue-700` 등 → 필 모양(9999px)
- `button.border-slate-300`, `a.border-slate-300` → 필 모양
- `div.bg-slate-100 > button` (탭 스위처) → 필 모양
- `.btn-primary`, `.input-field`, `.auth-tab.active` (login.html 인라인 스타일 덮어쓰기)
- `from-blue-700`, `to-blue-600` 등 그라디언트 → 인디고/네이비
- `bg-slate-700`, `bg-slate-800` → `#1c1e54` (brand-dark)
- `.hero-gradient` → 인디고/네이비 그라디언트
- 토스트, 스크롤-투-탑 버튼 → 인디고
- 단계 점, 푸터 — 새 CSS 클래스로 처리

### 2026-06-21
- PROGRESS.md 신설. 작업 추적 체계 도입.

## 다음에 할 일 / 미정 사항

> 📋 **공개 준비 절차는 [LAUNCH-CHECKLIST.md](LAUNCH-CHECKLIST.md)에 단계별로 정리됨**(2026-07-24). 사업자등록→포트원 라이브→도메인→이메일인증→최종점검→공개(git push) 순서·의존성·코드변경 지점 포함. 아래는 개발 이력.

- **백엔드 진행 중** (2026-07-19 착수, 스택 = Cloudflare Workers + D1):
  - ✅ Phase 1 회원·인증 — 배포·검증 완료(`https://chamroad-api.eslje75.workers.dev`).
  - ✅ Phase 2 사용자 데이터 서버 저장 — 배포·검증 완료(user_data 테이블, GET/POST /api/data, DataSync 동기화 계층).
  - ✅ Phase 3 AI 서류검토 — 배포·실검증 완료(POST /api/ai/review, claude-sonnet-5, 일 30회 제한, ANTHROPIC_API_KEY 시크릿 설정됨).
  - ✅ Phase 4 결제 — 포트원 PortOne V2, 실결제 E2E 검증 완료(prepare/complete, payments 테이블, 서버 금액검증, maintain 부여 확인). 테스트채널=토스 테스트.
  - **백엔드 4개 Phase + 관리자 페이지 완료.** 남은 것 = **실서비스 전환**:
    1. 사업자등록 → 포트원 **라이브 채널** 교체(테스트→라이브 store/channel/secret)
    2. **도메인**(chamroad.co.kr 등) 구매 → GitHub Pages CNAME + `api.챔로드도메인` Cloudflare Worker 커스텀 도메인 + main.js API_BASE 교체
    3. **이메일 인증 + 비밀번호 재설정** 도입(사용자 결정 2026-07-20: 도메인 설정 시점에 함께). Resend 등 발송서비스 + 도메인 SPF/DKIM. find-account.html을 실제 재설정 플로우로 교체. users에 email_verified 컬럼 추가.
    4. ✅**개인정보처리방침 전부 개정 완료(2026-07-21)** — 남은 것은 관리자 계정(eslje75@gmail.com) 선점 가입. 사업자등록 후에는 방침 12항의 운영 형태·상호를 갱신할 것.
    5. 공개 배포(git push). SEO·약관 공개 시점 확정.
- **공개 배포는 백엔드 완성 후** `git push origin main` — 그때까지 커밋은 하되 push 보류(또는 로컬 유지). 미배포 중이라 약관/방침·SEO는 초안 상태.
- **OG 이미지**: ✅ 완료(`og-image.png` 1200×630, 12페이지 연결). 문구·디자인 바꾸려면 카드 HTML 재렌더. 배포 후 카톡 공유 디버거(developers.kakao.com)로 캐시 갱신 권장.
- **3순위(공개 시)**: GA4 방문자 분석(측정 ID G-XXXX 필요, main.js 공통 삽입) / 커스텀 도메인(선택). 문의 채널은 이메일로 확정, 이미 푸터·약관에 노출.
- **텍스트량 과다(2026-07-27 측정, 편집 방향 미정)**: 한글 음절 수 기준 rehabilitation 9,853 / bankruptcy 8,519 / result 5,853 / maintenance 4,682 / supplement 4,554 / resources 4,366 / documents 4,335. 앞의 둘은 한 페이지를 끝까지 읽는 데 25~35분 분량 — 개인회생 면책 경험자도 피로감을 느낌. 용어 툴팁으로 "모르는 말" 문제는 해소했으나 **분량 자체는 그대로**. 검토 방향(미결정): 단계별 접기(현재 단계만 펼침) / "먼저 할 일 3가지" 요약 상단 배치 / 상세 설명을 툴팁·더보기로 내리기. 사용자 결정 필요.
- ✅**GLOSSARY 법률 검증 완료(2026-07-28, T2 반박 교차검증)** — 상세는 위 07-28 항목. 남은 후속: 용어 추가 후보(소명 64회·파산선고 17회·부채증명서 31회·예납금 15회·소송구조 5회·당연복권 4회 — 사이트 빈출인데 미등재)는 정의문 작성 시 다시 `/legal-verify` 필요.
- 신용회복위원회 채무조정 자격요건(연체 전/후 단계별)은 아직 ccrs.or.kr 공식 확인 전 — result.html 소액 안내는 사이트·전화번호만 노출해 우회 중 (신복위 "신청 다음 날 추심중단" 문구는 2026-07-04 공식 확인 완료)

## 알아둘 것
- Inter 폰트 Google Fonts에서 로드 (한글 지원 있음, 일부 글자는 시스템 폴백)
- `compare.html`, `documents.html`, `ai-review.html` 파일은 CSS 규칙으로만 처리 (별도 HTML 수정 없음). `find-account.html`은 2026-07-19 정직 안내 페이지로 전면 교체됨(백엔드 인증 도입). `app.html`은 2026-07-02 삭제됨 — 다시 만들지 말 것
- `login.html` 인라인 `<style>` 블록의 `.btn-primary` 등은 `styles.css`의 `!important` 규칙이 우선 적용

# 작업 진행 기록

`/clear` 이후 새 세션에서 바로 이어서 작업하기 위한 기록. Claude는 세션 시작 시 이 파일을 먼저 읽고, 의미 있는 작업 단위(기능 추가/수정 완료, 또는 멈추는 시점)마다 갱신한다.

## 🔴 다음 세션 시작 지점 (2026-08-15 기준)

`/clear` 직후 **이 블록만 읽고 바로 일을 시작할 수 있게** 정리해 둔 것. 아래 절들은 배경 기록이니 필요할 때만 볼 것.

### 지금 상태 한 줄
사이트는 chamroad.com에서 운영 중이고 **판매만 잠겨 있다**(`PAYMENTS_ENABLED = false` — `js/main.js`와 `api/src/index.js` **두 곳**).
**2026-08-15에 전부 커밋·push 완료**(`48c6c65`→`e33fcbd` 4개 커밋, Worker 배포 20초 성공·Pages 200 확인).
🔴**2026-08-16 보안 수정 4건이 미커밋 상태다**(아래 보안 감사 블록). `api/src/index.js`가 바뀌었으니 push하면 Worker가 자동 배포된다 — **먼저 읽고 판단할 것.**
**오픈 차단 사유는 사업자등록 하나뿐**이고, 나머지는 전부 "해도 되고 미뤄도 되는" 일이다.

⚠️**2026-08-15에 `/legal-verify` T3 풀 스윕을 돌려 P0 8건을 고쳤다.** 그 과정에서 나온 **재사용할 교훈 3개**는 꼭 보고 시작할 것:
①법령 원문은 **DRF API**로 얻는다(사람용 화면은 JS 렌더라 안 읽힌다) ②**면책 관련 문구는 허가/불허가를 갈라 적어야 한다**(공고 의무가 허가결정에만 있어 즉시항고가 14일 vs 1주로 갈린다)
③**한 곳을 고치면 같은 사실을 말하는 다른 곳을 함께 볼 것**(오늘 `discharge.html`만 고쳐 FAQ와 어긋난 일이 실제로 있었다). 상세는 아래 「현재 상태」 2026-08-15 항목 3개.

📅**날짜가 박힌 것 2개**(놓치면 조용히 당한다 — [OPERATIONS-COST.md](OPERATIONS-COST.md) 요약 절에도 표로 있다):
**2026-08-31** Anthropic 인트로 요금 종료(입력 $2→$3, 출력 $10→$15 · 조치 불필요, 실비만 1.5배) /
**2027-02-08** 솔라피 발신번호 `01037707164` 인증 만료(재인증 — 사업자등록 후면 사업자 명의 전환과 한 번에).

✅**Cloudflare WAF rate limiting — 2026-08-15 생성·검증·경로정정 완료.** 규칙은 `send-signup-code` + `request-reset` 두 경로를 덮는다.
Worker 커스텀 도메인에도 zone rate limiting이 **적용된다는 것을 실측으로 확인**(동시 12건 → 429 6건). 남은 구멍은 **로그인 경로**이고, 무료 플랜 규칙 1개 제약 때문이라 Cloudflare Pro 전까지는 그대로 둔다.

🔒**2026-08-16 첫 전면 보안 감사 — `security-auditor` 에이전트 신설 + 4건 수정(미커밋).**
감사 → adversarial-refuter 교차검증 순으로 돌렸고, **반박에서 최초 수정 하나가 무효 판정을 받아 다시 고쳤다.**

- ✅**주민번호 마스킹 구분자 확대**(`api/src/index.js` + `js/main.js` **두 벌 동일**) — HWP·Word가 하이픈을 **en dash(–)로 자동 변환**하고 PDF 복사는 U+2010을 낳아, **법원 서류를 붙여넣는 정상 경로가 곧 우회 경로**였다(14종 중 11종 통과). 전각 숫자·소프트하이픈도 포함. ⚠️구분자 주위 **공백·마침표는 일부러 제외** — 한 번 넓혔다가 `잔액 500000 - 1234567`(뺄셈)·개행 낀 불릿 두 줄이 전부 마스킹돼 서류가 깨졌다. **남은 한계**: 공백 구분(`901010 1234567`)은 못 잡고, 6-7 형식 계좌·법인등록번호는 과하게 가린다(원래부터). 정확히 하려면 라벨 근접 판정 필요. **`privacy.html` 제3항이 "모두 자동으로 가려집니다"라고 무조건 약속하고 있어 문구 조정 또는 판정 추가 중 하나가 필요하다.**
- ✅**관리자 화면 XSS** — `admin.html`의 `onclick="refund('${esc(email)}')"`. 🔴**`esc()`에 작은따옴표를 더하는 수정은 무효다** — HTML 파서가 속성값 문자 참조를 **JS 컴파일 전에** 디코드해 `&#39;`가 `'`로 되돌아간다(headless Chrome 실측). `data-*` + 위임 핸들러로 **구조를 바꿔** 해결. 덤으로 이메일에 `'`가 든 회원의 환불 버튼이 죽어 있던 기존 버그도 해소. **판매 오픈 전 필수였다**(결제 행이 있어야 버튼이 렌더되므로 지금은 미무장).
- ✅**AI 회수 TOCTOU** — `checkAiQuota`→Claude 호출→`consumeAiQuota` 사이에 API 왕복이 통째로 들어가 동시 요청이 전부 검사를 통과했다. `reserveAiQuota`로 **선차감 + 실패 시 롤백**, 원자성은 조건부 UPDATE의 `meta.changes`로. 시뮬레이션: 동시 200건 → 예약 10건(일일 상한 준수), 종전이면 200건 전부 Anthropic으로 나갔다. ⚠️**`OPERATIONS-COST.md`의 "앱 전체 일 30회"는 사실이 아니다** — 상한은 1인당이고 전역 백스톱은 없다.
- ✅**`/api/analytics` D1 쓰기 폭주** — 비인증으로 DB 쓰기가 가능한 **유일한 경로**라 요청당 UPSERT면 D1 일 10만 쓰기를 태울 수 있었다(쓰기가 마르면 세션 생성이 죽어 **로그인부터 멈춘다**). 아이솔레이트 메모리 버퍼 + 10초 주기 기록 + 라벨 종류 하루 200 상한. 시뮬: 동일 이벤트 10만 회 → 쓰기 10건(집계는 정확), 라벨 5만 종류 → 행 199개에서 멈춤. **남은 한계**: 상한이 아이솔레이트별이라 완전한 해결은 Workers 유료 전환 또는 Analytics Engine 이전.

- ✅**유료 콘텐츠 서버 게이팅 — 회생·파산 2페이지 완료(2026-08-16).** 2단계 이후 본문·서식예시를 `api/src/content/{rehab,bankrupt}-steps.js`로 옮기고 `GET /api/content/steps?type=`이 `activeEntitlements` 확인 후에만 내려준다. 정적 파일 71.2KB→35.2KB / 69.4KB→36.9KB. 1단계는 그대로 둔다(전상법 시행령 제21조의2 제1호 미리보기 근거). **콘텐츠 편집 방식이 바뀌었으니 [CLAUDE.md](CLAUDE.md)의 「유료 본문은 서버에 있다」 절을 먼저 볼 것.**
  검증: headless Chrome으로 ⓐ비로그인=미리보기(로드맵 8단계·잠금카드·유출 0) ⓑ구매자=4단계 본문 실제 렌더 ⓒ서버 500=오류카드+재시도 버튼 ⓓ미보유 403=미리보기. 서버 게이트는 가짜 D1로 9종(401/403/만료/타패키지/400) 통과. 항목 수는 이관 전과 동일(회생 34 / 파산 33).
  그 과정에서 잡은 결함 2건: ①동시 호출 시 뒤늦은 호출자가 대기하지 않고 실패로 받아 오류카드를 그리던 것 → 진행 중 요청을 공유 ②`error` 상태에서도 렌더마다 자동 재요청해 실패한 서버를 두드리던 것 → 자동 로드는 `none`일 때만.
  ⚠️**남은 것: `maintenance.html`·`supplement.html`** — 페이지 전체가 유료라 구조가 달라 이번 범위에서 뺐다. 같은 이관 필요.

- ✅**Turnstile fail-open 좁힘 + 시크릿 설정 여부 노출(2026-08-16).** 종전에는 오류가 나면 무조건 통과라, siteverify가 흔들리는 동안 캡차가 통째로 열리고 `send-signup-code`의 409로 회원 이메일을 자동 열거할 수 있었다. 이제 **3초 타임아웃 + 1회 재시도**, 두 번 다 실패해야 통과하고 그때 `TURNSTILE_FAIL_OPEN` 로그를 남긴다. 캡차가 **거절한 경우는 그대로 거절**(응답이 온 이상 판정을 따른다). 실제 장애 시 가입 전면 중단은 여전히 피한다(1인 운영이라 한밤중 장애를 즉시 못 본다). 409는 유지 — UX 트레이드오프로 이미 판단된 것이고, fail-open 창이 닫히면 열거 위험이 실질적으로 사라진다.
  또 **운영 대시보드에 시크릿 설정 여부 카드**를 넣었다(`/api/admin/ops-status`의 `secrets`). Turnstile 시크릿이 빠지면 검증이 무조건 통과라 자동 가입 방지가 통째로 사라지는데 화면에 아무 표시가 없었다 — **"`TURNSTILE_SECRET`이 실제로 설정돼 있는지 확인 불가"였던 미해결 항목이 이걸로 대시보드에서 바로 보인다.** 값은 내보내지 않고 불리언만.
  검증: 재시도 로직 8종(거절 유지 / 1회 실패 후 성공 / 2회 실패 fail-open + 로그 / 시크릿·토큰 없음) 통과, 카드 렌더 2종(전부 설정 / Turnstile 빠짐 = 빨강), `wrangler dev` 실전 호출로 `secrets` 응답이 로컬 환경과 일치하고 값 노출 없음 확인.

- ✅**보통 등급 4건 일괄 정리(2026-08-16).** 🔴**`phone_send_log` 테이블이 추가됐다 — push 전에 마이그레이션을 먼저 적용할 것**(`api/migrations/2026-08-16-phone-send-log.sql`, 원격은 `--command`로).
  - **본문 크기 상한 우회 차단**: `readJson`이 `Content-Length` 헤더만 보고 판단해, 헤더를 빼면(HTTP/2·chunked) `len=0`이 되어 상한이 통째로 무시됐다. 실제로 읽은 바이트로 재도록 바꿈(한글은 3바이트라 글자 수가 아니라 `TextEncoder`로 잰다). 검증: 20KB 본문을 `Transfer-Encoding: chunked`로 보내 **400**(고치기 전에는 통과해 401까지 갔다).
  - **수신번호 기준 SMS 한도**: 종전 한도는 전부 '계정당'이라 계정을 갈아타면 같은 번호로 계속 보낼 수 있었다. `phone_send_log`로 **번호 하나당 24시간 10통**을 세고, AI 회수와 같은 방식으로 **보내기 전 원자적 선점 + 발송 실패 시 환원**. 검증: 한도 소진 시 429, **다른 계정으로도 429**(핵심), 다른 번호는 통과, 발송 실패 3회 후 카운터 0으로 복귀, 25시간 경과 시 창 초기화.
  - **로그에서 개인정보 제거**: Resend·솔라피 오류 **응답 본문을 통째로** 기록하고 있었다(수신 주소·번호가 들어온다). 오류 유형 코드만 남기도록 변경 — Resend는 `name`만(`message`는 "Invalid `to` field: …"처럼 주소를 문장에 넣는다), 솔라피는 `errorCode`만. 검증: 로그에 `sms send fail 400 ValidationError`만 남고 전화번호 **0건**.
  - **계정 잠금 표적 공격**: 잠금 키가 이메일이라 남의 이메일로 일부러 틀려 표적 계정을 잠글 수 있다. **IP를 키에 넣는 방식은 채택하지 않았다** — CGNAT·공용 와이파이에서 정상 이용자를 무더기로 막아 더 나쁘다. 대신 429 안내에 **복구 경로를 명시**(비밀번호 재설정에 성공하면 잠금도 풀린다 — 이미 구현돼 있는데 안내가 없어 피해자가 15분 기다리는 것 말고 할 게 없어 보였다).

🔴**아직 안 고친 감사 발견:**
1. `maintenance.html`·`supplement.html` 유료 콘텐츠 서버 게이팅(페이지 전체가 유료라 구조가 다름).
2. 프롬프트 인젝션 — UNCERTAIN. 판정하려면 별도 키로 스테이징에서 적대적 초안 10~20건을 돌려야 한다. 저비용 보강안은 `question` 값이 물음표로 끝나지 않으면 서버가 버리는 후처리 검사.
3. **확인 못 한 것**: 포트원 `currency` 미검증의 실제 영향. (`TURNSTILE_SECRET` 설정 여부는 이제 운영 대시보드 「시크릿 설정」 카드에서 바로 보인다 — 배포 후 한 번 열어 확인할 것.)

---

## ✅ 사업자등록 전에 할 수 있는 일 — 위에서부터 집으면 된다

### A. 오픈 직전에 어차피 해야 하는 것 (미리 해 두면 오픈이 빨라진다)
1. ✅**전상법 제13조 제2항 각 호 원문 대조 — 2026-08-15 완료.** 결핍 5건을 찾아 4건 반영, 1건(전화번호)은 아래 3번에 걸려 있다. 상세는 「현재 상태」 2026-08-15 항목.
2. **UNCERTAIN 2건 재확인** — ⓐ송달료 **5,640원**의 2026-07-01 시행일(klac·easylaw로는 확인했으나 대법원 공지 미확인) ⓑ파산 인지 2,000원의 전자소송 10분의 9 감액 여부(실무자료는 1,800원이라 하나 법제처·법원 안내는 2,000원 — **현재 감액 미적용이 안전한 방향**이라 그대로 둠).
3. **📞 대표 전화번호 결정** — 사용자 결정 사항. **전화번호는 초기화면 법정 표시사항**이다(전상법 제10조 제1항 제3호, 과태료 500만원 이하). 표기 여부가 아니라 **어느 번호냐**만 정하면 된다.
   - ①개인 휴대폰(비용 0이나 푸터 상시 노출 → 크롤링·스팸·사생활) ②**사업용 번호(070·안심번호)+착신전환(권장)** — 사업자등록과 함께 처리하면 개인 명의 SMS 발신번호의 통신사 한도 문제도 같이 정리된다.
   - 🔴**근거가 하나 더 늘었다(2026-08-15).** 제13조 제2항 제2의2호의 위임 고시 별표 품목 (33) 디지털콘텐츠가 **「소비자 상담 관련 전화번호」**를 요구한다 → 초기화면 표시(제10조)와 **상품 정보 고지**(제13조②) **양쪽**에서 필요하다.
   - ⚠️나중에 바꾸면 **다섯 곳**을 동시에 고쳐야 한다 — 통신판매업 신고정보 · 약관 제11조 · `BUSINESS_INFO`(주문확인 메일) · `renderFooter()`(푸터) · **`pricing.html` 「상품 정보 및 거래조건」 블록**(그 자리에 ⚠️주석을 남겨 두었다).

### B. 결핍 보완 (콘텐츠 — 이용자 영향 큰 순서)
1. ✅**파산 선고 이후~복권 구간 페이지 — 2026-08-15 `bankruptcy-after.html` 신설(무료).** 상세는 「현재 상태」 2026-08-15 항목.
2. ✅**공고 확인처 — 2026-08-15 완료.** 「면책결정과 확정」 절에 「📢 그러면 공고는 어디서 보나」 블록 신설.
   근거는 「개인파산 및 면책신청사건의 처리에 관한 예규」 **제7조**(공고는 전자통신매체 방법을 원칙으로 하고 **법원 홈페이지 법원공고란**에 게시 — **관보가 아니다**).
   🔑**실제 창구를 찾아낸 경로**: 대법원 포털 「공고 &gt; 회생·파산」(`/portal/notice/insolvency/insolvency.jsp`)이 검색 화면을
   `https://ssgo.scourt.go.kr/ssgo/ssgo930/rhblBnkp.on`으로 **iframe 감싸고 있다.** 링크는 **포털 페이지 쪽**을 걸었다(내부 앱 URL은 바뀔 수 있다).
   ⚠️검색 화면이 SPA라 **입력 항목까지는 확인하지 못해** 단정하지 않고 "여기서 확인 / 못 찾으면 사건번호로 법원에 문의"로 적었다.
   함께 고친 것 = 표 각주의 「나의 사건검색」 링크가 **`href`가 루트라 라벨과 어긋나 있던 것**(`/portal/information/events/search/search.jsp`로 교정) + 각주에 "공고일은 사건검색이 아니라 아래 블록에서 본다"는 분리 안내.
3. ✅**서류 체크센터 누락 6종 — 2026-08-15 완료.** 서류 id 31→37. 전부 `opt: true`로 넣어 해당 없는 사람이 「해당없음」으로 넘길 수 있게 했다.
   `d-c5` 진단서·입원확인서(공통) / `d-i5` 국민연금·건강보험 자격득실(소득) / `d-i6` 사업자등록증명·폐업사실증명원(소득) /
   `d-a6` 퇴직금 예상액 증명서(재산 — **아직 받지 않았어도 재산이고, 2분의 1은 압류금지**) / `d-a7` 통장 거래내역서 1~2년(**보정 단골**) / `d-a8` 지방세 세목별 과세증명서(체납분은 비면책채권이라 목록에 별도 정리).
   ※면제재산목록은 같은 날 `d-b9`로 추가됨.
4. ✅**`취하`·중도 이탈 — 2026-08-15 완료.** `bankruptcy-after.html`에 접이식 「신청을 되돌릴 수 있나요?」 + FAQ 신설(`key: withdraw`).
   - **개인회생 제594조**(확인) — 개시결정 전에는 취하 가능하되 **제592조 보전처분·제593조 중지명령을 받은 뒤에는 법원 허가** 필요.
     🔴화면에 넣은 함정 경고 = **"추심이 멈춘 것을 보고 임의로 그만두면 안 된다 — 그 멈춤이 바로 보전처분·중지명령이다."**
   - **파산** — 선고 후에는 **제538조 「동의에 의한 파산폐지」**가 유일(채권신고기간 내 신고 채권자 **전원 동의** 또는 담보 제공 → 현실적으로 드묾).
   - ⚠️**파산선고 「전」의 취하를 정한 명문 규정은 찾지 못했다.** 제48조는 **회생절차 전용**(조문 제목이 「회생절차개시신청 등의 취하의 제한」)이고 파산에 대응물이 없다.
     화면에 **확인 못 했다고 명시**하고 법원 문의로 안내했다. 나중에 찾으면 여기부터 채울 것.
5. **영업소득자 경로가 얕다** — 사업 계속 여부, 필요경비 뺀 소득 산정, 폐업 시 처리, `새출발기금`(0건).
6. ✅**연대보증 — 2026-08-15 완료.** `bankruptcy-after.html` 「남는 채무」 절에 **제567조**(면책은 보증인에 대한 권리·담보에 **영향을 미치지 아니한다**) 블록 신설 + FAQ 딥링크(`key: guarantor`).
   🔴핵심 문장 = **"면책이 확정되면 채권자는 받을 곳이 보증인뿐이 되므로 그때부터 청구가 간다 — 미리 알리라."** 보증인이 대신 갚으면 생기는 **구상권도 내 채무**라 채권자목록 누락 주의(제566조 제7호와 연결).
   ⚠️**"보증인도 같이 신청해야 하나"는 답하지 않았다** — 그 사람의 재산·소득에 따라 갈리는 **판단**이라 변호사법 제109조 방어선을 지켰다. 사실(조문)만 적었다.
7. **심문기일 실무 준비** — `복장` 0건. 소요시간·지참물·동행 가능 여부·지각 시 처리.
8. **`개인워크아웃`·`새출발기금` 미기재**(compare.html) — 그 이름으로 검색해 온 사람이 자기 상황을 못 찾는다.
9. **`setup.html`이 안 묻는 것 3가지** — 세금·4대보험 체납(비면책채권+추가서류), 배우자 소득(부양가족 인정 시 증빙 요구), 질병·장애(파산 진술+진단서).
10. **비면책 채무를 어떻게 처리하는지 0건** — 면책 후 남은 세금·건강보험료·과태료를 어디로 가져가야 하는지(세무서·지자체 세무과·건강보험공단의 분납·징수유예 창구). `징수유예` 0건. **법률 판단이 아니라 "어디로 가라"이므로 방침에 안전하다.**

### C. 품질·구조
- **🔴 변호사법 3순위 지적(구조적)** — 유료 패키지가 절차 전 과정을 지시하는 형태는 **'사실상 사건 처리 주도'** 법리(대법원 2014도16204)에 닿을 수 있다. **유료라는 사실만으로 제109조 제1호의 유상성 요건은 이미 충족**되므로 방어는 "판단·선택·작성은 이용자가 한다"를 화면에서 실제로 지키는 것뿐이다. `ai-review`에서 심각도 등급을 걷어낸 판단이 그 방향 — **새 기능을 붙일 때마다 같은 기준을 재적용할 것.**
- **텍스트량 감량** — 방향 미정(사용자 결정). rehabilitation 9,853자 / bankruptcy 8,519자. 3안: ①단계별 접기(추천) ②상단 "지금 할 일 3가지" ③상세를 더보기로.
- **③검증된 지식 베이스** — 관할법원별 양식·실무 차이, 보정 유형. 정확성이 크게 오르지만 **자료 조사 시간이 가장 많이 든다.**
- **④재검토 이력** — 구현은 작다(`ai_records.topics` 이미 저장 중). ⚠️단 지난 질문 주제를 AI에 다시 보내야 해 **방침 개정이 필요**하다. 초안 원문은 절대 저장 금지.
- 진행센터 체크리스트 항목 안에 [작성예시][검토받기] 배치(현재는 `ai-review.html` 별도 페이지)
- `포괄적금지명령`·`별제권`이 GLOSSARY에는 있으나 **본문 어디에도 안 쓰인다** — 담보채무 콘텐츠 공백 신호.
- ✅**D1 백업·복구 — 2026-08-15 수립·검증 완료.** 상세는 **[BACKUP.md](BACKUP.md)**, 도구는 `tools\backup-d1.ps1`·`tools\restore-drill.js`.
  종전에는 **`PEPPER` 백업만 있고 DB 데이터 자체의 백업·복구 절차가 저장소 어디에도 없었다**(결제·이용권·청약철회 기록이 D1에 있는데).
  🔴**Time Travel 복구 창은 무료 플랜 7일**(유료 30일)이라 그것만 믿을 수 없어 정기 덤프를 함께 둔다.
  ⚠️덤프 = 개인정보라 **저장소 바깥**(`../_chamroad-backups`)에 두고 14개만 보관한다. 스크립트가 저장소 안 경로면 **실행을 거부**하고, `.gitignore`가 2차 방어선이다.
  ⚠️덤프에 `DROP TABLE`이 없어 **복원 대상은 빈 DB여야 한다.**
- ✅**규모 확대 계획(이용자 100명+) — 2026-08-15 조사·정리 완료.** [OPERATIONS-COST.md](OPERATIONS-COST.md) 「📈 규모가 커지면 손볼 것」.
  🔑**결론이 직관과 반대다: AI 한도는 올릴 필요가 없다.** Start 티어 Sonnet 5는 1,000 RPM / 입력 2M TPM인데
  우리 최악의 검토 1건이 입력 16,500토큰이라 **분당 120건 동시**까지 여유다 — 100명이 같은 순간 눌러도 안 걸린다.
  실제로 먼저 걸리는 것은 **Resend 일 100통**(누적이 아니라 하루 피크). 그다음이 크레딧 잔액이다.
  ⚠️Resend를 Pro로 올릴 때 **코드 상수 2개(`SIGNUP_EMAIL_DAILY_CAP` 60 · `EMAIL_DAILY_FREE` 100)를 같이 올려야 한다** —
  안 올리면 우리 코드가 계속 60에서 끊어 **돈만 내고 한도는 그대로**가 된다.
  ⚠️**Workers Paid $5/월과 Cloudflare Pro $20/월은 다른 상품이다**(전자=D1 Time Travel 7→30일, 후자=WAF 규칙 1→2개). 섞어 적지 말 것.
  🔴**2026-08-31에 Anthropic 인트로 요금이 끝난다**(입력 $2→$3, 출력 $10→$15). 규모와 무관하게 9월부터 AI 실비 1.5배.
  ※검토 1건 실비는 보통 58원 / 최악 177원(추정) — 149,000원 패키지 대비 원가 1.4% 이하라 **비용은 문제가 아니다.**
  진짜 위험은 **잔액 조회 API가 없어 `ops-status`에 못 띄운다**는 것 → 자동충전이 사실상 필수.
- **`bankruptcy-after.html`의 조문 인용 밀도** — 본문 대비 조·항·호 인용이 촘촘하고 「같은 조 제3항」식 상대 참조가 많다. **인용을 지우지 말고**(신뢰의 근거다) 표는 근거를 별도 열로, 본문은 문단 끝 각주로 빼는 방향(CLAUDE.md의 "결론은 보이고 근거는 접힘"과 같은 선).
- ✅**`bankruptcy.html` ⑩목록 — 2026-08-15 재작성.** 복권이 없던 것뿐 아니라 **순서가 틀려 있었다**(관재인 선임과 심문기일이 파산선고 **앞**에 놓여 있었는데, 제312조는 선임을 "선고와 동시에", 제558조는 심문을 선고 후로 정한다). 두 갈래·종결/폐지·의견청취·확정 기산점·복권까지 조문과 맞춰 다시 썼다.
- ✅**FAQ 도박 문항 — 2026-08-15 회생 대비 추가**(`key: gambling`). 파산은 제564조 제1항 제6호 불허가사유 + 제2항 재량면책, **개인회생은 도박·낭비로 늘어난 빚이어도 이용 가능**. 근거는 서울회생법원의 제도 비교 안내 원문(개인회생 「낭비, 도박으로 빚이 늘어난 것이어도 가능」 / 개인파산 「…면책이 불허가 될 수 있음」).

---

## 🔒 사업자등록 이후 순서 (뒤집지 말 것)
1. **사업자등록** → 통신판매업 신고 여부 판단(직전연도 50회 미만 **또는** 간이과세자면 면제)
2. `api/src/index.js`의 **`BUSINESS_INFO` 실값 교체**(현재 플레이스홀더, 대표자만 최은식) + **`terms.html` 제11조**에 동일 기재
   ※`privacy.html` 12항은 개인정보 보호책임자 조항이라 **고칠 것 없음**(2026-08-12 확인)
3. 🔴**사업자 신원을 「초기화면」에 표시** — 약관 안에만 적는 것으로는 부족하다(전상법 제10조 제1항, 과태료 500만원 이하). 구현은 **`js/main.js`의 `renderFooter()` 한 곳.** 사업자정보 공개페이지 링크도 함께.
4. 두 문서 **시행일을 오픈일로 새로 정하고 `CONSENT_VERSION` 동반 갱신** → **그때부터 부칙에 개정 이력을 하나씩 기록**(안내문은 두 문서에 이미 적혀 있다)
5. **포트원 라이브 채널** 시크릿 3종 교체 + 실결제 1건 검증 + 결제 모달 SMS 인증 E2E(유일한 미검증 구간)
6. `TEST_PAY_EMAILS` 시크릿 삭제 → **`PAYMENTS_ENABLED` 두 곳 true** → 오픈
상세는 `LAUNCH-CHECKLIST.md`.

---

### ⚠️ 이 프로젝트에서 반복해서 걸리는 것들
- **`npx wrangler`가 4.120.1을 받으면 D1 원격 쿼리가 `code 7403`으로 실패한다** → 항상 `npx wrangler@4.120.0`으로 고정.
- **Bash 툴에서 `npx.cmd`는 "C:\Program" 경로 분할 오류** → wrangler는 **PowerShell로** 실행.
- **문서·문자열 수정은 반드시 `Edit` 툴로.** Bash heredoc 안의 백틱은 명령 치환되고, **python 힙독의 `\n`은 실제 줄바꿈으로 들어가 파일을 깨뜨린다**(2026-08-13·14에 두 번 당했다. 둘 다 인라인 스크립트 파싱 검사로 잡았다).
- **D1 마이그레이션은 Worker 배포보다 먼저.** 새 코드가 SELECT하는 컬럼이 없으면 로그인이 통째로 깨진다.
- **개인정보 3원칙을 완화하지 말 것** — `CLAUDE.md`의 "개인정보" 절. 주민번호는 **동의로도 처리 불가**.
- **고지 ↔ 실제 동작이 어긋나면 그 자체가 분쟁 원인이다.** 2026-08-14에 실제로 하나 걸렸다(붙여넣기 화면의 "어디로도 전송하지 않습니다"인데 서류 간 대조에서 국외로 나갔다). 새 기능의 안내 문구를 쓸 때 **코드 경로를 먼저 확인할 것.**
- **`GLOSSARY`에 용어 추가 시 중복 키 확인** — 객체 리터럴이라 조용히 뒤엣것이 이긴다.
- **검증 에이전트 5종은 `챔로드/.claude/agents`에 있다.** CWD가 상위 폴더면 등록되지 않으므로(`Agent type not found`) 정의를 프롬프트에 주입해 general-purpose로 돌린다. 긴 검증은 세션 한도로 죽을 수 있는데, **새로 띄우지 말고 SendMessage로 재개**하면 문맥이 살아 있다.
- **PowerShell 스크립트(.ps1)는 반드시 UTF-8 BOM으로 저장할 것.** Windows PowerShell 5.1은 BOM이 없으면 ANSI로 읽어 **한글 주석이 깨지고 파서가 죽는다**(2026-08-15에 `backup-d1.ps1`이 그렇게 실패했다).
- 🔴**PowerShell에서 한글은 변수명으로 유효하다** — `"$tables개"`는 `tables개`라는 없는 변수로 읽혀 **조용히 빈칸**이 된다. 한글이 바로 붙는 자리에는 `${tables}개`로 경계를 줄 것. (실행해 보지 않았으면 못 잡았을 버그다.)
- **테스트 하네스는 스크래치패드에 있다**(`verify.js`·`verify2.js`·`verify3.js`·`paste.test.js`·`flow.test.js`·`chk2.js`). 세션이 바뀌면 사라지므로, 중요한 것은 다시 만들 것. `chk2.js`(인라인 스크립트 파싱 검사)는 **HTML을 고칠 때마다 돌릴 가치가 있다.**

---

## 현재 상태 (최신이 위로)

### 2026-08-15 (5차) — 운영 공백 2건: D1 백업·복구 + 가입 메일 남용 방어

「사업자등록·전화번호만 빼면 완벽한가」라는 사용자 질문에서 출발해 **운영 관점의 구멍**을 찾아 메웠다.
콘텐츠·법령 고지는 상당한 수준이지만 **운영은 아니었다**는 것이 점검 결론이었다.

**① D1 백업·복구** — 상세는 위 항목/[BACKUP.md](BACKUP.md).

**② 가입 인증메일 남용 방어(`SIGNUP_EMAIL_DAILY_CAP = 60`)**
- 🔴**구멍의 정확한 모양**: `send-signup-code`는 **주소별로만** 제한한다(60초 쿨다운·시간당 5회).
  **서로 다른 주소 200개면 200통이 나간다.** 유일한 방벽인 Turnstile은 **fail-open**(시크릿 미설정·네트워크 오류 시 통과)이라
  캡차가 흔들리면 Resend 일 100통이 통째로 탄다. 그러면 **기존 회원 비밀번호 재설정**과
  **주문확인 메일(전상법 제13조② 계약내용 서면 = 법정 의무)**까지 함께 멈춘다.
- **고른 해법**: 하루 **총** 발송량이 60을 넘으면 **가입 메일만** 끊는다 → 40통을 재설정·주문확인 몫으로 남긴다.
  ⚠️**스키마 변경이 필요 없다**(기존 `email_usage` 재사용) — 처음엔 `day` 키에 `#signup` 접미사를 붙일까 했으나
  관리자 월 집계가 `day >= monthStart` **문자열 비교**라 그 행까지 더해져 **월 통계가 오염**된다. 그래서 총량 기준으로 갔다.
  **IP를 저장하지 않으므로 개인정보가 늘지 않는다.**
- **관리자 화면 연결**: ops-status가 `signupCap`을 함께 내려보내고, 카드에 **「가입 메일 차단 중」 배지**가 뜬다.
  이게 없으면 운영자가 "67/100인데 왜 가입이 안 되지"에서 멈춘다.
- ✅**Cloudflare Rate Limiting 적용 완료(2026-08-15, 사용자가 대시보드에서 생성).** 절차·검증법은 [OPERATIONS-COST.md](OPERATIONS-COST.md) 0절.
  - 🔴**무료 플랜 제약이 결정적이다**: **규칙 1개** / 필드 **Path·Verified Bot만**(호스트명 조건 불가) / 카운팅 **IP만** / 기간·차단유지 **10초 고정**.
    내가 처음 안내한 "1분 5회·규칙 3종·호스트명 조건"은 **무료에선 불가능**이라 다시 썼다.
  - ✅**미확인이던 것이 확정됐다 — Worker 커스텀 도메인에도 zone rate limiting이 적용된다.**
    공식 문서에서 명시를 못 찾아 "가정하지 말고 검증" 단계를 넣어 뒀는데, `api.chamroad.com`에 동시 12건을 쏘자 **6건이 429로 차단**됐다(563ms).
  - 🔴**그 테스트가 내 실수도 잡았다** — 규칙에 넣은 `/api/auth/find-account`가 **존재하지 않는 경로**였다(404 6건).
    `find-account`는 **화면 파일 이름**이고 실제 API는 **`/api/auth/request-reset`**이다. **페이지 이름을 API 경로로 옮겨 쓰지 말 것.**
  - 기대치: 무료 플랜의 10초 창은 **연타 스크립트를 끊을 뿐** 분산 요청은 못 막는다. **실질 방어선은 `SIGNUP_EMAIL_DAILY_CAP`이고 WAF는 과속방지턱이다.**
- ⬜**(참고) 종전 기록 — 아직 안 한 것이었을 때의 메모.** IP 기준이라 코드가 아니라 대시보드에서 걸어야 하고,
  wrangler OAuth 토큰은 **zone read뿐이라 CLI로 못 한다.** 규칙 3종(가입/재설정/로그인)의 구체적 설정 절차를
  **[OPERATIONS-COST.md](OPERATIONS-COST.md) 0절**에 적어 두었다. **사용자가 대시보드에서 해야 한다.**

**③ 오픈 직전 점검 + 부재 시 대응 규칙** — [OPERATIONS-COST.md](OPERATIONS-COST.md)에 두 절 추가(문서 성격이 비용→운영 전반으로 넓어졌다).
- **오픈 직전**: 🔴솔라피 충전이 **가장 먼저**(번호 인증이 결제의 필수 관문이라 **잔액 0 = 결제 불가**) → Anthropic 자동충전 → Resend 여유 → 대시보드 확인 → D1 백업 1회.
  ⚠️발신번호 **인증 만료 2027-02-08**을 달력에 적을 것.
- **부재 대응 — 원문으로 확인한 기한**: 환급 **3영업일**(디지털콘텐츠는 **「청약철회등을 한 날」부터** 기산 — 전상법 제18조②, 우리 약관과 일치) /
  지연 시 **연 15%** / 개인정보 **열람 10일**(시행령 제41조④) / **정정·삭제는 지체 없이**(법 제36조②).
- 🔑**부재 리스크는 생각보다 좁다** — 마이페이지 청약철회는 **14일 이내+미개시+PG설정이면 운영자 없이 즉시 전액환불**된다(`handleWithdrawRequest`).
  손을 대야 하는 건 ①열람 시작분 ②14일 경과 ③PG 취소 실패 ④이메일 직접 요청 **넷뿐**이다.
- 🔴**확정 결정(2026-08-15 사용자) — 판매는 항상 열어 둔다. 부재를 이유로 `PAYMENTS_ENABLED`를 닫지 않는다. 다시 제안하지 말 것.**
  (내가 "2영업일 넘게 비우면 판매를 닫자"고 제안했으나 사용자가 원하지 않는 방향이라고 정정했다.)
  대신 **운영자가 매일 1회 관리자 대시보드의 「🧾 청약철회 신청」을 직접 확인**해 3영업일 기한을 지킨다.
  자동 전액환불로 끝난 건은 그 목록에 남지 않으므로 **목록에 뭔가 있다 = 내가 판단할 건이 있다**는 뜻이다.
  ⚠️**하루 확인을 거르면 곧바로 법정 기한 위험**(연 15% 지연배상금). 알림 연동·자동화는 **사용자가 추후 구현 예정**.
- **솔라피**: 만원 충전 완료, 자동충전은 초반 운영 후 판단(사용자) — 현 단계에서 우선순위 낮음.

**🔑실행해 봤기 때문에 잡은 것**: 처음에 존재하지 않는 `SUPPORT_EMAIL` 상수를 썼다. `node --check`는 통과한다 —
**한도에 걸리는 순간 500으로 터졌을 코드**였다. `wrangler dev --local`로 두 방향을 실제로 태워 확인했다:
오늘 99통 → **429 + 정확한 안내문**, 59통 → **통과**(그다음 로컬 스키마에서 멈춤). 실제 메일은 나가지 않았다(`.dev.vars`에 `RESEND_API_KEY` 없음).

### 2026-08-15 (3차) — 결핍 3건 보완: 면제재산 서류 · 관재인 구간 · 딥링크 ⚠️**아직 push 안 함**

풀 스윕이 남긴 「다음 세션」 항목 중 셋을 처리했다. 조문 5개를 새로 원문 확보했다(제484조·제530조·제545조·제321조·제488조).

**① `documents.html`에 면제재산목록 추가** (`d-b9`, 파산 탭, 신청서 바로 다음)
- 제383조 제2항·제3항. **`opt: true`로 뒀다** — 임차보증금도 예금도 없는 사람이 '해당없음'으로 넘길 수 있어야 한다
  (필수로 두면 체크리스트를 100%로 만들 수 없는 사람이 생긴다. 과거에 걸린 문제다).
- 문구는 반박 검증 결과를 따랐다: **"선고 후 14일"이 아니라 "파산신청서와 함께 — 늦어도 선고 후 14일"**.

**② `bankruptcy-after.html`에 관재인 사건의 「선고~채권자집회」 구간 신설** — 종전에는 카드 4줄 뒤 곧바로 면책 심리로 넘어가 수개월이 비어 있었다.
| 조문 | 화면에 들어간 내용 |
|---|---|
| **제321조 제1항** | 채무자는 **파산관재인·감사위원 또는 채권자집회의 요청에 의하여 필요한 설명을 하여야 한다**(의무). 반대로 **어떤 자료를 낼지는 법이 정하지 않는다** → 통지에 적힌 것을 따르라(확인 못 한 실무를 추측하지 않고 구간을 채우는 방법) |
| **제484조** | ①법원이 우편물을 관재인에게 배달 촉탁 가능 ②관재인이 **열어볼 수 있다** ③🔴**채무자는 열람을 요구할 수 있고, 파산재단과 관련 없는 것의 교부를 요구할 수 있다** — 모르면 체념하고 요구하지 않는다 |
| **제488조** | 제1회 채권자집회에서 관재인이 **파산선고에 이르게 된 사정과 채무자·파산재단의 경과·현상을 보고**한다 |
| **제530조 / 제545조** | 출구 두 갈래 — 배당까지 갔다면 **파산종결**(채권자집회 종결 시 결정+공고) / 재단 부족이면 **파산폐지**(제545조①, **채권자집회 의견청취 필수**, 즉시항고 가능) |
| **제557조 제1항** | 🔴**종결·폐지는 면책의 앞 단계다** — "면책신청이 있고 파산폐지결정 확정 또는 파산종결결정이 있는 때" 강제집행이 중지된다 |
- ⚠️**선고 때 못 본 「파산폐지」가 몇 달 뒤 오는 것은 제545조이고 동시폐지(제317조)와 다른 결정**이라는 경고를 넣었다. 모르면 "떨어졌다"로 읽는다.
- 복권 절의 **제538조**에 요건(신고 채권자 **전원의 동의**)을 밝혀 "또 다른 복권 경로"로 오해하지 않게 했다.
- 복권 ↔ 확정증명원 상호 참조 추가(당연복권은 결정문이 없어 **확정증명원이 복권 증명 수단**이다).

**③ FAQ·서류 딥링크 구현** — 25문항이 전부 접혀 있어 링크로 특정 답에 도달할 수 없었다.
- `resources.html?q=<key>`(또는 `#faq-<key>`) → 해당 문항을 **펼치고 스크롤 + 2.4초 테두리 강조**. `hashchange`도 처리.
- `documents.html?doc=<서류id>`(또는 `#wrap-<id>`) → **탭을 그 서류의 분류로 옮긴 뒤** 스크롤·강조.
  ⚠️브라우저 해시 스크롤은 `renderDocs()`보다 먼저 지나가므로(목록이 JS로 그려진다) 직접 스크롤해야 한다.
- 🔴**인덱스가 아니라 `key`로 가리킨다.** 배열 인덱스로 걸면 문항을 추가·삭제할 때 링크가 조용히 다른 답을 가리킨다.
  현재 key 4개: `job-limits` · `discharge-denied` · `seizure-release` · `credit-recovery`. **링크를 걸 문항에는 key를 붙일 것.**

**④ FAQ 압류 문항에 회생/파산 구분 추가**(사용자 지적에서 나왔다 — "면책 FAQ에 절차 구분이 필요하지 않나").
🔴**압류 효력이 사라지는 시점이 절차마다 다르다**: 파산은 **면책결정 확정 시**(제557조②), 회생은 **변제계획인가결정 시**(제615조③)로 **3~5년 더 이르다.**
FAQ가 파산 규칙만 적어 회생 이용자에게 "변제를 다 마칠 때까지 기다리라"로 읽혔다.
⚠️**이 불일치는 같은 날 `discharge.html`을 제615조③ 기준으로 고치면서 내가 만든 것**이다 — 한 곳을 고치면 같은 사실을 말하는 다른 곳을 반드시 함께 볼 것.
이제 `discharge.html`·`resources.html`·`bankruptcy-after.html`이 같은 말을 한다.

**🔑면책 FAQ에서 절차 구분이 필요한지 판정하는 기준**(다음에 또 물을 것이다):
- **구분 필요** — 목록 누락 채권(🔴회생 제625조②1호는 **기재 누락만으로 비면책, 악의 요건 없음** / 파산 제566조7호는 **악의 + 채권자가 선고를 안 때는 면책**이라는 단서), 압류 효력 상실 시점, 재신청 제한 기간(파산 7년·5년 / 회생 5년), 면책불허가 사유·심리 구조(파산만 제562조 이의신청·제558조 심문이 있다).
- **구분 불필요** — 조세·벌금·고의 불법행위·근로자 임금·양육비 등 **비면책채권 본체는 두 조문이 거의 같다**(제566조 ↔ 제625조②).
- 이미 잘 구분된 문항: 「한 번 면책받으면 다시는 못 받나요」·「모든 빚이 없어지나요」·「보증인에게 피해가 가나요」.

**검증**: 3개 파일 인라인 스크립트 파싱 통과, **딥링크 대상 실존 검사 5건 문제 0건**, 중복 서류 id 0건.
검사 스크립트는 스크래치패드 `linkcheck.js`(FAQ key·서류 id를 수집해 모든 `?q=`/`?doc=`/내부 html 링크가 실존하는지 확인) —
**"안내한 다음 행동이 도달 불가"는 이 프로젝트에서 반복해 걸린 유형이라 링크를 추가할 때마다 돌릴 가치가 있다.**

### 2026-08-15 — 🔴`/legal-verify` T3 풀 스윕(5종): P0 8건 수정 ⚠️**아직 push 안 함**

새 페이지를 만들었으므로 T3로 판정해 `legal-verifier`·`journey-auditor`·`gap-auditor`·`consistency-checker`를 병렬로 돌리고,
법률 축 결과를 `adversarial-refuter`에 넘겼다. **반박이 실제로 두 건을 뒤집었다** — 스킬의 순서를 지킨 값을 했다.

**🔴 가장 위험했던 발견 — 즉시항고 기간이 비대칭이다 (CONFIRMED)**
공고 의무는 **면책허가결정에만** 있다(파산 제564조 제3항 / 회생 **제624조 제4항** = "면책의 결정을 한 때").
따라서 **불허가·기각·각하 결정은 공고하지 않으므로** 제13조 제2항(공고일부터 14일)이 적용되지 않고,
제33조로 준용되는 **민사소송법 제444조**에 따라 **고지받은 날(결정정본 송달)부터 1주 · 불변기간**이다.
→ 파산 3곳(`bankruptcy-after.html` 표·본문, `resources.html` FAQ)과 **회생 1곳(`discharge.html`)** 을 고쳤다.
⚠️**이 비대칭은 회생·파산 양쪽에 다 있다.** 앞으로 면책 관련 문구를 쓸 때마다 허가/불허가를 갈라 적을 것.

**반박이 뒤집은 것 2건**
- ❌`legal-verifier`: "「압류가 저절로 풀리지 않는다」는 제557조 제2항과 정면으로 어긋난다" → **REFUTED.**
  **서울회생법원이 같은 표현을 쓴다**("면책결정이 확정되었다 하더라도 압류가 곧바로 해제되는 것은 아닙니다").
  법률 오류가 아니라 **일관성 결함**이었다 → 효력 상실(제557조②)과 외관 정리를 나눠 적고, **비면책채권·선고 후 신득재산은 실효 대상이 아니라는 범위**를 보탰다.
- ❌`gap-auditor`: 면제재산을 "**선고 후 14일**"로 최상단에 → **PARTIALLY REFUTED.** 제383조 제3항의 창구는 **파산신청일부터** 열리고,
  🔴**선고 전에 신청해야만** 제8항의 강제집행 중지·금지명령을 받을 수 있다. "선고 후 14일"로 쓰면 이용자가 선고를 기다려 그 보호를 놓친다
  → **"파산신청서와 함께 — 늦어도 선고 후 14일"** 로 적었다. 심각도도 내렸다(임차보증금 최우선변제분은 제1항+민사집행법 제246조①6호로 신청 없이도 빠질 수 있다).

**수정한 P0**
| 파일 | 무엇 |
|---|---|
| `privacy.html` | AI 전송 분량 고지 **6,000 → 20,000자**. 어제 상한을 올리며 방침을 놓쳤다 — **국외이전 고지 ↔ 실제 불일치**(CLAUDE.md 개인정보 ③ 위반 상태였다) |
| `bankruptcy-after.html` | 제566조 제7호 **「악의」 요건 + 단서** 복원(내가 새로 쓰며 빠뜨렸다. 사이트 다른 3곳은 원래 정확했다) / 즉시항고 표 2행 분리 / 제557조② 범위 / 확정증명원 발급시기·"문의 후"·"등" / **면제재산 절 신설** / 제558조 제4항(기일에서 선고되면 **공고·송달 없음**) / 제564조①5의2를 **구인의 명**으로 한정 |
| `discharge.html` | 불허가 1주 추가 / **「공고의 효력이 생긴 날」→「공고가 있은 날」**(제9조②의 효력발생일은 게재 **다음 날**이라 하루 늦어진다) / 압류 실효 시점을 **제615조 제3항 = 변제계획인가결정**으로 정정(면책 확정이 아니다) |
| `resources.html` | 🔴**공무원 결격사유가 사실과 반대로 적혀 있었다** — 국가공무원법 **제33조 제2호**·지방공무원법 제31조 제2호가 명문 결격사유다. 재직자는 제69조①/제61조① **단서**로 "신청기한 내 면책신청을 하지 않았거나 면책불허가·취소 확정" 경우만 당연퇴직 → **임용/재직을 갈라 적고**, 채무자 스스로 신청한 사건은 제556조③ 간주로 이미 충족됨을 밝혔다 / 불허가 즉시항고 1주 / 제319조 인용을 제564조①5의2로 정정 |
| `pricing.html` | **「모든 채무 소멸」→「일부 채무 제외(제566조)」** (회생 쪽은 원래 정확했고 파산만 빠져 있었다) |
| `bankruptcy.html` | 「파산 전 **2년**」→「파산선고 전 **1년**」(제564조①2호) / `b8-5` hint를 **공고일 기산**으로 / `b8-6`에 실효 범위·"문의 후" |
| `numcheck.html`·`sitemap.xml`·`CLAUDE.md` | 초기 표시 6000→20,000자 / 새 페이지 등록 / GLOSSARY 33→34 |

**⚠️화면에 쓰지 않기로 한 것**: 확정증명원 **수수료**. `gap-auditor`가 「민사소송 등 인지법」 제10조(500원)에서 도출했으나,
같은 조 **단서**가 "대법원규칙으로 정하는 신청서"를 면제하고 2011년에 500원 신청서 39종이 면제된 개정이 있어 **도출이 무너진다**(반박 REFUTED).
근거 예규는 본문이 **HWP 첨부**라 못 읽었다. **이는 CLAUDE.md에 이미 있는 함정의 재발이다**(파산 인지 2,000원에 인지법 조문을 붙이지 말 것 — 2026-08-12).

**🔑새 gotcha — 법원 도메인 접근**: `slb.scourt.go.kr`·`ecfs.scourt.go.kr`은 **열린다.** 다만 **EUC-KR + JSP 프래그먼트**라
WebFetch로는 껍데기만 온다 → **`curl … | iconv -f EUC-KR`** 로 본문을 얻을 것. 실제로 막힌 것은
`help.scourt.go.kr`·`pro-se.scourt.go.kr`(**DNS 실패**)다. 이 전제를 잘못 알아 `legal-verifier`가 확인 가능한 것을 "확인불가"로 닫았다.

**검증**: `node --check js/main.js` 통과, 7개 파일 인라인 스크립트·`details` 균형 정상, 반박이 걷어내라고 한 표현 **잔존 0건**, 500원·인지법 제10조 **유입 0건**.

**남은 것**: ~~면제재산목록~~ ~~관재인 구간~~ ~~딥링크~~ → **2026-08-15 3차에서 완료**(위 항목).
아직 남은 것 = **조문 인용 밀도 완화**(새 페이지는 조·항·호 인용이 촘촘하다 — 표는 근거를 별도 열로, 본문은 문단 끝 각주로 빼는 방향) /
**공고 확인처 링크**(예규 제7조 = 법원 홈페이지 법원공고란. 페이지 전체가 "공고일 기산"에 걸려 있는데 확인처가 없다) /
**서류 체크센터 누락 6종** / `bankruptcy.html` 8단계 example의 ⑩목록에 **복권이 없다** /
FAQ 「도박인 경우 파산 면책이 안 되나요」는 파산으로만 답한다 — **회생에는 그런 불허가사유 목록이 없다**는 대비를 한 줄 넣을 여지(저우선).

### 2026-08-15 — ✅`bankruptcy-after.html` 신설: 파산 선고 이후~복권 구간 ⚠️**아직 push 안 함**

콘텐츠 결핍 목록 B-1을 닫았다. **조문 8개를 원문으로 확보해** 작성했다(위 전상법 항목의 DRF API 방법을 그대로 썼다).

**결핍은 사실이었고, 형태가 더 나빴다.** `의견청취기일`·`채권자집회`·`복권`은 **`pricing.html` 로드맵에만** 있고
본문 어디에도 없었다 — 즉 **요금제가 약속한 단계의 설명이 없는 상태**였다. `b8-5`·`b8-6`은 이미 조문까지 정확했지만
그 **앞뒤(선고 직후의 갈림, 의견청취, 확정 이후)** 가 비어 있었다.

**확보한 조문과 그로부터 확정한 것**

| 조문 | 확인한 내용 |
|---|---|
| 제317조 제1항 | **동시폐지** — 파산재단으로 비용 충당이 부족하면 파산선고와 **동시에** 폐지 결정 |
| 제312조 제1항·제2항 | 관재인형은 선고와 동시에 채권신고기간(2주~3월)·**제1회 채권자집회(4월 이내)**·채권조사기일 지정, 뒤 둘은 **병합 가능** |
| 제556조 제1항·제3항 | 면책신청은 파산선고 확정 후 1월 이내. 단 **채무자가 파산신청을 했으면 동시에 면책신청한 것으로 간주** |
| 제558조 제1항·제5항 | 심문기일은 **"할 수 있다"**(임의) + **채권자집회·채권조사기일과 병합 가능** |
| 제562조 제1항 | 이의신청 30일의 기산점은 🔴**심문기일부터**(정하지 않으면 법원이 정하는 날) |
| **제563조** | 🔴**이의가 있으면 법원은 「채무자 및 이의신청인의 의견을 들어야 한다」** — 조문 제목이 「이의신청에 관한 **의견청취**」이고, **기일 지정은 요구하지 않는다** |
| 제564조·제565조·제13조② | 면책결정은 **공고**(제564조③, 송달 생략 가능) → 즉시항고 **공고가 있은 날부터 14일**(제13조②) → **확정 후에야 효력**(제565조) |
| 제574조·제575조 | **당연복권** 3가지(면책확정 / 제538조 파산폐지 확정 / 사기파산 유죄 없이 10년) + **신청에 의한 복권** + 면책취소 시 복권 실효(제574조②) |

**🔴이 페이지가 존재해야 하는 이유(가장 값나가는 한 줄)**: 면책결정은 **송달을 하지 않을 수 있고** 즉시항고 기간은
**공고가 있은 날**부터 센다. 그래서 **"결정문을 받은 날부터 14일"로 세면 기준일 자체가 어긋난다.** 우편물을 기다리는
이용자가 가장 틀리기 쉬운 지점이고, 어디에도 적혀 있지 않았다.

| 파일 | 변경 |
|---|---|
| `bankruptcy-after.html` | **신설(무료)**. 선고 직후 두 갈래(동시폐지/관재인) → 법정 기간 표 → 면책신청 간주(접이식) → 기일 병합 → 이의신청과 의견청취 → 결정·확정 기준일 → 확정증명원 → **복권**(당연/신청) → 비면책은 링크로 넘김 |
| `bankruptcy.html` | 유료 잠금 **바깥**에 「⚖️ 파산 선고 이후(무료)」 카드 추가 |
| `discharge.html` | 「함께 보면 좋은 안내」에 파산 이용자용 카드 추가 — 헤더 `면책신청`은 **회생 전용 페이지**로 가므로 파산 이용자가 잘못 들어오는 경로가 있었다 |
| `pricing.html` | 로드맵 문구 정정 — 「이의 있을 경우 **의견청취기일 진행**」은 **실무 운영을 법정 절차처럼 말한 것**이었다(제563조는 기일을 요구하지 않는다). 기산점(심문기일부터)도 함께 넣고 새 페이지로 링크 |
| `js/main.js` | GLOSSARY에 **`복권`** 추가(일상어 '로또'와 글자가 같아 뜻이 어긋나 읽히는 말 — 태깅 기준 1) + 푸터에 새 페이지 링크 |

**⚠️미확인으로 남긴 것 = 면책확정증명원의 발급 신청 방법·수수료·소요기간.** 법령이 아니라 **법원 실무**로 정해지는 부분이고,
서울회생법원의 해당 안내문(「면책확정증명원은 언제 발급받을 수 있나요?」)과 `help.scourt.go.kr`이 이 환경에서 열리지 않았다
(전자는 JS 렌더, 후자는 **DNS 자체가 안 잡힌다**). 훈련지식으로 적지 않고 **화면에 "확인하지 못했다"고 명시**하고 법원 링크를 뒀다.
확정 **시점**은 조문으로 확정했으므로(공고일+14일) 이용자가 알아야 할 핵심은 비어 있지 않다.

**무료로 둔 이유**: 절차 **설명**은 이미 무료(안내 페이지들)이고 유료의 실체는 체크리스트·서류점검·AI다.
`discharge.html`(회생 면책)이 무료인 것과 같은 선이며, **유료 뒤에 절차 정보를 감추지 않는 편이 변호사법 방어에도 맞는다**
(CLAUDE.md의 "판단·선택·작성은 이용자가 한다").

**검증**: `node --check js/main.js` 통과, 4개 파일 인라인 스크립트 파싱·`details`/`summary` 균형 정상, GLOSSARY `복권` 중복 0건.

### 2026-08-15 — ✅전상법 제13조 원문 대조 완료: 결핍 5건 발견, 4건 반영 ⚠️**아직 push 안 함**

전날 **"원문 확보 실패로 미검증"**으로 남겼던 유일한 법령 항목을 닫았다. 훈련지식으로 열거하지 않고 **원문을 실제로 확보**했다.

**🔑 원문을 얻은 방법(다음에 또 필요하다 — 이대로 하면 된다)**
- 법령 조문: `https://www.law.go.kr/DRF/lawService.do?OC=test&target=law&MST=282793&type=XML&JO=001300`
  (`JO`는 조문번호 6자리 = 제13조 → `001300`. `OC=test`로 **통했다**.) 시행령은 `target=law&LM=<법령명>`.
  ⚠️**`law.go.kr`의 사람용 화면(`/법령/...`, `lsInfoP.do`, `admRulInfoP.do`)은 JS 렌더라 WebFetch로 아무것도 안 나온다.** DRF API를 쓸 것.
- 행정규칙(고시): `target=admrul&LM=전자상거래 등에서의 상품 등의 정보제공에 관한 고시`
- 🔴**고시의 별표는 텍스트가 아니라 GIF 이미지다.** `https://www.law.go.kr/LSW/flDownload.do?flSeq=<이미지id>`로 받으면
  WebFetch가 스크래치패드에 저장해 주고, **그 파일을 `Read` 툴로 열면 눈으로 읽힌다.** 이렇게 별표를 확보했다.
  (PDF는 이 PC에 `pdftoppm`이 없어 `Read`가 실패한다 — PDF 경로는 기대하지 말 것.)
- ⚠️**쇼핑몰 솔루션 업체의 「상품정보제공고시」 안내 페이지를 믿지 말 것.** 교차확인해 보니 품목 번호와 항목이
  **법제처 별표와 달랐다**(엉뚱하게 KC인증·제조국·배송료가 들어 있었다). 별표 이미지가 정답이었다.

**확보한 원문 — 제13조 제2항은 각 호가 12개다**(제1호·제2호·**제2의2호**·제3호~제11호, 시행 2026-07-21).
제2항은 의무가 **둘**이다: ⓐ계약 **전** 표시·광고 또는 고지 ⓑ계약 체결 후 **「계약내용에 관한 서면」을 공급할 때까지 교부**.
우리 쪽에서 ⓐ는 `pricing.html`, ⓑ는 **주문확인 메일**(`sendOrderConfirmation`)이 맡는다.

| 호 | 요구사항 | 대조 결과 |
|---|---|---|
| 1 | 공급자·판매자 상호, 대표자 성명·주소·전화번호 | ⚠️구조는 있고 **값이 플레이스홀더**(사업자등록 후 교체 — 기존에 알고 있던 항목) |
| 2 | 명칭·종류·내용 | ✅ |
| **2의2** | **재화등의 정보에 관한 사항**(제4항 위임 고시) | 🔴**결핍 4건** — 아래 |
| 3 | 가격·지급방법·지급시기 | ✅ |
| 4 | 공급방법·공급시기 | ✅ |
| 5 | 청약철회등 기한·행사방법·효과 + **행사에 필요한 서식** | ⚠️기한·방법·효과는 충실하나 **서식이 없었다** |
| 6 | 교환·반품·보증·환불 + **환불 지연 배상금** 조건·절차 | ✅ (연 15%까지 기재돼 있었다) |
| 7 | 전자매체 공급 재화의 **전송·설치 등 기술적 사항** | 🔴**전무했다** |
| 8 | 소비자피해보상·불만·분쟁 처리 | ✅ (약관 제10조 + 메일) |
| 9 | 거래에 관한 약관(**확인 방법 포함**) | ✅ |
| 10 | 결제대금예치·소비자피해보상보험 선택 가능 | ✅**해당 없음으로 확정** — 아래 |
| 11 | 시행령 위임사항 | ⚠️**2건 결핍** — 아래 |

**✅제10호는 우리에게 해당하지 않는다(확정).** 제10호는 괄호에서 **제15조 제1항의 선지급식 통신판매에만** 적용하고
**제24조 제3항 각 호의 거래는 제외**하는데, 같은 항 **제2호가 「여신전문금융업법」 제2조 제3호에 따른 신용카드 거래**다.
우리 결제는 포트원 신용카드 → 제외에 해당. (게다가 결제 즉시 공급이라 선지급식으로 보기도 어렵다. **논거가 둘이라 안전하다.**)

**🔴제2의2호 — 여기가 진짜 구멍이었다.** 위임 고시의 별표 품목 **(33) 디지털콘텐츠(음원·게임·인터넷강의 등)**는 6항목을 요구한다:
①제작자 또는 공급자 ②이용조건·이용기간 ③상품 제공 방식(CD·다운로드·실시간 스트리밍 등) ④**최소 시스템 사양·필수 소프트웨어**
⑤청약철회 또는 계약의 해제·해지에 따른 효과 ⑥**소비자 상담 관련 전화번호**.
이 중 ⑤만 이미 충족(「결제·환불 안내」)이었고 ①③④는 없었으며 ②는 이용기간만 있었다. **④는 법 제7호와 같은 내용을 요구한다.**

| 파일 | 변경 |
|---|---|
| `pricing.html` | 「📦 상품 정보 및 거래조건」 **접이식 블록 신설**(`details.fold`) — 제작자·공급자 / 제공 방식(웹 브라우저 열람, 다운로드·설치·실물매체·배송 없음) / 이용조건(본인 계정, 공유·양도 불가) / **이용 환경 최소사양**(인터넷 연결 + 최신 브라우저 + 자바스크립트, 설치 프로그램 없음) / **추가 부담 비용 없음**(통신요금은 이용자 부담) / **판매조건 제한**(로그인·만19세·번호인증·원화·재구매 시 연장·회수 합산) / 미성년자 취소권 / 소비자 상담·분쟁 창구. 고시 6항목 + 법 제7호 + 시행령 제20조를 한 블록에서 받는다 |
| `pricing.html` | 결제 직전 confirm에 **미성년자 취소권 고지 추가**(법 **제13조 제3항**). 이 항은 "미성년자와 계약을 체결할 때 **미성년자에게** 고지"하라는 것이어서 **약관 제6조⑤에만 적어 둔 것으로는 이행되지 않았다.** 만19세 자기신고를 받더라도 실제 미성년자가 통과할 수 있으므로 그 경우에도 고지가 남는다 |
| `api/src/index.js` | 주문확인 메일(=계약내용 서면)에 **제공 방식·이용 환경·이용조건·추가 비용 행 추가**, 판매자 행에 **제작자·공급자 동일** 명기, **「청약철회 신청서(서식)」 블록 신설**(제5호 괄호의 '행사에 필요한 서식' — 주문번호·결제이메일·신청인·철회 대상·신청일·신청 문구), **미성년자 취소권 문단 추가** |

**시행령도 원문으로 확인했다** — 제11호 위임은 **시행령 제20조**(①추가 부담 비용 ②판매조건 제한) 두 호뿐이고, 제2항 단서의
'대통령령으로 정하는 사유'는 **제19조의2**(계약자가 동의한 경우 / 계약자 주소를 알 수 없는 경우)다. 우리는 단서를 쓸 일이 없다(구매자 본인에게 보낸다).

**⚠️남은 결핍 1건 = 소비자 상담 전화번호**(고시 별표 ⑥). 번호가 정해지지 않아 닫을 수 없다 →
「사업자등록 전에 할 수 있는 일」 A-3에 근거를 보강하고, `pricing.html` 블록 위에 ⚠️주석을, LAUNCH-CHECKLIST에 **고쳐야 할 다섯 곳**을 적어 두었다.

**약관(`terms.html`)은 건드리지 않았다.** 제2항 각 호는 "표시·광고하거나 고지"로 충족되고 그 자리는 `pricing.html`과 주문확인 메일이다.
약관 개정은 **시행 7일 전 공지 의무**가 걸리므로, 실익 없이 개정 절차를 만들지 않는 편이 맞다.

**검증**: `node --check api/src/index.js` 통과, `pricing.html` 인라인 스크립트 파싱 통과·`details`/`summary` 1/1 균형
(스크래치패드에 `chk2.js` 재작성 — 세션마다 사라지므로 또 만들어야 한다).

### 2026-08-14 — 🔴 AI 입력 상한: 이용자 원고를 말없이 잘라내던 버그 수정 ⚠️**아직 push 안 함**

공개 전 점검 항목을 추리다가 발견. **표시 불일치인 줄 알았는데 실제 데이터 손실이었다.**

**증상**: `ai-review.html`은 5,000자, `numcheck.html`(서류 간 대조)은 6,000자를 넘으면
`substring()`/`slice()`로 **입력을 잘라냈다.** 이용자가 붙여넣은 진술서 뒷부분이 경고 없이 사라지고,
그 잘린 원고로 검토가 돌아가 **AI 회수까지 차감**됐다(패키지당 8~12회뿐). 검토 결과에는
"뒷부분이 없다"는 신호가 전혀 없어 이용자도 운영자도 알아챌 수 없는 구조였다.
게다가 화면 표시(5,000)와 서버 상한(`AI_TEXT_MAX` 6,000)이 서로 달랐다.

**상한 재설정 — 6,000 → 20,000자.** A4 한 장이 한글 약 1,700자이므로 20,000자 ≈ **11.8장**으로,
진술서·경위서·보정서 어느 것도 한 건이 이 분량을 넘지 않는다. 6,000자는 3~4장이라
채무 발생 경위를 상세히 쓴 진술서가 잘리는 값이었다.
**모델 한계와는 무관하다** — `claude-sonnet-5` 컨텍스트는 1M 토큰이라 애초에 걸림돌이 아니었고,
이 상한의 목적은 비용·남용 방지뿐이다. 회수가 패키지당 8~12회로 묶여 있어 총액이 튀지 않는다.

| 파일 | 변경 |
|---|---|
| `api/src/index.js` | `AI_TEXT_MAX` 6,000 → **20,000**. `AI_BODY_BYTES` 32KB → **128KB** — 한글 20,000자는 UTF-8 60KB이고 checklist·context·이스케이프가 더 붙는다. 여기가 모자라면 `readJson`이 null을 반환해 "입력이 너무 깁니다"(413) 대신 **"잘못된 요청입니다"(400)**가 나가 원인을 알 수 없게 된다(계산 결과 여유 62.8KB 확보) |
| `ai-review.html` | 잘라내기 **삭제**. 넘으면 글자수를 빨갛게 표시만 하고 무엇을 지울지는 이용자가 정한다. 보내기 직전 검사 추가 — 몇 자 넘었는지 알려준다. 표시 `0 / 20,000자`로 서버와 통일 |
| `numcheck.html` | 서류 간 대조에도 같은 수정(같은 `AI_TEXT_MAX`를 공유한다) |

**회수 차감은 원래 안전했다** — 서버가 길이 검사(413) → 동의 확인 → `checkAiQuota` 순서라
길이 초과로 되돌아가는 요청은 회수를 태우지 않는다. 잘림 버그는 **길이 검사에 걸리지 않게**
프론트가 미리 잘라버려서 생긴 것이었다.

**검증**: `node --check api/src/index.js` 통과, 잘라내기 코드 잔존 0건,
프론트/서버 상한 3곳 모두 20,000 일치, 두 페이지 HTTP 200.

### 2026-08-14 — 텍스트량 감축 1차: 서문·UI 설명문 중복 제거 ⚠️**아직 push 안 함**

2026-07-27부터 "편집 방향 미정"으로 남아 있던 텍스트량 문제에 착수. **사용자 결정 = 중복 제거만**
(UI 설명문·도구 소개문 삭제). 경고·고지의 재배치와 문체 압축은 **하지 않기로** 했다 —
법률 문장을 자르는 위치를 정하는 판단이 들어가 재검증이 필요하기 때문.

**측정으로 확인한 것**: 분량 자체는 이미 잘 접혀 있었다. `rehabilitation.html` 10,619자 중
실제 절차 내용(9단계 항목명+힌트)은 1,048자뿐이고 상세 가이드 3,619자·작성예시 1,692자는
`자세히`·모달 뒤에 있다. 지치는 지점은 **본론에 닿기 전 서문**이었다 —
첫 조작(입력칸·체크박스) 전에 numcheck 653자 / bankruptcy 530자 / rehabilitation 412자.

**첫 화면 정적 텍스트(주석·접힌 내용 제외) 4,131자 → 3,047자 (26% 감소). 삭제된 법률 정보는 0.**

| 파일 | 변경 |
|---|---|
| `numcheck.html` | 「이 도구가 하는 것」5줄(탭 5개와 1:1 중복, 각 탭 안에 같은 설명 존재) + 「알아 두실 것」카드 → **배지 3개 한 줄**로 압축. 남긴 셋은 중복이 아니라서 남겼다: 저장 안 함(개인정보)·코드 계산(AI 아님)·판단 안 함(변호사법). 「언제 쓰는 건가요」 6항목은 `<details class="fold">`로 **접음**(삭제 아님, `guide-step2/3` id 유지 → `applyProcedure()` 그대로 동작). 중복이던 하단 각주 1개 제거. 1,689→1,155자 |
| `rehabilitation.html` | 진행률·단계지도의 **사용법 설명문 2개 삭제**("눌러서 이동하세요", "자세히에서 방법을…" — 버튼이 화면에 이미 보인다). 색은 학습이 필요하므로 **점 범례**로 대체. 서류도구 카드 설명문·숫자검산기 설명문·작성예시 인물 소개 압축. 871→628자 |
| `bankruptcy.html` | 위와 동일한 정리. 1,034→837자 |
| `documents.html` | 숫자검산기 설명문·양식 다운로드 안내문 압축, UI 설명 문장("각 서류의 공식 사이트 버튼을 이용하면 됩니다") 삭제. 537→427자 |
| `css/styles.css` | `details.fold` 규칙 추가 — 접이식 안내용. Tailwind `group-open:` 변형에 기대지 않고 CSS로 직접 처리(CDN 버전 편차 회피) |

**검증**: 4개 페이지 로컬 서버 HTTP 200, 정적 본문 태그 균형 정상(div 42/42·31/31·33/33·25/25).
`<script>` 안 template string의 div 불균형(62/61)은 **HEAD에서도 61/60으로 있던 기존 상태**이고
이번 변경분은 +1/+1로 균형.

**"안내가 부족해지지 않았나" 역검증에서 실제로 2건이 나와 고쳤다** (뺀 문장이 가리키던 대상이
정말 화면에 있는지 하나씩 확인한 결과):

1. 🔴 **numcheck에 `?tab=` 없이 오는 경로가 4개**(서류센터·푸터·회생 진행센터·AI 검토)인데,
   이들은 "숫자 검산기"라는 일반 링크를 누른 = **순서 안내가 가장 필요한 사람들**이다.
   그런데 「언제 쓰는 건가요」를 접어 버려 그 사람들이 5개 탭 앞에서 길을 잃게 됐다.
   → `orientNewcomer()` 추가: `?tab=`이 없으면 안내를 **펼친 채로** 띄우고,
   `?tab=cross`처럼 목적지를 갖고 온 사람에게만 접어 둔다.
2. 🔴 `button.roadmap-step`에 **`cursor: pointer`가 없었다**. `<button>`의 브라우저 기본 커서는
   화살표라, "눌러서 이동하세요"를 지운 뒤로는 단계 지도가 눌러지는 것처럼 보이지 않았다.
   → CSS에 `cursor: pointer` 추가(`.tab-btn`·`button.underline`도 같이).
   **말로 하던 안내를 지웠으면 그 자리를 UI 어포던스가 채워야 한다** — 이번 작업의 전제다.

이상 없음을 확인한 것: `자세히` 버튼은 **모든 항목에** 렌더되고(`itemRowHtml`),
`해당없음`은 `isOptional`(필수 아님)일 때만 뜬다 — 삭제한 문장보다 오히려 정확하다(필수 항목에
없는 버튼을 안내하지 않게 됨). documents의 `공식 사이트 ↗`는 데스크톱에만 보이지만(`hidden sm:inline-block`)
모바일은 행을 눌러 `showDocDetail()` 모달에서 같은 링크를 받으므로 경로가 살아 있다.
numcheck에서 뺀 5줄(합계·안분율·가용소득·청산가치·서류 간 대조)은 전부 해당 탭 안에 남아 있다.

**남은 것(사용자 판단 필요)**: `bankruptcy.html`의 빨간 「하지 말아야 할 것」 박스와
바로 아래 노란 「면책불허가 사유」 박스가 **6항목 중 3개 중복**(편파변제 / 직전 신규대출·카드 / 재산·수입 은닉).
두 박스를 합치면 화면이 눈에 띄게 짧아지지만 **법률 고지를 합치는 판단**이라 이번 범위(①②) 밖으로 두었다.

### 2026-08-14 — 검증 2축 결과 반영(P0 4건·허위 고지 1건 수정) + 용어 기준 재설정 ⚠️**아직 push 안 함**

전날 중단됐던 **여정·난이도 감사**와 **법률·고지 검증**을 재개해 받은 지적을 전부 처리했다.

**🔴 허위 고지 1건 — 내가 쓴 문구가 사실과 달랐다(법률 축 REFUTED)**
붙여넣기 안내에 *"어디로도 전송하지 않습니다"*라고 적었는데, **붙여넣은 채권자명·금액은 [서류 간 대조]를 실행하면 `CrossCheck.summaryForAI()`를 타고 국외(Anthropic)로 나간다.** 방침(privacy.html)에는 그 예외가 적혀 있었지만 **정작 이용자가 붙여넣기 직전에 보는 화면에는 없었다.** 방침과 화면이 서로 다른 말을 하는 상태였고, 분쟁이 나면 이용자는 자기가 본 화면을 든다. → 화면 문구에 전송 예외를 명시했다. ⚠️**이런 종류(고지 ↔ 실제 동작 불일치)가 이 사이트에서 가장 먼저 잡힐 유형이다.**

**🔴 개인정보 — 타인 정보 유입에 아무 대응이 없었다**
`maskIdNumbers`는 주민번호만 지운다. 조회 화면에 **보증인·공동명의자 이름**이 섞여 오면 그대로 채권자명에 들어가 국외로 나간다. → *"본인 채무 조회 화면만 붙여넣어 주세요. 다른 사람의 이름이 들어 있으면 지운 뒤 넣어 주세요"* 안내 추가.
**마스킹 시점도 고지와 어긋났다** — "자동으로 가려집니다"인데 버튼을 눌러야 실행됐다 → `oninput`으로 **입력되는 즉시** 가리도록 바꿨다(`maskPasteBox`).

**🔴 마이데이터 논거를 방식 기준으로 교체**
종전 주석의 *"우리가 조회하지 않으니 허가 불요"*는 **정의의 어느 요소도 부정하지 못한다** — 신용정보법 제2조 제9호의2는 주체가 아니라 **방식**("대통령령으로 정하는 방식으로 통합하여 제공")을 요건으로 삼기 때문이다. 결론은 맞지만 논거가 약해 반박당하면 결론까지 흔들린다. → **제22조의9 제4항(전송요구권에 따른 직접 전송)·제3항(접근매체·본인확인수단 사용 금지)** 을 근거로 다시 썼다. 🔴**자동 로그인·스크래핑을 붙이는 순간 즉시 허가 대상**이라는 경고도 주석에 박았다.

**🔴 여정 P0 4건**
1. **파산 이용자는 재산 합계(청산가치)를 끝내 계산할 수 없었다** — `rehabPlan`이 가용소득·변제횟수가 없으면 조기 반환해, 순서 안내·탭 배너·`ai-review`가 약속한 "재산 합계 계산"이 셋 다 거짓말이었다. → **재산만 있으면 청산가치만 계산해 돌려주는 경로**를 만들었다(`totals.assetsOnly`).
2. **붙여넣기 직후 [검산하기]를 누르면 무조건 빨간 오류 2건** — 합계 칸만 채워진 상태를 '틀렸다'고 판정했다. 안내가 시킨 대로 했는데 실패 화면을 보는 구조였다.
3. **되묻기(C안)가 정작 안내를 따른 이용자에게만 발동하지 않았다** — 합계 칸이 조회값 그대로면 `final === lookup`이라 영원히 안 뜬다. → **합계 칸이 '손대지 않은 조회값'이면 원금·이자 쪽을 확정값으로** 본다. 2와 3이 같은 원인이라 한 번에 해결.
4. **유령 채권자** — `총 합계`·`합계액`이 채권자로 들어가 합계를 부풀리고, `총 3건이 조회되었습니다`가 **3원짜리 채권자**가 됐다. → 합계 판정을 포함검사로 바꾸고, 숫자 뒤에 `건·개·명·회·%` 단위가 붙으면 금액에서 제외. `소계`는 합계로 받지 않는다(총계를 덮어쓴다).
   아울러 **`원금 18,000,000 이자 542,000`에서 이자를 채권액으로 넣던 것**(33배 과소)과 **`1,854만원`을 1,854원으로 읽던 것**(1만배 과소, 경고조차 없었음)을 고치고, 추측이 섞이면 반드시 `note`가 붙게 했다.

**그 밖에 반영**
- **부채증명서 안내 보강** — "부채증명서 기준"만으로는 부족하다. 법이 요구하는 것은 **산정기준일(회생은 신청일) 현재의 원금·이자 구분 기재**이고 부채증명서는 소명자료다. 미리 떼어 둔 증명서를 그대로 옮기면 기준일이 어긋나 보정을 받는다 → 그 경고를 넣었다.
- **제566조 제7호 단서**(채권자가 파산선고를 안 때는 면책) 추가. **주석이 본문을 못 따라가던 것**도 정정(회생 조문 하나만 들고 있었다).
- **조회에 안 잡히는 채권 안내** — 지인 채무·보증채무·구상권·통신비는 크레딧포유에 나오지 않는다. **면책 다툼은 대부분 여기서 난다.**
- **파산에 남아 있던 회생 전용 문구** — `creditorList`·`incomeExpense`·`rehabPlan`에 `procedure`를 넘겨 결과 문구까지 갈리게 했다(종전에는 화면 껍데기만 바뀌었다). 패널 제목·전달 버튼 라벨도 치환. ⚠️파산에서 **가용소득 0은 이상한 상태가 아니다** — 종전 문구는 그걸 문제처럼 말했다.
- **비용 결과의 확정 톤 완화**("계산됩니다" → "약 …원으로 예상됩니다"), **고지 문구 범위 확장**("입력하신 숫자" → "입력하거나 붙여넣은 내용"), `about.html`에 **유료 사실 한 줄**(무료 공익 서비스로 오인될 배치였다).
- **P2**: 알 수 없는 `?tab=` 값이면 화면이 백지가 되던 것, 덮어쓰기 감지에서 이자만 입력한 행을 놓치던 것.

**📌 용어 툴팁 기준 재설정 (사용자 결정)**
"중학생도 진행"은 **낱말이 쉬워야 한다는 뜻이 아니라 과정 안내가 쉽고 명확해야 한다는 뜻**이다. 툴팁이 많을수록 밑줄이 빽빽해져 정작 읽을 문장이 묻힌다. → **60개 → 33개.** 판단 기준과 제외 목록은 `CLAUDE.md`의 「용어 툴팁」 절에 적어 뒀다. 없앤 말이 처음 나오는 자리에서는 **문장 안에서 한 번 풀어 쓴다.**

**검증**: 전 JS `node --check`, 13개 HTML 인라인 파싱, 4개 스위트 전항목 통과(`paste.test.js`에 유령 채권자·단위 오인식 케이스 추가). ⚠️또 **python 힙독의 `\n`이 실제 줄바꿈으로 들어가 파일이 깨졌다** — 두 번째다. 문서·문자열 수정은 반드시 **Edit 툴**로 할 것.

**✅ 동선 정리 완료 (2026-08-14 후속)**
- **진행센터 → 검산기 링크 10곳 신설** — 회생 `r2-2`(채권자목록)·`r4-2`(재산)·`r5-1`(가용소득)·`r6-5`(변제계획안)·`r7-4`(비용)·`r7-0`(대조), 파산 `b3-1`·`b4-1`·`b5-1`·`b7-0`. ⚠️**모든 링크에 `proc=`를 실었다** — 안 실으면 파산 이용자가 '변제계획안' 제목 화면에 떨어진다(테스트가 전수 검사한다).
- **7단계 최종 체크에 '서류끼리 숫자가 맞는지 대조'(`r7-0`/`b7-0`) 신설** — 검산기가 마지막 단계로 강조하는 작업이 정작 진행센터 최종 점검에서 빠져 있었다. 두 파일 모두 **7단계 맨 앞**에 뒀다.
- **검산기 → 진행센터 복귀 링크**(`#back-link`) — 절차를 아는 경우에만 뜬다. 종전에는 브라우저 뒤로가기 외에 길이 없었다.
- **순서 안내를 진행센터 단계와 맞물리게 재배열** — 재산(4단계)을 수입·지출(5단계) 앞으로 옮기고 각 단계에 "(진행센터 N단계)"를 병기. 파산이면 변제계획안 단계를 통째로 감춘다.
- **막다른 화면 해소** — 비용 결과→채권자목록, 변제계획/재산 결과→서류 간 대조, 대조 결과→진행센터 복귀 버튼(`nextStepHtml`). 또 `CrossCheck.run`이 `head:'info'`를 반환해, **더 채울 게 없는 정상 종점에 회색 "아직 계산할 수 없습니다"가 뜨던 것**을 "지금 확인할 수 있는 것은 여기까지입니다"로 바꿨다.
- **`ai-review.html`이 절차를 함께 넘긴다**(`bk-`·`bankrupt-` 접두사로 판별), **`result.html` 진단 완료 카드에 무료 검산기 링크** 추가(종전에는 요금제 버튼뿐이라 무료 도구의 존재를 알 길이 푸터밖에 없었다).
- 검증: `flow.test.js` 신규(링크 10곳 전수·proc 누락 검사·7단계 위치·복귀 링크·순서 정합·다음 행동·head 반환) 전항목 통과 + 기존 4개 스위트 회귀 없음.

**🔴 아직 안 고친 것 — 오픈 전 판단 필요**
- 법률 축 **미검증**: 전상법 제13조 제2항 각 호 원문 대조(세션 한도로 실패), 송달료 5,640원 시행일·인지법 조문 재확인.
- **변호사 지적 예상 1순위는 여전히 사업자 신원 미기재 상태에서의 판매 개시**(전상법 제10조·제12조) — 결제를 닫아 둔 현재 판단은 옳다.
- **3순위 지적(구조적)**: 유료 패키지가 절차 전 과정을 지시하는 구조와 **'사실상 사건 처리 주도'** 법리(대법원 2014도16204). 유료라는 사실만으로 제109조 제1호의 유상성 요건이 이미 충족되므로, 방어는 **"판단·선택·작성은 이용자가 한다"를 화면에서 실제로 지키는 것**뿐이다. `ai-review`에서 심각도 등급을 걷어낸 판단이 그 방향이며, **새 기능을 붙일 때마다 같은 기준을 다시 적용할 것.**

### 2026-08-13 (3차) — 채권자 입력 자동화(A안)·조회값 되묻기(C안)·순서 안내 ⚠️**아직 push 안 함**

**출발점(사용자 질문)**: "숫자 검산기는 왜 필요한가? 채무현황을 한 번에 뽑아 주는 사이트를 연동하는 게 낫지 않나?"
→ 답: **둘은 다른 문제를 푼다.** 조회는 *얼마를 빚졌나*(입력값), 검산기는 *내가 쓴 서류들의 숫자가 서로 맞나*(정합성). 조회가 완벽해도 안분율 100%·가용소득·청산가치 보장·서류 간 대조는 그대로 남는다.
→ **자동 연동은 두 겹으로 막혀 있다**: ①금융회사에 접속해 데이터를 가져오는 것은 신용정보법상 **본인신용정보관리업(마이데이터) 허가** 사업이다(자본금 5억+물적·인적·대주주 요건). ②설령 열려도 **법원 기준 금액은 부채증명서**라 조회 잔액을 그대로 넣으면 '편한데 틀린 값'이 되고 그게 보정 사유가 된다. 조회는 **누락 방지**에 강하고 **금액 확정**에는 약하다.
→ 그래서 연동 대신 **입력 부담만 줄이고 확정은 사람이 하게** 만들었다.

**① A안 — 조회 결과 붙여넣기** (`js/numcheck.js`의 `parsePastedRows` + `numcheck.html`의 `applyPaste`)
크레딧포유·어카운트인포 화면이나 엑셀 표를 붙여넣으면 채권자 행으로 나눈다. **경계 셋을 코드 주석에 박아 뒀다**: ⓐ우리가 조회하지 않는다(마이데이터 선을 넘지 않는다) ⓑ금액을 확정하지 않는다(`lookup`=조회값으로만 표시) ⓒ**추측한 곳과 못 읽은 줄을 반드시 화면에 보여 준다**(조용히 사라지면 채권자 누락이 된다).
- 오탐 방지: 날짜·계좌번호·사업자번호를 금액으로 읽지 않고, 한 줄에 숫자가 여러 개면 "추측했다"고 알린다. 합계 줄은 `declaredTotal`로 분리해 합계 칸을 채운다. 200건 상한.
- **주민번호는 붙여넣는 즉시 `maskIdNumbers`로 가린다.** ⚠️정규식 복사본을 numcheck.js에 만들지 말 것(원본은 `js/main.js`와 `api/src/index.js` 둘뿐).
- 기존 입력이 있으면 **confirm으로 확인**하고 덮어쓴다(말없이 지우면 무엇이 사라졌는지도 모른다).

**② C안 — 조회값 되묻기** (`creditorList`의 `lookupGaps`)
조회 잔액과 확정 금액이 다르면 **`diff`가 아니라 `ask`**로 "어느 쪽이 부채증명서 금액인가요?"라고 묻는다. 기준일 차이로 다른 게 정상이라는 것까지 설명한다. **자동 연동이 구조적으로 못 하는 일이 이것**이다.

**③ 「🗺️ 언제 쓰는 건가요?」 순서 안내** — 검산기 상단에 0~4단계(비용 → 채권자목록 → 수입·지출 → 변제계획안 → 제출 직전 대조). 파산이면 3단계가 '재산목록'으로 바뀐다. 진행센터 진입 링크에 `?proc=rehab`을 붙여 절차가 처음부터 잡히게 했다.

**④ 스스로 잡은 법률 오류 1건** — 붙여넣기의 누락 경고를 "빠진 채권은 면책을 못 받는다"로 **뭉뚱그려 썼다.** 개인회생은 제625조 제2항 제1호로 맞지만 **개인파산은 제566조 제7호가 '악의'를 요구**한다. 이 화면은 파산 이용자도 본다 → **절차별로 문구가 갈리게** 고쳤다(절차 미상이면 둘을 함께 안내).

**⑤ 용어 사전 10개 추가**(`js/main.js` GLOSSARY, 60개) — 안분율·부채증명서·인지대·송달료·예납금·소송구조·전자소송·집행법원·확정증명원·생계비계좌. 중학생 기준으로 새로 쓴 말 중 사전에 없던 것들이다. ⚠️`송달료`가 이미 있어 **중복 키가 생겼던 것을 정리**했다(객체 리터럴이라 조용히 뒤엣것이 이긴다 — 추가 시 반드시 중복 검사할 것).

**⑥ 방침 반영** — `privacy.html` 제2항 '숫자 검산기' 행에 **붙여넣은 값**을 명시하고, **"금융회사·신용조회기관에 접속해 채무 정보를 가져오지 않는다"**와 주민번호 자동 마스킹을 적었다. 고지와 실제가 어긋나면 그 자체가 분쟁 원인이라 코드 동작 그대로 적었다.

**검증(직접)**: 전 JS `node --check`, 13개 HTML 인라인 파싱, 신규 `paste.test.js` 19항목(엑셀 탭 복사·번호 목록·합계 줄·계좌번호 오탐·200건 상한·되묻기 4케이스), 기존 3개 스위트 회귀 없음. `applyPaste`가 참조하는 DOM id 8개·함수 7개 실재 확인, `addRow → tr.dataset.lookup → readRows → creditorList` 경로 연결 확인. 작업 중 파서 버그 2건(이름 끝 `|` 잔존, '합계' 단어를 떼어 합계 줄 인식 실패)을 테스트가 잡아 고쳤다.

⚠️**미완 — 에이전트 검증 2건이 세션 한도로 중단됐다(재시도 필요)**
- **여정·난이도 감사**: 중학생 페르소나로 막히는 지점, 파산 분기 일관성, 붙여넣기 전 구간. 위 DOM·경로 확인은 내가 직접 했으나 **페르소나 추적은 미완**.
- **법률·고지 검증**: ⓐ붙여넣기의 신용정보법 경계(우리 주장: 마이데이터 해당 없음) ⓑ부채증명서 기준 안내 ⓒ비용 계산이 변호사법 제109조에 걸리는지 ⓓ전상법 제13조 제2항 고지 누락 여부 ⓔ"변호사가 가장 먼저 지적할 3가지". **오픈 전 반드시 재실행할 것.**

### 2026-08-13 (2차) — 결핍 보완 ⚠️**아직 push 안 함**

풀스윕의 결핍 감사가 "오픈 전에 메워야 한다"고 한 것들. **전부 공식 출처를 새로 조회해 확인했고, 결핍 감사의 제안 중 두 개가 부정확해 바로잡았다.**

**🔴 결핍 감사 제안의 정정 (그대로 썼으면 오도가 됐다)**
- **채무자대리인 무료지원은 신용회복위원회가 아니라 금융감독원 + 대한법률구조공단 사업이다.** 게다가 **대상이 한정된다** — 미등록·등록 **대부업자**의 불법추심 피해(우려) 또는 **법정 최고금리(연 20%) 초과** 대출. **은행·카드사 등 제도권 금융회사의 정상 추심은 대상이 아니다.** 이 한정을 빼고 쓰면 해당 없는 이용자가 전화를 걸게 된다. 신청: **금감원 ☎1332 → 3번 / 법률구조공단 ☎132 → 0번**.
- **압류방지 전용통장(행복지킴이통장)은 기초생활수급자 등 특정 급여 수급자 전용이다.** 일반 근로소득 급여는 대상이 아니다(easylaw 확인). "압류당한 사람이 쓸 수 있는 계좌"로 뭉뚱그리면 안 된다.

**✅ 대신 더 실용적인 제도를 찾았다 — 「생계비계좌」(2026-02-01 시행)**
압류가 걸리면 통장 전체가 묶이던 문제를 없애려고 **애초에 압류가 미치지 않는 전용 계좌**를 만든 제도. **월 250만원까지 보호**(한 달 누적 입금 한도도 250만원), **1인 1계좌**, 시중·지방·인터넷은행·저축은행·상호금융·우체국에서 개설. ⚠️**2026-02-01 이후 최초 접수된 압류명령 사건부터 적용**돼 이미 걸린 압류에는 소급되지 않는다 → **압류가 예상되면 미리 만들어 두라**고 안내했다. (출처: 정책브리핑 korea.kr, 법무부)

**한 일**
- `resources.html` — **FAQ 2문항 신설**(총 25문항): ①"서류 준비 중 독촉 전화" — 불법추심 신고(☎1332)·채무자대리인(대상 한정 명시)·중지금지명령·무료상담 창구 ②압류 FAQ에 **생계비계좌** 추가 + 행복지킴이통장과의 혼동 방지 주석.
- `index.html` — 히어로에 **"지금 독촉 전화를 받고 계신가요?"** 진입점 신설(`resources.html#faq`). 히어로 첫 줄이 추심을 말하는데 답이 없던 불일치를 메웠다. `resources.html`에 `id="faq"` 앵커도 함께 추가(없으면 죽은 링크가 된다).
- `about.html` — **「챔로드가 하지 않는 것」 절 신설**: 대필·대리·판정을 하지 않는 이유(변호사법 제109조·법무사법 제2조·제3조)와 **브로커 경고**(무자격 대행은 금지 행위이고 사고가 나도 구제받기 어렵다) + 대안 창구(☎132, 1600-5500, 소송구조).
- **`js/numcheck.js`에 `courtCost()` 신설 + `numcheck.html`에 「💵 법원 비용」 탭** — "돈이 얼마나 드나"에 숫자로 답하지 못하던 결핍. 채권자 수만 넣으면 인지대+송달료를 산식으로 계산한다(회생 10+n×8 / 파산 10+n×4 · 면책 10+n×3, 1회 5,640원). 검증: 회생 5인 = 309,000원, 파산 5인 = 312,200원.
  - **예납금은 합계에서 분리**했다 — 법정 금액이 아니라 법원 실무(선임 여부를 법원이 정한다)이므로 '통상 15만/30만'으로만 적고 붙는 경우의 총액을 따로 보여준다.
  - **판정하지 않는다 원칙 유지** — 산식 적용일 뿐이고, 단가 기준일과 "접수 시 자동 계산되는 금액을 확인하라"를 함께 낸다.

**검증**: 전 JS `node --check`, 15개 HTML 인라인 스크립트 파싱 0에러, 신규 스위트 `verify3.js` 전항목 통과(비용 산식 4케이스 실측 대조 포함), 1·2회차 스위트 회귀 없음.

**✅ 반박 교차검증 완료 (T2) — 환각 인용 0건 / 틀린 전화번호·금액 0건.** 판정 CONFIRMED 다수, 다만 **누락 3건을 잡아 보완**했다.
- **🔴 최대 지적 — 제도권 금융 채무자를 막다른 길로 보냈다.** "은행·카드사는 채무자대리인 대상이 아닙니다"로 끝내고 대안을 주지 않았는데, 이용자 다수가 여기 해당한다. → **「개인채무자보호법」(2024-10-17 시행) 안내를 신설**했다: 하나의 채권에 **7일 7회** 추심 연락 제한(방문·전화 합산, 법정 통지·본인 문의 답변은 제외), 재난·수술·입원·혼인·장례 시 **추심 유예**(3개월 이내 합의 기간). 금융위 보도자료로 직접 확인.
- **250만원 합산 오해** — 생계비계좌는 **별개 제도가 맞다**(민사집행법 **제246조의2 신설**, 법률 제20733호 / 급여압류금지는 제246조①4호·시행령 제3조로 서로 다른 조문·다른 대상 재산). 다만 **일반 예금도 이미 250만원까지 압류금지**(제246조①8호·시행령 제7조)이고 생계비계좌는 그 보호를 *신청 없이 자동으로* 받게 해 주는 그릇이다. "**한도가 새로 250만원 늘어나는 것이 아니라 원래 있던 보호를 미리 확보하는 것**"이라고 못박았다. ⚠️두 항목을 나란히 쓸 때 이 문장을 빼지 말 것.
- **채무자대리인 대상을 공식 문언보다 좁게 썼다** — "미등록·등록 대부업자" → **"불법사금융업자(미등록·등록 대부업자 포함)"**. 좁게 쓰면 자격 있는 이용자가 스스로 포기한다. 전용 내선 **1332 → 3번 → 6번**과 서민금융통합지원센터 방문 신청도 추가. 효과의 근거는 **채권추심법 제8조의2**(대리인 선임 통지 시 채무자 직접 접촉 금지)로 확인됨.
- **야간 연락을 단독 금지행위로 쓴 것은 구성요건 과장** — 제9조 제2·3호는 "야간에 **또는** 반복적으로 … **공포심·불안감을 유발하여** 평온을 심하게 해치는 행위"가 하나의 요건이다. 문구를 합쳤다.
- **계산기의 자기 불일치** — 사이트가 "신청과 함께 중지·금지명령을 내라"고 권하면서 그 인지대를 계산기가 빠뜨렸다. 안내 항목을 넣되 ⚠️**단가를 공식 출처로 확정하지 못해 합계에는 넣지 않았다**(실무 자료는 전자 1,800원/서면 2,000원이라 하나 법령·예규로 확인 못 함). 회생일 때만 뜬다.
- **UNCERTAIN으로 남긴 것**: 파산 인지 2,000원에 전자소송 10분의 9 감액이 붙는지(실무 자료는 1,800원이라 하나 법제처·법원 안내는 접수 방식 구분 없이 2,000원). 오차 200원이고 **과다 방향(안전)** 이라 그대로 두었다. 화면의 "실제 납부액은 전자소송 화면에서 확인" 문구가 흡수한다.
- **CONFIRMED로 확정(다시 의심하지 말 것)**: 송달료 5,640원·회수 계수(회생 10+n×8 / 파산 10+n×4 / 면책 10+n×3, **동시신청이라도 파산·면책 송달료는 합산이 맞다**) / 회생 인지 30,000원·전자 27,000원 / 파산·면책 인지 2,000원 / **파산 인지에 조문 번호를 붙이지 않은 판단이 옳았다**(인지법 제9조①1호는 *채권자가* 하는 파산신청만 규정) / 변호사법 제109조의 유상성 한정 / 법무사법 제2조에 **개인회생·파산 신청의 대리**가 명시돼 있어 '법무사만'이라 쓴 것이 맞다 / ☎1332·132·1600-5500 전부 현재 유효 / 생계비계좌 개설기관 10종·1인1계좌·누적입금한도 / 야간 21시~08시.
⚠️**교훈**: Bash 힙독 안의 python으로 문서를 고치면서 `\n` 이스케이프가 **실제 줄바꿈으로 들어가 페이지 스크립트가 깨졌다**(파싱 검사에서 잡음). PROGRESS 상단 경고("문서 수정은 Edit 툴을 쓸 것")가 python에도 그대로 적용된다.

**여전히 남은 결핍(오픈 후 판단)**: 파산 선고 이후~복권 전용 페이지(`bankruptcy-after.html` — 다만 `b8-5`·`b8-6` 보강으로 급한 불은 껐다), 서류 체크센터 누락 6종, `취하`·중도 이탈 경로, 영업소득자 경로, 연대보증 딜레마, 심문기일 실무 준비(복장·지참물), `개인워크아웃`·`새출발기금` 미기재, setup.html이 세금체납·배우자소득·질병을 묻지 않는 문제.

### 2026-08-13 — 약관·방침 이력 정리 + P1·P2 전량 수정 ⚠️**아직 push 안 함**

**① 개정 이력 방침 변경(사용자 결정)** — 위 상단 블록 참조. `terms.html` 부칙의 항목 3건과 `privacy.html` 14항의 이력 7건·전부개정 안내 배너를 삭제하고, 두 문서 시행일을 **2026-08-13**로 통일, `CONSENT_VERSION`을 `terms-2026-08-13/privacy-2026-08-13`로 맞췄다. **정식 개시 시점에 다시 올릴 것.**

**② 이 결정으로 절차 장벽이 사라져 미뤄 뒀던 방침 수정 3건을 함께 처리했다**
- **개인정보 보호법 제22조 제3항** — 제2항 수집 항목 표에 **'처리 근거' 열 신설**(10개 행). 동의(제15조①1)·계약 이행(①4)·법령상 의무(①2+전상법 제6조)·정당한 이익(①6)을 구분하고, **'동의 없이 처리'를 별도 표기**했다. 표 아래에 구분 공개의 취지를 한 문단으로 밝힘.
- **`totalRepay`(총 변제예정액)** 를 제2항 표·제6항 ⓑ **양쪽** 열거에 추가. 코드가 실제로 보내는데 방침에 없던 항목이다.
- **청약철회 방법** — `terms.html` 제6조④·`pricing.html`에 **마이페이지 [청약철회 신청]** 경로를 병기(실제 자동 전액환불 경로인데 이메일만 안내하고 있었다).

**③ P1 3건**
- **유료 접근권 락아웃 근본 차단** — `handleGetData`가 `plan_packages`를 **유효 entitlements와 합집합**으로 돌려주도록 바꿨다(user_data가 무엇을 잃든 결제가 살아 있으면 열린다). `handleSyncData`의 **`del` 루프에도 `PLAN_KEYS` 가드** 추가. `mypage.html` 초기화 목록에 누락됐던 `plan_expires_at`·`entitlements`·`content_access` 추가 + 확인창 문구를 사실대로("이용권은 삭제되지 않습니다") 고침.
- **파산 이용자에게 회생 전용 서류를 요구하던 문제** — `numcheck.html`에 `procedure()` 판별 신설(주소 `?proc=` → 구매 패키지 → profile 순), `crosscheck.js`가 파산이면 '변제계획안' 요구를 하지 않는다. `bankruptcy.html`의 검산기 링크에 `&proc=bankrupt` 추가, 채권자목록 제목을 중립화(회생일 때만 '개인회생채권자목록').
  ⚠️**변제계획안 탭을 감추지 않았다** — 그 안에 파산에도 필요한 **재산목록(청산가치)** 이 들어 있다. 탭 이름을 '📄 재산목록'으로 바꾸고 안내 배너를 넣는 방식으로 처리했다. 감추면 `bankruptcy.html:123`이 약속한 기능이 사라진다.
  ⚠️**검증에서 새 결함을 잡았다** — 변제계획안 요구만 빼니 파산 이용자에게 **결과 화면이 통째로 비었다**(회생 기준 조합이 전부 성립하지 않아서). 빈 화면 방지 안내를 넣었다.
- **재직자 청산가치 경고** — 무료 진단이 퇴직금을 안 물어 `totalAssets=0`이라 기존 경고가 직장인에게 절대 뜨지 않았다. `incomeType`이 `employed*`면 **퇴직금 1/2 산입 경고**를 띄우고 **퇴직금 예상액 증명서** 발급을 안내. 아울러 결과 화면에 **총 변제율**을 표기(numcheck는 이미 보여주는데 결과 화면만 없었다).

**④ P2** — 청약철회 **서버측 중복 접수 차단**(`already_requested`), 마이페이지에 **AI 잔여 회수** 표시(데이터는 이미 내려오고 있었다), `documents.html` 고지에 '법률대리 아님' 문구 보강, `supplement.html` 잠금 문구에 완주 패키지 포함, `sitemap.xml`에 `discharge.html` 추가, **`.preview-mode` CSS 신설**(P0의 2차 방어), 회생 맥락 '보정명령'→'보정권고' 3곳.
🔴**`robots.txt`는 일부러 고치지 않았다** — 일관성 검사가 `admin.html` Disallow 추가를 제안했지만, **Disallow는 경로를 오히려 공개한다.** noindex + 링크 0건이 맞다(LAUNCH-CHECKLIST 5단계 확정 사항). 다시 제안하지 말 것.

**검증**: 전 JS `node --check`, 편집한 **13개 HTML 인라인 스크립트 파싱 0에러**, `sitemap.xml` XML 파싱, 기능 테스트 2스위트 전항목 통과(1회차 회귀 없음). 하네스: 스크래치패드 `verify.js`·`verify2.js`·`chk2.js`.

⚠️**이번엔 `api/src/index.js`가 바뀌었다** — push하면 GitHub Actions가 **Worker를 자동 배포**한다. D1 스키마 변경은 없으므로 마이그레이션은 불필요.

### 2026-08-12 — `/legal-verify` 풀스윕(T3) + '지금 고칠 것' 수정 완료 ⚠️**아직 push 안 함**

**수정한 것 (전부 화면·클라이언트 JS. `api/**` 무변경이라 push해도 Worker 자동배포는 돌지 않는다. 방침·약관도 무변경이라 `CONSENT_VERSION` 그대로.)**
- `result.html` — `getHardBlockReason`이 문자열 대신 **`{label, text}`**를 반환하도록 바꿈(법적 성질이 케이스마다 달라 배지도 달라야 한다: 중복신청·한도초과=`신청불가` / 재신청 대기기간=`기각 가능`·`면책 위험`). **"파산이 유일한 방법" 오도 문구 삭제**, 파산 pros에서 재신청 대기기간 케이스 제외, cons에 후단 5년 경고 추가, "두 절차 모두 신청불가" 문구를 재량 여지에 맞게 완화. ⚠️`유일한 선택`은 **채무 한도 초과 케이스에만 1곳** 남아야 한다(제579조상 회생 신청 자체가 불가라 그건 정확하다).
- `js/diagnosis.js` — `rehab-done-recent`에 `bankrupt -= 40` 추가(후단 반영). **-60 금지**(재량면책).
- `rehabilitation.html`·`bankruptcy.html` — `activeStep = isPreview() ? 0 : firstIncompleteStep(saved)`, `toggleCheck`/`toggleNA`에 `if (isPreview()) return;`. `js/main.js lockSection`에는 **함정 경고 주석**을 달았다(자식이 1개면 조용히 통과한다).
- `bankruptcy.html` — `b8-5`를 제562조 기준으로 재작성(이의신청은 **면책결정 전**, 신청권자에 검사·파산관재인, 재량면책, 확정=공고 후 14일), **`b8-6` 신설**(면책 후 압류 해제).
- `resources.html` — 송달료 5,640원+산정식+기준일, 파산 인지대 2,000원+송달료식, 파산선고≠급여압류 해제, 신용정보 7년 삭제(다른 문항으로 넘김), 재신청 FAQ에 **후단 5년** 반영, **면책 후 압류 해제 FAQ 신설**(총 24문항).
- `discharge.html` — 4단계에 압류 해제 안내(서류 3종) 추가. `diagnosis.html` — "(재신청 불가)" 4곳 정정. `js/main.js:309` 주석 단정 완화. `CLAUDE.md` — FAQ 수, 제22조 제3항 오인용 정정.

**검증**: 전 JS `node --check` 통과, 편집한 6개 HTML 인라인 스크립트 파싱 0에러, 기능 테스트 전항목 통과(진단 점수 파산 감점 35→25이되 0 아님 / 차단 사유 객체 6케이스 / 미리보기 고정 / **취약점 재현 — 1단계 체크 시 `firstIncompleteStep`이 1을 반환하던 것을 확인**). 하네스: 스크래치패드 `verify.js`.
⚠️**교훈**: 인라인 `{ a: '...' }` FAQ 문자열 안에 작은따옴표를 넣어 페이지 전체 스크립트가 깨졌었다(파싱 검사에서 잡음). 한글 인용부호는 「」를 쓸 것.

**아직 안 한 것**: 오픈 시 방침 개정에 묶을 3건(privacy 제22조 제3항 '처리 근거' 열 / `totalRepay` 열거 / 청약철회 '방법'·서식 고지) + 결핍 보완 + P1·P2. 아래 목록 참조.

---

#### 풀스윕 원 결과 (발견 내역)

오픈 직전 1회 필수 항목을 실시했다. 5종 에이전트(법률검증→반박, 여정+결핍 짝, 일관성)를 돌리고 **지적마다 원문을 직접 대조**했다. 반박 교차검증에서 법률 판정 11건 중 **10건 CONFIRMED**.

⚠️**에이전트 실행 환경**: CWD가 상위 폴더(`법률웹제작`)면 `.claude/agents`의 5종이 등록되지 않는다(`Agent type not found`). 그때는 각 에이전트 정의를 프롬프트에 그대로 주입해 general-purpose로 돌릴 것. 또 **긴 검증 도중 세션 한도로 죽을 수 있다** — 그때는 새로 띄우지 말고 SendMessage로 재개하면 문맥이 살아 있다.

**🔴 오픈 차단 3건**
1. **`result.html:341` — 적극적 오도(가장 위험).** `rehab-done-recent`(개인회생 면책 5년 미경과)에게 *"법원을 통한 채무 해결은 개인파산·면책이 유일한 방법"*이라 권한다. 그런데 **제564조 제1항 제4호 후단**("제624조에 의하여 면책을 받은 경우에는 면책확정일부터 5년")이 바로 그 사람의 파산 면책도 불허가사유로 삼는다. 믿고 신청하면 인지대·송달료·예납금(최대 30만원)을 쓰고 불허가를 맞는다. 관련 결함이 셋 더: `js/diagnosis.js:287`이 `rehab-done-recent`에 **파산 감점을 안 한다**(반대 방향인 `bankrupt-done-recent`는 양방향 반영 — 비대칭), `result.html:75`가 파산 점수를 0으로 강제, `resources.html:60`이 후단을 아예 언급 안 함. ⚠️감점은 **-40 권장(-60 금지)** — 제564조 **제2항 재량면책** 여지가 있어 "파산 절대 불가"로 읽히면 안 된다.
2. **유료 잠금이 뚫린다(P0).** `js/main.js:1172` `lockSection`은 `container.children`을 `slice(keep)`로 지우는데 **회생·파산 진행센터의 `steps-container.innerHTML`은 루트 `<div>` 한 개**라 제거 대상이 0이다. 1단계만 열리는 건 `activeStep`이 우연히 0이기 때문이고, **1단계를 체크하고 새로고침하면 `firstIncompleteStep`이 2단계를 반환**해 8단계까지 무료로 열람된다(`toggleCheck`에 미리보기 가드 없음). 보정 장치인 `chamroad:gate` 리스너는 **미리보기 이용자에게 절대 실행되지 않는다**(비로그인은 `requirePackage`가 리스너 등록 전에 동기 발사, 로그인·미구매는 `enterPreview`의 `changed`가 false). 로드맵 클릭(`gotoStep`)은 정상 차단돼 있으니 **최초 렌더 경로만** 뚫린 것. → `activeStep = isPreview() ? 0 : firstIncompleteStep(saved)` + `toggleCheck/toggleNA`에 `if (isPreview()) return;`. maintenance·supplement는 카드가 형제라 정상.
   **법적 연동**: 이 미리보기가 시행령 제21조의2 제1호를 충족시키는 유일한 조치이고 환불 제한이 그것과 세트로만 유효하다(법 제17조 제2항 단서).
3. **파산 완주 패키지 판매문구 vs 실제 콘텐츠.** `pricing.html:294` "채권자 이의신청 대처 가이드"에 대응하는 `bankruptcy.html:302`(`b8-5`)가 *"기간이 지나면 확정된다"*뿐이고 **이의가 들어왔을 때가 없다.** 게다가 **순서가 틀렸다** — 제562조 제1항은 "제558조 심문기일부터 30일(심문기일을 정하지 않은 경우 법원이 정하는 날) 이내"라 **이의신청은 면책결정 전** 단계다. 신청권자도 채권자만이 아니라 **검사·파산관재인 포함**. 확정은 제564조 제4항 즉시항고 + 제13조 제2항 14일. 고치면 판매문구 문제와 법률 오류가 함께 해소된다.

**🔴 법률 사실 오류 (공식 출처로 확정, 반박 통과)**
- **송달료 1회 5,500원 → 5,640원**(2026-07-01 인상). `rehabilitation.html:265`·`resources.html:57`. 시기별: 5,200(~2025-05-31) / 5,500(~2026-06-30) / **5,640(2026-07-01~)**. 근거는 klac 소송비용 자동계산 + easylaw csmSeq=568. 산정식도 확정 — 회생 10회+채권자수×8 / 파산 10+×4 / 면책 10+×3. **숫자 옆에 "(2026.7.1. 기준)"을 박을 것** — 우편요금 연동이라 매년 바뀐다.
- **`resources.html:65` "개인파산도 파산선고 시 강제집행의 효력이 사라집니다" — 틀렸다.** 제348조는 *"파산재단에 속하는 재산에 대하여… **파산재단에 대하여는** 효력을 잃는다"*는 **상대적 실효**이고, 제382조 고정주의상 **선고 후 급여는 파산재단이 아니라** 압류가 이어진다. 급여압류는 제557조 제2항(면책확정)이라야 실효. 앞 문장이 회생 중지·금지명령이라 **대칭으로 읽히는 것**이 위험하다.
- **면책 확정 후에도 압류는 자동으로 안 풀린다 — 사이트 전체 0건.** 서울회생법원 FAQ(seqnum=163)가 문언 그대로: *"면책결정이 확정되었다 하더라도 압류가 곧바로 해제되는 것은 아닙니다… **면책결정정본, 채권자목록, 면책결정 확정증명원**을 발급받아 집행법원에 해제신청."* 이 서류 3종을 그대로 적을 것.
- **신용정보 등록기간 `resources.html:64` "통상 7년" → 5년.** 같은 파일 `:79`·`compare.html:220`은 5년이라 **한 파일 안에서 충돌.** 서울회생법원 FAQ(seqnum=164) *"공공정보에 면책사실이 5년간 등록"*. 7년은 옆 문항의 재신청 제한 7년이 옮겨붙은 것으로 보인다. → `:64`는 숫자를 빼고 `:79`로 넘길 것(단일 출처 원칙).
- **`diagnosis.html:438,461` "(재신청 불가)"** — 제595조는 "기각**할 수 있다**"(재량), 제564조는 면책**불허가사유**(+제2항 재량면책). 신청 자체는 가능하다. `result.html`·`js/diagnosis.js`는 정확해 **화면만 틀렸다.**
- **파산 인지대 "소액(수천 원)" → 2,000원**(파산 1,000+면책 1,000). ⚠️**조문 번호를 붙이지 말 것** — 근거가 인지법이 아니라 「민사접수서류에 붙일 인지액… 예규」다(반박 에이전트가 인지법 제9조 5항 2호 인용을 깨뜨렸다).

**✅ "정확"으로 확정 (다시 의심하지 말 것)**
- **전자상거래법 시행령 제21조의2 — law.go.kr 원문 확인 완료.** 조 제목 "시험 사용 상품 등의 제공 방법", 제1호 "일부 이용의 허용: 디지털콘텐츠의 일부를 미리보기, 미리듣기 등으로 제공", 위임근거 "법 제17조제6항 단서"가 **조문 본문에 명기**. `js/main.js:1093`·`api/src/index.js:124-125` 인용이 호 번호·문언 모두 일치. **환불 제한 정책의 법적 구성은 유효.** U-LEX 의존 항목 해소.
- 청약철회 14일이 법정 7일보다 유리 → 제21조 제1항 제1호 위반 아님 / 주민번호는 동의로도 불가(제24조의2에 동의가 열거돼 있지 않음) / 압류금지 250만원(2026-02-01 시행) / **2026 기준중위소득 6개 값 전부 고시 일치** / numcheck 근거 조문 5건 + 제625조②1(악의 불요) vs 제566조7(악의 필요) / 채무한도 15억·10억 / 당연복권 / **변호사법 제109조 제1호는 유상성이 구성요건**이고 AI가 초안을 쓰지 않고 점검·질문만 하도록 프롬프트로 강제돼 있어 설계가 지켜짐 / 주민번호 마스킹 화면·서버 정규식 완전 일치(9케이스, 오탐 0) / 최대잉여법 1원 오차 없음 / 표준생계비 단일 출처 / AI 게이트 순서(동의→회수→호출→성공 시 차감) 결함 없음 / 패키지 키 8개 지점 일치 / 가상사례 수치 전부 재계산 일치 / 깨진 링크 0건.

**🟡 P1 (오픈 전후 판단)**
- **마이페이지 "저장된 데이터 초기화"가 결제한 접근권을 영구 소멸시킨다.** `js/main.js:691`이 *"Storage.remove를 쓰면 안 된다"*고 경고해 둔 바로 그 일을 `mypage.html:761`이 한다. 서버는 `put` 루프만 `PLAN_KEYS`로 막고 **`del` 루프는 무방비**(`api/src/index.js:989`). 근본 해결은 `handleGetData`에서 `plan_packages`를 entitlements로 재구성(1줄).
- **파산 이용자에게 회생 전용 서류를 요구한다.** 파산 진행센터 → 숫자검산 진입 시 제목이 「개인회생채권자목록」이고, 서류 간 대조가 *"변제계획안은 아직 계산하지 않으셨습니다"*라고 **존재하지 않는 서류**를 안내. `bankruptcy.html:123`이 약속한 "재산목록 합계"도 단독 탭이 없다.
- **재직자 청산가치 경고가 사실상 없다.** 진단이 퇴직금을 의도적으로 안 물어 `totalAssets=0`이라 경고 조건이 성립하지 않는다. 월 260만 사례에서 **퇴직금 580만원만 넘으면** 36개월 계획이 제614조 1항 4호를 못 맞추는데 화면엔 "면책 가능 6,510만원"이 굵게 뜬다. 결과 화면에 **변제율 표기**(numcheck는 이미 4.3%로 정확히 표시)를 추가할 것.
- **`privacy.html`에 개인정보 보호법 제22조 제3항 미반영** — 동의 없이 처리하는 항목(접속기록, 전상법 제6조 법정 보존)의 **법적 근거를 동의 항목과 구분해 공개**해야 한다. 수집 항목 표에 "처리 근거" 열 1개 추가가 최소 분량. ⚠️방침 개정이라 7일 전 공지·`CONSENT_VERSION` 동반(단 고지 추가라 '불리한 변경 30일'은 아님).
- **방침에 없는 항목이 AI로 나간다** — `summaryForAI`가 `totalRepay`(총 변제예정액)를 보내는데 `privacy.html:209` 열거에 없다(월 변제액×횟수의 산술 결과라 실질 위험은 낮음). 같은 항목을 **제2항 표·제6항·제14항이 서로 다르게 열거**한다(제2항에 총 채권액·가구원 수 누락). 코드 기준으로 제6항이 맞다.
- **청약철회 '방법' 고지 누락** — 실제 자동 전액환불 경로는 마이페이지 버튼인데 `terms.html:107`·`pricing.html:343`은 이메일만 안내(주문확인 메일은 둘 다 안내). 전상법 제13조②5의 '서식' 링크도 메일에 없다.
- 회생 맥락 "보정명령"→보정권고 3곳 / `documents.html:107`만 '법률대리 아님' 문구 누락 / robots.txt에 `admin.html` 미차단·sitemap에 discharge.html 누락 / supplement 잠금 문구가 완주 패키지 보유자에게 오독 / 청약철회 서버측 중복 INSERT 방지 없음 / 마이페이지에 AI 잔여 회수 미표시(데이터는 이미 내려옴) / `.preview-mode` CSS 규칙 0건.

**🟡 결핍(있어야 하는데 없는 것) — 독립 기준을 먼저 세우고 대조**
- **면책 후 할 일이 통째로 없다**(위 압류 해제 + 확정증명원). 사용자 피해 가능성 최대, 보완 비용 최소.
- **파산 선고 이후~복권 구간이 비어 있다**: `의견청취기일`·`채권자집회`·`면책확정증명원` 전 파일 0건. 회생은 maintenance+discharge로 받쳐 놨는데 파산엔 대응물이 없다 → `bankruptcy-after.html` 신설(discharge.html과 같은 무료·같은 형식) 검토.
- **추심 응급 대응 0건**: `불법추심`·`금감원 1332`·**`채무자대리인`(신복위 무료지원)**·압류방지통장 전부 0건. index 히어로 첫 줄이 "추심, 독촉으로…"인데 답이 "신청서와 함께 중지·금지명령"뿐이고 준비에 2~4주가 걸린다. **외부 링크 3개면 메워진다.**
- `브로커`·`사무장` 0건(셀프 진행을 권하는 사이트가 그 반대편 위험을 경고 안 함) / 비용 총액 계산기 없음(numcheck에 「비용」 탭, 입력=채권자 수) / `취하`·중도 이탈 0건 / 서류 6종 누락(퇴직금 산정서·통장 거래내역·과세증명·자격득실확인서·폐업사실증명·진단서) / `개인워크아웃`·`새출발기금` 0건 / 연대보증 딜레마 / 심문기일 실무 준비(복장·지참물).

**미검증으로 남긴 범위(묵시적 누락 금지)**: 실제 구매 상태 경로(`PAYMENTS_ENABLED=false`라 세션 생성 불가 — 코드 정독만) / AI 실호출 응답 렌더 / 포트원 결제·환불 왕복 / 가입·이메일·OTP 흐름 / admin 화면 / documents·compare·maintenance·supplement 법률 문구 전체 / FAQ 23문항 중 11문항 / 진행센터 단계별 가이드 본문 대부분.

**⚠️`CLAUDE.md` 자체의 오인용 발견**: "입증책임은 처리자에게 있다 — 제22조 제3항"이라 적었으나, 제22조 제3항의 입증책임은 **"동의 없이 처리할 수 있다는 것"의 입증책임**이지 동의를 받았다는 입증책임이 아니다. 민감정보 동의의 입증은 제23조·제22조 제1항 제5호 체계.

### 2026-08-11 (2차) — 운영 대시보드에 잔액·한도 상태 카드 ✅배포·검증 완료

"솔라피 잔액이나 Anthropic 크레딧을 대시보드에서 한 번에 볼 수 없나?"에서 출발. **소진돼도 에러가 안 나고 기능만 멎기 때문에** 콘솔을 매일 열지 않으면 이용자 문의로 알게 되는 구조였다.

**조회 가능성이 항목마다 달라 화면도 다르게 만들었다** (`admin.html` 최상단 `⚡ 운영 상태`):

| 항목 | 조회 방법 | 표시 |
|---|---|---|
| 솔라피 SMS | ✅ 실시간 잔액 API | 잔액 + 포인트 + 남은 문자 건수 |
| Resend 이메일 | 잔액 개념 없음 → **우리가 센다** | 오늘 발송 / 일 한도 |
| Anthropic | ❌ **잔액 API 자체가 없음** | 호출 수(비용 대리지표) + 콘솔 링크 |

- **🔴Anthropic 잔액은 조회할 수 없다(2026-08-11 공식 문서 확인)**. 사용량·비용 Admin API(`/v1/organizations/cost_report`)는 존재하지만 ①*"The Admin API is unavailable for individual accounts"* ②Admin API 키는 **조직(Organization) 계정의 admin 역할**만 발급 → 개인 계정은 불가. **없는 숫자를 추정해 채우지 말 것** — 1인 운영에서 틀린 잔액은 없느니만 못하다. 다음에 또 물어보면 이 항목을 먼저 볼 것.
- **솔라피 경로는 실호출로 확정**: `GET https://api.solapi.com/cash/v1/balance`, 발송(`sendSms`)과 **동일한 HMAC-SHA256** 인증. 공식 문서(docs.solapi.com)가 응답하지 않아 문서로는 못 찾았고 배포 후 실호출로 검증했다. 실측 응답 `{balance: 9000, point: 264}`.
- **임계값은 금액이 아니라 남은 문자 건수**로 잡았다(55건 미만=충전 필요 / 165건 미만=여유 적음). 처음엔 "1만원 미만=여유 적음"이었는데 실제 9,000원이 경고로 떠서 과했다. **유료 결제 1건당 인증문자 1통**이므로 남은 건수가 곧 받을 수 있는 결제 건수다. 포인트도 발송에 쓰이므로 합산.
- **`email_usage` 테이블 신설**(운영 DB 적용 완료) — `sendEmail` 성공 시 일자별 카운트. 메모리에 있던 "가입이 인증메일에 의존 → Resend 일 100통 소진 시 신규 가입 중단" 위험이 이제 숫자로 보인다. ⚠️집계 실패가 메일 발송을 깨지 않도록 try/catch로 감쌌다(표가 없어도 메일은 나간다).
- **검증**: 운영 API 실호출로 솔라피 잔액 확인 + jsdom으로 카드 렌더 4개 시나리오(정상/잔액부족/메일한도 도달/조회실패). 임시 관리자 세션은 삭제·401 확인, DB 원상 복원.
- **사용자 확인 완료**: 운영 대시보드 화면 확인함.
- **결정**: **`www.chamroad.com`은 연결하지 않는다**(사용자, 2026-08-11). 다시 제안하지 말 것.

### 2026-08-11 — 개인정보 조치 4건 ✅배포 완료 (D1 `sensitive_consent_at` 적용됨)

사용자 질문("미국 전송이 실제로 되나? 동의가 필요한가? 민감정보를 지우게 하면 검토가 제대로 되나?")에서 출발해 공식 출처로 확인한 결과 **실제 위반 소지 2건**을 찾아 전부 조치했다.

**① 🔴주민등록번호 자동 마스킹 (제24조의2 — 동의로도 처리 불가)**
- 종전: "○○으로 가린 뒤 넣어 주세요" 안내 + confirm에서 [예]를 누르면 **그대로 전송**. 안내를 읽은 사람만 보호받았다.
- 지금: `maskIdNumbers()`가 화면·서버 **양쪽에서 같은 정규식**으로 지운다(`○○○○○○-○○○○○○○`). 외국인등록번호(7번째 자리 5~8)도 포함.
- 서버 마스킹 지점 3곳: AI 검토 본문 / AI 대조 본문 / **`/api/data` 동기화의 직렬화 문자열**. 마지막 것 덕에 앞으로 새 기능이 생겨도 저장 경로는 자동 보호된다.
- 오탐 방지: 앞뒤가 숫자면 제외 → 전화번호·사업자번호·계좌번호·금액은 건드리지 않음(테스트로 고정).

**② 🔴검토 기록의 서류 본문 80자 저장 폐지**
- `saveRecord()`가 초안 앞 80자를 `preview`로 저장했고, 이것이 **서버 D1까지 동기화**됐다. **법원 서류의 첫머리는 대개 "신청인 ○○○ (주민등록번호 …)"** 라 실제로 걸릴 확률이 높았다.
- 지금은 **AI가 만든 질문 주제(`topics`)만** 남긴다 — 이용자가 무엇을 보완해야 했는지 되짚기에 오히려 낫고 이용자가 쓴 문장이 아니다. `lawNotes.excerpt`(원문 인용)도 저장하지 않는다.
- 마이페이지가 **옛 기록의 preview를 열 때 조용히 지운다** — 이용자가 아무것도 안 해도 정리된다.

**③ 민감정보 별도 동의 (제23조) — "지우고 넣으세요"를 없앴다**
- 채무 발생 경위에는 질병·치료가 사실상 반드시 들어가는데, 지우게 하면 검토가 의미를 잃는다. 제23조 제1항 제1호는 **다른 동의와 별도로** 동의를 받으면 처리할 수 있게 한다 → **동의를 제대로 받고 있는 그대로 받는다.**
- `users.sensitive_consent_at`(동의 시각) 신설. 입증책임이 처리자에게 있어(제22조 제3항) 화면 체크만으로는 부족.
- 서버 `requireSensitiveConsent()`가 AI 두 엔드포인트를 막고, **회수 차감보다 먼저** 검사한다. 마이페이지에 **철회** 창구(철회 없는 동의는 온전하지 않다).
- 화면 문구 교체: "개인정보는 제거하고 입력" → **"작성하신 그대로 붙여넣으세요. 주민등록번호는 자동으로 가려집니다."**

**④ `inference_geo: 'us'` — 방침의 "미국"을 사실로**
- Anthropic 공식 문서 확인: 기본값 `'global'`은 **"어느 지역에서든 추론이 돌 수 있다"**는 뜻(저장은 미국만). 방침에 "이전되는 국가: 미국"이라 단정해 둔 것이 사실과 어긋날 수 있었다. `'us'`로 고정(요금 1.1배).

**방침·약관**: privacy.html 시행일 **2026-08-11**. 제2항에 별도 동의 안내 블록·주민번호 자동 마스킹 블록 신설, "민감정보는 지우고 입력" 문구 삭제, 제6항 국외이전에 미이전 항목·민감정보 별도 동의 명시, 제14항 변경이력. ⚠️`CONSENT_VERSION`은 **아직 `terms-2026-08-10/privacy-2026-08-10`** — 배포 전에 `privacy-2026-08-11`로 올릴 것.

**검증**: 새 스위트 2개(`privacy.compliance` — 서버·클라이언트 마스킹 결과가 무작위 300건에서 완전 일치, 오탐 8종 통과 / `consent.page` — 동의 없이는 전송 안 됨, 동의 후 **주민번호만 가려지고 우울증·폐업 서술은 그대로 나감**, 저장물에 본문·주민번호 없음). 기존 8개 스위트 회귀 없음(2개는 새 동의 게이트에 맞춰 세션 픽스처 갱신).

**✅ 배포 완료 (2026-08-11)** — D1 컬럼 추가 → `CONSENT_VERSION` 갱신 → push 순으로 진행했다.
⚠️**교훈(다음에도 같음)**: 새 코드가 SELECT하는 컬럼은 **D1을 먼저** 적용해야 한다. 컬럼 없이 배포하면 세션 조회가 전부 실패해 **로그인이 통째로 깨진다.** 배포 후 `/api/auth/login`이 500이 아닌 401을 내면 정상(쿼리는 돌았고 자격증명만 틀린 것).


### 2026-08-10 (4차) — 이용자 절차 상황을 AI에 함께 보내기 ✅배포 완료(2026-08-11 push)

3차의 차별점 4가지 중 **②맥락 주입**. 챗봇 이용자는 매번 "제 상황은…"부터 설명해야 하지만, 이 서비스는 진단 답변으로 이미 알고 있으면서 **AI에게 한 톨도 주지 않고 있었다.**

- **`js/aicontext.js` 신설** — `diagnosis_data`·`profile`·`plan_package`에서 **절차 플래그만** 뽑는다: 절차(회생/파산), 소득 형태, 부양가족, 연체 기간, 최근 대출, 법적조치, 과거 채무조정 이력, 재직·부수입·지인채무·담보·폐업·재산처분 여부.
- **🔴 안 보내는 것**: `hasHealthIssues`(건강)·`debtCauses`(도박 등) — 「개인정보 보호법」 제23조 관련이라 `SENSITIVE_FIELDS`로 서버 동기화에서도 이미 빼 둔 값이다. **연령·모든 금액도 제외**(금액이 필요한 대조는 crosscheck가 별도 통로로 담당 — 통로를 나눠야 방침에 정확히 적힌다).
- **고지가 전송과 구조적으로 일치**: 화면의 "무엇이 전송되나요?" 목록을 `describe()`가 생성한다. 손으로 쓴 목록이 아니라서 어긋날 수 없다. **기본 켬 + 끄기 가능**(끄면 `payload()`가 null).
- **서버 `sanitizeContext()`** — 클라이언트를 믿지 않고 enum 키만 통과시킨다. 방침 일치 + **프롬프트 인젝션 차단**(이용자 문자열이 프롬프트에 지시로 섞이지 않게).
- **`AI_CONTEXT_HINT`** — 맥락이 있을 때만 붙는 지침. ①맥락을 초안에 쓰인 사실인 양 `present`에 담지 말 것 ②맥락에 법을 적용해 결론 내지 말 것 ③초안과 다르면 판정하지 말고 되물을 것(진단은 대략 답한 값일 수 있다). 서류검토·서류간대조 **양쪽에 적용**.
- **🐛 발견·수정**: `if (p.closedBiz)` 느슨한 비교라 문자열 `'yes'`가 통과했다. 서버는 `=== true`만 받으므로 **화면에는 "폐업 이력 있음"이 뜨는데 실제로는 안 나가는** 고지 불일치가 생길 수 있었다 → 클라이언트도 `=== true`로 통일.
- **방침·약관은 3차와 같은 8/10 개정에 묶었다**(아직 배포 전이라 이중 개정을 피함). privacy 제2항 표·제6항 국외이전 ②에 ⓒ절차 상황 추가 + **"이전하지 않는 항목"(건강·채무발생원인·연령·세부 금액) 문단 신설**.
- **검증**: `aicontext.test.js`(민감정보·금액·연령 미전송을 항목별로 못박음, 조작된 저장소 값 폐기), `server.sanitize.test.js`(**Worker 실제 코드를 vm으로 로드** — 클라이언트가 주민번호·금액·인젝션 문구를 밀어 넣어도 `procedure` 하나만 통과함을 확인), jsdom으로 카드 표시·토글 끄기·**실제 전송 본문 검사**. 기존 5개 스위트 회귀 없음.

**차별점 4가지 중 남은 것**: ③검증된 지식 베이스(관할법원별 실무·보정 유형) ④검토 이력("지난 지적 3건 중 2건 반영" — D1 테이블 필요, 초안 원문은 저장 금지).

### 2026-08-10 (3차) — 서류 간 대조(횡단 검증) ✅배포 완료(2026-08-11 push)

**질문에서 출발한 작업.** "이용자가 다른 LLM에 직접 물어보지 않고 이 플랫폼을 써야 할 명확한 이유가 뭔가?" — 점검 결과 **당시 답은 '없음'이었다.** AI 검토가 서버로 보내던 것은 `docType/docLabel/checklist/text`뿐이라 사실상 "좋은 프롬프트를 붙인 ChatGPT"였고, 우리는 법무사법 때문에 대필도 안 하니 오히려 덜 해 주는 것으로 보일 수 있었다.

**결론: 해자는 프롬프트가 아니라 '여러 서류를 동시에 들고 있는 것'.** 개인회생 서류는 6종이 물려 있고 보정명령의 상당수가 그 사이의 어긋남에서 나오는데, 서류를 하나씩 채팅창에 붙여넣는 방식으로는 **구조적으로** 못 잡는다. 이걸 구현했다.

**① `js/crosscheck.js` — 코드 층 (무료·전송 0)**
- 채권자목록 총액 ↔ 변제계획안 확정채권액 총액 / 채권자 **구성**과 채권자별 금액 대조 / 수입·지출의 가용소득 ↔ 계획안 월 변제액(제614조 제2항 제2호) / 무료 진단 결과와의 대조(총채무·채권자 수·부양가족·재산 — 진단은 개략치라 `diff`가 아니라 `ask`로만).
- `normName()`으로 `(주)`·`주식회사`·공백 표기 차이를 흡수 — 안 하면 같은 은행을 다른 채권자로 보고 거짓 경고가 쏟아진다.
- **브라우저 안에서만 돈다.** 그래서 무료로 줄 수 있고 방침 개정도 필요 없었다.

**② `numcheck.html`에 '🔗 서류 간 대조' 탭**
- 앞 3탭에서 [검산하기]를 누를 때마다 결과가 `LAST`(사건 파일)에 쌓이고, 대조 탭이 그걸 읽는다. **추가 입력이 사실상 없다** — 검산기에 이미 넣은 값을 재활용.
- 상태칸(✅계산됨/⬜아직)으로 무엇을 더 넣으면 대조가 늘어나는지 보여 준다.
- ⚠️`LAST`는 **메모리에만** 있다(localStorage·서버 저장 0, 새로고침하면 사라짐). 저장을 붙이면 방침부터 고쳐야 한다.

**③ `POST /api/ai/crosscheck` — AI 층 (유료, 코드가 못 하는 것만)**
- 진술서의 **서술**과 숫자가 어긋나는지만 본다. "2022년에 처음 대출받았다"고 썼는데 목록에 2020년 채권이 있는 경우 등 — 코드로는 불가능한 영역.
- 전용 프롬프트·스키마(`conflicts`/`unexplained`). **숫자를 계산하지 말고 주어진 값을 인용만** 하도록 못 박음(서류검토 프롬프트 금지 6번과 같은 이유). 대필·판정 금지는 동일.
- 회수는 서류검토와 **한 주머니** — `handleAiReview`에서 회수 로직을 `checkAiQuota`/`consumeAiQuota`로 뽑아 공유(중복 구현하면 갈라진다).
- 서버 `sanitizeFigures()`가 클라이언트가 보낸 숫자 요약을 **다시 거른다** — 방침에 적힌 이전 항목과 실제 전송 항목이 같아야 하므로.

**④ 개인정보 경계선**
- 나가는 것은 `CrossCheck.summaryForAI()` 하나로 모았다: 채권자명·채권액, 총액, 수입·지출 합계, 가용소득, 가구원 수, 재산 합계, 월 변제액·횟수. **원금/이자 세부와 개별 지출 항목은 안 나간다.**
- 화면에도 접이식으로 "무엇이 전송되나요?"를 붙였고, 주민번호 패턴 사전 경고는 ai-review와 동일 기준.
- **`privacy.html` 개정**: 제2항 표에 'AI 서류검토·서류 간 대조' 행 갱신 + **'숫자 검산기 = 수집하지 않음'** 행 신설, 제5항 위탁표·제6항 국외이전 ②에 금액 요약 명시, 제14항 변경이력. 시행일 2026-08-10.
- **`terms.html` 개정**: 제5조에 AI 회수가 서류검토와 서류 간 대조에 **공통 사용**됨을 명시(+숫자 검산은 무료). 부칙 추가. 시행일 2026-08-10.
- **`CONSENT_VERSION` → `terms-2026-08-10/privacy-2026-08-10`**.
- ⚠️**즉시 시행으로 처리했다** — 이용자가 직접 실행할 때만 전송되는 선택 기능이고 유료 판매 미개시라 영향받는 이용자가 0명이라는 근거를 양쪽 변경이력에 적어 뒀다(2026-08-08 AI 체험 폐지 때와 같은 판단 구조). 보수적으로 가려면 시행일을 7일 뒤로 미루고 그때 push할 것.

**⑤ 검증** — 전부 통과.
- `crosscheck.js` 단위 테스트: 정합한 사건 파일에서 오탐 0, 채권액·구성·가용소득 불일치 각각 검출, **표기 차이((주)○○은행 ↔ ○○은행 주식회사)는 거짓 경고 안 냄**, 진단 차이는 `diff`로 올리지 않음, `summaryForAI`가 원금·이자를 내보내지 않음.
- jsdom으로 대조 탭 실제 실행: 상태칸, 두 종류 미만일 때 안내, 불일치 검출, **AI 호출 시 전송 본문 검사**(원금·개별 지출 미포함 확인), 결과 렌더, 잔여 회수, **판정 문구 없음**, AI 응답에 태그가 섞여도 실행되지 않음(XSS).
- 기존 4개 스위트 회귀 없음. 전 JS `node --check`, 인라인 스크립트 파싱 0에러.

**배포됨**(2026-08-11). 운영 API 실호출로 전 구간 검증 완료 — 맥락 주입이 실제로 작동하는 것도 확인(초안에 없는 압류·추심/부양가족을 AI가 물었다).

### 2026-08-10 (2차) — 숫자 검산을 AI에서 코드로 분리 ✅배포 완료(2026-08-11 push)

오래 미뤄 뒀던 "숫자 검산은 코드로" 항목을 구현했다. **AI에게 산술을 시키지 않는다**가 이 작업의 전제다 — 언어모델의 계산은 틀려도 그럴듯해 보이고, 법원 서류에서 금액이 어긋나면 그대로 보정명령이 된다.

**① 새 파일 2개**
- **`js/numcheck.js`** — 순수 계산 모듈(DOM 없음, Node에서 그대로 테스트 가능). 3종:
  - `creditorList` — 행별 원금+이자 = 합계, 세로 합계, 목록에 적은 합계와의 차액, **안분율 합 100.00%**, 빈 이름·0원·중복 채권자 질문
  - `incomeExpense` — 총수입−총지출, 표준생계비 대조, **가용소득 = 수입 − max(실제 지출, 표준생계비)** (진단 `calcRepayment`와 같은 기준), 적어 둔 가용소득과의 차액
  - `rehabPlan` — 가용소득×횟수 = 총 변제예정액, **청산가치 이상인지**(미달이면 필요한 회차까지 계산), 변제기간 상한, 채권자별 안분율·월 배당액·변제율
- **`numcheck.html`** — 탭 3개 UI. 행 추가/삭제, 금액 콤마 자동, **예시 숫자 넣어 보기**, 탭 사이로 값 넘기기(채권 목록 → 계획안, 가용소득 → 계획안).
  - **무료·비로그인**. ⚠️**입력값을 저장하지 않는다**(localStorage·서버 전송 0). 저장을 붙이면 새 개인정보 항목이 생겨 `privacy.html` 개정이 필요해지므로 일부러 뺐다.

**② 최대잉여법(largest remainder)** — 비율을 각각 반올림하면 합이 100%가 안 된다(예시 케이스는 99.99%). 내림 후 남은 몫을 버려진 소수가 큰 순서로 되돌려 **합을 정확히 100.00%·가용소득 전액**으로 맞춘다. 법원 양식의 합계 칸이 딱 떨어져야 해서 필요한 처리다.

**③ 표준생계비를 `js/main.js`로 이전** — `MEDIAN_INCOME_2026`·`getStandardLiving`이 `diagnosis.js`에 있어 진단 페이지에서만 쓸 수 있었다. 검산기도 같은 값을 써야 하므로 모든 페이지가 읽는 `main.js`로 옮기고 diagnosis.js에는 "복사본 만들지 말 것" 주석만 남겼다. ⚠️**중복 선언하면 `const` 충돌로 페이지 전체가 죽는다.**

**④ AI에서 산술 제거(`api/src/index.js`)** — `AI_SYSTEM_PROMPT` 절대금지에 **6번(숫자를 계산하지 않는다)** 신설. 합계·차액·안분율·곱셈을 직접 계산하거나 검산 결과를 말하지 말고 질문으로만 돌리도록 했다. 진술서 보강지침의 "시기·금액이 어긋나는 곳"도 **계산하지 말고 두 곳을 인용해 되묻도록** 수정. ⚠️이 변경은 `api/**`라 push하면 GitHub Actions가 Worker를 자동 배포한다.

**⑤ 화면 연결** — ai-review에 ⓐ공통 안내(숫자는 검산기에서) ⓑ**금액이 들어가는 서류 7종을 고르면 해당 탭으로 보내는 배너**(`NUMCHECK_TAB`) ⓒ"검토가 하는 것" 목록에서 '숫자 불일치 지적' 삭제. 진행센터(회생·파산)·서류 체크센터·푸터·`sitemap.xml`에 링크 추가. 검산기 카드는 **유료 잠금 바깥**에 뒀다(무료 도구).

**⑥ 근거 조문(공식 출처 확인 2026-08-10)** — `js/numcheck.js` 상단 주석에 문언까지 적어 뒀다.
- 청산가치 보장 = 제614조 제1항 제4호 / 변제기간 3년·예외 5년 = 제611조 제5항 / 가용소득 정의 = 제579조 제4호 / 가용소득 전부 제공 = 제614조 제2항 제2호
- 목록 누락 채권 = **제625조 제2항 제1호** — 개인회생은 "기재되지 아니한 청구권"이라 **'악의'를 요구하지 않는다**(개인파산 제566조 제7호와 다름). 기존 파산 쪽 안내문("알면서 일부러 누락한")은 파산 조문 기준이라 그대로 정확.

**⑦ 검증** — scratchpad에 하네스를 만들어 실제로 돌렸다(전부 통과).
- `numcheck.js` 단위 테스트: **`rehabilitation.html`의 실전 작성예시 숫자를 정답지로 사용** — 안분율 39.70/27.15/17.59/15.56%와 월 배당액 421,398/288,186/186,710/165,163원이 **게시된 예시와 완전히 일치**(최대잉여법이 예시 작성자의 손계산을 그대로 재현). 총 변제예정액 38,212,452 ≥ 청산가치 10,260,000도 일치. + 무작위 200회로 합계가 항상 딱 떨어지는지 확인.
- **jsdom으로 `numcheck.html`을 실제 실행**: 탭 전환·예시 로드·검산·불일치 감지·탭 간 값 전달·입력 부족 안내·**채권자명 XSS(태그가 실행되지 않고 문자로 남음)** 확인.
- **회귀**: `diagnosis.html`(상수 이전 후 `calcRepayment` 동일 값), `ai-review.html`(배너 표시/숨김·링크 탭) jsdom 실행 확인. 전 JS `node --check`, 인라인 스크립트 파싱 0에러.

**배포됨**(2026-08-11). 라이브 확인 완료 — https://chamroad.com/numcheck.html

### 2026-08-10 — 결제 E2E 완주 + 자동 배포 + 가입 이메일 인증 ✅전부 배포됨

하루 종일 이어진 세션. **테스트 채널에서 결제 전 구간이 실제로 한 바퀴 돌았고**, 그 과정에서 나온 문제를 그때그때 고쳤다. 아래 항목은 모두 배포·검증 완료.

**① 판매를 열지 않고 결제 E2E 하는 통로**
- 서버 `PAYMENTS_ENABLED`(판매 잠금)에 예외: `ADMIN_EMAIL` + 시크릿 `TEST_PAY_EMAILS`(현재 `lexus17@naver.com`). 프론트는 `?paytest=1`로 버튼만 열림 — 실제 허용은 서버 `payAllowlisted()`가 판정.
- ⚠️**판매 오픈 시 `TEST_PAY_EMAILS` 시크릿 삭제**하고 `PAYMENTS_ENABLED`를 **두 곳**(js/main.js + api/src/index.js) 모두 true로.
- 로컬에서 운영 API를 보려면 `?api=prod`(localhost 전용 스위치, `?api=local`로 복귀). 로컬 기본값은 wrangler dev(:8787)이라 이걸 모르면 "서버에 연결할 수 없습니다"만 뜬다.

**② 결제 E2E 결과 — 전 구간 통과**
가입 → 결제(19,000원 correction-rehab) → 콘텐츠 이용 → **주문확인 서면 메일 실발송 확인** → 청약철회 신청 → (이용 개시 기록 있어 자동환불 대상 아님 → 운영자 검토) → 관리자 [환불 처리] → `payments=refunded` / `withdrawal_requests=resolved` / entitlement·plan 키 회수까지 DB로 확인.
- **마이페이지 청약철회 3단계 트래커** 신설(접수 → 확인·검토 → 환불 완료). 검토 중이면 사유와 법정 처리기한(신청일부터 3영업일, 전상법 제18조②) 안내. ⚠️점은 `position:absolute`로 둘 것 — 인라인 `<span>`은 width/height가 안 먹어 안 보인다.
- **관리자 🧾청약철회 신청 섹션** 신설(결제 내역 위). 사유·이용 개시 여부·경과일을 카드 하나에 모으고 [환불 처리] 버튼. 미처리 건은 호박색 강조 + 제목에 건수.
- 환불 시 **신청 자동 종결 + 이용자에게 완료 메일 자동 발송**(접수 메일에서 약속한 것을 지키도록).
- **콘텐츠 첫 열람 전 사전 고지 모달** — "이 부분을 열면 환불 대상에서 제외됩니다". 닫으면 열지 않으므로 환불 기회 유지. 법 제17조⑥의 '청약철회 불가 표시'를 실제 개시 시점에 수행. 호출 지점 6곳 전부 `confirmContentOpen()` 경유.
- 🐛**발견·수정**: `plan_expires_at`이 `PLAN_KEYS`에 없어 환불 후에도 남고 클라이언트가 위조 가능했다(이용권 판정은 `plan_packages`가 하므로 접근은 안 열렸음). 서버가 저장하도록 바꾸고 보호 목록에 추가.

**③ Worker 자동 배포 (GitHub Actions)**
- `.github/workflows/deploy-worker.yml` — `api/**`가 바뀐 push에서만 `wrangler deploy`. 배포 전 `node --check`로 문법 오류 차단.
- ⚠️두 번 실패 후 성공. 원인 ①액션 기본 wrangler 3.90은 `wrangler.jsonc`를 못 읽음 → **`wranglerVersion: '4.120.0'` 고정** ②wrangler 4는 **Node 22** 이상 필요.
- 필요 시크릿: `CLOUDFLARE_API_TOKEN`(등록 완료). **이제 push만 하면 화면·서버가 함께 반영된다.** D1 마이그레이션만 수동 유지(되돌리기 어려운 작업).

**④ 가입 이메일 인증(코드 입력) — 종전 '소프트' 방침 뒤집음(사용자 결정)**
- `email_otp` 테이블(계정이 없으니 **이메일이 키**), `POST /api/auth/send-signup-code` · `verify-signup-code`. 확인된 코드 **30분 내**에만 가입 허용, `email_verified=1`로 생성 후 코드 행 삭제(한 인증으로 여러 계정 방지).
- 가입 폼: [인증번호 받기] → 코드+카운트다운 → ✓인증완료(이메일 칸 잠금). 이메일 수정 시 인증 무효화.
- **기존 미인증 계정 2개는 예외로 유지**(사용자 결정).
- ⚠️이제 **가입이 메일 발송에 의존**한다. Resend 무료 = **일 100통**이라 소진되면 신규 가입이 중단된다. 넘치면 Pro $20/월(수탁자 동일 → 방침 개정 불필요).

**⑤ Turnstile(자동 가입 방지)**
- 사이트키 `0x4AAAAAAEK4Lz3VPkz6RFFv`(공개값·main.js), 시크릿 `TURNSTILE_SECRET`. 토큰 검증은 **메일이 나가는 send-signup-code 단계**에서(캡차를 두 번 풀게 하지 않음). 시크릿 미설정·네트워크 오류 시 **통과**(fail-open) — 캡차 장애가 가입 전면 중단이 되면 안 되므로.
- ⚠️최초 위젯은 시크릿 키가 `wrangler secret`의 **이름 자리에 잘못 등록**돼 평문 노출 → **위젯째 재발급**했다. `secret put <이름>`의 이름 자리에 값을 넣지 말 것.

**⑥ 보안 점검(전수) + 조치**
양호: PBKDF2 10만회+PEPPER·timingSafeEqual / 세션토큰 해시 저장 / **SQL 전건 파라미터 바인딩** / 로그인 5회 잠금·계정 열거 방지 / 결제 서버측 금액검증 / **시크릿 커밋 이력 0건**.
조치: 가입 레이트리밋 부재 → Turnstile / 서버측 판매 잠금 / mypage `esc()` / 탭 간 계정 전환 시 새로고침(옛 계정 화면의 저장이 새 계정 데이터를 오염시킬 수 있었다) / 관리자만 헤더에 [운영 대시보드](서버가 `isAdmin` 반환 — 관리자 이메일을 공개 JS에 박지 않기 위해).

**⑦ UI·가독성**
- **한글 줄바꿈 전역 적용**: `body { word-break: keep-all; overflow-wrap: break-word }` + 문단 `text-wrap: pretty` + 제목 `balance`. 메일 템플릿에도 동일 적용. "전액이"가 "전액/이"로 갈라지던 문제.
- 로그인/가입: 실패 메시지가 스크롤 밖이라 "눌렀는데 반응 없다"고 느끼던 문제 → 오류로 자동 스크롤 + 버튼 처리 중 상태. 이메일 중복은 칸 바로 아래 인라인 + [이 이메일로 로그인].
- 마이페이지 상단에 **현재 로그인 계정(이메일)** 표시 — 이름이 같은 계정이 둘이라 헷갈렸다.
- SMS·이메일 인증코드 **5분 → 3분** + 입력칸 안 카운트다운(서버가 `expiresIn` 내려줌).

**⑧ 백업·이식성(사용자 완료)**
- `.claude/agents`(검증 5종)·`skills/legal-verify`를 **저장소에 포함**. 기계별 `settings*.json`은 계속 제외. `launch.json`이 사라진 임시 경로를 가리키던 것 → **`tools/serve.js`** 신설로 이식 가능하게.
- **PEPPER를 비밀번호 관리자에 백업**(Cloudflare 시크릿은 재조회 불가 — 잃으면 전 회원 비밀번호 재설정). GitHub·Cloudflare **2FA 활성화 + 복구코드 저장** 완료.

**남은 것 = 사업자등록 → 포트원 라이브 → 판매 오픈.** 순서 뒤집지 말 것.

### 2026-08-09 — 보안 점검 + 조치 3건(Turnstile·판매잠금·XSS) ⚠️Worker 재배포 필요

사용자 요청으로 코드 전반 보안 점검 후, 나온 항목을 전부 조치. **점검 결론: 인증·SQL·결제 검증·시크릿 관리는 양호, 실질 위험은 가입 레이트리밋 부재 1건.**

- **🔴가입 레이트리밋 부재 → Cloudflare Turnstile 도입(사용자 선택)**: 로그인은 5회/15분 잠금이 있는데 **가입은 무제한**이었다. 가입 1건 = 인증메일 1통이라, 스크립트로 **Resend 일일 한도(약 100통)를 태우면 정상 이용자의 비밀번호 재설정 메일이 막히는** 서비스 중단 경로였다.
  - 서버: `verifyTurnstile()` — `TURNSTILE_SECRET` 미설정이면 **통과**(설정 전에도 가입이 막히지 않게), 설정됐는데 토큰이 없거나 거부되면 403. **네트워크 오류는 통과**(캡차 장애가 가입 전면 중단이 되면 안 됨). 검증은 계정 생성·메일 발송 **앞**에 둔다(비용 드는 작업 전에 차단).
  - 프론트: `js/main.js`에 `TURNSTILE_SITE_KEY`(현재 빈 문자열 = 위젯 미표시). `login.html`은 **explicit 렌더**(회원가입 탭이 숨겨진 채 시작해 자동 렌더는 폭 0이 됨), 키가 없으면 스크립트조차 안 불러온다. 실패 시 `turnstile.reset()`(토큰 1회용).
  - ⚠️**키 2종은 사용자가 대시보드에서 발급해야 켜진다.** 순서 주의: **사이트 키 push가 먼저, 시크릿 등록이 나중.** 시크릿만 먼저 넣으면 토큰이 안 와서 가입이 전부 막힌다. 절차는 LAUNCH-CHECKLIST 0단계.
- **판매 잠금 서버측 추가**: `PAYMENTS_ENABLED`가 `js/main.js`(프론트)에만 있어 **API 직접 호출로 우회 가능**했다. `api/src/index.js`에도 같은 상수를 두고 `/api/payment/prepare`에서 503. ⚠️**판매 오픈 시 두 곳을 함께 true로** 바꿔야 한다(체크리스트 2단계에 명시).
- **XSS 하드닝(`mypage.html`)**: 이 페이지만 `esc()` 없이 `innerHTML`을 쓰고 있었다(AI 검토 이력의 서류 초안 미리보기 80자, 계정 이름, 결제내역). 자기 데이터가 자기 브라우저에서만 렌더돼 **교차 사용자 공격은 불가(self-XSS)**지만, 세션 토큰이 localStorage에 있어 HTML 해석 자체를 차단. `esc()` 정의 후 4곳 적용.
- **점검에서 이상 없던 항목**: PBKDF2-SHA256 10만회+salt+PEPPER·`timingSafeEqual` / 세션토큰 32B 난수·DB엔 SHA-256 해시만·변경 시 전 세션 무효화 / **SQL 전건 파라미터 바인딩(문자열 결합 0건)** / 로그인 5회 잠금·없는 이메일도 카운트(열거 방지) / 재설정 응답 동일화 / 결제 서버측 `PAID`+금액 일치 검증 / 이용권은 서버 `plan_packages`가 최종 판정 / OTP 해시저장·쿨다운·횟수제한 / 본문·키·값 크기 상한 / **시크릿 커밋 이력 0건, 저장소 키 패턴 스캔 0건** / admin은 `ADMIN_EMAIL` 대조 + noindex + 사이트 내 링크 0건.
- **조치 불필요로 정리한 것**: 세션 토큰 localStorage 보관(쿠키 전환 시 CSRF 방어를 새로 만들어야 해 현 구조 유지), `/api/analytics` 무인증(개인정보 없음·부가기능), CSP 부재(GitHub Pages는 헤더 설정 불가 + 인라인 스크립트 다수).
- **검증**: `node --check` 2건, 인라인 스크립트 파싱 0에러.

### 2026-08-08 (8차) — AI 무료 체험 1회 폐지 ⚠️Worker 재배포 필요

**사용자 결정.** 계기: "탈퇴 후 재가입이 바로 되나?" 확인 중 발견한 비용 누수 — `ai_trial`이 `users` FK CASCADE라 **탈퇴 → 재가입만 하면 체험 1회가 계속 되살아난다.** 검토 1회마다 Anthropic API 실비(선불 크레딧)가 나가므로 남용 시 돈이 직접 샌다. 방어책(탈퇴자 이메일 해시 보관)은 개인정보 최소수집과 충돌 → **체험 자체를 폐지.**

- **✅법적 확인 완료(2026-08-09 조문 원문 확인)**: 시행령 제21조의2 본문 = "통신판매업자는 **법 제17조제6항 단서**에 따라 **다음 각 호의 구분에 따른 방법 중 하나 이상의 방법으로** 소비자에게 시험 사용 상품 등을 제공하여야 한다"(U-LEX 법률우주 조문 전문에서 문언 그대로 확인, 2016-09-29 신설). 각 호 = 제1호 일부 이용의 허용(미리보기·미리듣기) / 제2호 한시적 이용 / 제3호 체험용 콘텐츠 / 제4호 제1~3호가 곤란한 경우 정보 제공. **즉 택일(하나 이상)이며 누적이 아니다.** 챔로드는 **제1호(유료 콘텐츠 미리보기 — `js/main.js` requirePackage/enterPreview)를 유지**하므로, 제3호에 해당하던 AI 체험을 폐지해도 조치 요건은 그대로 충족된다.
  - 법 제17조⑥ 단서는 "**청약철회 불가 사실의 표시**"와 "시험 사용 상품 제공"을 **함께** 요구하는데, 표시 쪽도 유지되고 있음을 확인(`pricing.html` 340행 "이미 열어 본 부분은 환불되지 않습니다", 580행 결제 전 확인창, `terms.html` 제6조②).
  - ⚠️law.go.kr 자체는 본문을 렌더하지 않아 조문 확인은 U-LEX 미러 + 검색 요약 2건 교차로 했다. 판매 오픈 직전 `/legal-verify` 풀스윕 때 국가법령정보센터 원문으로 최종 대조할 것(문언은 이미 일치 확인됨 — 형식 확인 목적).
- **백엔드(`api/src/index.js`)**: `AI_TRIAL_QUOTA` 상수·체험 조회/기록 분기 삭제. 이용권 없으면 즉시 403("서류검토 AI는 패키지를 구매하신 회원만 이용할 수 있습니다"), 회수 소진은 별도 문구. `quota`는 항상 `type:'package'`. **6차에서 추가했던 `POST /api/admin/reset-trial`과 관리자 [체험 초기화] 버튼·`trial_used` 컬럼도 함께 제거**(체험이 없어져 존재 이유 소멸).
- **DB**: `ai_trial` 테이블 폐지. `api/migrations/2026-08-08-drop-ai-trial.sql`(=`DROP TABLE IF EXISTS ai_trial`). 폐지 시점 운영 DB 행 수 **0건**(실사용 이력 없음) 확인 후 결정. schema.sql에는 "다시 만들지 말 것" 주석만 남김.
- **프론트**: `ai-review.html` 상단 안내를 "패키지를 구매하신 회원이 이용" 으로 정정(안 그러면 미구매자가 긴 초안을 다 쓰고 나서야 403을 본다), 체험 잔여 안내 블록 삭제.
- **약관(`terms.html`)**: 제5조의 "구매하지 않은 회원도 1회 무료 체험" 삭제 → "패키지를 구매한 회원만". 시행일 2026-08-08, **부칙(변경 이력) 절 신설**. ⚠️약관 제9조는 이용자에게 불리한 변경에 30일 전 공지를 요구하나, ①체험은 계약상 급부가 아닌 무상 부가 혜택이고 ②개정 시점까지 이용자 0명(전체 회원 2명, 그중 1명은 관리자) ③유료 판매 미개시라 **영향받는 이용자가 없어 즉시 시행**. 이 판단 근거를 부칙에 명시했다. **`CONSENT_VERSION` → `terms-2026-08-08/privacy-2026-08-08c`**.
- **검증**: `node --check` 통과, 인라인 스크립트 파싱 0에러, 잔여 참조(`ai_trial`/`'trial'`/`reset-trial`) 전수 확인.
- ⚠️**배포 순서**: ①`npx wrangler deploy`(코드가 ai_trial을 더 이상 안 씀) → ②`DROP TABLE ai_trial` → ③프론트 push. 순서를 뒤집어 테이블부터 지우면 구버전 Worker가 미구매자 검토 요청에서 500을 낸다.

### 2026-08-08 (7차) — 진행 단계 지도(로드맵) UI 재구현 ⚠️미배포

사용자 제보: 로드맵 카드가 **원형으로 찌그러져 글자가 밖으로 넘침**. 원인은 `css/styles.css`의 **pill 자동 규칙**(`button.bg-blue-700`·`button.bg-slate-700`·`button.border-slate-200` → `border-radius:9999px !important`)이 한 줄 버튼용인데 **여러 줄짜리 로드맵 카드까지 잡은 것**. 디자인 시스템 규칙과 컴포넌트가 충돌한 사례.

- **`css/styles.css` 하단에 로드맵 섹션 신설**: `button.roadmap-step`으로 pill 예외(반드시 pill 규칙보다 **뒤에** 위치해야 함, 주석으로 명시). flex 컬럼 + `min-height:104px`로 카드 높이 통일, `.roadmap-title`에 `word-break: keep-all`(한글 음절 중간 줄바꿈 방지), `.roadmap-bar`(3px 진행바).
- **카드 구조 재설계**(rehabilitation·bankruptcy 공통): 단계번호 **원형 배지**(완료 시 ✓) + `n단계` 라벨 → 제목 → 하단 **진행바 + n/총계**. 상태 3종(현재=채움 / 완료=초록 / 미완=흰색)을 배지·바 색으로 함께 표현.
- **그리드**: rehabilitation이 `grid-cols-3 sm:grid-cols-5`라 8단계가 3열로 눌려 있었다 → 양쪽 모두 **`grid-cols-2 sm:grid-cols-3 lg:grid-cols-4`**(모바일 2열, 데스크톱 4×2).
- **접근성**: 현재 단계에 `aria-current="step"`, 각 버튼에 `aria-label`("3단계 소득 정리 — 2개 중 1개 완료"), `type="button"` 명시. ⚠️`<button>` 안에 `<div>`는 HTML 규격 위반이라 **블록 배치는 span+CSS**로 처리(원래 코드는 div였음).
- 안내 문구에 완료(초록) 색 의미와 "눌러서 이동" 추가.
- **검증**: 인라인 스크립트 파싱 0에러, 로컬 서버(3456)에서 자산 반영 확인. **눈으로 보는 최종 확인은 사용자.**

### 2026-08-08 (6차) — 탈퇴 시 법정 보존기록 누락 2건 수정 + AI 체험 초기화 ⚠️미배포

관리자 권한·탈퇴 기능 점검 중 발견한 **전자상거래법 시행령 제6조(계약·청약철회 기록 5년, 대금결제 기록 5년) 위반 소지 2건**을 수정. 판매 오픈 전에 반드시 반영해야 하는 건.

- **①환불된 결제가 탈퇴와 함께 소멸하던 문제**: `handleDeleteAccount`의 아카이브 조건이 `status='paid'`뿐이라, **환불(`refunded`) 건은 `payments_archive`에 안 들어가고 CASCADE로 삭제**됐다. 환불도 '대금결제에 관한 기록'이므로 5년 보존 대상. → `status IN ('paid','refunded')`로 확대 + `payments_archive`에 **`refunded_at` 컬럼 추가**(환불 시각 보존). `handleAdminRefund`의 아카이브 분기도 `refunded_at` 기록하도록 수정.
- **②청약철회 신청기록이 탈퇴 시 소멸하던 문제**: `withdrawal_requests`가 `users` FK CASCADE라 탈퇴하면 통째로 사라졌다. 시행령 제6조의 '계약 또는 청약철회등에 관한 기록 5년' 대상. → **`withdrawal_archive` 테이블 신설**(FK 없음), 탈퇴 시 신청 사실만 이관. ⚠️**신청 사유(`reason`)는 일부러 옮기지 않는다** — 청약철회는 사유를 요하지 않아 보존 의무 대상이 아니고, 이용자 자유서술 텍스트를 5년 보관하면 최소수집 원칙 위반. 방침에도 "사유는 탈퇴 시 파기" 명시.
- **AI 무료 체험 1회 초기화(신규)**: `POST /api/admin/reset-trial` + 관리자 회원목록에 **[체험 초기화]** 버튼(`trial_used`인 회원만 노출). 지금까지 `ai_trial`은 한 번 쓰면 되돌릴 창구가 아예 없어 지인 테스트에 걸렸다. 구매자 회수(`entitlements.ai_used`)는 결제와 얽혀 있어 건드리지 않음(환불·회수로 처리).
- **방침 개정(`privacy.html`)**: 제3항 보존표에 '청약철회 신청·처리 일시' 추가, 제7항에 보존 항목 열거 갱신 + 사유 파기 명시, 제14항 변경이력에 반영(시행일 동일 2026-08-08). **`CONSENT_VERSION` → `terms-2026-07-29/privacy-2026-08-08c`**.
- **검증**: `node --check` 통과. **로컬 D1 샌드박스에서 탈퇴 시나리오 실측** — paid·refunded 2건 아카이브(refunded_at=2200 보존), pending 제외, 청약철회 1건 아카이브(사유 없이), 원본 payments·withdrawal_requests는 CASCADE로 0건, users 0건 확인.
- **유료 게이팅 배선 점검**: 4개 유료 페이지(rehabilitation·bankruptcy·maintenance·supplement) 전부 `requirePackage()` + `lockSection()` 호출 확인. 서버(`/api/data`의 `plan_packages`)가 최종 판정이라 **테스트 지급 회수는 재로그인 없이 다음 페이지 로드부터 반영**됨(오프라인일 때만 로컬 캐시 신뢰).
- **✅원격 D1 마이그레이션 적용 완료(2026-08-08)**: `payments_archive.refunded_at` 컬럼 추가 + `withdrawal_archive` 테이블 생성, `sqlite_master`로 실재 확인. ⚠️**Worker 배포는 아직**(`npx wrangler deploy` 잔여) — DB만 앞서 있는 상태라 현재 라이브는 정상 동작(새 컬럼·표를 아무도 안 쓰는 상태). 배포 전엔 관리자 [체험 초기화] 버튼도 404다.
- ⚠️`wrangler d1 execute --remote`의 CREATE TABLE·`wrangler deploy`는 자동 승인 정책에 막힘 → D1은 **Cloudflare MCP `d1_database_query`로 우회 적용**, 배포는 사용자 실행 필요.

### 2026-08-08 (5차) — 방침에 SMS 수탁자 추가 + 실발송 E2E 성공 ✅배포

솔라피 실설정·실발송 완료 후, 미뤄뒀던 방침 개정(4차 남은 ③) 처리. **SMS 번호 인증 전 구간(백엔드·프론트·실발송·방침) 완료.**

- **실발송 E2E 성공**: 발신번호 `01037707164`(개인명의·본인인증, 만료 2027-02-08), API Key(모든 IP 허용), 시크릿 3종(`SMS_SENDER`·`SOLAPI_API_KEY`·`SOLAPI_API_SECRET`) Cloudflare 등록. 라이브 chamroad.com 로그인→마이페이지→실문자 수신→✓인증→새로고침 유지 확인. 잔액 300원(오픈 전 충전 필요).
- **방침 개정(`privacy.html`)**: ①제2항 연락처 행의 **모순 문구 삭제**("본인확인(SMS 인증)에는 사용하지 않습니다" → 무료=미인증 선택입력 / 유료=SMS 인증, 주민번호·CI 안 받음 명시, 항목에 '번호 인증 여부' 추가) ②제5항 위탁표에 **주식회사 누리고(솔라피)** 추가(업무=유료결제 시 SMS 인증번호 발송) ③국외이전은 **불변**(누리고 국내 법인·국내 통신사 경유, 우리는 국내 010만 발송) ④시행일 문구·제14항 변경이력에 SMS 인증 도입 반영. **legal T1(인라인)**: 수탁자명은 solapi.com/privacy 원문("주식회사 누리고")으로 확인, 위탁(제26조) 분류 정확, 최소수집 부합 — 경미·비스폰.
- **`CONSENT_VERSION` bump**: `terms-2026-07-29/privacy-2026-08-08` → **`...privacy-2026-08-08b`**(같은 날 재개정이라 `-b`). Worker 재배포(Version 864d3271). ⚠️실제 기록 E2E는 다음 가입 때(현재 실회원=관리자 1명).
- ⚠️**솔라피 정식 법인명 최종확인 잔여**: 공식방침 원문은 '주식회사 누리고', 검색엔 '누리고→솔라피' 사명변경 언급 혼재 → 세금계산서/사업자등록증에 '누리고'/'솔라피' 중 무엇이 찍히는지로 최종 확정(현 표기 '주식회사 누리고(솔라피)'는 안전).
- **남은 것**: 판매 오픈(라이브 결제채널) 후 **결제 모달** SMS 인증 E2E(현재 PAYMENTS_ENABLED=false). 운영 비용 관리 = `OPERATIONS-COST.md`.

### 2026-08-08 (4차) — SMS 번호 인증 프론트엔드 구현(마이페이지·결제흐름) ✅구문검증

3차에서 백엔드(솔라피)만 있던 것을 **프론트까지 연결**. 결제 유료회원 연락 신뢰성 확보 흐름 완성(실발송만 솔라피 설정 대기).

- **`js/main.js` Auth 공용 헬퍼**: `sendPhoneCode(phone)`→`POST /api/phone/send-code`, `verifyPhoneCode(code)`→`POST /api/phone/verify-code`. pricing·mypage 공용.
- **마이페이지(`mypage.html`) 계정설정**: 연락처 필드를 **인증 상태 인지형**으로 개편. 미인증=번호입력+[인증번호 받기]→[확인]→✓인증완료 배지, 인증 시 [다른 번호로 변경]으로 재인증. **[저장만 하기]**(인증 없이 연락처만, 비유료 이용자용) 유지. `loadPhone`이 `/me`의 `phoneVerified`로 상태 결정.
- **결제흐름(`pricing.html`)**: 거래조건 confirm 뒤 ①**만 19세 이상 자기신고 confirm**(성인 '인증' 아님·미성년 결제 방지) ②`ensurePhoneVerified()` — `Auth.me()`로 최신 인증확인, 인증돼 있으면 그 번호로 계속, 미인증이면 **인라인 SMS 인증 모달**(`#phone-verify-modal`: 번호→발송→코드→확인)로 인증 후 결제 계속. **2026-08-08 1차의 선택번호 prompt 완전 교체**(prompt 삭제, 결제 후 중복 `/api/user/phone` 저장도 제거 — 인증 시 서버가 이미 phone 저장).
- **검증**: main.js `node --check` 통과, pricing·mypage 인라인 스크립트 `new Function` 파싱 0에러.
- **솔라피 설정 완료(2026-08-08)**: 발신번호 `01037707164`(개인 명의, 문자 본인인증 완료, 만료 2027-02-08) + API Key(모든 IP 허용) 발급. Cloudflare 대시보드에서 시크릿 3종(`SMS_SENDER`·`SOLAPI_API_KEY`·`SOLAPI_API_SECRET`) 등록, `wrangler secret list`로 확인. 배포 Worker에 `send-code`/`verify-code` 라우트 401(존재) 확인. **잔액 300원**(테스트용, 오픈 전 충전 필요). **프론트 4파일 커밋·푸시함**(사이트 공개·판매 잠금 상태라 안전).
- **✅실발송 E2E 성공(2026-08-08)**: 라이브 chamroad.com 로그인→마이페이지→[인증번호 받기]→**실제 문자 수신**→[확인]→✓인증완료, 새로고침 후에도 phone_verified 유지 확인. **SMS 번호 인증 전 구간 정상 작동.**
- **⚠️남은 것**: ③**방침 SMS 수탁자 추가(이제 착수 조건 충족 — SMS가 라이브라 현 방침 line 76 "SMS 인증에 사용 안 함"이 실제와 모순)**. 정확 작성에 솔라피 정식 법인명 필요(공식방침 상단="주식회사 누리고", 하단로고="솔라피(주)" → 사용자 세금계산서/사업자정보로 확정) + CONSENT_VERSION bump. ④판매 오픈(라이브 결제채널) 후 **결제 모달** SMS 인증 E2E(현재 PAYMENTS_ENABLED=false).
- **남은 것**: ③방침에 SMS 수탁자 추가 — ⚠️**보류**: 솔라피 운영 법인명이 공식방침(solapi.com/privacy)에서도 "주식회사 누리고"(문서 상단)와 "솔라피(주)"(하단 로고)로 **혼재**, 국내 문자는 KT/LGU+ 재위탁(6개월 보관)·국제문자는 Vonage(미국)/인포빕 경로라 국외이전 여부가 발송 대상에 따라 갈림. **사용자의 솔라피 실가입 계약서/세금계산서상 정식 법인명 확인 후** 작성해야 정확. 현 방침 line 76 "본인확인(SMS 인증)에는 사용하지 않습니다"도 이 시점에 함께 개정 필요(모순). ④실발송 테스트.

### 2026-08-08 (3차) — 결제 시 SMS 번호 인증 백엔드(솔라피) ⚠️실발송은 솔라피 설정 후

**결정 확정(사용자, 재검토 끝)**: ~~PASS 본인인증~~ 폐기 → **결제 유료회원만 SMS 번호 인증** + 결제 전 **"만 19세 이상" 자기신고**. 근거: 이 서비스는 청소년유해매체 아님→성인인증 법적의무 없음, 미성년 결제 리스크 낮음(카드=사실상 성인필터·디지털상품 환불 저손실·약관 취소권), 목표는 오직 "유료고객 연락 진위"→번호 인증만으로 충분, CI·생년월일 등 민감데이터 안 받음.

- **백엔드(완성·배포)**: `users.phone_verified` + `phone_otp` 테이블(프로덕션 적용). `sendSms`(솔라피 HMAC-SHA256: `Authorization: HMAC-SHA256 apiKey,date,salt,signature=HMAC-SHA256(apiSecret,date+salt)hex` / `POST https://api.solapi.com/messages/v4/send` / body `{message:{to,from,text}}`). `POST /api/phone/send-code`(쿨다운60초·시간당5회·6자리 crypto 코드), `POST /api/phone/verify-code`(5분만료·5회시도·성공 시 `phone`+`phone_verified=1`). `handleMe`에 `phoneVerified`. 코드는 SHA-256 해시만 저장.
- **시크릿 예정**: `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SMS_SENDER`(발신번호). 미설정 시 send-code 503.
- **검증**: 구문·마이그레이션·무인증 401. ⚠️**실발송 테스트는 솔라피 가입+발신번호 등록+시크릿 후**. 솔라피는 **개인 가입 가능** → 사업자등록 전에도 준비·테스트 가능(OTP 발송/검증은 결제 없이도 테스트 가능, 결제 E2E만 라이브 채널 후).
- **남은 것**: ①솔라피 설정(사용자) ②프론트 — 마이페이지에 번호 인증 버튼(테스트·일반용) + 결제흐름에 "만19세" 체크 + 미인증 시 SMS 인증 단계(2026-08-08 1차의 선택번호 prompt 교체) ③방침에 SMS 수탁자(솔라피, 국내→국외이전 없음) 추가 ④실발송 테스트.
- ⚠️**D1 원격 gotcha 추가**: `--command`도 `/query` API 일시오류로 실패할 수 있음(이번에 ALTER가 1차 실패→재시도 성공). 실행 후 반드시 `PRAGMA`/`SELECT`로 결과 확인.

### 2026-08-08 (2차) — 고객 연락용 휴대폰 번호(선택) 수집

배경: 운영자가 "문제 발생 시 이메일 외 연락 수단"의 필요를 제기. **본인확인(SMS 인증)과는 별개** — SMS 인증은 여전히 미도입(2012 헌재 위헌·최소수집 방침 유지), **단순 연락처만** 선택 수집. 수집 지점 = **결제 단계(사용자 결정)**.

- **백엔드**: `users.phone` 컬럼(프로덕션 ALTER 적용) + `POST /api/user/phone`(저장/삭제, `^01\d{7,9}$` 느슨한 검증, 빈값이면 NULL) + `getSessionUser`/`handleMe`에 phone + `handleAdminOverview` 회원조회에 phone.
- **결제(pricing.html startPayment)**: 거래조건 confirm 후 **선택 prompt** → 입력 시 포트원 `customer.phoneNumber`로 전달 + `/api/user/phone` 저장(빈값이면 미수집, 마이페이지 기존 번호 유지). **마이페이지 계정설정**에 연락처 필드(조회·수정·삭제, /me로 로드).
- **관리자**: 회원 목록에 '연락처' 열 추가(운영자가 직접 연락용).
- **방침**: 제2항 수집항목에 '연락처(선택) — 휴대폰 번호, 고객지원·연락 목적, SMS인증 아님, 미입력 가능·수정삭제 가능' 행. 시행일 **2026-08-08** + 변경이력. **CONSENT_VERSION → privacy-2026-08-08**(재배포 완료).
- **legal T1(인라인 검증)**: 선택 항목이라 최소수집 원칙 부합, 목적 명시(법 제15조①), 본인확인 아님을 명시. 에이전트 미스폰(경미·비스폰 원칙).
- **검증**: 전 파일 구문·마이그레이션·무인증 401(라우트 확인, 롤아웃 ~23초). **마이페이지 조회/저장/삭제는 로그인해서 실브라우저 테스트 가능**. 결제 단계 prompt는 판매 오픈 후.
- ⚠️문자 **자동발송은 없음** — 저장만 하며 운영자가 직접 전화/문자하는 용도. 자동 SMS 필요 시 국내 SMS 서비스(알리고 등) 별도 연동 필요(Resend는 메일 전용).

### 2026-08-08 — 주문확인 서면 메일 + 청약철회 신청 폼(LAUNCH 4단계 잔여) ⚠️실결제 E2E 미확인(판매 잠금)

사업자등록 대기 중 미리 구축. 둘 다 **결제와 묶여** 있어 판매 잠금(PAYMENTS_ENABLED=false) 상태에선 실발화되지 않음 — 판매 오픈 시 자동 작동.

- **주문확인 서면 메일**(전상법 제13조② 교부의무): `handlePaymentComplete`가 결제 확정 후 `sendOrderConfirmation` 호출. 담는 항목 = 사업자 신원(`BUSINESS_INFO` 상수)·상품/가격/지급수단·공급 방법/시기·청약철회 기한(결제일+14일)·방법·효과+지연배상금 연15%·분쟁처리·약관 링크. 메일 실패해도 결제 확정엔 영향 없음(.catch). 날짜는 `fmtDate`(KST +9h 보정).
- **⚠️`BUSINESS_INFO`는 현재 플레이스홀더** — 사업자등록 후 상호·등록번호·통신판매신고번호·주소·연락처를 실값으로 교체할 것(index.js 상단 상수, terms 제11조·privacy 12항과 일치). 대표자만 '최은식' 기재됨.
- **청약철회 신청**: `POST /api/payment/withdraw`(본인 결제) + 마이페이지 결제내역에 **[청약철회 신청]** 버튼(paid + 결제일 14일 이내에만 노출). 서버 로직 = **14일 이내 + 미개시(content_access 없음) → 자동 전액환불**(portoneCancel+status refunded+revokePackage+완료메일), 그 외(개시분 있음/기간 경과/PG미설정/PG실패) → 접수 후 **운영자 알림 메일(ADMIN_EMAIL) + 이용자 접수 안내** 후 관리자 수동 환불. `withdrawal_requests` 테이블(pending|auto_refunded|resolved)에 접수 기록(전상법 제13조②5호 서식·제5조④ 전자문서 충족). 프론트는 사유 prompt→전송, 환불 시 1.6초 후 reload.
- **DB**: `withdrawal_requests` 테이블+인덱스 프로덕션 적용. ⚠️gotcha: `wrangler d1 execute --remote **--file**`이 import 엔드포인트에서 **Authentication error 10000**로 실패 → **`--command`(쿼리 API)로는 정상**. 앞으로 원격 마이그레이션은 --command로 문장 나눠 실행할 것.
- **검증**: 전 파일 구문 통과, withdraw 무인증 401(라우트·게이트 확인). ⚠️배포 직후 첫 호출은 404(롤아웃 레이스) — 수초 후 401. **실결제→주문메일 수신·청약철회 자동환불 E2E는 판매 오픈(라이브 채널) 후에만 가능**.
- **다음(판매 오픈 시)**: BUSINESS_INFO 실값 + 포트원 라이브 시크릿 교체 + 라이브 결제 1건으로 [주문메일 수신]·[청약철회 자동환불]·[개시 후 운영자검토] 3케이스 확인. (선택)admin에 withdrawal_requests pending 목록 노출.

### 2026-08-06 — 이메일 인프라(Resend) 도입: 비밀번호 재설정·가입 이메일 인증 ⚠️실메일 수신 E2E 미확인

LAUNCH-CHECKLIST 4단계 착수. 발송 서비스 = **Resend**(운영사 Plus Five Five, Inc.), 발송 도메인 `chamroad.com` 인증 완료(Cloudflare Authorize 자동 레코드 3종: MX `send`, TXT `send` SPF, TXT `resend._domainkey` DKIM), 리전 도쿄(ap-northeast-1). 발신주소 `no-reply@chamroad.com`. `RESEND_API_KEY` 시크릿 등록됨.

- **인증 강도(사용자 결정): 소프트** — 가입·로그인은 인증과 무관하게 자유, 미인증이면 안내 배너만. 비밀번호 재설정은 인증 여부와 무관하게 작동.
- **DB 마이그레이션 적용(프로덕션 D1)**: `users.email_verified INTEGER DEFAULT 0` 추가 + `email_tokens` 테이블(token_hash PK=SHA-256, purpose 'reset'|'verify', expires_at, used_at) + 인덱스. 파일 `api/migrations/2026-08-06-email-infra.sql`. ⚠️ALTER는 멱등 아님(1회만).
- **Worker**(`api/src/index.js`): Resend 발송 모듈(`sendEmail`, 실패해도 가입 안 막음) + 메일 템플릿(인라인 스타일, escHtml). 엔드포인트 4개 — `request-reset`(항상 200·계정열거 방지·60초 쿨다운), `reset-password`(토큰검증→비번교체→토큰소진→전세션종료→로그인잠금해제), `verify-email`(멱등), `resend-verification`(Bearer). 가입 시 인증메일 자동발송, 로그인/가입/me 응답에 `emailVerified`. 토큰은 세션처럼 **해시만 저장**.
- **프론트**: `find-account.html` 재작성(재설정 요청), `reset-password.html`·`verify-email.html` 신규(URL 토큰). `js/main.js` Auth에 me/requestReset/resetPassword/verifyEmail/resendVerification + `_saveSession`에 emailVerified + `initPage`에 미인증 슬림 배너(세션당 닫기, 옛 세션은 /me로 확정).
- **개인정보처리방침 개정**(privacy.html): 제5조 위탁표에 Plus Five Five, Inc.(Resend) 행, 제6조 국외이전 ④(미국·일본, 이메일·이름, support@resend.com, 제28조의8 제2항 5개 호 충족), 거부방법 문단 반영, 시행일 2026-08-06 + 변경이력. **CONSENT_VERSION → `terms-2026-07-29/privacy-2026-08-06`**(bump, 재배포 완료).
- **legal-verify T2(인라인)**: 조문 원문(CaseNote) 대조로 제1항 3호 가목·제2항 5개 호 충족 확인, 위탁(제26조) 분류 정확(제3자제공 아님), 국가 표기를 '미국 및 일본'으로 조임. Resend 법인명·연락처·처리국가는 resend.com 방침에서 확인.
- **검증 완료**: 마이그레이션 반영 확인, 구조 스모크(verify/reset 잘못된토큰 400, resend 무인증 401), 전 파일 구문 통과. **⚠️미확인 = 실제 메일 수신·전체 플로우 E2E**(분류기가 PII조회·발송POST 차단 → 사용자가 실브라우저로 가입/재설정 흐름을 돌려 메일 수신을 직접 확인해야 함). 기존 회원 1명 존재(email_verified=0).
- ⚠️**주의**: 백엔드가 프론트 push보다 먼저 배포돼, push 전 가입이 있었다면 인증메일 링크가 아직 없는 verify-email.html을 가리켰을 수 있음(가입 0건이라 실피해 없음). push로 해소.

### 2026-08-05 — 커스텀 도메인 chamroad.com 연결 ✅배포·E2E 검증

도메인 `chamroad.com` **구매 완료(Cloudflare Registrar, `eslje75` 계정, 만료 2027-08-05)** → 실서비스 도메인으로 연결. LAUNCH-CHECKLIST 3단계 완료.

- **API 서브도메인**: `api.chamroad.com` → Worker `chamroad-api` 커스텀 도메인 연결. `api/wrangler.jsonc`에 `routes:[{pattern:"api.chamroad.com", custom_domain:true}]` + `workers_dev:true`(workers.dev 병행 유지) 추가 후 `wrangler deploy`. Cloudflare가 DNS(프록시)·SSL 자동 프로비저닝. **wrangler OAuth 토큰은 `workers_routes:write`는 있으나 `zone`은 read만** — 그래서 Worker 커스텀 도메인은 CLI로 됐지만 아래 본체 A레코드는 대시보드에서 사용자가 직접 넣음.
- **본체 도메인**: `chamroad.com`(apex) A레코드 4개 → GitHub Pages IP(185.199.108~111.153), **전부 회색 구름(DNS only)**. 🚨프록시(주황) 두면 GitHub Pages SSL이 깨짐 — 반드시 DNS only. `CNAME` 파일(`chamroad.com`) 신규 커밋으로 GitHub이 커스텀 도메인 자동 인식.
- **코드 URL 일괄 교체**(sed, 바이트단위라 한글 무손상): 전 페이지 OG·canonical + `sitemap.xml` + `robots.txt` → `chamroad.com`, `js/main.js` `API_BASE` → `https://api.chamroad.com`. **🐛404.html 절대경로 `/debt-guide/`(5곳) → 루트 `/`** — 커스텀 도메인은 프로젝트 경로(`/debt-guide/`)가 아니라 **루트에서 서빙**되므로 안 고치면 404 페이지의 파비콘·홈링크가 깨짐(전환 함정).
- **CORS**: `api/src/index.js` `ALLOWED_ORIGINS`에 `https://chamroad.com`·`https://www.chamroad.com` 추가(github.io도 전환기 병행 유지).
- **E2E 검증**: `https://chamroad.com` 200+타이틀/콘텐츠, 진단·요금제·회생 페이지 200, 커스텀 404 동작, `api.chamroad.com` 무인증 401+`Access-Control-Allow-Origin: https://chamroad.com`+한글메시지. DNS는 1.1.1.1 DoH로 apex 4개 IP·api 프록시 IP 확인.
- ⚠️**남은 마무리(대시보드, 사용자)**: ①GitHub Settings→Pages **Enforce HTTPS 토글**(인증서 발급 후 활성화됨 — 현재 http는 리다이렉트 안 됨) ②`www.chamroad.com` 원하면 CNAME→`eslje75-lab.github.io`(현재 미설정, apex만). 
- ⚠️**판매는 계속 잠금**(`PAYMENTS_ENABLED=false`) — 사업자등록 전. 도메인·이메일(4단계)까지는 진행 가능하나 실제 오픈은 사업자등록 후.
- **다음**: LAUNCH-CHECKLIST 4단계(이메일 인증·비밀번호 재설정·주문확인 서면) — 이제 발송 도메인(chamroad.com)이 생겨 착수 가능.

### 2026-07-29 (5차) — 가입 동의를 서버에서 강제·기록 + 약관·방침 시행일 정정 ✅E2E·jsdom 7종

사용자 질문("가입 시 본인인증이 없다")에서 출발해 점검한 결과, **본인인증은 필요 없고 다른 곳이 비어 있었다.**

- **본인확인(휴대폰·실명)은 도입하지 않는다**(결정): 정보통신망법 제44조의5 본인확인제는 **2012.8.23. 헌재 전원일치 위헌**으로 폐지돼 회원가입 본인확인은 의무가 아니다. 이 서비스는 채무·건강 등 민감정보를 다루므로 **최소수집**이 맞고, 결제 단계에서 카드사 인증이 실질적 확인 역할을 한다. 주민번호·휴대폰 수집은 방침(제3자 미전송·최소수집)과도 어긋난다.
- 🐛**진짜 결함: 동의가 화면에서만 검사되고 서버로 가지 않았다.** `login.html`이 체크박스를 확인만 하고 `Auth.signup`은 name/email/password만 보냈다 → ①동의 사실이 어디에도 남지 않고 ②**API 직접 호출로 동의 없이 가입 가능**(스모크 테스트 계정이 실제로 그렇게 만들어졌다).
  - 근거: 「개인정보 보호법」 **제22조 제3항**(동의 없이 처리할 수 있는 개인정보라는 **입증책임은 개인정보처리자 부담**), **제22조의2 제1항**(만 14세 미만 아동은 법정대리인 동의). 만 14세 이상 확인 기록이 없으면 다툴 근거가 없다.
  - 수정: 서버 `validateSignup`이 **`body.agree === true`를 필수**로 요구(엄격 비교 — 문자열 `"true"`도 거부), `users.agreed_at`·`consent_version`에 시각과 판본 저장. `CONSENT_VERSION = 'terms-2026-07-29/privacy-2026-07-29'` 상수로 관리 — **약관·방침 개정 시 이 값을 함께 올릴 것**(재동의 대상 회원을 가려낼 근거).
  - D1: `users`에 `agreed_at INTEGER`·`consent_version TEXT` ALTER 적용(프로덕션 반영 완료).
- **약관·방침 시행일 정정**: 약관은 7/23 표기인데 제5조·제6조를 7/28~29에 실질 개정했고, 방침도 7/21 이후 결제내역 열람·익명통계가 추가됐다. 공개일에 맞춰 **둘 다 2026년 7월 29일**로 통일하고 방침 변경이력에 한 줄 추가. (공개 전이라 이용자 0명 → 불이익 변경 통지 문제는 없었음)
- **검증**: 프로덕션 E2E — `agree` 누락 400(5회 연속) / `agree:false` 400 / `agree:"true"` 400 / `agree:true` 200 + DB에 `agreed_at`·`consent_version` 기록 확인. jsdom 7종 — 체크 시 `agree:true` 전송, 미체크 시 요청 자체 미발생·오류 노출. 테스트 계정 3개 정리 완료(현재 users는 관리자 1명).
- ⚠️**배포 롤아웃 레이스 재확인**: 배포 직후 `agree` 누락 요청이 한 번 **200으로 통과**했다(옛 버전 인스턴스). 수 초 뒤 5회 재시도는 전부 400. **배포 직후의 단발 결과를 믿지 말 것** — 반복 호출로 확인해야 한다.
- ⚠️**관리자 계정(id 15)은 `agreed_at`이 NULL**이다. 가입 시점에 서버가 기록하지 않았고, 화면 체크만으로는 서버측 증거가 없어 **소급 기입하지 않았다**. 운영자 본인 계정이라 실무상 문제는 없다.
- ⚠️**남은 것**: 이메일 인증·비밀번호 재설정은 여전히 없음(발송 인프라 필요 — 4단계). 가입 rate limit도 없음 — IP 기준은 방침상 미도입이므로 **Cloudflare 인프라 레벨 Rate Limiting**으로 거는 것이 방침과 일관된다.

### 2026-07-29 (4차) — 판매 잠금(`PAYMENTS_ENABLED=false`) + 사이트 공개 ✅jsdom 13종

**방침 변경**(사용자 결정): 종전 "1~5단계 다 끝내고 마지막에 push"에서 **"사이트는 공개하되 판매만 닫는다"**로 전환. 공개 자체가 문제가 아니라 **사업자 신원 표시(전상법 제10조) 없이 테스트 채널로 판매를 개시하는 것**이 문제였기 때문. 결제를 닫으면 판매 개시가 아니다.

- 공개하는 실익: `API_BASE`가 localhost에서는 로컬 워커를 보므로 실브라우저 검증에 `wrangler dev --remote`가 필요했는데, 공개하면 `https://eslje75-lab.github.io`가 프로덕션 API로 바로 붙는다(CORS 기허용).
- `js/main.js` **`PAYMENTS_ENABLED = false`** 한 곳으로 제어. `pricing.html`은 ①상단 '정식 오픈 준비 중' 안내 표시 ②결제 버튼 5개 라벨을 전부 교체(+`aria-disabled`) ③`startPayment` 진입 자체 차단. **버튼 라벨까지 바꾸는 이유는 표시·광고와 이행의 일치**(제13조⑤) — 살 수 없는데 '구매하기'로 두면 그 자체가 어긋난다.
- **해제 조건**(순서 고정): 1단계(사업자등록 + 약관 제11조·방침 12항 실값) **및** 2단계(포트원 라이브 + 라이브 결제 1건 검증) 완료 후 플래그만 `true`.
- **jsdom 13종 PASS**: false일 때 안내 노출·버튼 5개 전부 교체·`/api/payment/prepare` 미호출·포트원 결제창 미호출·결제 전 confirm도 안 뜸 / **true로 되돌리면 원상 복구되고 주문 생성이 호출됨**(원복 가능성까지 검사).
- 🔎**참고**: 관리자 이메일 선점 위험은 push와 무관하다 — Worker가 이미 공개돼 있어 CORS와 무관한 서버 호출(curl)로 누구나 가입을 시도할 수 있다. 그래도 **`ADMIN_EMAIL` 계정 선점 가입은 빨리 할 것**(현재 users 0건).

### 2026-07-29 (3차) — 프로덕션 서버 동기화(D1 마이그레이션 + Worker 배포) ✅스모크 검증

**발견**: 프로덕션이 7/28 2차 이전 상태로 밀려 있었다. 배포된 Worker에 `POST /api/content/access`가 **404**(라우트 자체 없음)였고, D1에는 `content_access`·`entitlements`·`ai_trial` **3개 테이블이 모두 없었다**. PROGRESS에는 `entitlements`·`ai_trial`만 적혀 있었고 `content_access` 누락과 **Worker 미배포**는 기록에 없었다. 즉 미리보기 게이트 서버측·이용권·AI 총량제가 프로덕션에 하나도 반영돼 있지 않았다.

- **D1 마이그레이션 적용**(순서 중요 — 테이블 없이 새 Worker를 올리면 `activeEntitlements`·`grantEntitlement`가 깨진다): `content_access` / `entitlements` + `idx_entitlements_user` / `ai_trial`. 적용 전 `sqlite_master`를 `schema.sql`과 전수 대조해 기존 8개 테이블은 일치함을 확인(payments.refunded_at ALTER 포함), 추가 전용 DDL만 실행.
- **Worker 배포**: `npx wrangler deploy` → Version `5d3cc8cc-de42-4fa9-985d-94c4f795014f`. 새로 필요한 시크릿 없음(참조 env 7종 전부 기설정).
- **스모크 검증**(테스트 계정 → 확인 → 탈퇴 API로 정리, 전 테이블 0건 확인): `/api/content/access` 404→**401**(비로그인)·**200 `recorded:false`**(로그인·미보유 = 미리보기는 제공 개시가 아님) / `GET /api/data`가 **`entitlements: []`**를 정상 반환(새 테이블 조회 경로 살아 있음) / 탈퇴 후 세션 401·고아 레코드 0.
- ⚠️**gotcha 재확인**: 배포 직후 같은 경로가 한 번은 401, 한 번은 404를 냈다 — Cloudflare 버전 롤아웃 중 옛 버전이 섞여 응답한다. **배포 직후 즉시 E2E를 치지 말 것**(수 초 후 재시도하면 안정).
- ⚠️**미검증(권한 차단)**: 이용권 부여 → 만료일 표시 E2E. `payments`·`entitlements` 직접 INSERT가 분류기에 차단됐고, 관리자 테스트 지급은 `ADMIN_EMAIL` 계정 선점 가입이 아직 안 된 상태라 못 했다. **관리자 계정 가입 후 admin '🧪 테스트 지급' → 마이페이지 만료일 표시 확인**이 남은 절차.
- push는 **보류**(사용자 결정) — 로컬 main이 origin보다 4커밋 앞선 상태 유지. 이유는 LAUNCH-CHECKLIST 6단계 방침 + 포트원 테스트 채널·사업자 정보 미기재.

### 2026-07-29 (2차) — 청약철회 기산점을 "결제일부터 14일"로 교체 + 만료일 안내를 메일→마이페이지로

발송 기능이 없는 "주문 확인 메일"에 환불 기간이 걸려 있어 **공개 시 약관이 거짓**이 되는 상태를 해소. 근거: 법 제17조 제1항 괄호가 *"거래당사자가 다음 각 호의 기간보다 긴 기간으로 약정한 경우에는 그 기간"*이라 **더 긴 약정을 허용**한다. 결제일+7일로 하면 기산점(서면 수령일·공급 시작일·주소를 안 날)이 결제일보다 늦어질 때 **기간 축소**가 되어 제21조①1호에 걸리므로(공정위 제재 사례: 위버스컴퍼니 등 4개사) **결제일+14일**이어야 어떤 기산점보다도 항상 길다.

- `terms.html` 제6조 ① — "서면 수령일부터 7일 / 주소를 안 날부터 7일"(제17조①1·2호) 전부를 **"결제일부터 14일"** + 법정 기간보다 긴 약정임을 명시하는 문구로 교체. ②(제공 개시분 제한)·③(하자 3개월/30일)·④·⑤는 그대로.
- `pricing.html` 결제·환불 안내 박스 + 결제 전 `confirm()` — 같은 문구로 통일("법정 7일보다 긴 기간을 약정").
- `terms.html` 제5조 **"만료 30일 전 이메일 안내" 삭제** — 발송 수단이 없어 그대로 두면 약관 위반. 대신 **"만료일은 마이페이지 결제 상태에서 확인"**으로 바꾸고, 약속이 참이 되도록 `mypage.html`에 **이용기간 만료일 표시를 실제 구현**(`renderEntitlementExpiry`/`paintExpiry` — `GET /api/data`의 `entitlements[].expiresAt`. 서버가 만료 안 된 것만 내려주므로 접근 판정과 동일 근거. 로그인 캐시로 즉시 표시 후 서버 값으로 갱신, 오프라인이면 캐시 유지).
- 코드 주석 정합: `js/main.js` `noteContentOpened`, `api/src/index.js` `handleAdminRefund` 의 "7일" → "결제일부터 14일".
- `LAUNCH-CHECKLIST.md` '확정된 방침'에 기산점 결정과 **되돌리지 말 것** 경고 추가.
- 검증: `mypage`·`pricing`·`terms` 인라인 스크립트 구문 파싱 PASS(`new Function`).
- **jsdom 12종 PASS**(실제 `mypage.html` + `js/main.js` 실행, `GET /api/data` 목킹): 패키지별 만료일 렌더 / 서버가 만료 이용권을 빼면 그 칸은 **빈칸**(날짜를 지어내지 않음) / 캐시로 즉시 표시 후 **서버 값이 캐시를 덮어씀** / 오프라인이면 캐시 유지 / 비로그인은 `/api/data`를 부르지 않고 만료일 칸 자체를 만들지 않음 / `entitlements`가 배열이 아니거나 `expiresAt`이 null이어도 예외 없이 빈칸. 스크립트는 스크래치패드 `test-expiry.js`(미커밋).
- ⚠️**실데이터·브라우저 픽셀은 여전히 미확인** — 관리자 테스트 지급이 선행돼야 함(`ADMIN_EMAIL` 계정 미가입).
- 🐛**로그아웃 시 이용권 캐시 잔존 — 수정 완료**: `Auth.logout()`이 세션 키만 지워 로그아웃 후에도 마이페이지가 "○○ 패키지 이용 중"으로 보였다(공용 PC에서 다음 사용자에게 구매 사실 노출. 콘텐츠 접근 자체는 서버 게이트가 막으므로 표시만의 문제였다). `Auth._PLAN_CACHE` 6개 키를 **raw `localStorage.removeItem`으로** 제거하도록 수정.
  - ⚠️**`Storage.remove()`를 쓰면 안 된다** — `DataSync.queueDel`이 서버 `user_data`에서도 지우는데, `handleGetData`가 `plan_packages`를 user_data에서 읽으므로 **정상 구매자가 잠긴다**. 코드 주석에 근거를 남겨 뒀다. jsdom 테스트가 `/api/data/sync` 미호출을 검사한다.
- 🔎**미수정·서버측 관련 결함**: 같은 이유로 **마이페이지 '저장된 데이터 전체 초기화'(`confirmReset`)가 유료 접근을 날린다** — `Storage.remove('plan_packages')` + `DataSync.clearServer()`(clearAll)로 서버 user_data가 비워지고, `handleGetData`가 `owned`(=user_data.plan_packages)를 기준으로 필터링하므로 **`entitlements` 행이 멀쩡히 살아 있어도 `plan_packages: []`가 되어 잠긴다**. 현재는 확인창이 "이용권 정보가 사라지니 문의하라"고 안내할 뿐. 근본 수정안: `handleGetData`가 `owned`를 필터링하는 대신 **활성 `entitlements`와 합집합**을 만들도록 바꾸면(레거시 키는 그대로 통과) 초기화·동기화 사고에도 유료 접근이 자동 복구된다. `handleSyncData`의 **del 경로에 `PLAN_KEYS` 가드가 없는 것**(put에는 있음)도 함께 볼 것. → **Worker 재배포 필요**.

### 2026-07-27 — 진단 단순화(퇴직금 제거) + 법률 용어 자동 툴팁 ✅jsdom 검증

사용자 문제 제기: ① 무료진단에서 예상 퇴직금 금액을 물으니 "얼마로 써야 하지" 부담으로 피로감, ② 사이트 전반 텍스트가 많고 모르는 용어가 자주 나옴. 플랫폼 목적은 **정확한 변제금 계산기가 아니라 셀프 준비 안내**임을 재확인.

- **무료진단에서 예상 퇴직금 입력 제거**: `diagnosis.html` 4단계 재산 필드 6→5개, `diagnosis.js`의 `severanceValue`/`severanceHalf` 삭제(totalAssets에서 빠짐). 근거 = 사이트 표준 예시(가용 1,061,457×36=38,212,452 vs 청산가치 10,260,000)에서 퇴직금 유무가 판정을 바꾸지 않음. 판정이 뒤집히려면 저소득+장기근속이라는 좁은 조합이어야 함. 정확한 반영은 유료 단계 `setup.html`(q-employed)에서 계속 수행.
- `setup.html` 프리필: `severanceValue > 0` → 재직 판정하던 줄 삭제(무료진단이 더는 안 물음). setup에서 직접 답하게 둠.
- `result.html` 변제금 3종 요약 아래 **추정치 안내 문구 상시 노출** — "재직 중이면 예상 퇴직금 절반이 재산에 더해지고, 부양가족·법원 인정 생계비 범위에 따라 달라진다".
- **법률 용어 자동 툴팁**(`main.js` GLOSSARY 48개 + `tagTerms`/`initGlossary`): 페이지마다 수동으로 `<span class="term-tip">`을 심는 대신 본문 텍스트 노드를 훑어 자동 태깅. **용어당 화면에서 1회만**(점선 도배 방지), 긴 용어 우선 매칭(`면책결정`이 `면책`보다 먼저), 링크·버튼·입력창·헤더·푸터·제목(h1~h3) 제외. `taggedTerms()`가 매 회차 DOM에서 다시 읽어 탭 전환으로 본문이 다시 그려져도 자가 복구. MutationObserver(200ms 디바운스)로 JS 렌더 콘텐츠도 커버 — 남은 용어 없으면 즉시 반환해 순환 없음.
- **터치 대응**: 기존 term-tip은 `:hover`만 있어 모바일에서 안 열렸음. `.tip-open` 토글(탭/바깥탭/ESC) + `:focus-visible` + `tabindex/role/aria-label` 추가(`styles.css`, `main.js`).
- **jsdom 검증 PASS**: 중복 태깅 없음 / 링크·버튼·헤더·푸터 안 태깅 안 됨 / `면책결정`·`면책불허가`·`보정권고` 통째로 매칭 / 본문 텍스트 무손상 / 재실행 시 증가 없음(멱등).
### 2026-07-29 — 면책신청 무료 분리 + 이용권(기간·회수) 체계 도입 ✅legal-verifier 검증·회귀 5종

**출발점**: 회생 완주 패키지 9단계가 "변제 완료 후 면책신청"이라 상품 수명이 5년+였고, 이용기간을 정할 수 없었다. 조문을 확인해 보니 개인회생 면책은 **제624조 제1항이 "변제를 완료한 때에는 … 직권으로 면책의 결정을 하여야 한다"는 기속행위**이고 심문·이의기간·관재인 조사가 없다. 반면 개인파산은 심문(제558조)·관재인 조사(제560조)·이의신청(제562조)·불허가사유 7가지(제564조①)로 전혀 다른 난이도. → **회생 면책만 무료로 분리**.

- **`discharge.html` 신규(무료)**: 개인회생 면책신청 안내. `rehabilitation.html` 9단계 제거(8단계까지 = 인가까지). pricing은 이미 "서류 준비 → **인가 완료**"로 광고 중이었으므로 오히려 광고와 실제가 일치하게 됨. NAV_LINKS·푸터 연결.
  - ⚠️ **특별면책(제624조②, 3요건)은 무료로 빼지 않았다** — 회생위원 조사·면담·의견서·이해관계인 의견청취가 붙는 완전히 다른 절차. `maintenance.html`(변제기간 관리)에 그대로 유지.
- **이용권 체계**(`entitlements` 테이블): 패키지별 **이용기간 + AI 회수**를 한곳에서 관리. `grantEntitlement()`는 재구매 시 **남은 기간 끝에서 연장 + 회수 합산**(덮어쓰기 아님 — 보정 추가 대응은 여러 번 사는 상품). `GET /api/data`가 `plan_packages`를 **만료되지 않은 것만**으로 덮어써 `requirePackage`가 만료를 자동 반영. 환불·회수(`revokePackage`) 시 이용권도 삭제.
  - 기간: 회생 완주 24 / 파산 완주 24 / 변제기간 관리 84 / 보정 추가 대응 12개월. `addMonths()`는 말일 보정 포함(1/31+1개월=2/28).
- **AI 검토 총량제**: 완주 12회 / 관리·보정 각 8회 / **미구매 1회 무료 체험**(`ai_trial`). 일일 상한은 30→**10**으로 낮춰 남용 방지용으로만 남김. 실패한 호출은 미차감(성공 시에만 batch로 차감). 소진 시 403 + 요금제 유도.
  - 체험 1회는 **시행령 제21조의2 제3호(체험용 디지털콘텐츠)**도 충족 → 환불 제한 근거가 미리보기(제1호)와 **이중**이 됨.
- **동시 세션 1개**: 로그인 시 `DELETE FROM sessions WHERE user_id = ?`. IP·기기 미수집이라 개인정보 방침 유지. ⚠️**시간을 나눠 쓰는 순차 양도는 못 막는다** — 본인확인 없이는 불가능하며, 공개 후 실제 남용이 관측되면 그때 도입하기로 보류.
- **결제 전 고지 3곳**(전상법 제13조② + 시행령 제20조2호 '판매조건 제한'): pricing 안내 박스 / 결제 전 confirm(`PACKAGE_TERM_MONTHS`·`PACKAGE_AI_QUOTA` 상수) / terms 제5조. ⚠️**프론트 상수는 서버 `PACKAGE_TERMS`와 반드시 일치해야 하며, 테스트가 이 일치를 검사한다.**
- **legal-verifier 검증 반영**(조문은 직접 재대조함):
  - 🐛 **비면책채권 제7호(근로자의 임치금·신원보증금) 누락** — 제625조②는 **8개 호**인데 7개만 적었음. 추가.
  - 🐛 **"조세 등의 청구권"이 과대** — 제583조①2호 한정(원천징수 조세·부가세·개별소비세·주세·특별징수 지방세)임을 명시. 안 그러면 "모든 세금은 안 없어진다"로 읽힘.
  - **채권자 즉시항고 존재**(제627조) — "이의신청 절차도 없다"는 서술 수정. 공고 효력일부터 14일(제13조②) 경과로 확정됨을 4단계에 추가.
  - **회생위원 조사·보고 있음**(서울회생법원 준칙 제451호) — "관재인 조사 없음"은 맞으나 회생위원은 있음.
  - 제625조③에 **담보** 포함(보증인만 쓰고 담보 누락했었음).
  - 면책신청서 실제 양식 확인 → **환급계좌란**이 있어 남은 적립금이 그 계좌로 환급됨. 누락 시 불이익이라 강조 박스 추가.
  - **신용정보 해제는 법원이 직권 통보**(준칙 제452호) / 결정문은 **제증명 신청**으로 받아야 함(송달 생략 가능) / 면책신청 종기 = 절차 종료 전(대법원 2012마811) / 서울회생법원은 **6개월 미신청 시 직권면책** 안전망.
  - 전자소송 링크는 **양식 배포처가 아니라 제출 경로** → 라벨 분리.
  - 약관 보완: 잔여 회수 **소멸·환불 불가 명시**(약관규제법 제5조 작성자 불이익 원칙 대비), **만료 30일 전 안내** 조항 신설, 일일 제한이 회수를 없애지 않음을 명시.
- **회귀 5종 PASS**: 이용권(기간계산·부여·재구매누적·만료경계·고지일치) / AI 스키마·설계불변식 / 미리보기 7종 / 게이트 7종(위조차단) / 용어 툴팁.
- ⚠️**공개 전 필수(미구현)**: ~~①주문 확인 메일 ②만료 30일 전 안내 메일~~ → **2026-07-29 (2차)에서 기산점을 결제일+14일로 옮기고 만료 안내를 마이페이지 표시로 대체해 둘 다 해소**(위 항목 참조). 남은 것은 ③`ai_trial`·`entitlements` 테이블 **D1 마이그레이션 적용**.

### 2026-07-28 (3차) — 서류검토 AI를 '판정자'에서 '질문자'로 전환 ✅검증 7종

배경: "N건 발견" 식 판정 출력이 실용성도 낮고 변호사법·법무사법 경계에도 걸린다는 사용자 지적. 조문을 확인해 보니 금지되는 것은 **작성·감정·법률상담** 세 가지이고, **점검과 질문은 어디에도 없다**(법무사법 제2조①1 "법원에 제출하는 서류의 작성" + 제3조① / 변호사법 제109조1 "감정·대리·법률상담 또는 법률관계 문서 작성"). 따라서 결과를 흐리게 만들 필요가 없고, **판정만 안 하면 구체적으로 말해도 된다**는 결론.

- **핵심 전환**: AI가 판정하지 않고 **질문한다**. "채무 발생 경위가 불충분합니다"(감정) → "2020~2022년에 채무가 늘어난 사정이 적혀 있지 않습니다. 이 기간에 어떤 일이 있었나요?"(정보 제공). 문장은 언제나 이용자가 쓴다 = 대필 회피 + 셀프 진행 플랫폼 목적과 일치.
- **금지를 프롬프트가 아니라 스키마로 강제**(`api/src/index.js` `AI_REVIEW_SCHEMA`): 출력 형식을 `output_config.format`(json_schema)으로 고정하고, **대체 문장·판정을 담을 필드를 아예 만들지 않았다**. `{ present[], questions[{topic,question}], lawNotes[{excerpt,statute,note,question}] }` 뿐. 프롬프트로만 막으면 모델이 넘어가므로 구조로 막는 것. ⚠️**필드 추가 시 이 원칙을 깨지 말 것** — 코드 주석에 근거 조문과 함께 남겨둠.
- **`severity`(심각도 등급) 제거**: 등급을 매기는 것 자체가 '감정'. `ai-review.html`의 `SEV` 상수 삭제, 결과 화면에서 등급 뱃지 제거.
- **"이상 없음"을 절대 출력하지 않는다**: 질문이 0건일 때 "이번 점검에서 추가로 여쭐 항목을 찾지 못했습니다 + 법원의 판단을 예측하지 않습니다"로 사실만 진술. "제출해도 된다"는 판단은 이용자 몫.
- **🐛 잠재 버그 수정**: `claude-sonnet-5`는 `thinking`을 생략하면 **adaptive thinking이 기본 동작**인데(Opus 4.8/4.7과 반대) `max_tokens`가 사고+응답을 함께 제한한다. 기존 1024로는 구조화 출력이 중간에 잘려 JSON 파싱이 실패한다 → **4096으로 상향** + `stop_reason === 'max_tokens'` 감지해 502로 올림(잘린 JSON을 조용히 통과시키지 않음). `output_config.effort: 'medium'` 추가.
- **진술서 전용 보강**(`AI_NARRATIVE_HINT`, `isNarrativeDoc()`): 진술서·경위서·생활상황 계열에만 붙는 점검 항목(채무 시작 계기 / 증가 기간의 사정 / 사용처 / 상환 노력 / 현재 상황 / 시기·금액 불일치). 채권자목록 등에는 붙지 않음.
- **`lawNotes` 설계**: 초안에 도박·투자·사치성 소비 등 면책불허가 사유로 검토될 표현이 있을 때만 생성. **"이 표현은 위험하다"(판단)가 아니라 "이 조항은 …을 면책불허가 사유로 정합니다"(사실) + "관련 사정을 함께 적으셨나요?"(질문)**. 없는 위험을 만들어내지 않도록 빈 배열 허용을 명시.
- **검증 7종 PASS**(`node`, 실제 API 미호출·fetch 목킹): 스키마가 structured outputs 제약 준수(모든 object에 `additionalProperties:false`+`required`, 미지원 키워드 `minLength`/`maxItems` 등 없음) / **설계 불변식**(스키마에 `suggestion`·`severity`·`score`·`rewrite`·`example`·`verdict`·`summary` 필드 부재) / 요청 본문에 `json_schema`·`effort`·`max_tokens≥4096` 실림, sonnet-5가 거부하는 샘플링 파라미터 없음, messages가 user로 끝남(프리필 미지원) / 진술서엔 보강 지침 붙고 채권자목록엔 안 붙음 / `max_tokens` 절단 감지 / `refusal` 감지 / 정규화 상한·불량항목 제거.
- ⚠️**아직 안 한 것**(설계는 확정, 구현 미착수): ①숫자 검산은 코드로 분리(채권자목록 합계·안분율 100%, 변제계획안 가용소득×기간≥청산가치, 수입지출 정합성 — `diagnosis.js` 계산 로직 재사용) ②진행센터 체크리스트 항목 안에 [작성예시][검토받기] 배치 ③`drafts` 테이블로 재검토 이력("지난번 지적 3건 중 2건 반영") ④회수 정책을 문서별→패키지 총량제.

### 2026-07-28 (2차) — 환불 정책 전환(이용 개시 후 제한) + 미리보기 게이트 ✅T2 검증·jsdom 17케이스

사용자 결정: "이미 이용했으면 환불해 주면 안 된다 — 쓰고 환불하는 악용이 늘 것". **LAUNCH-CHECKLIST '확정된 방침(뒤집지 말 것)'의 종전 환불 정책을 의도적으로 뒤집은 것**이므로 체크리스트도 함께 갱신함.

- **법적 제약**: 전자상거래법 제17조 제2항 제5호는 디지털콘텐츠 '제공 개시' 후 청약철회 제한을 허용하지만, **같은 항 단서 + 제6항 단서**에 따라 ①청약철회 불가 사실의 표시 ②시행령 제21조의2의 시험사용 제공을 **모두** 해야 제한이 유효하다. 표시만 하고 미리보기가 없으면 **제한 자체가 무효**가 되어 종전보다 나빠진다.
- **미리보기 게이트로 전환**(`main.js`): 전체 화면 `showPaywall` 오버레이 → `enterPreview`/`lockSection` 방식. 유료 4종에서 **1단계 체크리스트 전체 + 항목별 상세 가이드**를 미구매자에게 열고, 2단계 이후·서식 예시 라이브러리는 잠금. 시행령 제21조의2 **제1호(일부 이용의 허용)** 이행. `chamroad:gate` 이벤트로 서버 판정 확정 시 재렌더. **`showPaywall`/`hidePaywall` 삭제됨**.
- **`content_access` 테이블 신설**(`api/schema.sql`) + `POST /api/content/access`(`handleContentAccess`) — 보유자가 콘텐츠를 처음 연 시각을 최초 1회 기록. `GET /api/payment/history`에 `first_access_at` LEFT JOIN으로 함께 내려줌(환불 판단 근거). 미구매자의 미리보기 열람은 기록하지 않음.
- **🐛 반박 검증이 잡아낸 자체 결함**: 최초 구현이 `unlockContent()`에서 바로 이용 개시를 기록했는데, 결제 후 1.2초 뒤 진행센터로 **자동 이동**하므로 소비자가 아무것도 열지 않았는데 기록이 찍혀 **약관 ①(7일 전액 환불)이 한 번도 작동할 수 없는 상태**였음(제21조①1 기만적 청약철회 방해 소지). → `noteContentOpened()`로 분리해 **2단계 이상 이동·서식 예시 열람** 등 명시적 열람 행위에만 바인딩.
- **문구 개정**: `terms.html` 제6조 ①~⑤ 전면 개정, `pricing.html` 안내 박스·결제 전 고지. 반박 통과분만 반영 — "제공이 **완료**"→"제공이 **개시**"(법문 일치), 기산점을 "결제일"→"**계약내용 서면(주문 확인 메일) 수령일**"(제17조①1·2), 지연배상금 **연 15%**(시행령 제21조의3), 디지털콘텐츠 **반환의무 없음**(제18조①단서), 서면 **발신주의**(제17조④), 위약금 청구 없음(제18조⑨).
- **반박으로 기각한 수정안**: ①"일할 또는 단위별 계산 환불" — 단계별 대금을 표시한 적이 없어 **산정 불가능한 약속**이라 오히려 채무불이행 근거가 됨 ②"미리보기 화면 자체에 환불 제한 표시가 법정 의무" — 제6항 단서의 "표시와 함께"는 두 의무의 접속(and)이지 동일 화면 요구가 아님 ③"1단계 공개는 시험사용으로 부족" — 시행령 제21조의2는 "하나 이상"만 요구하고 분량 기준 없음 ④"제17조② 단서(철회권 부활)를 약관에 적을 의무" — 제13조② 어느 호에도 없는 제재규범 ⑤대법원 2024다221042 인용 — **개인회생 청구이의 사건으로 전자상거래법과 무관**(오인용) ⑥공정위 지침 "Ⅱ.10.나.(2)" 단락 번호 — 원문 확인 실패.
- **약관 ② 가분성 열거 삭제**: "회차·기간 단위 패키지(보정·변제관리)만 가분적"이라고 열거한 것이 **완주 패키지 구매자의 법정 철회권을 박탈**(약관규제법 제9조1호 무효 소지)해, 법문 그대로 "가분적 디지털콘텐츠로 구성된 계약의 제공이 개시되지 아니한 부분"으로 대체. 가분성 판단을 약관이 선점하지 않게 함.
- **jsdom 검증 17케이스 PASS**: 미리보기 7종(유출 없음·DOM 복구 가능·CTA 분기·멱등) + 게이트 회귀 7종(**로컬 위조 차단 유지 확인**) + 용어 툴팁 회귀.
- ⚠️**공개 전 필수(LAUNCH-CHECKLIST 4단계에 추가함)**: **결제 시 계약내용 서면(주문 확인 메일) 발송**. 이걸 안 보내면 제17조①2호로 기산점이 안 잡혀 환불 제한 방어가 무너지고, 이미 "주문 확인 메일을 받은 날부터 7일"로 써 둔 약관·안내가 거짓이 된다. 청약철회 서식(제13조②5호 괄호)·사업자 신원 실값 기재도 함께.
- ⚠️**별건 발견(미수정)**: `pricing.html`의 "서류검토AI 지원 2회"가 실제 구현과 다름 — `ai-review.html`에 `requirePackage` 호출이 없고 `handleAiReview`는 세션만 확인하며 `DAILY_AI_LIMIT=30`. 즉 **미구매자도 하루 30회 사용 가능**. 광고와 이행의 불일치(제13조⑤)이자, "완주 패키지에 회수 단위 급부가 있다"는 가분성 논거를 스스로 만드는 자충수. 표시를 실제에 맞추거나 구현에 게이트를 넣어야 함 — **사용자 결정 필요**.

### 2026-07-28 — GLOSSARY 법률 검증(T2: legal-verifier → adversarial-refuter + consistency-checker) ✅반영 완료

- **에이전트 판정 충돌을 반박 검증으로 확정**: 채권자목록 누락 채권의 비면책에 '악의' 요건이 필요한지를 두고 legal-verifier(불필요)와 consistency-checker(필요)가 정반대 판정. 반박 결과 **legal-verifier 승** — 제625조 제2항 제1호(개인회생)는 '악의' 없이 "기재되지 아니한 청구권", 제566조 제7호(파산)는 "**악의로** 기재하지 아니한 청구권". consistency-checker가 제624조 제3항 제1호(면책**불허가** 사유의 악의)를 제625조 제2항으로 오독한 것. **→ 사이트 5곳은 이미 절차별로 정확해 수정 0곳.** `js/main.js` 채권자목록 툴팁도 현행 "면책되지 않을 **수 있습니다**"가 회생·파산 공용으로 유일하게 양쪽 참이라 **유지**(강화 금지). 대법원 2025.10.16. 2024다221042가 "원칙적 소극"으로 정식화해 "무조건" 표현도 금지.
- **반영한 수정(반박 통과분)**: 회생위원 지정→**선임**(제601조) / 가용소득에 **세금·보험료** 공제 추가(제579조4호 나~라목+시행령17조, 4대보험료 전부) / 표준생계비 60%는 **원칙이며 증감 가능**(재민 2004-4 제7조②) / 기준중위소득 = **가구**소득 중간값 / 원천징수 3.3%는 '프리랜서'가 아니라 **사업(영업)소득** / 면책·면책결정에 **비면책채권·보증인 책임 잔존** 명시(제566조·제625조②③) / 인가결정 "**납입은 보통 인가 전부터**"(재민 2004-4 제7조③, 서울준칙 제432호 — hedge 필수, 서울 전용 준칙이라 단정 금지) / 특별면책 3요건(제624조②) / 파산재단에서 압류금지·면제재산 제외(제383조) / 임차보증금 **면제재산 신청 가능**(제383조②1호, 기한=파산선고 후 14일) / 압류에 **월 250만원 압류금지** 추가(민사집행법 시행령 제36056호, 시행 2026.2.1, 종전 185만원) / 청산가치 문구를 result.html과 동일하게 통일('전부' 과장 제거) / 가처분소득을 result.html 기존 툴팁 문구로 통일.
- **반박으로 기각한 수정안**: ①채권자목록 강화(위) ②특별면책 "5년간 파산면책 제한" — 제564조①4호는 "제624조에 의하여 면책"이라 **정상 완주 면책에도 동일 적용**되므로 특별면책만의 불이익처럼 쓰면 오독 ③가처분소득 "일반 경제 용어입니다" — 경제학 disposable income은 생계비를 안 빼므로 오히려 악화 ④포괄적금지명령 "법인회생 전용" — 제593조⑤가 개인회생에 준용하므로 틀림.
- **🐛 실제 버그 발견·수정**: 자동 태깅이 합성어 앞부분만 잘라 **정반대 뜻**을 붙이고 있었음 — `비면책` 채무 → `비[면책]`("빚 책임이 없어짐" 툴팁), `압류금지`재산 → `[압류]금지`. GLOSSARY에 `비면책`·`압류금지`·`보정명령`·`송달료`·`수임료` 키를 추가해 길이 우선 정렬로 해결. jsdom 회귀 테스트 추가(합성어 5쌍) — 전 항목 PASS.
- 부수 일관성 수정: `rehabilitation.html:214` "최저생계비"(2015년 폐지 용어) → "표준생계비" / `resources.html:76` 동시폐지 서술을 `result.html:642`("관재인 선임이 대부분, 동시폐지는 예외적")와 정합.
- ⚠️**미반영(확정 보류)**: 표준생계비 가산 3항목(주거비·의료비·미성년자녀 교육비)은 **서울회생법원 준칙 제405호 전용** — 본문에 쓸 때 "서울회생법원 기준" 병기 필요, 60자 툴팁에는 미포함. `포괄적금지명령`·`별제권`·`재량면책`은 사이트 본문 등장 0회라 툴팁이 렌더링되지 않음(정의는 고쳐 둠).

- ⚠️**남은 것**: 텍스트량 과다 문제는 2026-08-14에 1차 감축(서문·UI 설명문 중복 제거) 완료 — 아래 "다음에 할 일" 참조.

### 2026-07-24 (3차) — 유료 콘텐츠 서버 게이팅(잠금 오버레이) ✅로직 검증

배경: 유료 센터 4종이 결제 게이트 없이 URL로 열려 있었음(정적 사이트 시절부터 미구현). 공개 전 필수 작업.
- **잠금 대상 4종**(사용자 결정): rehabilitation(rehab-full), bankruptcy(bankrupt-full), maintenance(maintain), supplement(type별 rehab/bankrupt 완주+보정). 방식=잠금 오버레이.
- `main.js requirePackage(allowed, pkgName)` + showPaywall/hidePaywall. 각 페이지 initPage 뒤 호출(supplement은 TYPE 정해진 뒤).
- **서버 검증 기반**: 로컬 plan은 위조 가능하므로 GET /api/data의 plan_packages로 최종 판정. 로컬 캐시로 즉시 표시(구매자 UX) 후 서버 재확인해 미보유면 차단. 네트워크 실패 시 로컬 신뢰(정상 구매자 보호). **테스트 지급·결제 모두 서버 반영되어 통과**.
- 접근 매핑: 완주=해당 진행센터, 완주+보정추가=보정센터(완주에 보정1회 포함), 변제관리=별도(완주로 접근 불가). 매핑 로직 12케이스 node 검증 전부 PASS.
- privacy 제8항 이전에 결제내역 열람 갱신 완료(2차).
- ⚠️**한계**: GitHub Pages 정적 호스팅이라 HTML 소스는 공개됨 → 완벽한 DRM 아님(기술적 우회 가능). 정상 경로·일반 사용자에겐 게이트로 작동하며 "결제 안 하면 정상적으로는 못 봄" 목적 달성. 완전 차단은 콘텐츠를 Worker에서 동적 서빙하는 아키텍처 변경 필요(과함, 미채택).
- ⚠️**배포**: 정적 파일 변경(Worker 무관) → git push가 배포인데 **미공개라 보류**. 프로덕션 반영은 공개 시점. 지금은 로컬(live server)에서 동작.
- **실코드 E2E 검증 완료(2026-07-24, jsdom으로 실제 main.js 실행)**: 비로그인→잠금, 서버보유→통과, 서버미보유→잠금, **로컬 위조(서버엔 없음)→서버확인 후 잠금(위조 차단 실증)**, 정상구매→통과, 네트워크실패+로컬보유→관용통과, 네트워크실패+로컬없음→잠금. 오버레이 CTA 분기(비로그인=로그인+next, 로그인미구매=구매하기)까지 7/7 PASS. 실제 픽셀 화면만 미확인(브라우저).

### 2026-07-24 (2차) — 마이페이지 결제 내역 열람 ✅배포·E2E 검증

- `GET /api/payment/history` (Bearer) — 본인 결제 내역. status IN ('paid','refunded','test')만, 최신순 50건(미완료 pending/failed 제외).
- mypage.html: 결제 상태 카드 아래 '🧾 결제 내역' 섹션(내역 있을 때만 표시). 패키지명·결제일·주문번호·금액·상태뱃지(결제완료/환불됨/무료지급). 환불건은 취소선.
- privacy 제8항 열람권: "결제 내역은 이메일 요청" → "마이페이지에서 직접 확인"으로 갱신(정합).
- **E2E 검증**: 비로그인 401, 지급 전 빈 배열, 테스트지급 후 test 1건 정확 조회. 테스트 계정 정리(전 테이블 0건).

### 2026-07-24 — 로그인 무차별 대입(brute-force) 방어 ✅배포·E2E 검증

- `login_attempts(email, fail_count, window_start, locked_until)` 테이블(프로덕션 D1 반영).
- 정책: 최근 15분 윈도우 내 실패 5회 → 15분 잠금(상수 LOGIN_MAX_FAILS/WINDOW/LOCK). 성공 시 실패기록 삭제(리셋).
- handleLogin: 잠금 확인(429) → 인증 → 실패 시 recordLoginFail → 성공 시 DELETE. **존재하지 않는 이메일도 카운트**해 잠금 여부로 계정 존재가 새지 않게 함. user 없어도 `valid = user && verifyPassword`로 같은 실패 경로.
- **E2E 검증**: 5회 실패 후 6회째 429 잠금, 잠긴 동안 올바른 비번도 429(방어 정상), 3회 실패→성공 시 리셋되어 카운트 초기화 확인. 테스트 계정·잠금기록 정리(전 테이블 0건).
- 참고: IP 기준 제한은 미도입(개인정보·방침 고려) — 필요 시 Cloudflare 인프라 레벨 Rate Limiting으로 보완 가능. change-password·delete-account는 세션 필요라 브루트포스 표면 작아 미적용.

### 2026-07-23 (3차) — 자체 익명 분석(데이터 개선 도구) ✅배포·검증

목적: 이탈 구간·전환율 파악. GA4/Clarity 대신 자체 익명 집계 선택(사용자 결정) — 민감정보 사이트라 제3자·세션리플레이 리스크 회피, 방침 "제3자 안 씀" 유지.
- **완전 익명**: 개인·세션·IP 저장 안 함. `analytics(day, event, label, count)` 테이블에 날짜별 카운트만 UPSERT.
- `POST /api/analytics {event, label}` — 인증 불필요(익명). 이벤트는 화이트리스트(`ANALYTICS_EVENTS`)만 허용, label은 `[\w\-./]`만 남기고 40자 컷(오염·XSS 방지). 실패해도 조용히 무시.
- 수집 이벤트: pageview(페이지별), diag_step(진단 2·3·4단계 전진), diag_complete, pay_start, pay_complete.
- 프론트: `main.js track()` + initPage에서 pageview 자동. diagnosis.js nextStep/submit, pricing startPayment에 삽입.
- admin: '📈 이용 분석'(최근 30일) — 페이지뷰 상위8 + 진단 퍼널(시작→2·3·4단계→완료 이탈 막대) + 결제 전환율. overview 응답에 analytics 배열 추가.
- privacy 제9항: "익명 통계만 집계, 개인·IP 미포함, 제3자 미전송" 한 줄 추가.
- **검증**: 익명 전송 E2E — 정상 이벤트 정확 집계, 미허용 이벤트(evil_hack) 거부, injection 라벨 정제 확인. 테스트 데이터 정리(analytics 0건).
- ⚠️미결(별개): 유료 콘텐츠 페이지 결제 게이트 부재 — 공개 시 서버 게이팅 도입 여부 결정 필요.

### 2026-07-23 (2차) — 관리자 테스트 지급 기능 ✅배포·E2E 검증

목적: 개발 중/지인 테스트 시 실제 결제 없이 유료 패키지를 회원에게 부여.
- `POST /api/admin/grant {email, package}` — 회원 이메일로 패키지 지급. payments에 `status='test'` 기록(매출 SUM은 'paid'만이라 제외됨) + grantPackage 호출.
- `POST /api/admin/revoke-test {paymentId}` — 테스트 지급 회수. 포트원 취소 없이(실결제 아님) status='revoked' + 같은 패키지 다른 활성('paid'|'test') 없을 때만 revokePackage.
- admin.html: '🧪 테스트 지급' 카드(이메일+패키지 select+지급) + 결제내역에 test='테스트'/revoked='회수됨' 뱃지 + test건 '회수' 버튼.
- **E2E 검증**(admin 임시가입→grant→target서버 plan=premium 확인→overview에 test행 확인→revoke→plan 사라짐 확인→전 계정 정리, DB 0건): 전 경로 통과.
- ⚠️지급 후 대상 회원은 **다시 로그인해야** 반영(syncOnLogin이 서버 plan을 받아옴). admin UI 안내문에 명시.
- 참고: 유료 콘텐츠 페이지(rehabilitation/maintenance/supplement 등)는 **결제 진입 게이트가 없어** URL 직접 접근으로도 열림 — 테스트엔 편하나 공개 시점엔 서버 게이팅 도입 여부 별도 결정 필요(미결).

### 2026-07-23 — 이용약관·환불정책 실제 결제구조 정합 + 환불 백엔드 + 결제위조 차단 ✅배포·검증

배경: terms.html 제5조가 "실제 결제·판매가 이루어지지 않습니다"라는 낡은 거짓(포트원 실결제 구현·검증 완료 상태). 청약철회·환불 조항 자체가 없었음. 전자상거래법 정합성 점검에서 시작해 환불 백엔드까지 구현.

**1) 환불 정책 = "결제일 7일 무조건 전액 환불"로 확정(사용자 결정 2026-07-23)**
- 처음엔 "이용 개시 후 제한"으로 설계했으나 교차검증 2종이 뒤집음:
  - (법률) 전자상거래법 제17조②단서+제6항 후단: 디지털콘텐츠 철회제한을 유효화하려면 "표시"만으론 부족, **시험사용 상품(미리보기/체험판) 제공까지 함께** 필요. 없으면 이용개시 후에도 전액환불 의무.
  - (코드) "AI검토=이용개시" 판정이 서버 데이터로 불가(ai_usage는 날짜별 카운트뿐, 결제 연결·시각 없음). 게다가 AI검토는 plan 게이트 없는 **무료 기능**이라 유료 제공개시 지표로 부적절.
  - → 제한을 아예 주장하지 않는 7일 무조건 환불이 이 서비스 규모(1인·소량·AI무료)에 최적. 시험판·이용개시추적 불필요, 공정위 리스크 최소.

**2) terms.html 전면 개정(11개 조)** — 제4조 회원가입·계정, 제5조 유료서비스·결제(공급시기=결제 즉시), **제6조 청약철회·환불**(①7일 무조건 전액 ②하자·불이행 3개월/30일 제17조③ ③이메일 요청→청약철회일 3영업일 환급+지연배상 제18조 ④미성년자 취소권), 제10조 분쟁해결(소비자상담 1372·한국소비자원·전자거래분쟁조정), 제11조 운영자 정보(최은식, 사업자등록 후 상호·신고번호 기재 유보).

**3) 환불 백엔드(신규)** — `POST /api/admin/refund`(admin 전용): 포트원 V2 결제취소(`POST /payments/{id}/cancel`, 전액) → `payments.status='refunded'`+`refunded_at`(스키마 ALTER, 프로덕션 D1 반영) → `revokePackage`로 이용권 회수(plan_packages에서 제거, 대표 재설정 or 남은 것 없으면 plan* 전체 삭제). admin.html 결제목록에 환불 버튼 + refunded 뱃지. 매출 SUM은 status='paid'만이라 환불건 자동 제외.

**4) 결제 위조 차단(보안, 검증 중 발견)** — `Storage.save`가 모든 cdg_* 키를 서버로 올리므로, 콘솔에서 `cdg_plan='premium'` 심고 로그인하면 **결제 없이 서버에 프리미엄 부여**되는 취약점이 있었음. 서버 `handleSyncData` put에서 `PLAN_KEYS`(plan/plan_packages/plan_type/plan_package/plan_package_name) 거부 → 이용권은 결제 검증한 grantPackage/revokePackage만 쓰기 가능. 환불 후 로컬 plan이 재로그인 시 부활하던 문제도 이 방어로 함께 해결. ✅E2E로 위조 차단 확인.

**5) pricing.html** — 결제 전 confirm 고지 + 상시 환불안내 박스를 "7일 무조건 환불"로 단순화. 낡은 "정식 출시 준비 중" 메타 문구 제거.

**6) 재검증(adversarial) 반영** — 개정본을 다시 반박검증해 5개 신규 이슈 발견·처리:
- (A, 高) 포트원 취소 성공 후 DB UPDATE 실패 시 영구 불일치 → `portoneCancel` 헬퍼가 '이미취소'를 멱등 성공으로 처리(재클릭 502 루프 해소), UPDATE에 `WHERE status='paid'` 가드.
- (C, 中) 회당 상품(correction-*) 중복구매 시 1건 환불이 전체 회수 → 환불 후 "같은 user+package의 다른 paid 결제 수"를 세어 0일 때만 revokePackage.
- (#5) 탈퇴자 환불 경로 없음(payments CASCADE 삭제, archive 미조회) → handleAdminRefund가 payments에 없으면 `payments_archive` fallback 조회 후 포트원 취소 + archive.status='refunded'. ⚠️단 admin 대시보드는 아직 탈퇴자 결제를 목록에 안 띄움 → API는 payment_id로 호출 가능하나 UI 버튼은 없음(운영자가 D1에서 payment_id 찾아 호출, 또는 포트원 콘솔 수동). 후속 UI 과제.
- (B, 高) 환불 후 localStorage plan이 재로그인 시 부활 → **4)의 PLAN_KEYS 서버 가드로 이미 해결**(로컬 plan 올려도 서버 거부).
- (D, 低) 메인 환불 시 부가옵션이 대표로 승격 → 드문 엣지, 미수정(문서화).

**검증**: revokePackage 단위검증(단일→전체회수, 대표환불→재설정) + 권한차단 E2E(비로그인·비관리자 403) + **plan 위조차단 E2E 통과**(diagnosis_data는 저장, plan 계열 전부 거부 확인). **미검증(실카드 필요)**: 실제 포트원 결제→환불 버튼 전체 경로, archive fallback은 사용자가 실카드 1건으로 최종 확인 필요.

**gotcha**: Cloudflare Workers `wrangler deploy` 직후 몇 초 전파 지연 — 배포 직후 즉시 E2E 치면 옛 버전이 응답할 수 있음(재시도로 확인). 포트원 V2 취소 = `POST https://api.portone.io/payments/{id}/cancel` + `Authorization: PortOne {API_SECRET}`, body `{reason}`, amount 생략 시 전액.

**남은 것**: 실카드 결제→환불 최종 E2E(사용자), 로그인 시도 횟수 제한(미구현), 결제내역 열람 API(현재 이메일 요청 안내), 사업자등록 후 terms 제11조·통신판매 신고번호 기재.

### 2026-07-21 — 개인정보처리방침 전부 개정 + 회원탈퇴 기능 ✅배포·E2E 검증 완료

배경: 방침이 "서버를 운영하지 않는 정적 사이트, 서버로 전송·수집·보관되지 않음" 기준이라 백엔드 도입 후 **내용 전체가 사실과 반대**였음. 법 제30조·시행령 제31조 기준으로 재작성하고, 방침이 약속하는 권리(삭제권)를 실제로 행사할 수 있도록 탈퇴 기능을 함께 구현.

**1) 회원탈퇴 (신규)**
- `POST /api/auth/delete-account` (Bearer + `{password}` 재확인) — `DELETE FROM users` → FK CASCADE로 sessions·user_data·ai_usage·payments 동시 삭제.
- ⚠️ 삭제 **전에** 완료 결제(`status='paid'`)를 신설 테이블 `payments_archive`(users FK 없음)로 이관 — 전자상거래법 시행령 제6조 5년 보존 의무. CASCADE가 결제기록까지 지우는 문제를 막는 핵심.
- `mypage.html` 계정 설정에 탈퇴 UI(경고 + 비밀번호 + confirm 2단), `main.js` `Auth.deleteAccount()` + `DataSync.cancelPending()`(대기 중 동기화가 지운 데이터를 되올리는 것 방지).
- **프로덕션 E2E 검증**: 가입→데이터저장→오답비번 401→탈퇴 200→세션 무효 401→같은 이메일 재가입 성공. D1 직접 조회로 고아 레코드 0건 확인.

**2) 민감정보 서버 저장 중단 (중요)**
- 진단의 `hasHealthIssues`(건강상태)·`debtCauses`(의료비·도박 포함)가 서버 저장되고 있었음 = 법 제23조 민감정보 무단 처리.
- `DataSync.SENSITIVE_FIELDS` + `_strip()`로 **업로드 경로에서만 제거**(정기 flush·로그인 시 게스트 이관 양쪽). 계산은 브라우저에서 하므로 진단 결과는 그대로. 부작용: 다른 기기 로그인 시 이 두 항목은 옮겨지지 않음(방침에 명시).

**3) 서버 수정 2건** — 새 세션 생성 시 그 계정의 만료 세션 삭제(만료행 무기한 잔존 문제), `clearAll` 시 `ai_usage`도 삭제(삭제 창구 부재 문제).

**4) 방침(privacy.html) 14개 항 전면 재작성** — 처리목적/항목/보유기간/제3자제공/위탁/국외이전 3건 상세/파기/권리행사/자동수집장치/안전성조치/자동화된 결정/보호책임자/구제방법/변경이력. 개인정보 보호책임자 = **최은식**(법 제31조②상 개인 운영자는 본인이 당연히 CPO).

**5) login.html** — 가입 동의의 "이용약관" 버튼이 `alert('준비 중')` 스텁이었음 → 실제 terms.html·privacy.html 링크 + "만 14세 이상" 확인 문구로 교체.

**교차검증(에이전트 2종 상호반박)에서 잡아 고친 것**
- 코드 대조: "지정 도메인 외 요청 차단"(→CORS일 뿐), "마이페이지에서 결제내역 열람·설정에서 정정"(→그런 API 없음, 이메일 요청으로 정정), 진단 항목 과소열거, `profile`·게스트 소급이관·Tailwind CDN 국외전송 누락, 탈퇴해도 타 기기 localStorage 사본은 남음.
- 법령 대조: **위임장 근거 오류**(시행규칙 별지 제11호 ✗ → 시행령 제45조②+「개인정보 처리 방법에 관한 고시」 별지 제11호서식 ○), 열람 10일 근거(시행령 제41조④⑤) 명시하고 정정·삭제·처리정지는 "지체 없이"로 분리, **행정심판 문구 삭제**(공공기관 전용 조항), 카드사를 수탁자에서 제외(독자 처리자), 회원정보 "탈퇴 즉시 파기" ↔ 결제기록 이메일 5년 보관 모순에 단서 추가.
- 법 제30조①·시행령 제31조① 필수 기재사항 **전 항목 충족** 판정(민감정보 공개가능성·가명정보·국외직접수집은 해당없음으로 생략 적법).

**gotcha**: D1은 FK CASCADE가 켜져 있어 `users` 삭제 시 결제기록까지 사라진다 — 법정 보존 대상은 반드시 별도 테이블로 먼저 옮길 것. D1 실제 저장 리전은 API 응답의 `served_by_region`으로 확인 가능(현재 APAC/SIN) — 방침의 국외이전 국가 기재 근거.

**후속 과제(미착수)**: 결제내역 열람 API(현재 이메일 요청으로 안내 중), 로그인 시도 횟수 제한, terms.html 환불·청약철회 조항이 실제 결제 구조와 맞는지 점검.

### 2026-07-20 (3차) — 비밀번호 정책 강화

- **정책**: 8자 이상 + **영문·숫자·특수문자 모두 포함**. 서버(`passwordError` — validateSignup·change-password 공용)와 클라(`js/main.js` 동일 `passwordError` — login 가입·mypage 비번변경)에 이중 적용. 위반 시 구체적 사유 표시.
- **UI**: 가입·비번변경 화면 라벨/플레이스홀더에 "(8자 이상, 영문·숫자·특수문자 포함)" 안내.
- **검증**: 프로덕션에서 8자미만/영문없음/숫자없음/특수문자없음 각각 400 + 정책충족 200 확인.
- **이메일 중복체크**: 이미 있음(가입 시 409 "이미 사용 중인 이메일" + DB email UNIQUE). 추가 작업 불필요.
- **이메일 인증**: 미구현. 이메일 발송 서비스(Resend 등) 필요 → 도메인·실서비스 전환 시 password reset과 함께 도입 검토(find-account.html이 "이메일 인증 준비 중"으로 안내 중).

### 2026-07-20 (2차) — 관리자 페이지(운영 대시보드) ✅배포·검증

- **`admin.html` 신설**(noindex·nofollow, 네비 미노출 — 운영자가 URL 직접 접근): 통계카드(전체회원·오늘가입·결제완료건·누적매출·AI검토누적) + 결제내역 표(최근100, 일시·이메일·패키지·금액·상태badge) + 회원목록 표(최근100). 미로그인/비관리자는 "접근 권한 없음" + 로그인 링크.
- **서버**: `requireAdmin`(세션 이메일 === `ADMIN_EMAIL`) + `GET /api/admin/overview`(통계+회원100+결제100 한 번에). 프로덕션 시크릿 `ADMIN_EMAIL=eslje75@gmail.com` 설정.
- **검증**: 관리자 200(통계·목록 정상)/비관리자 403/무인증 403 — 로컬 통과 + 프로덕션 게이팅(403) 확인.
- ⚠️ **운영자는 `eslje75@gmail.com` 계정을 반드시 (강한 비번으로) 가입해야 관리자 접근 가능.** 그 이메일로 로그인한 세션만 admin API 통과. 공개 전 선점 가입 권장.
- **미커밋**(직전 커밋 9807271 이후 admin.html·index.js·PROGRESS 변경분).

### 2026-07-20 — 백엔드 Phase 4: 결제(포트원 PortOne V2) ✅실결제 E2E 검증 완료

- **자격증명 설정 완료(프로덕션 시크릿)**: `PORTONE_STORE_ID`(store-…), `PORTONE_CHANNEL_KEY`(channel-key-…), `PORTONE_API_SECRET`(값은 포트원 콘솔에서만 확인). 테스트 채널 = 토스페이먼츠 테스트. 설정 중 삽질 교훈: ①V1 아닌 **V2** 채널이어야 store-/channel-key- 형식 ②**채널 키**(포트원 생성)와 PG **클라이언트 키**(test_ck_) 혼동 주의 ③API Secret은 **연동정보→식별코드·API Keys→V2**에 있고, **복사 버튼**으로 전체 복사(드래그하면 …로 잘림→401).
- **실결제 E2E 성공(토스 테스트모드, 실제 무출금)**: 결제창 호출→카드결제→서버 complete가 포트원 조회로 status=PAID+금액 149000/29000 일치 검증→`maintain` 패키지 부여(plan=premium, plan_packages=["maintain"])→마이페이지 "이용 중" 표시까지 전 과정 확인. 최초 시도는 "해지된 카드" PG거부로 FAILED(정상 동작, 카드 문제)—토스 테스트는 별도 테스트카드 없이 실제 유효카드 입력(무출금).
- **프론트 버그 수정**: 결제 실패 시 조용히 삼키던 것 → `payRes.message` 그대로 표시.
- **정리 완료**: 임시 디버그 엔드포인트·PAY_DEBUG 제거, main.js API_BASE 원복(localhost 분기 복원), 로컬 정적서버 종료, 테스트 데이터 전량 삭제(users/payments/user_data=0).
- **⚠️ 실서비스 전환 시**: 사업자등록 후 포트원 콘솔에서 **라이브 채널** 연결→라이브 store/channel/secret로 시크릿 교체. 현재는 테스트채널이라 실결제 안 됨.

### 2026-07-19 (5차) — 백엔드 Phase 4: 결제(포트원 PortOne V2) 코드 완성·구조검증 (완료, 위 5차→최종은 7/20 항목)

- **PG 결정(사용자)**: 포트원(PortOne) — 여러 PG·간편결제(카카오/네이버페이)를 하나의 연동으로 확장 가능. V2 SDK 사용.
- **흐름**: 브라우저 `PortOne.requestPayment()` → paymentId → 서버가 `GET api.portone.io/payments/{id}`(API Secret)로 **status=PAID + amount.total 일치 검증** → 패키지 부여. 금액은 서버 `PACKAGES` 표가 소스(클라 금액 불신, 위변조 방지).
- **DB**: `payments(payment_id PK, user_id, package, amount, status pending|paid|failed, created_at, paid_at)`.
- **API**: `POST /api/payment/prepare`(Bearer — 주문 생성, 서버가 paymentId·금액 확정, storeId·channelKey 반환) / `POST /api/payment/complete`(Bearer — 포트원 조회·검증·확정, 멱등 처리, `grantPackage`가 user_data의 plan·plan_packages·plan_package(_name)·plan_type에 부여 — 부가옵션 correction-*는 대표 패키지 안 덮음).
- **프론트(`pricing.html`)**: 목업 모달 제거. `mockPayment/mockComplete` → `passesPurchaseGuards`(자격·오구매 가드 전량 보존) + `startPayment`(로그인 게이트 → prepare → PortOne 결제창 → complete → 서버가 준 plan 로컬 반영 → 패키지 페이지 이동). SDK `https://cdn.portone.io/v2/browser-sdk.js`.
- **가격**: rehab-full 149,000 / maintain 29,000 / bankrupt-full 49,000 / correction-rehab·correction-bankrupt 각 19,000 (기존 pricing.html과 동일, 서버 PACKAGES에 확정).
- **구조검증 통과**: 무인증401 / 알수없는상품400 / prepare200(paymentId·서버확정금액149000·storeId 반환) / complete 더미시크릿502 / **남의 주문 404(소유권)** / 서버 금액권위 확인. 전부 로컬. 프로덕션 DB 청결(users/payments/user_data=0).
- **활성화 남은 작업(자격증명 3개 필요)**: 포트원 콘솔에서 ① 테스트 채널 연결(토스/이니시스 등 테스트 PG) ② Store ID·Channel Key·V2 API Secret 확보 → 프로덕션에 `wrangler secret put PORTONE_STORE_ID / PORTONE_CHANNEL_KEY / PORTONE_API_SECRET` 설정. 설정 전엔 prepare/complete가 503으로 정상 실패. 실결제는 사업자등록 후 라이브 채널로 교체.

### 2026-07-19 (4차) — 백엔드 Phase 3: AI 서류검토 실제 Claude API ✅배포·실검증 완료

- **목업 제거**: `ai-review.html`의 `runReview()`가 규칙 기반 키워드 검사 → **실제 Claude API 호출**로 전환. 목업 흉내 금지 방침대로, 실패 시 옛 키워드 검사로 위장하지 않고 정직한 오류만 표시.
- **법적 안전 설계(변호사법 리스크 대응)**: Worker 시스템 프롬프트가 AI를 '서류 완성도 점검 보조자'로 엄격 제약 — 법률자문·결과예측·법적결론 금지, 누락·불명확·수치불일치·형식·개인정보노출만 지적. 응답은 {summary, issues[], confirmed[], suggestions[]} JSON.
- **엔드포인트**: `POST /api/ai/review`(Bearer 필수). 입력 30~6000자. `ai_usage(user_id, day, count)` 테이블로 **일 30회 제한**(비용·남용 방지, 성공 시에만 증가). 프론트는 로그인 게이트(게스트는 로그인 유도) + 주민번호 패턴 사전 경고 + 정적 작성팁·세트 진행률 유지.
- **모델**: `AI_MODEL = 'claude-sonnet-5'` — **검증 완료**. ⚠️ 이 계열은 **assistant 프리필 미지원**(400 "does not support assistant message prefill"). 그래서 JSON 프리필 기법 제거하고 messages는 user로 끝냄 + 응답에서 text 블록만 뽑아 JSON 파싱(코드펜스 대비 {…} 정규식 폴백). `ANTHROPIC_VERSION='2023-06-01'`.
- **API 키**: 사용자가 발급해 `wrangler secret put ANTHROPIC_API_KEY`로 프로덕션 시크릿 설정 완료(Cloudflare 암호화 보관, 코드·깃 미노출). 만료 없음 + 월 지출 한도 권장.
- **실검증(프로덕션, 실제 과금)**: 진술서 초안(일부러 모호 요소 포함)으로 호출 → HTTP 200, ~12.6초. AI가 "대략 6천만원→정확금액 필요"·모호한 날짜·채권자명 누락 정확히 지적, 마스킹 인식, **결과예측·단정 표현 0건(법적안전 통과)**. 구조 테스트도 전부 통과(401/503/400/413/502). 검증 계정·디버그 시크릿 전부 삭제(users/ai_usage/sessions=0).
- **관찰**: 응답 ~12초로 다소 김(버튼에 "AI가 검토 중입니다..." 표시로 대응). 필요 시 모델을 Haiku로 낮추면 비용↓·속도↑(정확도 트레이드오프).

### 2026-07-19 (3차) — 백엔드 Phase 2: 사용자 데이터 서버 저장 ✅배포·검증 완료

- **설계 원칙**: 기존 동기 localStorage를 **캐시로 유지**하고 로그인 시에만 서버 미러링 — 수십 개 `Storage.load` 호출부를 async로 안 바꿔도 되고 게스트도 그대로 작동.
- **DB**: `user_data(user_id, key, value TEXT=JSON, updated_at, PK(user_id,key))` 신설(프로덕션+로컬 적용).
- **API 추가**(`api/src/index.js`): `GET /api/data`(전체 조회 {key:value}), `POST /api/data/sync`({put:{k:v}, del:[k], clearAll} 배치 upsert/삭제, D1 batch=트랜잭션). 한도: 키 64자·값 100KB·요청당 60키·본문 512KB. 전부 Bearer 필수.
- **프론트(`js/main.js`)**: `Storage.save/remove`가 로컬 즉시 반영 + `DataSync`로 700ms 디바운스 백그라운드 푸시(로그인 시만). `pagehide`에 keepalive 플러시로 이탈 시 유실 방지. **`DataSync.syncOnLogin()`**(로그인/가입 직후 자동): 서버에 데이터 있으면 **서버 우선**(로컬 앱데이터 교체 — 다른 기기·이전 게스트 잔여 제거), 서버 비어있으면 **게스트 로컬데이터를 계정으로 이관**. auth 키(`cdg_auth*`)는 동기화 제외. `mypage` 초기화는 `DataSync.clearServer()`로 서버까지 삭제.
- **검증**: 로컬 통합 10/10(게스트→가입 이관, 기기A↔B pull, 서버 우선 재로그인, 로그아웃 중 게스트저장 서버격리, clearServer) + 프로덕션 스모크 8/8(한글값 보존·put/del/clearAll·무인증401). 검증 계정 전부 삭제(users/data/sessions=0).
- **미완**: 페이지 로드마다 실시간 pull은 미구현(로그인 시점 동기화로 커버 — 같은 세션 유지 중 타기기 변경은 재로그인 때 반영). Phase 3(AI검토)·4(결제) 남음.

### 2026-07-19 (2차) — 파비콘 자산 보강 + 도메인 방침

- **파비콘 완비**: 기존 `favicon.svg`(전 페이지 연결됨)에 더해 `apple-touch-icon.png`(180, iOS 홈화면·full-bleed), `icon-192.png`·`icon-512.png`(PWA), `favicon.ico`(16+32 멀티, 구버전·크롤러) 생성. `manifest.json` 신설(테마색 #533afd). **20개 전 HTML head에 apple-touch-icon·manifest·favicon.ico·theme-color 링크 삽입**(404.html은 `/debt-guide/` 절대경로, 나머지는 상대). sharp+png-to-ico로 SVG에서 렌더(도구는 scratchpad, 프로젝트 미포함).
- **도메인 방침(사용자 결정 2026-07-19)**: 브랜드 romanization = **`chamroad`**(API·D1과 일치). 사이트 비공개(push 보류) 중이라 **구매는 공개 시점에**. 이름만 확정, 연 비용 절감. 공개 시 `chamroad.co.kr`(또는 .kr/.com) 구매 → GitHub Pages(CNAME) + `api.chamroad.*`를 Cloudflare Worker 커스텀 도메인으로 연결(Worker가 이미 Cloudflare라 간단). 구매 시 main.js `API_BASE`도 커스텀 도메인으로 교체.

### 2026-07-19 — 백엔드 착수: Cloudflare Workers + D1 인증 API (Phase 1) ✅로컬 검증 완료

- **결정(사용자)**: 스택 = Cloudflare Workers + D1 / 결제 = 토스페이먼츠 테스트 모드로 개발(사업자등록 후 라이브 키 교체) / 순서 = 회원·인증 → (다음) 데이터 저장 → AI 검토 → 결제.
- **인프라**: D1 `chamroad` 생성(id `a3de34d7-6d87-4f7e-abc0-8a1a3c5d6827`, APAC). 프로덕션 DB에 스키마 적용 완료 — `users`(PBKDF2 해시), `sessions`(토큰 SHA-256 해시만 저장).
- **`api/` 신규(Worker 프로젝트)**: `wrangler.jsonc`(D1 바인딩) / `schema.sql` / `src/index.js` — 엔드포인트: signup·login·logout·me·change-password. 보안: PBKDF2-SHA256 10만회 + PEPPER(서버 시크릿, HMAC 프리해시), 세션토큰 32바이트(DB엔 해시만), 로그인 실패 메시지 통일(이메일 존재 노출 방지), CORS 화이트리스트(github.io + localhost 3456/5500), timingSafeEqual, 비번 변경 시 타 세션 전체 무효화. `.dev.vars`(로컬 PEPPER)는 gitignore.
- **프론트 교체**: `js/main.js` Auth 객체 → API 연동(login/signup/changePassword는 async fetch, 세션 캐시는 localStorage `cdg_auth_session`에 유지해 getSession/isLoggedIn 동기 조회 보존, logout은 로컬 즉시+서버 백그라운드). base64 비밀번호 저장(`cdg_auth_users`) 폐기. `login.html` doLogin/doSignup async 전환+중복제출 방지. **`API_BASE` 프로덕션 URL은 배포 후 교체 필요(`CHAMROAD_API_URL_TODO` 플레이스홀더)**.
- **find-account.html 전면 교체**: 이름+이메일만으로 비밀번호 재설정은 서버에선 계정탈취 경로라 제공 안 함(목업 금지 원칙) → "이메일 인증 방식 준비 중 + 문의(eslje75@gmail.com) + 로그인 상태면 마이페이지에서 변경" 정직 안내로 재작성. **mypage.html에 계정 설정 카드(비밀번호 변경 UI) 신규** — change-password API 실연동.
- **검증**: wrangler dev 로컬에서 curl 11개 시나리오(가입/중복409/오입력401/로그인/me/로그아웃/비번변경/옛비번 거부/검증400/CORS 허용·차단) + UTF-8 한글 저장 + **실제 main.js Auth 코드를 node로 로드해 로컬 API 상대 통합 테스트 7항목 전부 통과**.
- **✅ 배포 완료(2026-07-19)**: `npx wrangler login`(인증됨) → 프로덕션 PEPPER 시크릿 설정(로컬과 다른 새 값) → `npx wrangler deploy`. **엔드포인트: `https://chamroad-api.eslje75.workers.dev`**. main.js `API_BASE`를 이 URL로 교체 완료(localhost는 8787 dev로 자동 분기). 프로덕션 D1에 스키마 반영 확인(users/sessions/idx). **실배포 검증 통과**: 가입/로그인/me/틀린비번401/CORS 허용헤더/HTTPS + node fetch로 한글(홍길동) 왕복 정상 확인. 검증용 테스트 계정은 프로덕션 DB에서 삭제 정리함(현재 users 0건).
- **배포 계정**: Cloudflare `eslje75` 계정. Worker 이름 `chamroad-api`. 재배포는 `cd api && npx wrangler deploy`. 시크릿 변경은 `npx wrangler secret put PEPPER`(변경 시 기존 비밀번호 검증이 전부 깨지므로 주의 — 초기 운영 중엔 건드리지 말 것).

### 2026-07-18 (4차) — 결핍 감사 반영: 생활·불안 안내 대폭 보강 (gap-auditor 결과)

- **배경**: 결핍 감사 결과 "절차 안내는 탄탄한데 신청서 바깥의 삶(비용·노출·생계·진행 중 제약)이 비어 있음" → 우선순위 G2→G1→G3→G4 + G5~G9 FAQ 묶음으로 반영. **모든 수치·법리 공식 출처 검증**(easylaw·scourt·law.go.kr·서울회생법원 실무).
- **검증한 핵심 사실**: 회생 인지대 30,000원(전자 27,000)·송달료 1회 5,500원×채권자비례·외부 회생위원 예납금 15만(서울 등 내부위원 없음), 파산 인지대 소액·관재인 예납금 30만·소송구조 가능 / 급여압류 1/2·최저 250만 보호(민집 제246조)+압류금지채권 범위변경 신청 / 편파변제·신규대출·재산은닉 = 면책불허가·파산취소 사유 / 법원은 회사에 통지 안 함·등본/가족관계증명서에 회생파산 기록 안 남음·면책정보 신용정보원 7년 / 파산 자격제한 직업(변호사·회계사·법무사·공인중개사·보험설계사·경비원 등, 일반직원·공무원은 원칙 결격 아님)·면책 시 당연복권(제574조) / 기각 시 즉시항고 1주 불변기간·재신청·파산전환 / 해외여행 금지규정 없음(파산은 관재인 면담·기일 출석 협조) / 신용회복 시점(회생 인가후 1년 성실변제 공공정보 조기삭제, 파산 면책 5년 후 삭제).
- **G2 준비 중 금지행동 경고 박스**: rehabilitation·bankruptcy 상단(0단계 카드 다음)에 빨강 박스 — 편파변제(특히 가족·지인 먼저 갚기)·신규대출/카드·재산은닉/가족통장 급여수령 금지 + "모든 상환 멈추고 목록부터" 원칙.
- **G3 비용**: r7-4·b7-4 가이드에 검증 금액 반영(수임료 없이 법원비용만·소송구조 안내). FAQ에 상세 비용 항목.
- **G1 압류 대응**: setup 맞춤결과(압류 페르소나)에 급여 1/2·250만 보호+범위변경 신청 추가. FAQ 전용 항목.
- **resources.html FAQ 14→23개·4개 카테고리 개편**(제도 이해3/자격·비용6/진행 중 생활·상황8/결과·이후6): 신규 9문항 — 비용, 회사·가족 노출·등본 기록, 급여/통장 압류 생계, 이사, 해외여행, 이직/사업자, 파산 직업제한, 상속·재산 증가 신고, 기각 대처, 신용 회복 시점, 전자소송 접수 방법. 렌더를 카테고리 그룹으로 변경.
- 검증: 4파일 구문 통과, FAQ 23개·카테고리 4·필드 누락 0·toggle 인덱스 0~22 고유, 진행센터 항목수 37/31 유지(회귀 없음). gap-auditor 에이전트는 .claude/agents에 상주.

### 2026-07-18 (3차) — 관할법원 안내·찾기 기능 (사용자 요청)

- **질문(사용자)**: 관할법원은 등본상 주소지 기준? 이용자가 자기 관할법원을 알 수 있는 안내 필요.
- **법률 검증(공식 출처)**: 관할 = 채무자의 보통재판적(=주소, 실무상 주민등록상 주소) 소재지 관할 **지방법원 본원·회생법원 전속**(채무자회생법 제3조, easylaw 확인). 지원(支院)은 접수 안 함 — **춘천지법 강릉지원만 예외**(강릉·동해·삼척·속초·양양·고성). **2026. 3. 1. 대전·대구·광주 회생법원 개원 → 회생법원 6곳**(법률신문·각급법원설치법 개정 확인). 선택 특례: 울산·경남→부산회생, 충북→대전회생, 전북·제주→광주회생에도 신청 가능. 수원회생법원 관할 = 경기 남부 19개 시·군, **부천·김포는 인천지법**(공식 관할구역 확인).
- **구현**: `js/main.js`에 `COURT_FINDER_DATA`(17개 시·도, 경기/강원/경남은 시·군 2단계 선택) + `initCourtFinder()` 공용 컴포넌트 — 지역 선택 → 관할법원 + 선택 특례 표시 + 대법원 관할법원찾기 링크 + "주민등록상 주소 기준·전입신고 먼저" 주의. **rehabilitation·bankruptcy·setup 3개 페이지에 마운트.** r7-5·b1-2 가이드도 본원 원칙·강릉지원 예외·회생법원 6곳 기준으로 갱신.
- 검증: 매핑 데이터 node 실행 검증(17개 시·도, 신설 회생법원 3곳, 부천·김포=인천, 강릉지원, 특례 4종, 양산=울산) + 구문 검사 통과.

### 2026-07-18 (2차) — setup 질문지 모순 조합 방지 (사용자 지적)

- **지적(사용자)**: "소득 없음"인데 "재직 중 예"처럼 논리적으로 불가능한 답 조합이 가능했음.
- **수정(`setup.html`)**: 소득 형태에 따라 무관한 질문 자동 숨김 — 재직 여부(퇴직금)는 **급여소득일 때만**, 부수입은 **'소득 없음'이 아닐 때만** 표시(`syncConditionalQuestions`). 숨겨진 질문은 값도 '아니요'로 강제(collectAnswers에서 이중 방어), 필수 검증도 보이는 질문만 요구. 보이는 질문만 1부터 동적 번호 재부여(.q-card/.q-num). 프리필(프로필·무료진단) 후에도 동기화 실행 — 진단에서 퇴직금을 입력했어도 소득 없음이면 재직=아니요로 정리됨.

### 2026-07-18 — 진행센터 온보딩 개편: 맞춤 준비 진단(setup.html) + 단계 로드맵 (사용자 요청)

- **요청(사용자)**: 처음 하는 사람은 "뭐부터 어떻게?"가 안 보임 → ① 단계 안내를 버튼 형태로 만들어 단계별로 안내 ② 진단 없이 결제한 사용자도 자기 상황을 입력해 필요한 서류를 확실히 알게 하는 사전 질문지. 상호 논의로 3가지 확정: 로드맵+한 단계씩 표시 / 별도 페이지 공용 / 명칭 "맞춤 준비 진단"(AI 표현 안 씀 — 규칙 기반이라 정확성 보장, 목업 흉내 금지 방침 유지).
- **`setup.html` 신규(맞춤 준비 진단, ?type=rehab|bankrupt)**: 질문 7개(+파산 전용 2개: 폐업·2년내 처분) — 소득형태(5종)/재직(퇴직금)/보유재산(멀티)/개인간채무/담보채무/법적조치/부수입. **무료진단 데이터로 프리필**(incomeType·severanceValue·재산값·hasLegalAction·securedDebt), 기존 프로필 있으면 그것 우선. 저장 시:
  - `computeNA()` 규칙 매핑 → 서류센터(d-i1~4·d-a2·a3·a5·d-d3)와 체크리스트(r2-3·r3-2·r7-6·b3-3)에 **'해당없음' 자동 반영**(`applyNA`: 미처리 항목만 na, 이전 자동처리분 중 이번에 해당하게 된 것은 해제, 사용자가 직접 체크/처리한 것은 불가침)
  - 맞춤 결과: 요약 칩 + "준비 안 해도 되는 항목 N건" 목록 + "특히 챙길 것"(압류→중지·금지명령, 재직→퇴직금 1/2, 폐업→폐업사실증명원 등) + 진행센터/서류센터 CTA
  - `cdg_profile` 저장(autoNA 기록 포함). noindex.
- **진행센터 2종 개편(rehabilitation·bankruptcy)**: 레이아웃 = 히어로 → **0단계 카드**(프로필 없으면 "내 상황 설정하고 시작" 강조 CTA / 있으면 요약 칩+수정 링크) → 진행률 바 → **진행 단계 지도**(9/8단계 버튼 그리드: 단계명·n/m·완료✓ 초록·현재 단계 강조) → **선택된 한 단계만 체크리스트 표시**(이전/다음 단계 버튼, 진입 시 첫 미완료 단계 자동 선택) → 도구 바·작성예시(하단 이동). 체크/해당없음 시 로드맵 뱃지 실시간 갱신.
- **mypage**: 초기화 목록에 'profile' 추가. CLAUDE.md 페이지 표에 setup.html 추가.
- **검증(DOM 실행)**: setup NA 매핑·재적용 시나리오(압류 없음→있음 변경 시 r7-6 자동 해제, 사용자 체크 r2-1 유지), rehab 로드맵 9버튼·첫 미완료 단계 자동 선택·gotoStep 이동·체크 저장·진행률 4/37·전 항목 처리 시 100%, bankrupt 8버튼·CTA·이동 — 전부 통과. 구문 검사 4파일 통과.

### 2026-07-17 (5차) — 진행센터 ↔ 서류 체크센터 역할 분리 (중복 축소, 사용자 지적)

- **지적(사용자)**: 셀프 진행센터의 1~4단계(기본정보·채권자·소득·재산 준비)와 서류 체크센터가 같은 서류를 두 번 체크하는 것처럼 중복돼 보임. 왜 두 화면인지·구분이 필요한지 질문 → 상호 검토 후 "역할 분리 + 중복 축소" 방향 채택.
- **정리 원칙**: **서류 발급은 서류 체크센터가 전담**(발급처·공식 양식·발급 방법), **진행센터는 전체 절차 순서 + 서류센터에 없는 진행 고유 작업**(채권자 목록 작성, 소득 산정, 청산가치 계산, 진술서 경위, 서명 준비 등)만 담당. 진행센터의 순수 서류-발급 항목은 "서류 체크센터에서 발급·체크" 위임 항목으로 대체.
- **`rehabilitation.html`(회생 45→37항목)**: 1단계=기본정보&서류준비(발급 위임+주소최신화+서명, 3), 2단계=채권자 목록 작성(목록·금액발생일·개인간채무, 3), 3단계=소득 정리(평균산정+근로외소득, 2), 4단계=재산·청산가치 정리(재산목록화+청산가치계산, 2). 5~9단계(수입지출/작성/최종체크/보정/면책)는 그대로. 각 위임 항목에 documents.html 링크.
- **`bankruptcy.html`(파산 36→31항목)**: 1단계=기본정보&서류준비(발급 위임+관할, 2), 2단계=채무 발생 경위(진술서, 4 — 진행 고유라 그대로), 3단계=채권자 목록 작성(4, b3-2만 발급 위임으로 문구 조정), 4단계=재산·처분이력(재산목록화+처분이력, 2). 5~8단계 그대로.
- **`mypage.html`**: 진행률 분모 회생 45→37, 파산 36→31로 갱신(REHAB_STEPS/BANKRUPT_STEPS 실제 항목 수와 일치). 진행센터 상단 도구 안내 문구를 "이 체크리스트=진행 순서 / 서류 발급=서류 체크센터"로 역할 명시(회생·파산 양쪽).
- **검증**: node로 항목 수 실측(회생 37=3+3+2+2+4+6+6+7+4, 파산 31=2+4+4+2+4+5+5+5, mypage 공식과 일치·id 중복 0), 회생 진행센터 인라인 스크립트 DOM 실행(필수 29 체크 78% → 선택 8 해당없음 100% 도달 → 해제 시 복귀, 정상), 3개 파일 구문 검사 통과, #example-library 크로스링크 유지, 삭제 항목 id 외부 참조 0건. 김○민 청산가치 예시(10,260,000)·박○수 합계(87,400,000) 일관성 유지.

### 2026-07-17 (4차) — '전체 서류/유형 보기' 토글 제거 (사용자 지적)

- **지적(사용자)**: 서류 체크센터에서 회생 패키지 이용 중인데 '전체 서류 보기'로 파산 서류까지 볼 수 있는 건 의미 없고 혼란만 줌 → 구매한 절차 서류만 보이면 됨.
- **`documents.html`**: '전체 서류 보기' 토글 버튼·`showAll` 상태·`toggleShowAll()` 제거. `visibleDocs()`는 구매 절차 기준으로만 필터, `applyPkgUI()`는 반대 절차 탭·양식 링크를 항상 숨김. 안내 문구(💡 ○○ 패키지 이용 중)는 유지.
- **`ai-review.html`**: 동일한 '전체 유형 보기' 토글도 같은 이유로 제거(`showAllGroups`·`toggleShowAllGroups()`), `visibleGroups()`/`applyPkgNotice()` 단순화. 보정 패키지(correction-*)는 기존대로 보정·소명 + 해당 절차 유형 표시.
- **미구매 사용자**: 기존과 동일하게 전체 표시(안내 배너 없음) — PROC_FILTER null 분기 유지.
- 구문 검사 통과, 잔존 참조 0건.

### 2026-07-17 (3차) — 플로팅 '임시저장' 버튼 추가 (사용자 요청)

- **요청(사용자)**: 셀프 진행 중 스크롤 어느 위치에서든 임시저장 버튼이 보였으면 함(준비할 게 많아 수시 저장 필요).
- **구현**: `js/main.js`에 `initTempSave(getStatus)` 공통 함수 신설 — 화면 **좌하단 고정** 플로팅 pill 버튼(스크롤탑=우하단·토스트=우하단과 비겹침). 클릭 시 저장 확인 → 초록 "저장됨"으로 1.8초 전환 + 토스트 `진행 상황을 저장했습니다 · N/M 처리 완료` + 마지막 저장 시각(HH:MM) 버튼에 표시. `css/styles.css`에 `#temp-save-btn` 스타일(+모바일).
- **연결**: 체크리스트 5개 페이지(`rehabilitation`·`bankruptcy`·`maintenance`·`supplement`·`documents`)가 초기 렌더 직후 `initTempSave(() => countProgress(...))` 호출(documents는 visibleDocs/docHandled 기준). 비체크리스트 페이지에는 미노출.
- **참고**: 체크리스트는 이미 항목 변경 시 자동 저장되므로 이 버튼은 **저장 확인·안심 + 저장 시각 표시** 용도(데이터 유실 없음). DOM 실행 테스트로 버튼 생성·클릭·토스트·시각 기록 확인, 6개 파일 구문 검사 통과.

### 2026-07-17 (2차) — 진행률 '해당없음' 처리 도입 (조건부 항목 100% 도달 불가 버그, 사용자 지적)

- **문제(사용자)**: 셀프 진행센터 체크리스트에서 근로소득자는 '자영업자 소득증명', 무주택자는 '부동산 등기' 같은 **'해당 시 필수' 항목을 체크할 수 없는데** 진행률 분모(전체 항목)에는 포함돼 **아무리 준비해도 100%에 도달 불가**. 재산자료 등 해당 없는 항목도 동일.
- **해결**: 필수가 아닌 항목('해당 시 필수/권장', '권장')에 **"해당없음" 버튼** 추가 → 저장값을 `true`(체크)/`'na'`(해당없음) **3-state**로 확장. 체크 또는 해당없음이면 '처리 완료'로 진행률에 카운트. 기존 데이터(`true`)와 호환. '해당없음' 처리 시 체크박스 비활성+뱃지 표시.
- **적용 파일**: 체크리스트 4종(`rehabilitation`·`bankruptcy`·`maintenance`·`supplement`) — `isOptional`/`stateOf`/`countProgress`/`toggleNA` 공통 패턴. `documents.html` — 조건부 서류 10종(부동산 등기·차량·임대차·자영업 소득증명·근로 소득자료·면책신청서 별도·대출약정서·압류서류)에 `opt: true` 부여 후 동일 처리, 카운트 라벨 '준비 완료'→'처리 완료'. `styles.css`에 `.checklist-item.na` 스타일. `mypage.html`: `calcProgress`가 `true||'na'` 카운트(진행률 카드 100% 도달 가능), `calcChecks`(서류 '준비 완료' 수)는 `=== true`만(해당없음 제외). 각 페이지 안내문에 "해당없음 버튼으로 넘기면 진행률에 반영" 문구.
- **조건부 항목 수**: rehabilitation 12(선택)/45, bankruptcy 5/36, maintenance 3/16, supplement 5/13, documents 10(opt)/30.
- **검증**: rehabilitation 인라인 스크립트를 실제 DOM 스텁으로 구동 — 필수 33개만 체크 시 **73%에서 멈춤(버그 재현)** → 선택 12개 해당없음 처리 시 **100%(45/45) 도달** 확인, 해당없음 해제 시 98%로 복귀·저장값 삭제 정상. 전 페이지 100% 도달 시뮬(node)·6개 파일 구문 검사 통과.

### 2026-07-17 — 2차 검증(일관성·법률 전수·UI 무결성) 완주 + 확정 14건 수정

- **2차 워크플로우 완주(에이전트 11개, 오류 0)**: 탐색 3종(일관성/법률 전수/UI 무결성) → P0/P1 8건 반박 검증. 결과: CONFIRMED 6(중복 제외 5) / **REFUTED 2** / P2 10. 법조문 20여 개·수치·링크·핸들러·Storage 키 등 40+ 항목 "이상 없음" 전수 확인(2026 기준중위소득 고시값, 압류금지 250만원 시행령, 예납금 30만원 실무준칙 등 공식 출처 대조 포함).
- **반박 검증이 걸러낸 오탐 2건(수정 안 함)**: ① 제619조 변제계획 변경에 사정변경 요구는 '실무 관행'이라는 지적 → 대법원 2018마6364 결정상 법적 요건으로 확립 — 현재 문구가 정확 ② 신복위 "최대 10년" 지적 → 프리워크아웃·신속채무조정은 일반 채무자도 10년이라 기관 카드 기준 현행 표기가 맞음. **반박 없이 고쳤다면 오히려 부정확해졌을 사례.**
- **P1 수정 5건**: ① result.html 조문표기 — "제579조(변제계획 인가 요건)" → 제579조 4호(가용소득)+제614조(변제계획의 인부) 2줄로 정정 ② rehabilitation r8-2 및 8단계 예시 제목 '보정명령'→'보정권고'(회생 트랙 용어 통일) ③ **about.html footer-placeholder 누락** — 법률대리 아님 고지·약관 링크가 이 페이지만 미렌더였음(+toast div 추가) ④ pricing 오구매 confirm이 원점수만 봐서 하드블록(신청불가) 사용자를 놓침 → rehab-full/bankrupt-full에 한해 diagnosis_data 기반 하드블록 전용 confirm 우선 표시(maintain·correction은 진행 중 고객이므로 제외 — 반박자 지적 반영) ⑤ mypage 소액 채무(1,000만 미만) 분기 — result와 동일하게 배지·변제액 숨기고 신복위 안내로 대체.
- **P2 수정 9건**: plan_type도 plan_package와 동일 보호(추가옵션 구매 시 미덮어쓰기), mypage 진행률 카드를 plan_packages 트랙 기준으로(양쪽 보유 시 둘 다 표시), compare 회생 기간 "신청~인가 통상 5~9개월"로 pricing 산식과 정합, 신복위 해제조건 "완제 시 즉시·1년 이상 성실상환 시"(ccrs 확인), index 수임료 비단정화, submitDiagnosis 전 단계 재검증(goStep 우회 차단), 죽은 코드 제거(updateProgress, toggleAllSections/expand-all 참조), mypage initPage('home')→('mypage') 네비 하이라이트 수정, maintenance 김○민 사례 날짜 플레이스홀더화(시간선 모순 해소).
- **P2 잔여(선택)**: maintenance 제619조에 판례 병기, compare 신복위 10년에 제도별 주석 — 반박자 의견상 현행도 정확해 보류.
- 검증: 페르소나 계산 재현·전 수정 파일 인라인 스크립트 구문검사·45/36 항목 대조 통과. **커밋 완료, push 보류 유지.**

### 2026-07-13 — 상호반박 검증 에이전트 구축 + E2E 감사 발견 17건 수정

- **검증 에이전트 4종 신설(`.claude/agents/`)**: `legal-verifier`(법률 공식출처 검증), `adversarial-refuter`(반박 교차검증), `journey-auditor`(페르소나 E2E 감사), `consistency-checker`(페이지 간 일관성). 법률 콘텐츠 수정 후 워크플로우: legal-verifier → adversarial-refuter 순.
- **E2E 감사(회생·파산 신청자 페르소나 2종, 워크플로우 + 인라인 반박검증)** — 19건 발견, 17건 수정:
  - **P0 법률 문구(`result.html` 3곳 + 1곳)**: "회생 인가 후 추심·압류 자동 중지"(130행) / "신청 즉시 보전처분으로 강제집행 중단"(525·877행) → **중지·금지명령(제593조) + 개시결정 시 자동 중지(제600조)** 기준으로 정정 (법령 재검증 완료. 보전처분(제592조)은 채무자 재산처분 금지라 오용이었음). "이자 신청 시점부터 동결"→개시결정 후 이자 후순위(기검증 제581조→제446조 준용)로 정정.
  - **파산관재인 안내(`result.html` buildTrusteeWarning)**: "동시폐지가 가장 일반적" 삭제 — **현행 실무는 관재인 선임이 원칙**(easylaw·서울회생법원 실무준칙 361호 확인, 예납금 원칙 30만원 명시). 재산 500만 미만 숨김 조건 제거 → 파산 우세면 항상 표시(무재산자도 예납금 대비 필요).
  - **진단 재산 입력에 '예상 퇴직금' 필드 신설**: 절반만 청산가치 산입(`diagnosis.js` severanceHalf) — 재산목록 예시(퇴직금 1/2)와 진단 계산 일치. `debtTypes` 데드코드 제거. 연체기간 선택지 경계 명확화("1개월 이상~3개월 미만" 식). 생활비 "별도 처리" 오해 문구 수정.
  - **pricing 패키지 저장 개선**: `plan_packages` 배열 누적 저장 + 완주 패키지 보유 중 보정 추가옵션 구매 시 `plan_package` 덮어쓰기 방지. **진단 결과와 반대 절차 패키지 구매 시 confirm 경고**(오구매 방지). mypage는 보유 패키지 전체 뱃지+바로가기 표시, 초기화 목록에 `ai_reviewed`·`plan_packages` 추가.
  - **rehabilitation 3항목 신설(42→45)**: r7-6 중지·금지명령 신청(제593·600조), r8-6 회생위원 면담 준비, r8-7 개인회생채권자집회 — pricing 패키지 구성 약속("회생위원 면담 준비 체크리스트"·"채권자집회 대응")과 실내용 일치화. mypage 진행률 공식 45로 갱신.
  - **bankruptcy 3단계 채권자목록 예시**: 박○수 사례 누락 2건(◇◇카드 700만·김○○ 1,000만) 추가, 합계 87,400,000원 명시.
  - **mypage**: 파산 우세 사용자에게 "개인회생 월 변제액 0원" 노출 차단(scores 비교 분기, 파산용 요약으로 대체), AI 기록 라벨 우선순위 `r.label` 우선으로 수정.
  - **ai-review**: 필수 키워드 동의어 지원(`이름|성명|신청인` 등 9개 규칙 확장) — "성명" 표기 정상 문서 오탐 제거.
  - **기간 통일**: 파산 "신청~면책 통상 6개월~1년"으로 pricing(2곳)·compare(2곳) 통일 (기존 6~8개월 vs 선고~면책 6개월~1년 모순).
  - **result.html**: 점수 30 미만 카드의 "유리한 점" 제목을 "여건이 갖춰지면 유리해질 수 있는 점"으로 조건부화(무소득자 자기모순 해소), diagnosis_repay 부분 누락 시 재진단 안내 가드.
- **검증**: node로 페르소나 2종 계산 재현(회생 84점·1,061,457원 = 김○민 예시와 일치 / 파산 100점·0원), 수정 8개 파일 인라인 스크립트 구문 검사 통과, 체크리스트 45/36 유일 id 확인, 가격·패키지 키·Storage 키 쓰기/읽기 짝 전수 대조 이상 없음.
- **미수정(보류·로드맵行)**: 유료 페이지 접근 게이트(체험 단계 의도 여부 사용자 결정 필요), "서류검토AI 2회" 횟수 제한(백엔드에서 구현), 파산 우세→회생 패키지 구매 UI 차단 강화.
- **미커밋분 포함 스냅샷 커밋**(push는 계속 보류 — 백엔드 완성 후 일괄 공개 방침 유지).

### 2026-07-07 (5차) — 사이트 전역 가독성 개선 (사용자 지적: 회색 글씨 작고 연함)

- **원인 3가지를 `css/styles.css`에서 전역 처리** (개별 HTML 수정 없이 기존 Tailwind 오버라이드 패턴 사용):
  - ① body `font-weight` 300(Light)→**400** — 작은 한글이 얇게 렌더되던 근본 원인.
  - ② 회색 한 단계씩 진하게: `.text-slate-400` #94a3b8(대비 2.9:1 WCAG 미달)→**#64748b**(4.8:1), `.text-slate-500`→**#475569**(7.1:1), `--c-ink-mute`(푸터 링크·badge-low 등)→#475569. **다크 배경 예외**: `.bg-slate-700/800`, `.hero-gradient` 내부의 slate-400/500은 #b6c2d4로 밝게 유지(mypage 다크 히어로 확인).
  - ③ 최소 글자 상향: `.text-xs` 12→**13px**(LH 1.25rem, `leading-relaxed` 등 명시 행간은 재선언으로 유지), `.text-[11px]` 11→**12px**, 모바일 compare-table 12→13px.
- `result.html` 흰 배경 위 `text-slate-300` 문구 1곳(정성 비교 안내)은 slate-400/500으로 개별 수정 — slate-300 자체는 다크 히어로 전용이라 전역 미변경.
- 브라우저 검증(computed style 실측): documents(11px 안내문 12px·#64748b), mypage 다크 히어로 밝기 유지, rehabilitation 작성예시 모달 표 13px·오버플로 없음, index 저대비 잔존 스캔 0건(감지 1건은 인디고 그라디언트 위 blue-200 = 정상), 푸터 링크 #475569, 콘솔 에러 0. preview_screenshot은 여전히 타임아웃 플레이크.
- **미커밋·미배포 유지.**

### 2026-07-07 (4차) — 작성예시 내용 검증 Q&A + 도구 모음 중복 버튼 제거

- **사용자 질문 3건(작성예시 정확성) 공식 출처 재검증 — 콘텐츠 수정 불요 확인**: ① 채권자목록 원금/이자 구분 기재는 실제 양식 그대로(법률구조공단 "채권현재액(원금, 이자)" 기재요령 + 개시결정 후 이자는 후순위개인회생채권이라 구분 필요 — 제581조2항→제446조 준용) ② 수입 목록 "세후 월평균" = 최근 1년 세후 소득(상여 포함)÷12가 원칙, 직장 변동 시 변동 후 소득만(가용소득 공제 항목은 제579조4호) ③ 대출금 사용내역서는 법정 제출 서류가 아니라 보정권고 대응용 실무 문서 — 서류 체크센터(공식 발급 서류 목록)에 없는 것이 의도된 구성.
- **도구 모음 "실전 (서류) 작성예시" 버튼 제거(사용자 지적 — 바로 아래 섹션과 중복)**: `rehabilitation/bankruptcy/maintenance/supplement.html` 4개 파일 모두 `#example-library` 앵커 버튼 삭제, 그리드 `sm:grid-cols-3`→`2`. maintenance/supplement는 도구 설명 문구도 2종 기준으로 조정("예시는 바로 아래" 안내 추가). `#example-library` 섹션·id는 유지 — ai-review.html의 크로스 페이지 링크(`rehabilitation.html#example-library` 등) 정상 동작.
- 브라우저 검증: 4페이지 도구 카드 2개·grid-cols-2·작성예시 섹션과 버튼 6종 전부 유지, 콘솔 에러 0. (preview_screenshot은 이번에도 타임아웃 플레이크 — snapshot/eval로 확인)
- **미커밋·미배포 유지.**

### 2026-07-07 (3차) — 구매 패키지 기준 공용 페이지 필터링 (사용자 지적 반영)

- **문제(사용자)**: 회생 패키지를 샀는데 서류 체크센터·AI 검토에 파산 내용까지 다 보여서 혼란 — 결제한 서비스에 맞는 내용만 나오게.
- **방식**: 구매 시 저장되는 `plan_package`(rehab-full/maintain/correction-rehab → 회생, bankrupt-full/correction-bankrupt → 파산)를 읽어 필터. **미구매면 기존처럼 전체 표시(안내 배너 없음)**. 필터 중에는 상단에 "💡 ○○ 패키지 이용 중 — △△ 서류만 표시" 배너 + **"전체 서류 보기" 토글**(막다른 화면 방지, 토글 왕복 가능).
- **`documents.html`**: 반대 절차의 탭 버튼·서류(회생 필터 시 파산 8종 제외 → 22개 / 파산 필터 시 회생 6종 제외 → 24개)·상단 법원 양식 링크(`data-proc` 속성으로 회생/파산 구분) 숨김. `visibleDocs()`가 renderDocs/toggleDoc의 기준 목록. 숨긴 탭이 현재 탭이면 '전체'로 복귀.
- **`ai-review.html`**: `visibleGroups()`로 서류 유형 그룹 필터 — 완주 패키지는 해당 절차 세트 + 보정·소명(8종), **보정 추가 대응은 보정·소명 그룹을 맨 앞 + 기본 선택 '보정서'**. 기본 선택 유형도 패키지 기준(`defaultType()`).
- **`supplement.html`**: `?type` 파라미터 없이 열리면 구매 패키지 기준으로 회생/파산 기본 유형 결정 (기존: 무조건 rehab).
- 브라우저 검증(패키지 시드 주입→정리): rehab-full(문서 22개·파산 탭/링크 숨김·토글 30↔22, AI 검토 8종·기본 회생신청서), bankrupt-full(24개·회생 숨김·파산 링크만), correction-bankrupt(AI 검토 보정 그룹 첫번째·기본 보정서, supplement 무파라미터→보정명령·박○수 예시), 미구매(30개·14종·배너 없음·탭 4링크 전부) — **콘솔 에러 0**.
- **미커밋·미배포 유지.**

### 2026-07-07 (2차) — 보정·변제기간 센터 상세화 + AI 검토 서류 14종 확장 (사용자 지적 반영)

- **`supplement.html`(보정 대응 센터)**: 13개 전 항목에 "자세히" 패널(필수 뱃지+구체 방법+링크+항목별 예시), 도구 3종(서류센터/실전예시/AI검토) 상단 배치, **실전 작성예시 3종 신설(보정서/소명서/사용내역표)** — `?type`별로 김○민(회생·개회 사건번호)/박○수(파산·부호 미표기) 사례 분기, 수치는 각 진행센터 페르소나와 동일(회생 대출 700만 소명 / 파산 인출 300만 소명). 기한 문구 "통상 2~4주" → **"보정서 기재 기한 기준(통상 1~2주, 더 길게 주어지기도)"**로 rehabilitation.html과 통일. 모달 2xl+HTML 표 지원. 항목 수·저장 키 불변.
- **`maintenance.html`(변제기간 관리 센터)**: 16개 전 항목 "자세히" 패널, 도구 상단 배치, **실전 작성예시 3종 신설** — ① 변제계획 변경신청(이직 소득 260→190만, 월 변제 1,061,457→361,457 변경 전후 표) ② 소득 변동 증빙 정리표(상황별 서류·발급처) ③ 특별면책 신청 서면(제624조2항 3요건 구조 — 기납입 25,474,968원(24회) ≥ 청산가치 10,260,000원, 회생 페르소나와 수치 일관). 7/6 기검증 법조문 문구(619조·611조5항·624조2항)는 그대로 유지.
- **`ai-review.html`(사용자 질문: "6종이면 충분한가, 전체 업로드가 낫지 않나")**: 검토 서류 **6종 → 14종** (회생 세트 6 + 파산 세트 6 + 보정서·소명서 2) — 셀프 진행센터의 제출 서류 세트와 1:1 대응. **그룹별 "N/6 검토함" 추적**(`ai_reviewed` 키, ✓뱃지·미검토 서류 목록 표시), 유형별 실전 작성예시 링크(입력창 상단 + 결과 팁), "다른 서류 이어서 검토" 버튼. 검토 기록에 label 저장 + `mypage.html` 라벨 폴백(`DOC_LABELS[k]||r.label||k`) 1줄 수정. **파일 업로드(HWP/PDF)+실제 AI 피드백은 백엔드 도입 시 구현으로 명시**(목업으로 흉내 내지 않음 — 7/2 점검 19번 이슈 방침 유지). 유형 키는 기존 6종 값 보존(기록 호환), 신규만 추가.
- 브라우저 검증: supplement rehab/bankrupt 분기(문구·예시·저장 키 분리·토글 양방향), maintenance 수치·모달, ai-review 검토 실행→키워드 확인·팁·세트 현황 1/6·뱃지·선택 유지·기록 라벨, mypage 라벨 표시 — **콘솔 에러 0**. (supplement 체크 테스트에서 "저장 안 됨"처럼 보인 것은 7/6 테스트 잔존 체크가 남아 있던 것 — 실제 토글·저장 정상)
- **미커밋·미배포 유지.**

### 2026-07-07 — 회생·파산 체크리스트 "체크만 하는 서비스" 문제 개선 (사용자 지적 반영)

- **문제(사용자)**: 유료 진행 페이지가 체크박스+한 줄 힌트뿐이라 처음 하는 사람이 못 따라감. 작성예시가 단계당 1개뿐, AI검토·서류센터 링크는 맨 아래, 법원 양식 다운로드 연결 없음, 진짜 제출물처럼 보이는 예시 부재.
- **`rehabilitation.html` / `bankruptcy.html` 전면 개편**:
  - 도구 바로가기(서류 체크센터·실전 작성예시·AI 검토)를 **최상단**으로 이동 (기존 하단 박스 제거)
  - **42/36개 전 항목에 "자세히" 접이식 패널**: 필수 여부 뱃지(필수/해당 시 필수/권장) + 구체적 발급·작성 방법 + 공식 사이트 링크(크레딧포유·어카운트인포·온크레딧·내보험찾아줌·정부24·efamily·홈택스·등기소·고용24·법원 양식) + 일부 항목별 예시 모달. **항목 수·id 불변 → mypage 진행률 42/36 그대로(브라우저 확인)**
  - **"실전 서류 작성예시" 라이브러리 신설**: 가상 인물 완성형 문서 각 6종. 회생(김○민 38세 1인가구): 채권자목록/재산목록/수입지출목록/진술서/변제계획안/대출금 사용내역서(보정 대비). 파산(박○수 52세 코로나 폐업): 동시신청서/진술서/채권자목록/재산목록/현재의 생활상황/수입지출목록. HTML 표 렌더용 `showDocExample`(innerHTML, 정적 콘텐츠) 추가, 모달 max-w-lg→2xl. 모든 예시에 "가상 사례·법원 최신 양식 기준" 경고 배너.
  - **수치 일관성**: 회생 예시 실수령 260만 − 1인 표준생계비 1,538,543(diagnosis.js `getStandardLiving`과 동일) = 가용 1,061,457 × 36회 = 38,212,452 ≥ 청산가치 10,260,000(퇴직금 1/2 포함), 채권 4건 합 46,700,000·안분율 합 100%. 파산 예시 채무 6건 합 87,400,000.
- **`documents.html`**: 상단 "법원 공식 양식 내려받기" 카드(서울회생법원 양식모음 회생 gubun=25/파산 gubun=24 — 페이지 실존 확인, 전자소송포털 PSPA17M01/02). 서류 20→**30개**(회생 양식 6종 세트 완성, 파산 동시신청 세트+주민등록초본, 혼인관계증명서 추가). 각 서류에 발급 단계(issue)·유의사항(note)·공식 링크 → "발급 방법" 모달 + 행 내 "공식 사이트 ↗" 버튼. **id는 의미 기준 유지**(d-r2=변제계획안·d-r3=수입지출 예전 체크 호환 — 시드 테스트로 확인, 신규는 d-r4~6/d-b4~8/d-c4). 부정확했던 '보험다모아'(비교공시 사이트) → **'내보험찾아줌'(cont.insure.or.kr)** 정정. 기존 등본 "3개월 이내" → 회생은 "신청일 기준 2개월 이내 원칙"으로 정정(easylaw).
- **법률 검증(공식 출처, 작성 전 전부 확인)**: easylaw(회생 신청서 기재사항·첨부서류·관공서 서류 2개월, 파산 진술서 구성: 학력경력→생활상황→파산에 이르게 된 사정→과거 면책 이력), 대한법률구조공단 resu.klac.or.kr(채권자목록 원금/이자 구분·재산목록 담보공제 실가치·변제계획안 4원칙), 제610조(변제계획안 신청일부터 14일, 실무 동시제출)·제611조(3년 원칙, 제614조1항4호 청산가치 등 특별사정 시 5년), 전자민원센터(파산 동시신청 = 신청서+진술서+4서면, 주민등록**초본**(주소변동 포함)·혼인관계증명서(이혼 포함), 부본 1부 / 회생 채권자목록 부본 = 채권자 수+2), 제566조 악의 누락 채권 비면책(7/6 기검증 재사용), 크레딧포유·어카운트인포·온크레딧·내보험찾아줌 실존·용도 확인. **대출금 사용내역서는 법정 양식이 아니라 보정 실무 관행으로 소개**(민간 출처 복수 일치, 단정 표현 회피 — "요구하는 경우가 많음").
- **브라우저 검증**: 3페이지 렌더·자세히 토글·모달(텍스트/HTML 표 양쪽)·체크 저장/복구·파산탭 신규 서류·문서 id 중복 0·mypage 42/36·진단/결과 스모크 — **콘솔 에러 0**. preview_screenshot은 이번에도 백지/타임아웃 플레이크(페이지 문제 아님 — inspect로 모달 렌더 실측 확인).
- **미커밋·미배포 유지** (백엔드 완성 후 일괄 공개 방침).

### 2026-07-06 (2차) — 패키지별 구매 후 전용 페이지 분리 (사용자 지적 반영)

- **문제**: 어떤 패키지를 사도 `plan='premium'`만 저장되고 회생/파산 전체 안내 페이지로 이동 → 변제기간 관리(29,000)·보정 추가 대응(19,000) 구매자가 회생 완주 패키지와 동일한 화면을 봄.
- **신규 페이지 2개**:
  - `maintenance.html` (변제기간 관리 센터) — 패키지 구성 그대로 가이드 3개: ① 변제계획 변경 신청(제619조, 변제기간 한도 3년·특별사정 시 5년은 제611조 5항) ② 실직·소득 변동 대처 ③ 특별면책 신청(제624조 **2항** — 3요건, 개인회생절차 종료 전 서면 신청). 체크리스트 저장 키 `maintain_checks`. **법률 근거 전부 공식 출처 검증**: easylaw.go.kr 면책결정 페이지(특별면책은 2항, 3항은 불허사유 — 첫 검색 요약이 3항이라 했으나 easylaw로 정정), law.go.kr 검색(619조·611조5항·2017 개정 경과), 서울회생법원 변제계획변경·특별면책 소개자료.
  - `supplement.html` (보정 대응 센터) — `?type=rehab`(보정권고)/`?type=bankrupt`(보정명령)로 용어·문구 분기. 3단계(내용 확인→자료 준비→보정서 작성·제출) 체크리스트, 저장 키 `supplement_checks_rehab`/`_bankrupt` 분리.
- **`pricing.html`**: `mockPayment`에 패키지 키 3번째 인자 추가(rehab-full/maintain/correction-rehab/bankrupt-full/correction-bankrupt). `mockComplete`가 `plan_package`·`plan_package_name` 추가 저장 + `PACKAGE_DEST` 맵으로 패키지별 이동.
- **`mypage.html`**: 결제 상태에 "프리미엄" 대신 **구매한 패키지명 + 전용 안내 바로가기 링크** 표시. 데이터 초기화 목록에 신규 키 5개 추가.
- 브라우저 검증: 변제기간 패키지 구매→maintenance.html 이동, 보정권고 구매→supplement.html?type=rehab 이동, type별 용어 분기(보정권고를/보정명령을 조사 포함), 체크리스트 저장·진행률, 마이페이지 패키지명·링크 — 전부 확인, 콘솔 에러 0건. (preview_screenshot 도구만 타임아웃 — 페이지 문제 아님, eval/snapshot으로 검증)
- **미커밋·미배포** (백엔드 완성 후 일괄 공개 방침 유지).

### 2026-07-06 — compare 장단점 정확성 보완 + FAQ 4건 추가 (전부 공식 출처 검증)

- **compare.html 장단점 문구 수정(사용자 지적 반영)**: ① 개인회생 단점 "소득 대부분을 변제에 사용"(과장) → "생계비를 제외한 소득(가용소득)을 변제에 사용". ② 신복위 장점 "재산 처분·자격 제한 없음" → "재산 처분 없음, 공무원 등 **법적** 자격 제한 없음"으로 범위 명확화. ③ 신용정보 등록에 따른 카드·대출 제한을 **3개 카드 모두** 단점에 추가(한쪽만 쓰면 오독): 신복위(완제 또는 1년 경과 시 해제·ccrs 확인), 개인회생(변제 중 카드 발급 제한, 1년 이상 성실상환 시 조기 해제 가능 — 2025-07-18 제도), 파산(면책 후 5년 경과 시 해제·ccrs 비교표).
- **resources.html FAQ 10→14개**: 신규 4건 모두 공식 출처 검증 후 작성 — ① 개인회생 채무 한도(담보 15억/무담보 10억, 제579조 — easylaw 신청자격 페이지) ② 비면책채권(세금·벌금·고의 불법행위 손배·양육비 등 + 회생은 목록 미기재/파산은 악의 누락 채권 비면책, 제566조·제625조 — easylaw 면책의 효력) ③ 가족·보증인 영향(면책은 본인만, 보증인 의무 존속, 제567조·제625조) ④ 재신청 제한(파산 면책 후 7년·제564조1항4호 / 회생 신청 전 5년 내 면책 시 기각·제595조5호 — 7/2 기검증). 브라우저 검증: 14개 렌더·토글 정상·콘솔 에러 0.
- 여전히 **미커밋·미배포** (백엔드 완성 후 일괄 공개 방침 유지).

### 2026-07-04 (추가) — compare.html 카드에 장단점 섹션 추가

각 제도 카드(신용회복위원회/개인회생/개인파산·면책)의 기존 사실 나열(채무처리·기간·신청기관·추심중단)만으로는 지식 없는 사용자가 유불리를 판단하기 어렵다는 지적(사용자) → 카드마다 CTA 버튼 위에 **👍 장점 / 👎 단점** 박스(초록/빨강) 2~3개 불릿씩 추가.

**법률 근거 확인(공식 출처):**
- 신복위 원금감면 폭 "0~70%"(협약 가입 금융회사 한정, 채권사 동의 필요) — ccrs.or.kr 채무조정제도 비교 페이지(MENU_ID=490) 및 개인워크아웃 안내(MENU_ID=1930) 확인
- 개인회생 청산가치 보장(재산 가치만큼만 변제 반영, 재산 유지 가능)은 기존 diagnosis.js 로직과 일치
- 개인파산 면책 전 공무원 임용 등 자격 제한(복권 전까지) — easylaw.go.kr 확인. 개인회생은 파산선고 자체가 없어 해당 없음

**구현:** 각 카드의 마지막 사실 블록(추심·독촉 중단) 다음, CTA `<a>` 앞에 `border-t` 구분선 + `bg-green-50`/`bg-red-50` 박스 삽입. 전역 CSS(`styles.css:35`)에 이미 `word-break:keep-all` 적용돼 있어 개별 클래스 생략. 데스크톱 3열·모바일 1열 모두 브라우저 검증(스크린샷), 콘솔 에러 0건.

### 2026-07-04 — 배포 전 정비 + 전략 결정(백엔드 먼저 완성 후 일괄 공개)

배포 전 정비를 진행하다가 **전략을 "백엔드까지 완성해 한 번에 공개"로 결정**(사용자). 이에 따라 공개 배포는 **보류**하고, 로컬에서 백엔드 개발·전체 플로우 테스트를 이어감. **관리자 페이지는 백엔드 도입 시 함께**(지금 불필요 — 정적 사이트라 관리할 서버 데이터 없음, GitHub Pages 공개 노출·보안 무의미). 참고 결정: 문의는 이메일(eslje75@gmail.com) / **개인 운영(사업자등록 전)**.

**결제 버튼 — 비활성화했다가 원복(현재 mock 결제 유지):**
- 처음엔 공개 배포 대비로 구매 버튼 5개를 "출시 예정"으로 비활성화하고 `mockPayment/closePay/mockComplete`·`#pay-modal`을 제거했으나, **백엔드 먼저 완성 전략으로 바뀌면서 `git checkout`으로 원복**. 이유: 결제 후 premium 플로우(서류검토 등)를 로컬에서 계속 테스트해야 함. premium은 `localStorage cdg_plan='premium'` 플래그 하나로 열리므로, mock 결제 또는 콘솔에서 `localStorage.setItem('cdg_plan','premium')`로 재현 가능. 원복 후 SEO 메타 블록만 다시 얹음.
- **미배포 전제라 약관/개인정보처리방침·SEO는 "초안"으로 보유** — 공개 시점에 백엔드 반영해 확정.

**법률 표현 정정(`compare.html`) — 유지(정확성 수정이라 롤백 안 함):**
- "파산선고와 동시에 **모든** 추심 즉시 중단"(과장) → "파산선고 시 재산에 대한 강제집행 효력 상실, **면책결정으로 채무 최종 소멸**". 근거: easylaw.go.kr, 채무자회생법 제348조. 신복위 "신청 다음 날 추심중단"(line 60)·개인회생 금지명령 셀(line 104)은 공식 확인 결과 정확 → 유지.

**작성해 둔 초안·자산(미배포, 백엔드 완성 시 재검토):**
- **이용약관·개인정보처리방침 신설(`terms.html`, `privacy.html`)**: 개인 운영 기준 작성. 핵심 — 백엔드 없는 정적 사이트라 회원가입·진단 입력값은 **이용자 브라우저(localStorage)에만 저장, 운영자 서버 수집 없음**을 명시(실제 수집 개인정보는 이메일 문의분에 한정). 푸터(`js/main.js renderFooter`)에 약관·방침·소개·문의 링크 줄 추가 → 전 페이지 자동 노출. **⚠️ 백엔드로 회원·진단 데이터를 서버 저장하게 되면 개인정보처리방침 수집·저장 조항은 반드시 개정 필요(현재 문구는 "서버 저장 안 함" 기준).**

**SEO·기본기 자산 (백엔드와 무관하게 유효 — 유지):**
- **meta description·OG·favicon**: 전 16개 HTML `<head>`에 삽입. 색인 대상 공개 12개(index/diagnosis/compare/rehabilitation/bankruptcy/documents/ai-review/resources/pricing/about/terms/privacy)는 description+og(type/site_name/title/description/url)+favicon. 인증·동적 4개(mypage/login/result/find-account)는 `robots noindex`+favicon.
- **신규 자산**: `favicon.svg`(로고 마크, 인디고 배경), `robots.txt`(auth 페이지 Disallow + Sitemap), `sitemap.xml`(공개 12페이지), `404.html`(자체완결 브랜드 404, 자산은 `/debt-guide/` 절대경로).
- **OG 이미지 완료(`og-image.png`, 1200×630)**: 카톡·SNS 공유 썸네일. 브랜드 카드(HTML 디자인)를 **Chrome 구 headless**(`chrome --headless --screenshot`, Edge headless=new는 --screenshot 미출력이라 실패)로 렌더. 12개 색인 페이지 og 블록에 `og:image`(+width/height)·`twitter:card=summary_large_image` 추가. **주의: og:image URL은 배포 도메인(github.io/debt-guide/) 절대경로라, 실제 배포 전에는 카톡 디버거에서 404 — 배포 후 정상.** 재생성 시: `scratch-og-image.html`류 디자인을 로컬서버로 띄우고 Chrome headless 스크린샷.
- 검증: 로컬 프리뷰(포트 3456)에서 privacy/terms 헤더·푸터 렌더, index meta·og·favicon, favicon/robots/sitemap/404 전부 HTTP 200, pricing 원복 후 구매버튼 5·mockPayment·모달 복구, 콘솔 에러 0건.

**상태:** 이번 변경 전체 **미커밋·미배포**(백엔드 완성 후 일괄 공개 예정). 유지 항목 = compare 법률정정 + 약관/방침 초안 + 푸터 링크 + SEO 자산 전체. 원복 항목 = pricing 결제 버튼(mock 유지).

**다음 큰 작업 = 백엔드.** 교체 지점은 격리돼 있음: `js/main.js`의 `Auth` 객체(로그인/가입/찾기/재설정)와 `Storage` 객체(진단·plan 저장) 본문을 `localStorage` → `fetch(API)`로 교체하면 되고, 이를 호출하는 login/mypage/diagnosis/result 페이지는 거의 안 바뀜. 결제는 새로 붙이고, 개인정보처리방침 개정 동반.

### 2026-07-03 — 소액 채무 안내 + 홈 버튼 입체화 등 UI 손질

- **소액 채무 안내(`result.html`)**: 총채무 **1,000만원 미만**이면 적합도 배너·위험 스트립·다음 단계 CTA·상세 아코디언을 모두 숨기고, "채무 규모가 크지 않은 편입니다 → 신용회복위원회 채무조정 우선 안내"(안 A) 카드 + **총 채무 숫자 1칸만** 표시. 신복위는 자격요건까지 다루지 않고 **사이트(www.ccrs.or.kr) + 대표번호 1600-5500(평일 09:00~18:00)** 만 안내(전화번호는 ccrs.or.kr 공식 확인). 구현: `LOW_DEBT_THRESHOLD`(=10,000,000) 상수 + `isLowDebt()`/`buildLowDebtGuidance()`/`buildLowDebtKeyNumber()`. `renderResults()` 날짜 로드 직후 조기 분기, 하단 `renderCta()` IIFE에도 `if(isLowDebt) return` 가드. 무데이터 분기는 그 앞이라 회귀 없음. **기준 1,000만원은 법정 최소액이 아니라 "법원 절차 비용·부담 대비 실익" 근거의 heuristic — 사용자 승인.**
- **홈 진행 버튼 입체화(`index.html`+`css/styles.css`)**: "무료 채무진단"/"챔로드 셀프진행" 버튼에 3D 눌림 효과. Tailwind arbitrary value가 CDN에서 안 먹어 `.btn-3d-white`/`.btn-3d-primary` 클래스를 styles.css에 직접 추가. 흰 버튼엔 pulse 애니메이션.
- **잔손질(`result.html`)**: 중복이던 "상세 정보 전체 펼치기" 버튼 삭제, mini-disclaimer 가독성 상향(text-sm/slate-500/medium), 하단 고지 여백 mt-8→mt-16.
- **커밋(`b3faf78`) + `git push origin main` 완료** — 이전 2~3차 미배포분 포함 전체 배포됨. GitHub Pages 반영 대기.

### 2026-07-02 (3차) — 진단결과 페이지 "간단 결과 우선" 구조로 개편

`result.html` 정보 과다 문제 해결 — "결론 먼저, 상세는 접기(무료)" 구조로 재구성. **진단결과 유료화 아이디어는 사용자 결정으로 폐기** (신뢰 훼손·퍼널 역효과·변호사법 109조 리스크 검토 결과).

**새 페이지 구조 (첫 화면 1~1.5스크롤):**
1. 추천 절차 배너 → 2. 한 줄 참고 고지(`#mini-disclaimer`) → 3. 핵심 숫자 3칸(`buildKeyNumbers` — 회생 우세: 월변제·총변제·면제예상 + 0원/청산가치 경고 노트, 파산 우세: 총채무·재산·채권자 수) → 4. 위험 요약 스트립(`buildRiskStrip` — "위험 N건" 접힌 빨간 띠, 펼치면 상세) → 5. 다음 단계 CTA → 6. 접힌 아코디언 4종(적합도 카드 / 두 절차 비교 / 변제금 계산 근거 / 파산관재인 주의) → 7. 전체 펼치기·재진단 → 8. 하단 고지

**구현 메모:**
- 공통 셸: `accordionShell(id, title, html)` / `toggleSection(id)` / `toggleAllSections()` — 본문 `.acc-body`, 화살표 `.acc-chev` 클래스 기준
- 기존 계산·문구 빌더 함수는 그대로 재사용. `buildDischargeRiskWarning` → `getDischargeRisks`(수집) + `buildRiskStrip`(요약 띠) + `buildRiskDetail`(본문)로 분리
- 삭제된 요소: 상단 disclaimer 박스(하단으로 통합), `#discharge-risk-warning`·`#result-cards`·`#tossup-advice`·`#trustee-warning`·`#detail-sections-container`·`#cards-heading` 고정 컨테이너, `makeAccordion` 데드코드
- 회귀 테스트 통과: 회생 우세+청산가치(첫 화면 799px) / 파산 우세+위험 2건 스트립 / 근접 점수(아코디언 4종) / 무데이터 가드 / 개별·전체 토글, 콘솔 에러 0건
- **아직 커밋/푸시 안 함** (2차 수정분 포함 전체 미배포 상태)

### 2026-07-02 (2차) — 점검 발견 이슈 일괄 수정 완료

아래 "1차 점검" 목록의 **1~18, 20~23번을 모두 수정**하고 브라우저 회귀 테스트(페르소나 4종 + 무데이터 + 마이페이지 진행률 + login next 차단)까지 통과. 콘솔 에러 0건. **아직 커밋/푸시 안 함.**

**수정 내용 요약:**
- `result.html` — hasRecentLoan 버그 수정(3개월/1년 분리 경고), 조문 정정(도박·투기 6호, 신용거래 2호, 재신청 기각 595조 5호), "불허가 후 7년 대기" 문구 삭제→즉시항고 안내, 사기죄는 형법 347조로 표기, "이자 3회" 속설 삭제, 배너·카드·상세의 변제 여력을 diagnosis_repay 기준으로 통일, 청산가치 경고 박스 신설, 무데이터 접근 시 진단 안내 표시, 미사용 변수 제거
- `js/diagnosis.js` — calcScores도 표준생계비(effLiving) 기준 사용, 파산 면책 이력(bankrupt-done-recent) 시 rehab -15 반영
- `diagnosis.html` — uncheckLegalNone 추가(해당없음 자동 해제), 데드코드 제거, 나이 라벨 * 추가
- `mypage.html` — rehab 42항목으로 수정(111% 버그), 미사용 상수 제거
- `pricing.html` — 당연복권·심문기일·금지명령 표현 정정 / `bankruptcy.html` — 압류금지 250만원(2026.2. 시행령)·심문기일 표현 / `resources.html` — 회생위원·압류금지 250만원 / `rehabilitation.html` — 회생위원 용어·직권면책 병기
- `login.html` — next 파라미터 same-origin 검증 / `ai-review.html` — 베타(규칙 기반) 안내 문구 / `index.html` — 인라인 hex 제거·?v=4 제거
- `app.html`·`build.ps1` **삭제** / `CLAUDE.md` 최신화(localStorage, 요금제 4종, launch.json)

**남은 항목 (별도 결정 필요):** 19·21번 — 실서비스 전환 시 백엔드(회원·결제) + 실제 AI 검토 API 연동. compare.html "파산선고와 동시에 추심 중단" 표현도 추후 검증 권장.

### 2026-07-02 (1차) — 전체 사이트 정밀 점검 완료 (수정은 아직 안 함)

전 페이지 코드 분석 + 실제 브라우저 진단 플로우 테스트(페르소나 3종) + 법률 수치 공식 출처 검증 완료. **발견 이슈만 기록하고 코드는 건드리지 않았음.** 다음 세션에서 P0부터 수정.

**P0 — 확정 버그 (동작 오류, 런타임 재현됨):**
1. `result.html` `buildReasons()`가 존재하지 않는 `data.hasRecentLoan` 필드 참조 → 최근 신규대출 경고가 적합도 카드에 절대 표시 안 됨. `data.recentLoan === 'within-3m'` 등으로 수정 필요
2. `diagnosis.html` `toggleLegalNone()` — '해당없음' 체크 상태에서 압류 등 다른 항목 체크 시 해당없음이 해제 안 됨 → 둘 다 체크되면 `hasLegalAction=false`로 계산 (압류 입력이 무시됨)
3. `mypage.html` 개인회생 진행률 분모 38 하드코딩 — 실제 `rehabilitation.html`은 9단계 42항목 (4+5+4+5+4+6+5+5+**4**) → 전부 체크 시 111% 표시. 하드코딩 대신 실데이터 기준으로 계산 권장
4. `app.html` + `build.ps1` — 예전 폴더("클로드코드 작업") 시절 단일파일 빌드 산출물. 어디서도 링크 안 되지만 GitHub Pages에 공개 배포 중 (옛 사이트명·옛 요금 10만원·신복위 포함 옛 진단). 저장소에서 삭제 필요

**P1 — 법률 정확성 (공식 출처로 검증 완료):**
5. 도박 면책불허가 근거: 사이트는 "제564조 제1항 **제4호**" → 실제는 **제6호** (`result.html` buildDischargeRiskWarning)
6. 1년 내 신용거래: 사이트는 "**제5호**" → 실제는 **제2호** (`result.html` 같은 함수)
7. "면책불허가 확정 후 **7년 대기 후 재신청**" 문구 → 법적 근거 없음. 제564조 1항 4호의 7년은 '면책을 **받은** 후' 기준 (`result.html` 면책불허가 안내 박스)
8. `pricing.html` 파산 절차 "복권 **신청**으로 자격 제한 해제" → 면책결정 확정 시 **당연복권**(제574조), 신청 불필요. 신청복권(제575조)은 면책 못 받고 5년 경과 시
9. "월 **150만원** 이하 급여 보호" → 민사집행법 시행령 개정으로 2026-02-01부터 압류금지 최저 **250만원** (`bankruptcy.html` 4단계 예시, `resources.html` 마지막 FAQ)
10. '변제**관리**인' 용어 → 개인회생은 '**회생위원**' (`rehabilitation.html` 8·9단계, `resources.html` FAQ)
11. 제595조 5호: **신청 전 5년 이내 면책(파산 면책 포함)** 받은 경우 개인회생 기각 → 현재 diagnosis.js는 '회생 면책 5년'만 반영, 파산 면책 이력의 회생 영향 미반영
12. 심문기일 출석: `pricing.html` "강제 아님, 권장" vs `bankruptcy.html` "반드시 출석" **상호 모순** — 공식 출처 확인 후 통일 필요
13. "사기죄(채무자회생법 **제643조**)로 형사고소" (`result.html` 2곳) — 643조는 사기회생죄, 파산 쪽은 사기파산죄(제650조)·형법 347조 영역. 조문 재확인 후 수정 필요
14. "이자 3회 이상 납부 이력이 있으면 사기 혐의 기각 가능성 높음" — 법적 근거 불명확한 속설. 단정 표현 완화 필요

**P2 — 로직/UX/구조 개선:**
15. `calcScores`는 입력 생활비, `calcRepayment`는 표준생계비 기준 → 같은 결과 페이지에 "변제 여력 월 100만원"(배너·카드)과 "월 변제액 0원"(상세)이 동시 표시되는 모순 (런타임 재현: 소득 250만·부양 1명 케이스)
16. 청산가치 보장 미반영: 총재산 > 3년 변제총액이면 변제금 상향·기간 연장(최대 5년) 필요한데 계산·경고 없음
17. `result.html` 진단 없이 직접 접근 시 "소득 없음" 등 허위 카드 표시 → 데이터 없으면 diagnosis.html 안내/리다이렉트
18. `login.html` `?next=` 무검증 `location.replace()` → `javascript:` URL 실행 가능. 상대경로만 허용하도록
19. AI 서류검토가 실제로는 키워드 포함 여부 검사 목업 — 유료 패키지 핵심 기능으로 판매 중이므로 실제 AI 연동(백엔드 필요) 또는 안내 문구 조정 필요
20. `index.html` 서비스 카드 인라인 `style="box-shadow: 0 0 0 2px #1d4ed8"` — CLAUDE.md 인라인 hex 금지 규칙 위반 (옛 파랑)
21. Auth/결제 전부 localStorage 목업 — 실서비스 전 백엔드 필수 (비밀번호 base64 저장, 결제 시 아무 패키지나 'premium' 부여)
22. CLAUDE.md 자체가 낡음: "sessionStorage 저장"(실제 localStorage), "프리미엄 10만원 단일 플랜"(실제 149,000/49,000/29,000/19,000 4종)
23. 잔손질: diagnosis.html 하단에 삭제된 드롭다운(`prior-adj-container`) 참조 데드코드, mypage `REHAB_TOTAL` 미사용 상수, result.html `hasFinance`·`hasNonFinanceDebt` 미사용 변수, index.html만 `styles.css?v=4` 캐시버스팅, 나이 필드 필수인데 라벨에 * 없음

**로컬 테스트 환경:** `.claude/launch.json` 추가됨 (`chamroad-static`, node 정적 서버, 포트 3456) — Claude가 브라우저 자동 테스트에 사용

### 2026-06-28 — 전체 반응형 정비 완료

모바일·태블릿에서 레이아웃이 깨지지 않도록 전체 사이트 반응형 정비.

**변경된 파일:**
- `css/styles.css` — `@media (max-width: 640px)` 확장: `.section`/`.section-sm` 패딩 축소, `#toast` 모바일 위치(좌우 여백 16px·전체 너비), `#scroll-top-btn` 위치 조정, `term-tip` 툴팁 너비·위치 모바일 최적화
- `diagnosis.html` — 단계 라벨에 `step-label` 클래스 추가(모바일 자동 숨김), 최근 대출 선택 그리드 `grid-cols-3` → `grid-cols-1 sm:grid-cols-3`, 재산 입력 라벨 `w-36` → `w-24`
- `pricing.html` — 요금제 카드 5개 모두 `flex-col sm:flex-row` 레이아웃으로 변경(모바일 세로 적층)
- `js/main.js` — 푸터 패딩 `py-16 px-6` → `py-10 md:py-16 px-4 md:px-6`, 그리드 gap 모바일 축소

### 2026-06-28 — Stripe 디자인 시스템 적용 완료

DESIGN-stripe.md 기준으로 전체 사이트 디자인 시스템 적용.

**변경된 파일:**
- `css/styles.css` — 완전 재작성. Stripe 디자인 토큰(CSS 변수), Inter 폰트, blue→indigo 색상 매핑, 필 버튼 CSS 규칙, Stripe 그림자, 라이트 푸터 클래스
- `js/main.js` — 내비게이션 로고 색상(그라디언트→인디고 단색), 푸터 재작성(다크→화이트)
- `index.html` — 히어로 CTA 버튼 `rounded-xl` → `rounded-full`

**CSS가 자동 처리하는 항목 (HTML 변경 불필요):**
- 모든 `bg-blue-700` 요소 → `#533afd` (인디고)
- `a.bg-blue-700`, `button.bg-blue-700` 등 → 필 모양(9999px)
- `button.border-slate-300`, `a.border-slate-300` → 필 모양
- `div.bg-slate-100 > button` (탭 스위처) → 필 모양
- `.btn-primary`, `.input-field`, `.auth-tab.active` (login.html 인라인 스타일 덮어쓰기)
- `from-blue-700`, `to-blue-600` 등 그라디언트 → 인디고/네이비
- `bg-slate-700`, `bg-slate-800` → `#1c1e54` (brand-dark)
- `.hero-gradient` → 인디고/네이비 그라디언트
- 토스트, 스크롤-투-탑 버튼 → 인디고
- 단계 점, 푸터 — 새 CSS 클래스로 처리

### 2026-06-21
- PROGRESS.md 신설. 작업 추적 체계 도입.

## 다음에 할 일 / 미정 사항

> 📋 **공개 준비 절차는 [LAUNCH-CHECKLIST.md](LAUNCH-CHECKLIST.md)에 단계별로 정리됨**(2026-07-24). 사업자등록→포트원 라이브→도메인→이메일인증→최종점검→공개(git push) 순서·의존성·코드변경 지점 포함. 아래는 개발 이력.

- **백엔드 진행 중** (2026-07-19 착수, 스택 = Cloudflare Workers + D1):
  - ✅ Phase 1 회원·인증 — 배포·검증 완료(`https://chamroad-api.eslje75.workers.dev`).
  - ✅ Phase 2 사용자 데이터 서버 저장 — 배포·검증 완료(user_data 테이블, GET/POST /api/data, DataSync 동기화 계층).
  - ✅ Phase 3 AI 서류검토 — 배포·실검증 완료(POST /api/ai/review, claude-sonnet-5, 일 30회 제한, ANTHROPIC_API_KEY 시크릿 설정됨).
  - ✅ Phase 4 결제 — 포트원 PortOne V2, 실결제 E2E 검증 완료(prepare/complete, payments 테이블, 서버 금액검증, maintain 부여 확인). 테스트채널=토스 테스트.
  - **백엔드 4개 Phase + 관리자 페이지 완료.** 남은 것 = **실서비스 전환**:
    1. 사업자등록 → 포트원 **라이브 채널** 교체(테스트→라이브 store/channel/secret)
    2. **도메인**(chamroad.co.kr 등) 구매 → GitHub Pages CNAME + `api.챔로드도메인` Cloudflare Worker 커스텀 도메인 + main.js API_BASE 교체
    3. **이메일 인증 + 비밀번호 재설정** 도입(사용자 결정 2026-07-20: 도메인 설정 시점에 함께). Resend 등 발송서비스 + 도메인 SPF/DKIM. find-account.html을 실제 재설정 플로우로 교체. users에 email_verified 컬럼 추가.
    4. ✅**개인정보처리방침 전부 개정 완료(2026-07-21)** — 남은 것은 관리자 계정(eslje75@gmail.com) 선점 가입. 사업자등록 후에는 방침 12항의 운영 형태·상호를 갱신할 것.
    5. 공개 배포(git push). SEO·약관 공개 시점 확정.
- **공개 배포는 백엔드 완성 후** `git push origin main` — 그때까지 커밋은 하되 push 보류(또는 로컬 유지). 미배포 중이라 약관/방침·SEO는 초안 상태.
- **OG 이미지**: ✅ 완료(`og-image.png` 1200×630, 12페이지 연결). 문구·디자인 바꾸려면 카드 HTML 재렌더. 배포 후 카톡 공유 디버거(developers.kakao.com)로 캐시 갱신 권장.
- **3순위(공개 시)**: GA4 방문자 분석(측정 ID G-XXXX 필요, main.js 공통 삽입) / 커스텀 도메인(선택). 문의 채널은 이메일로 확정, 이미 푸터·약관에 노출.
- **텍스트량 감축 — 1차 완료(2026-08-14), 2차는 미정**: 서문·UI 설명문 중복 제거로 4개 페이지 첫 화면 4,131→3,047자(26%↓). 상세는 위 2026-08-14 항목. **남은 후보(사용자 결정 필요)**:
  - `bankruptcy.html` 빨간 박스 ↔ 노란 박스 6항목 중 3개 중복 → 합칠지 여부(법률 고지 병합이라 보류 중)
  - 경고·고지를 "결론 먼저 / 근거는 접기"로 재배치 — 정보 삭제 없이 화면상 400→120자 수준. 자르는 위치 판단이 필요해 `/legal-verify` 동반 권장
  - 전 페이지 문체 압축(문장을 짧게 다시 쓰기) — 감축폭 최대이나 법률 서술 전면 재작성이라 재검증 필수
  - 아직 손대지 않은 페이지: `result.html`(7,965자·아코디언 구조는 이미 양호) / `resources.html` 6,349 / `ai-review.html` 4,365 / `supplement.html` 4,855 / `maintenance.html` 4,980
- ✅**GLOSSARY 법률 검증 완료(2026-07-28, T2 반박 교차검증)** — 상세는 위 07-28 항목. 남은 후속: 용어 추가 후보(소명 64회·파산선고 17회·부채증명서 31회·예납금 15회·소송구조 5회·당연복권 4회 — 사이트 빈출인데 미등재)는 정의문 작성 시 다시 `/legal-verify` 필요.
- 신용회복위원회 채무조정 자격요건(연체 전/후 단계별)은 아직 ccrs.or.kr 공식 확인 전 — result.html 소액 안내는 사이트·전화번호만 노출해 우회 중 (신복위 "신청 다음 날 추심중단" 문구는 2026-07-04 공식 확인 완료)

## 알아둘 것
- Inter 폰트 Google Fonts에서 로드 (한글 지원 있음, 일부 글자는 시스템 폴백)
- `compare.html`, `documents.html`, `ai-review.html` 파일은 CSS 규칙으로만 처리 (별도 HTML 수정 없음). `find-account.html`은 2026-07-19 정직 안내 페이지로 전면 교체됨(백엔드 인증 도입). `app.html`은 2026-07-02 삭제됨 — 다시 만들지 말 것
- `login.html` 인라인 `<style>` 블록의 `.btn-primary` 등은 `styles.css`의 `!important` 규칙이 우선 적용

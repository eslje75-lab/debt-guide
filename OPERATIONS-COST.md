# 운영 체크리스트 — 비용·한도·남용 방어·부재 대응

챔로드를 운영하면서 **돈이 나가거나 한도가 있는 외부 서비스**, 그리고 **한도를 남이 태우지 못하게 하는 방어**와 **운영자가 자리를 비울 때 지켜야 할 법정 기한**을 모은 문서. 특히 ⚡표시(선불 충전식)는 **잔액을 다 쓰기 전에 미리 충전**해야 서비스가 안 끊긴다.

> 최초 작성 2026-08-08(비용). 2026-08-15에 남용 방어(0절)·오픈 직전 점검·부재 대응을 추가했다.
> 요금·무료한도는 각 서비스가 수시로 바꾸므로, 아래 수치는 기준점일 뿐 **실제 값은 각 대시보드에서 확인**한다.

---

## 🚀 오픈 직전 점검 — 판매를 열기 전에 이것부터

판매가 열리는 순간 **잔액 부족이 곧 결제 실패**가 된다. 순서대로 확인할 것.

- [ ] ⚡**솔라피 충전** — 🔴**가장 먼저.** 결제 전 휴대폰 번호 인증이 **필수 관문**이라, 잔액이 0이면 **결제 자체가 진행되지 않는다.** 문자 1건 18원 기준으로 예상 결제 건수의 몇 배를 미리 넣어 둘 것. 확인처: console.solapi.com
  - ⚠️ 발신번호 `01037707164` **인증 만료 2027-02-08** — 만료되면 발송이 끊긴다. 달력에 적어 둘 것.
  - ⚠️ 개인 명의 발신번호라 통신사 **일일 발송량 제한**이 있을 수 있다(정확한 한도 미확인). 사업자 명의 전환 시 함께 정리.
- [ ] ⚡**Anthropic 자동충전 설정** — 크레딧이 떨어지면 AI 검토·대조가 **조용히** 멈춘다(잔액 조회 API가 없어 관리자 화면도 못 알려 준다). console.anthropic.com → Billing.
- [ ] **Resend 한도 여유 확인** — 일 100통. 오픈 직후엔 가입·주문확인 메일이 함께 늘어난다. 넘칠 것 같으면 **Pro($20/월)**로 올린다(수탁자가 같아 **방침 개정 불필요**).
- [ ] **관리자 대시보드에서 세 값이 정상 표시되는지** 한 번 열어 확인(솔라피 잔액 / 오늘 메일 / AI 호출).
- [ ] **D1 백업 1회 + 복구 리허설** — [BACKUP.md](BACKUP.md). 결제가 열리면 잃을 수 있는 데이터의 성격이 달라진다.

---

## 🕐 부재 시 대응 규칙 — 법정 기한이 걸린 것들

1인 운영이라 **자리를 비우는 동안에도 시계는 간다.** 아래는 원문으로 확인한 기한이다.

| 무엇 | 기한 | 근거 |
|---|---|---|
청약철회 접수 후 **대금 환급** | **3영업일** — 디지털콘텐츠는 **「청약철회등을 한 날」부터** 기산(반환할 물건이 없으므로) | 전자상거래법 제18조 제2항 |
환급을 지연하면 | **연 15%** 지연배상금 | 같은 조 + 시행령 제21조의3 |
이용자의 **청약철회 가능 기간** | 결제일부터 **14일**(법정 7일보다 길게 약정) | 약관 제6조① / 법 제17조 제1항 |
개인정보 **열람** 요구 | **10일** 이내 | 개인정보 보호법 제35조 제3항 + 시행령 제41조 제4항 |
개인정보 **정정·삭제** 요구 | **지체 없이** | 같은 법 제36조 제2항 |

### 다행인 것 — 대부분은 자동으로 처리된다

마이페이지 [청약철회 신청]은 **①결제일부터 14일 이내 ②콘텐츠 미개시 ③PG 설정됨**이면
**운영자 개입 없이 즉시 전액 환불**하고 이용권 회수·완료 메일까지 자동으로 끝낸다(`handleWithdrawRequest`).
즉 3영업일 기한을 자동 경로가 지킨다.

**운영자가 반드시 손을 대야 하는 것은 이 네 가지뿐이다:**
1. 이용자가 **콘텐츠를 이미 열람**한 경우(부분 환불 판단이 필요)
2. **14일이 지난** 청약철회 요청
3. **PG 취소가 실패**한 경우
4. **이메일로 직접 온** 청약철회·문의

### 그래서 규칙 (2026-08-15 사용자 결정)

🔴 **판매는 항상 열어 둔다. 자리를 비운다고 `PAYMENTS_ENABLED`를 닫지 않는다** — 사용자 결정이다. **다시 제안하지 말 것.**
대신 **운영자가 매일 청약철회 현황을 직접 확인**하는 방식으로 3영업일 기한을 지킨다.

- [ ] **매일 1회 관리자 대시보드의 「🧾 청약철회 신청」 섹션 확인.** 위 4가지(열람 시작분·14일 경과·PG 실패·이메일 요청)가 여기에 쌓인다.
  자동 전액환불로 끝난 건은 여기에 남지 않으므로, **목록에 뭔가 있다는 것 자체가 "내가 판단해야 할 건이 있다"는 뜻**이다.
- [ ] 문의 메일 **자동응답**을 걸어 두되, ⚠️**자동응답이 법정 기한을 늦추지는 않는다.** "언제까지 답하겠다"를 적을 것.
- [ ] 3영업일이 빠듯해질 것 같으면 **먼저 연락해 상황을 알린다** — 지연 자체보다 연락 없는 지연이 분쟁이 된다.

**추후 예정(사용자)**: 청약철회 발생 시 **알림 연동·자동화**. 그때까지는 위 수동 확인이 방어선이다.
⚠️그러므로 **하루 확인을 거르면 곧바로 법정 기한 위험**이라는 점만 분명히 해 둔다 — 연 15% 지연배상금이 붙는다.

---

## 🛡️ 0. 남용 방어 — 한도를 남이 태우지 못하게 (2026-08-15)

이 문서의 나머지가 "돈이 떨어지면 멈춘다"라면, 이 절은 **"남이 태워도 멈춘다"**를 막는 부분이다.

### 지금 걸려 있는 방어

| 대상 | 방어 | 어디 |
|---|---|---|
가입 인증메일 | Cloudflare **Turnstile** | ⚠️시크릿 미설정·네트워크 오류 시 **통과(fail-open)** — 캡차가 흔들리면 무방비 |
가입 인증메일 | 주소별 **60초 쿨다운 · 시간당 5회** | 주소를 바꿔 가며 오는 요청은 **못 막는다** |
가입 인증메일 | 🔴**하루 총 발송 60통 넘으면 가입 메일만 차단**(`SIGNUP_EMAIL_DAILY_CAP`) | 2026-08-15 추가. Resend 일 100통 중 **40통을 재설정·주문확인 몫으로 남긴다** |
휴대폰 OTP | 60초 쿨다운 · 시간당 5회 | 솔라피 잔액을 태우는 경로 |
로그인 | 이메일별 실패 잠금(`login_attempts`) | |

**왜 40통을 남기나**: 가입이 막히는 것은 미룰 수 있지만, **기존 회원의 비밀번호 재설정**과 **결제 주문확인 메일**은 미루면 안 된다. 특히 주문확인 메일은 **전자상거래법 제13조 제2항의 계약내용 서면 교부로 법정 의무**다. 남이 만든 트래픽 때문에 우리 의무가 깨지는 구조를 끊은 것이다.
관리자 대시보드의 「이메일 발송(오늘)」 카드가 **「가입 메일 차단 중」 배지**로 상태를 알려 준다.

### ⬜ Cloudflare Rate Limiting — 무료 플랜 기준 절차

애플리케이션에서는 **IP를 저장하지 않는다**(방침). 그래서 IP 기준 차단은 코드가 아니라 **Cloudflare에서** 거는 것이 방침과 일관된다.
⚠️**대시보드 작업이라 CLI로는 안 된다** — wrangler OAuth 토큰에는 zone read 권한만 있다.

**🔴먼저 알아야 할 무료 플랜 제약**(2026-08-15 공식 문서 확인). 이걸 모르면 만들다가 막힌다.

| | 무료 플랜 |
|---|---|
규칙 수 | **1개** (Pro 2 / Business 5) |
쓸 수 있는 필드 | **Path, Verified Bot** — ⚠️**호스트명 조건을 못 넣는다** |
카운팅 기준 | **IP만** |
기간(Period) | **10초 고정** |
차단 유지(Mitigation timeout) | **10초 고정** |

→ 규칙이 하나뿐이므로 **가장 약한 곳 하나**에 건다. 호스트명을 못 걸지만 `/api/auth/...` 경로는 `api.chamroad.com`에만 있으므로 **경로만으로 충분**하다.

**설정 위치**: Cloudflare 대시보드 → `chamroad.com` → **Security rules** → **Create rule** → **Rate limiting rules**

**만들 규칙** — **Edit expression**에 아래를 그대로 붙여넣는 것이 가장 안전하다(항목을 하나씩 고르다 군더더기가 섞이기 쉽다).

```
(http.request.uri.path eq "/api/auth/send-signup-code") or (http.request.uri.path eq "/api/auth/request-reset")
```

- **Rule name**: `auth-mail-burst`
- **With the same characteristics**: `IP`
- **When rate exceeds**: **5** requests per **10 seconds**
- **Then take action**: `Block`, **Duration** 10 seconds

🔴**경로를 정확히 쓸 것.** 이 둘이 **메일을 쏘는 비로그인 엔드포인트**다.
⚠️**`/api/auth/find-account`는 API 경로가 아니다** — 그건 화면 파일 이름(`find-account.html`)이고, 실제 API는 **`/api/auth/request-reset`**이다(2026-08-15에 이 실수를 실제로 했고, 연타 테스트에서 404가 떠서 발견했다).
`/api/auth/resend-verification`도 메일을 보내지만 **로그인이 필요해** 노출이 낮다 — 조건을 더 넣고 싶으면 이것을 세 번째로.
로그인(`/api/auth/login`)은 코드에 이메일별 실패 잠금이 이미 있어 후순위다.

**임계값 근거**: 우리 코드가 이미 **같은 주소 60초 쿨다운**을 걸고 있어 정상 이용자는 10초에 5회를 만들 수 없다. 실수로 연타해도 걸리지 않는 선이다.

**✅검증 완료 (2026-08-15) — Worker 커스텀 도메인에도 rate limiting이 적용된다.**
공식 문서에서 명시를 찾지 못해 미확인으로 뒀던 항목인데, 실제로 걸어 보고 확인했다.
`api.chamroad.com`에 **동시 12건**을 쏘자 **6건이 429로 차단**됐다(소요 563ms). 가정이 아니라 실측이다.

재확인이 필요하면(규칙을 고친 뒤 등) 아래로 돌린다. 빈 본문 `{}`이라 **메일이 나가지 않고 계정 존재도 노출되지 않는다** — 서버 검증에서 400으로 끊기지만 rate limiting은 그 앞 단계에서 요청 수를 세므로 카운트에는 들어간다.

```javascript
// node <파일>.js  — 동시 12건 발사 후 상태코드 집계
const U = 'https://api.chamroad.com/api/auth/send-signup-code';
Promise.all(Array.from({length:12}, () =>
  fetch(U, {method:'POST', headers:{'Content-Type':'application/json'}, body:'{}'})
    .then(r => r.status).catch(() => 'ERR')
)).then(s => console.log(s.join(' ')));
```
429가 섞여 나오면 걸린 것이다. 대시보드 **Security → Events**에도 기록이 남는다.
⚠️**404가 나오면 경로가 틀린 것이다** — 라우트 목록은 `api/src/index.js`의 `ROUTES`에서 확인할 것.

**기대치를 정확히**: 무료 플랜의 10초 창은 **단순 연타 스크립트**를 끊을 뿐, 여러 IP로 천천히 도는 요청은 못 막는다.
🔴**실질적인 방어선은 위 「하루 60통 넘으면 가입 메일 차단」(`SIGNUP_EMAIL_DAILY_CAP`)이고, WAF 규칙은 과속방지턱이다.** 순서를 거꾸로 이해하지 말 것.

## ⚡ 1. 솔라피 (SMS 문자 인증) — 선불 충전식

- **용도**: 결제 유료회원 휴대폰 번호 인증(발송·검증). `sendSms`.
- **과금**: 문자 1건당 대략 **단문 SMS 18원 / 장문 LMS 45원**. 챔로드 인증문자는 길이에 따라 이 사이.
- **현재 잔액**: 10,000원 (2026-08-08 충전). 발송량 늘면 소진 전 재충전.
- **다 떨어지면**: 발송 API가 실패 → 서버가 `502`("문자 발송에 실패했습니다") 반환 → 사용자가 번호 인증을 못 함 → **결제 진행 불가**.
- **확인처**: https://console.solapi.com (상단 '총 잔액')
- **운영 법인**: 주식회사 누리고(솔라피). 발신번호 `01037707164`(인증 만료 2027-02-08 — 재인증 필요).

## ⚡ 2. Anthropic API (AI 서류검토) — 선불 크레딧/자동결제

- **용도**: 유료회원 AI 서류검토(`POST /api/ai/review`). 모델 `claude-sonnet-5`.
- **과금**: **입력 $3 / 출력 $15 per 1M tokens** (2026-08-31까지 인트로 요금 입력 $2 / 출력 $10). 검토 1건은 서류 길이에 비례.
- **다 떨어지면**: API가 결제오류 반환 → AI 검토 기능만 실패(다른 기능은 정상). 크레딧 소진 시 자동충전 안 걸어두면 멈춤.
- **서버측 사용 제한(비용 방어용, 이미 코드에 있음)**: 앱 전체 **일 30회** + 패키지별 회수(예: 12회, 1일 최대 10회). 이 한도는 돈이 아니라 남용 방지용.
- **확인처**: https://console.anthropic.com → Billing (크레딧 잔액·자동충전 설정)
- **시크릿**: `ANTHROPIC_API_KEY`

## 3. Resend (이메일 발송) — 월 무료한도 + 초과 시 유료 플랜

- **용도**: 비밀번호 재설정·가입 인증·주문확인 메일(`sendEmail`). 발신 `no-reply@chamroad.com`.
- **무료한도**: **일 100통 / 월 3,000통 / 도메인 1개** (2026-08-15 resend.com/pricing 확인).
- **초과하면**: 그 달 추가 발송 막힘 또는 유료 플랜 필요 → 재설정·인증 메일이 안 감.
- **다음 단계**: Pro **$20/월 = 월 5만통 / 도메인 10개**. 수탁자가 그대로라 **방침 개정 불필요**.
- **확인처**: https://resend.com (Usage) — 발송량이 무료한도에 근접하면 유료 플랜 검토.
- **운영사**: Plus Five Five, Inc. / 시크릿 `RESEND_API_KEY`

## 4. Cloudflare (Workers + D1 서버·DB) — 무료한도 + 초과 시 $5/월

- **용도**: 백엔드 API(Worker `chamroad-api`) + 데이터베이스(D1). 회원·결제·데이터 전부 여기.
- **무료한도**: Workers 요청 **10만/일**(호출당 CPU 10ms), D1 읽기 **500만 row/일** · 쓰기 **10만 row/일** · 저장 **5GB**
  (2026-08-15 developers.cloudflare.com 확인).
- **초과하면**: 요청이 막히거나 **Workers Paid($5/월)** 전환 필요. 방문자가 크게 늘기 전엔 무료로 충분.
- ⚠️ **Workers Paid($5/월)와 Cloudflare Pro($20/월)는 다른 상품이다.** 전자는 Workers·D1 한도와
  **D1 Time Travel 7일→30일**, 후자는 도메인(zone) 플랜이라 **WAF rate limiting 규칙 1개→2개**. 섞지 말 것.
- **확인처**: https://dash.cloudflare.com → Workers & Pages / D1 (Usage)
- **계정**: `eslje75`

## 5. 도메인 chamroad.com — 연 1회 갱신

- **만료**: 2027-08-05 (Cloudflare Registrar, `eslje75` 계정).
- **놓치면**: 도메인 실효 → 사이트·API 전부 다운. 자동갱신 켜두는 것 권장.
- **확인처**: https://dash.cloudflare.com → Domain Registration

## 6. 포트원 + PG(결제대행) — 거래 수수료 (충전 아님, 판매 오픈 후)

- **과금 방식**: 선불 충전이 아니라 **결제 건당 수수료 %가 정산금에서 차감**. 챔로드가 미리 낼 잔액은 없음.
- **주의**: 라이브 채널 전환 + 사업자등록 후에만 발생. 수수료율은 PG 계약에 따름.
- **확인처**: 포트원 관리자콘솔 / PG사 정산내역.

---

## 📈 규모가 커지면 손볼 것 — 이용자 100명 이상 (2026-08-15 조사)

결론부터. **AI 한도는 올릴 필요가 없다.** 100명 규모에서 실제로 막히는 것은 **이메일(Resend)**이고,
돈이 새는 게 아니라 **잔액이 조용히 떨어지는 것**이 위험하다. 아래는 근거와 전환 시점.

### 무엇이 먼저 막히나 — 순서

| 순위 | 한도 | 지금 | 100명이면 | 넘으면 | 상향 비용 |
|---|---|---|---|---|---|
| 1 | **Resend 일 100통** | 여유 | **일일 피크에서 걸림** | 가입·주문확인 메일 중단 | Pro **$20/월** (월 5만통) |
| 2 | **Anthropic 크레딧 잔액** | 수동 충전 | 소진 속도 4~5배 | AI 검토·대조 중단 | 자동충전 설정(무료) |
| 3 | D1 Time Travel 7일 | 무료 | 복구 창이 좁음 | 실수 발견이 늦으면 복구 불가 | Workers Paid **$5/월** (30일) |
| 4 | WAF 규칙 1개 | 가입 경로만 | 로그인 경로 무방비 | 연타 방어 구멍 | Cloudflare Pro **$20/월** (2개) |
| 5 | 솔라피 잔액 1만원 | 약 550건 | 약 3~4개월치 | 결제 번호인증 중단 | 충전(자동충전 검토) |
| — | Workers 10만 req/일 | 여유 10배 이상 | **여전히 여유** | — | 당분간 불필요 |
| — | **Anthropic rate limit** | 여유 100배 | **여전히 여유** | — | **상향 요청 불필요** |

### ① Resend — 유일하게 실제로 걸리는 한도

누적 100명이 문제가 아니라 **하루 피크**가 문제다. 유료회원 1명당 최소 2통(가입 인증 + 주문확인)이고,
가입만 하고 안 사는 사람도 1통씩 쓴다. 여기에 비밀번호 재설정·인증 재발송이 얹힌다.

- 우리 코드는 `SIGNUP_EMAIL_DAILY_CAP = 60`에서 **가입 메일만** 먼저 끊는다. 남은 40통을 주문확인이 쓰는 구조라
  Resend가 100통에서 끊기 전에 우리가 먼저 제동을 건다. **이 설계가 유료 전환 전까지 버텨 주는 장치다.**
- **전환 신호**: 하루 신규가입이 40건에 자주 닿으면(= 관리자 화면 발송량이 60에 근접) 그때 Pro로 올린다.
- 올릴 때 **같이 올려야 하는 코드 상수 2개**: `SIGNUP_EMAIL_DAILY_CAP`(60)과 `EMAIL_DAILY_FREE`(100).
  Resend만 올리고 코드를 그대로 두면 **우리 코드가 계속 60에서 끊는다** — 돈만 내고 한도는 그대로가 된다.
- 수탁자(Plus Five Five, Inc.)가 그대로라 **개인정보처리방침 개정은 필요 없다.**

### ② Anthropic — 요금은 싸다. 위험한 건 잔액이다

**🔴 2026-08-31에 인트로 요금이 끝난다.** 입력 $2→**$3**, 출력 $10→**$15** (per 1M tokens). 둘 다 +50%.
100명과 무관하게 **16일 뒤 자동으로 오르는 값**이므로 아래 계산은 전부 인상 후 기준이다.

검토 1건 실비 (`geo='us'` 요금 1.1배 포함, 1,450원/$ 기준 · **토큰 수는 추정**):

| | 입력 | 출력 | 1건 | 완주 패키지 12회 전부 소진 |
|---|---|---|---|---|
| 보통 (서류 4천자) | ~4,500 tok | ~1,500 tok | **약 58원** | 약 700원 |
| 최악 (`AI_TEXT_MAX` 2만자) | ~16,500 tok | 4,096 tok(상한) | **약 177원** | 약 2,100원 |

149,000원짜리 패키지 대비 **최악으로 써도 원가 1.4%**. 100명이 전부 완주 패키지를 사서 회수를 다 태워도
총 AI 실비는 **7만~21만원**이다. Start 티어 월 지출 상한 $500(약 72만원)에도 한참 못 미친다.

- **rate limit은 논외다.** Start 티어 Sonnet 5 = 1,000 RPM / 입력 2M TPM / 출력 400k TPM.
  우리 최악의 1건이 입력 16,500 토큰이므로 **분당 120건 동시**까지 여유가 있다. 100명이 같은 순간에 눌러도 안 걸린다.
  상향 요청은 필요 없다 — 필요해지는 시점은 이용자가 아니라 **동시 사용**이 분당 100건을 넘을 때다.
- **진짜 위험은 다른 것**: 잔액 조회 API가 없어 `ops-status`에 띄울 수가 없다. 떨어지면 **예고 없이 AI만 멈춘다.**
  → **콘솔에서 자동충전을 걸어 두는 것이 사실상 필수**다. 100명 규모에선 "월 1회 확인"으로는 늦다.
- 비용이 튀면 손댈 순서: `AI_EFFORT`(medium→low) → `AI_TEXT_MAX` → 패키지 `aiQuota`.
  단 **`aiQuota`는 고지 사항**이라 바꾸면 `pricing.html`·결제 확인창·`terms.html` 제5조를 함께 고쳐야 한다(전상법 제13조②).
- 토큰 수는 추정치다. 정확히 알려면 실제 서류로 `POST /v1/messages/count_tokens`를 한 번 돌려 재보정할 것.

### ③ Cloudflare — 사용량 때문이 아니라 안전장치 때문에 올린다

100명 규모에서 Workers 10만 req/일, D1 쓰기 10만 row/일은 **10배 이상 여유**다. 그런데도 유료를 검토하는 이유는 둘:

- **Workers Paid $5/월** → D1 Time Travel **7일 → 30일**. 회원이 늘수록 "잘못된 마이그레이션을 일주일 안에 알아챌"
  확률이 떨어진다. 백업이 있어도 복구 창이 넓은 쪽이 안전하다.
- **Cloudflare Pro $20/월** → rate limiting 규칙 **1개 → 2개**. 지금은 규칙 하나를 가입·재설정 경로에 몰아 쓰고 있어
  **로그인 경로가 비어 있다.** 규칙이 2개가 되면 로그인도 덮을 수 있다.

둘은 별개 상품이니 필요한 쪽만 사면 된다. 급한 순서는 Workers Paid가 먼저다(백업·복구가 로그인 연타보다 치명적).

### ④ 백업 — 회원이 늘면 주기와 보관을 다시 정한다

현재 164kB / 16테이블 / 주 1회 / 14개 보관. 100명이면:

- 주 1회로는 **최대 7일치 회원 가입·결제가 한 번에 날아간다.** 주 2회 이상으로 좁힐 것.
- 덤프에 든 개인정보 양이 함께 늘어난다. 보관 개수(14)를 늘리는 것은 **위험을 늘리는 것**이기도 하니
  개수를 늘리기보다 주기를 좁히는 쪽을 택한다. [BACKUP.md](BACKUP.md)의 경고를 그대로 적용.

### ⑤ 솔라피 — 금액이 아니라 명의가 문제

100명 결제 = 인증문자 100~130건 ≈ **2,300~5,900원**. 만원으로 충분하다. 다만 둘은 규모와 무관하게 걸린다:

- 발신번호 `01037707164`는 **개인 명의**다. **통신사 일일 발송량 제한은 확인하지 못했다** — 대량 발송 전에 솔라피에 확인할 것.
- **인증 만료 2027-02-08.** 사업자등록 후 사업자 명의로 재등록하는 편이 낫다(만료 갱신과 한 번에 처리).

### ⑥ 운영 루틴 — 100명이면 수동 확인이 무너진다

지금은 **매일 1회 청약철회 목록을 사람이 확인**해서 3영업일 기한을 지킨다(위 「부재 시 대응 규칙」). 100명 규모에선
주문 건수가 늘어 하루 거르는 것만으로 법정 기한 위험이 생긴다. 사용자가 예고한 **알림 연동·자동화가 필요해지는 시점이 여기**다.
판매를 닫는 방식은 쓰지 않는다(확정 결정).

---

## 요약 — 운영자가 신경 쓸 우선순위

| 우선 | 서비스 | 왜 | 점검 주기 |
|---|---|---|---|
| 🔴 높음 | **솔라피 잔액** | 떨어지면 결제 인증 중단 | 판매 오픈 후 수시 / 발송량 보고 충전 |
| 🔴 높음 | **Anthropic 크레딧** | 떨어지면 AI 검토 중단 | 자동충전 설정 + 월 1회 |
| 🟡 중간 | Resend 발송량 | 무료한도 초과 시 메일 중단 | 월 1회 |
| 🟡 중간 | Cloudflare 사용량 | 트래픽 급증 시 유료 전환 | 월 1회 |
| 🟢 낮음 | 도메인 갱신 | 연 1회, 자동갱신 켜면 안심 | 연 1회(만료 전) |
| 🟢 낮음 | PG 수수료 | 정산에서 자동 차감 | 정산 때 |

**날짜가 박힌 것 두 개** — 잊으면 조용히 당한다.

| 날짜 | 무슨 일 | 해야 할 것 |
|---|---|---|
| **2026-08-31** | Anthropic 인트로 요금 종료 (입력 $2→$3, 출력 $10→$15) | 별도 조치 없음. 다만 9월부터 AI 실비가 1.5배 — 위 계산은 이미 인상 후 기준 |
| **2027-02-08** | 솔라피 발신번호 인증 만료 | 재인증. 사업자등록 후라면 사업자 명의로 전환하며 한 번에 |

이용자가 늘었을 때 무엇을 어떤 순서로 올릴지는 위 **「📈 규모가 커지면 손볼 것」** 참조.
한 줄 요약: **AI 한도는 손댈 필요 없고, 먼저 걸리는 것은 Resend 일 100통이다.**

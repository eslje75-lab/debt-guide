# 운영 체크리스트 — 비용·한도·남용 방어·부재 대응

챔로드를 운영하면서 **돈이 나가거나 한도가 있는 외부 서비스**, 그리고 **한도를 남이 태우지 못하게 하는 방어**와 **운영자가 자리를 비울 때 지켜야 할 법정 기한**을 모은 문서. 특히 ⚡표시(선불 충전식)는 **잔액을 다 쓰기 전에 미리 충전**해야 서비스가 안 끊긴다.

> 최초 작성 2026-08-08(비용). 2026-08-15에 남용 방어(0절)·오픈 직전 점검·부재 대응을 추가했다.
> 요금·무료한도는 각 서비스가 수시로 바꾸므로, 아래 수치는 기준점일 뿐 **실제 값은 각 대시보드에서 확인**한다.

---

## 🚀 오픈 직전 점검 — 판매를 열기 전에 이것부터

판매가 열리는 순간 **잔액 부족이 곧 결제 실패**가 된다. 순서대로 확인할 것.

- [ ] ⚡**솔라피 충전** — 🔴**가장 먼저.** 결제 전 휴대폰 번호 인증이 **필수 관문**이라, 잔액이 0이면 **결제 자체가 진행되지 않는다.** 문자 1건 18원 기준으로 예상 결제 건수의 몇 배를 미리 넣어 둘 것. 확인처: console.solapi.com
  - ⚠️ 발신번호 `01037707164` **인증 만료 2027-02-08** — 만료되면 발송이 끊긴다. 달력에 적어 둘 것.
  - ⚠️ 개인 명의 발신번호라 통신사 **일일 발송량 제한**이 있을 수 있다(정확한 한도 미확인). 사업자 명의 전환 시 함께 정리.
- [ ] ⚡**Anthropic 자동충전 설정** — 크레딧이 떨어지면 AI 검토·대조가 **조용히** 멈춘다(잔액 조회 API가 없어 관리자 화면도 못 알려 준다). console.anthropic.com → Billing.
- [ ] **Resend 한도 여유 확인** — 일 100통. 오픈 직후엔 가입·주문확인 메일이 함께 늘어난다. 넘칠 것 같으면 **Pro($20/월)**로 올린다(수탁자가 같아 **방침 개정 불필요**).
- [ ] **관리자 대시보드에서 세 값이 정상 표시되는지** 한 번 열어 확인(솔라피 잔액 / 오늘 메일 / AI 호출).
- [ ] **D1 백업 1회 + 복구 리허설** — [BACKUP.md](BACKUP.md). 결제가 열리면 잃을 수 있는 데이터의 성격이 달라진다.

---

## 🕐 부재 시 대응 규칙 — 법정 기한이 걸린 것들

1인 운영이라 **자리를 비우는 동안에도 시계는 간다.** 아래는 원문으로 확인한 기한이다.

| 무엇 | 기한 | 근거 |
|---|---|---|
청약철회 접수 후 **대금 환급** | **3영업일** — 디지털콘텐츠는 **「청약철회등을 한 날」부터** 기산(반환할 물건이 없으므로) | 전자상거래법 제18조 제2항 |
환급을 지연하면 | **연 15%** 지연배상금 | 같은 조 + 시행령 제21조의3 |
이용자의 **청약철회 가능 기간** | 결제일부터 **14일**(법정 7일보다 길게 약정) | 약관 제6조① / 법 제17조 제1항 |
개인정보 **열람** 요구 | **10일** 이내 | 개인정보 보호법 제35조 제3항 + 시행령 제41조 제4항 |
개인정보 **정정·삭제** 요구 | **지체 없이** | 같은 법 제36조 제2항 |

### 다행인 것 — 대부분은 자동으로 처리된다

마이페이지 [청약철회 신청]은 **①결제일부터 14일 이내 ②콘텐츠 미개시 ③PG 설정됨**이면
**운영자 개입 없이 즉시 전액 환불**하고 이용권 회수·완료 메일까지 자동으로 끝낸다(`handleWithdrawRequest`).
즉 3영업일 기한을 자동 경로가 지킨다.

**운영자가 반드시 손을 대야 하는 것은 이 네 가지뿐이다:**
1. 이용자가 **콘텐츠를 이미 열람**한 경우(부분 환불 판단이 필요)
2. **14일이 지난** 청약철회 요청
3. **PG 취소가 실패**한 경우
4. **이메일로 직접 온** 청약철회·문의

### 그래서 규칙

- [ ] **2영업일 넘게 자리를 비우면 → `PAYMENTS_ENABLED`를 `false`로 돌려 판매를 닫는다**(두 곳: `js/main.js`·`api/src/index.js`, push하면 반영).
  새 결제가 없으면 **새 청약철회도 생기지 않는다.** 기존 건만 관리하면 되므로 노출이 급격히 줄어든다. 🔴**가장 확실한 안전장치다.**
- [ ] 부재 중에도 **관리자 대시보드의 「🧾 청약철회 신청」 섹션을 하루 1회** 확인한다(위 4가지가 여기에 쌓인다).
- [ ] 문의 메일 **자동응답**을 걸어 두되, ⚠️**자동응답이 법정 기한을 늦추지는 않는다.** "언제까지 답하겠다"를 적을 것.
- [ ] 장기 부재가 예정되면 **사이트 공지**로 처리 지연 가능성을 미리 알린다(약관상 의무는 아니나 분쟁을 크게 줄인다).

---

## 🛡️ 0. 남용 방어 — 한도를 남이 태우지 못하게 (2026-08-15)

이 문서의 나머지가 "돈이 떨어지면 멈춘다"라면, 이 절은 **"남이 태워도 멈춘다"**를 막는 부분이다.

### 지금 걸려 있는 방어

| 대상 | 방어 | 어디 |
|---|---|---|
가입 인증메일 | Cloudflare **Turnstile** | ⚠️시크릿 미설정·네트워크 오류 시 **통과(fail-open)** — 캡차가 흔들리면 무방비 |
가입 인증메일 | 주소별 **60초 쿨다운 · 시간당 5회** | 주소를 바꿔 가며 오는 요청은 **못 막는다** |
가입 인증메일 | 🔴**하루 총 발송 60통 넘으면 가입 메일만 차단**(`SIGNUP_EMAIL_DAILY_CAP`) | 2026-08-15 추가. Resend 일 100통 중 **40통을 재설정·주문확인 몫으로 남긴다** |
휴대폰 OTP | 60초 쿨다운 · 시간당 5회 | 솔라피 잔액을 태우는 경로 |
로그인 | 이메일별 실패 잠금(`login_attempts`) | |

**왜 40통을 남기나**: 가입이 막히는 것은 미룰 수 있지만, **기존 회원의 비밀번호 재설정**과 **결제 주문확인 메일**은 미루면 안 된다. 특히 주문확인 메일은 **전자상거래법 제13조 제2항의 계약내용 서면 교부로 법정 의무**다. 남이 만든 트래픽 때문에 우리 의무가 깨지는 구조를 끊은 것이다.
관리자 대시보드의 「이메일 발송(오늘)」 카드가 **「가입 메일 차단 중」 배지**로 상태를 알려 준다.

### ⬜ 아직 안 한 것 — Cloudflare 인프라 레벨 Rate Limiting

애플리케이션에서는 **IP를 저장하지 않는다**(방침). 그래서 IP 기준 차단은 코드가 아니라 **Cloudflare에서** 거는 것이 방침과 일관된다. ⚠️**대시보드 작업이라 CLI로는 안 된다** — wrangler OAuth 토큰에는 zone read 권한만 있다.

**설정 위치**: Cloudflare 대시보드 → `chamroad.com` → **Security → WAF → Rate limiting rules**

**걸 규칙(권장)**
1. **가입 인증메일 요청** — `URI Path equals /api/auth/send-signup-code`, 같은 IP 기준 **1분에 5회 초과 시 차단**(또는 관리형 챌린지).
2. **비밀번호 재설정 요청** — `/api/auth/find-account`도 같은 값으로.
3. **로그인** — `/api/auth/login`, 1분 20회 정도(정상 이용자가 걸리지 않을 선).

⚠️**주의**: `api.chamroad.com`은 **Worker 커스텀 도메인**이다. 규칙이 그 호스트에 실제로 적용되는지 저장 후 확인할 것(호스트명 조건을 함께 넣는 편이 안전하다).
⚠️Free 플랜은 사용할 수 있는 rate limiting 규칙 수가 제한된다 — 우선순위는 **①가입 → ②재설정 → ③로그인** 순이다.

## ⚡ 1. 솔라피 (SMS 문자 인증) — 선불 충전식

- **용도**: 결제 유료회원 휴대폰 번호 인증(발송·검증). `sendSms`.
- **과금**: 문자 1건당 대략 **단문 SMS 18원 / 장문 LMS 45원**. 챔로드 인증문자는 길이에 따라 이 사이.
- **현재 잔액**: 10,000원 (2026-08-08 충전). 발송량 늘면 소진 전 재충전.
- **다 떨어지면**: 발송 API가 실패 → 서버가 `502`("문자 발송에 실패했습니다") 반환 → 사용자가 번호 인증을 못 함 → **결제 진행 불가**.
- **확인처**: https://console.solapi.com (상단 '총 잔액')
- **운영 법인**: 주식회사 누리고(솔라피). 발신번호 `01037707164`(인증 만료 2027-02-08 — 재인증 필요).

## ⚡ 2. Anthropic API (AI 서류검토) — 선불 크레딧/자동결제

- **용도**: 유료회원 AI 서류검토(`POST /api/ai/review`). 모델 `claude-sonnet-5`.
- **과금**: **입력 $3 / 출력 $15 per 1M tokens** (2026-08-31까지 인트로 요금 입력 $2 / 출력 $10). 검토 1건은 서류 길이에 비례.
- **다 떨어지면**: API가 결제오류 반환 → AI 검토 기능만 실패(다른 기능은 정상). 크레딧 소진 시 자동충전 안 걸어두면 멈춤.
- **서버측 사용 제한(비용 방어용, 이미 코드에 있음)**: 앱 전체 **일 30회** + 패키지별 회수(예: 12회, 1일 최대 10회). 이 한도는 돈이 아니라 남용 방지용.
- **확인처**: https://console.anthropic.com → Billing (크레딧 잔액·자동충전 설정)
- **시크릿**: `ANTHROPIC_API_KEY`

## 3. Resend (이메일 발송) — 월 무료한도 + 초과 시 유료 플랜

- **용도**: 비밀번호 재설정·가입 인증·주문확인 메일(`sendEmail`). 발신 `no-reply@chamroad.com`.
- **무료한도(대략)**: 월 3,000통 / 일 100통 수준. **정확한 현재 한도는 대시보드 확인.**
- **초과하면**: 그 달 추가 발송 막힘 또는 유료 플랜 필요 → 재설정·인증 메일이 안 감.
- **확인처**: https://resend.com (Usage) — 발송량이 무료한도에 근접하면 유료 플랜 검토.
- **운영사**: Plus Five Five, Inc. / 시크릿 `RESEND_API_KEY`

## 4. Cloudflare (Workers + D1 서버·DB) — 무료한도 + 초과 시 $5/월

- **용도**: 백엔드 API(Worker `chamroad-api`) + 데이터베이스(D1). 회원·결제·데이터 전부 여기.
- **무료한도(대략)**: Workers 요청 **10만/일**, D1 읽기 **500만 row/일**·쓰기 **10만 row/일**·저장 5GB 수준. **정확한 값은 대시보드 확인.**
- **초과하면**: 요청이 막히거나 **Workers Paid($5/월)** 전환 필요. 방문자가 크게 늘기 전엔 무료로 충분.
- **확인처**: https://dash.cloudflare.com → Workers & Pages / D1 (Usage)
- **계정**: `eslje75`

## 5. 도메인 chamroad.com — 연 1회 갱신

- **만료**: 2027-08-05 (Cloudflare Registrar, `eslje75` 계정).
- **놓치면**: 도메인 실효 → 사이트·API 전부 다운. 자동갱신 켜두는 것 권장.
- **확인처**: https://dash.cloudflare.com → Domain Registration

## 6. 포트원 + PG(결제대행) — 거래 수수료 (충전 아님, 판매 오픈 후)

- **과금 방식**: 선불 충전이 아니라 **결제 건당 수수료 %가 정산금에서 차감**. 챔로드가 미리 낼 잔액은 없음.
- **주의**: 라이브 채널 전환 + 사업자등록 후에만 발생. 수수료율은 PG 계약에 따름.
- **확인처**: 포트원 관리자콘솔 / PG사 정산내역.

---

## 요약 — 운영자가 신경 쓸 우선순위

| 우선 | 서비스 | 왜 | 점검 주기 |
|---|---|---|---|
| 🔴 높음 | **솔라피 잔액** | 떨어지면 결제 인증 중단 | 판매 오픈 후 수시 / 발송량 보고 충전 |
| 🔴 높음 | **Anthropic 크레딧** | 떨어지면 AI 검토 중단 | 자동충전 설정 + 월 1회 |
| 🟡 중간 | Resend 발송량 | 무료한도 초과 시 메일 중단 | 월 1회 |
| 🟡 중간 | Cloudflare 사용량 | 트래픽 급증 시 유료 전환 | 월 1회 |
| 🟢 낮음 | 도메인 갱신 | 연 1회, 자동갱신 켜면 안심 | 연 1회(만료 전) |
| 🟢 낮음 | PG 수수료 | 정산에서 자동 차감 | 정산 때 |

# D1 백업·복구 런북

운영 데이터베이스(Cloudflare D1 `chamroad`)를 잃지 않기 위한 절차. **2026-08-15에 실제로 돌려 보고 작성했다.**

## 무엇이 걸려 있나

D1에 **16개 테이블**이 있고, 그중 잃으면 되돌릴 수 없는 것들이다.

| 테이블 | 잃으면 |
|---|---|
`users` | 회원 계정 전체(비밀번호 해시 포함) — 재가입 외에 복구 방법이 없다 |
`payments` · `payments_archive` | **결제 기록.** 법령상 보존 의무가 있는 데이터다 |
`entitlements` | 누가 무엇을 언제까지 이용할 수 있는지 — 유료 이용자의 권리 그 자체 |
`withdrawal_requests` · `withdrawal_archive` | 청약철회 신청·처리 이력(전자상거래법 대응 근거) |
`user_data` | 이용자가 저장한 진행 상황 |
`content_access` | 콘텐츠 최초 열람 시각 — **청약철회 가능 여부의 판단 근거**(약관 제6조②). 이게 없으면 분쟁에서 우리가 증명할 수 없다 |

`PEPPER`(비밀번호 페퍼)는 **DB가 아니라 Cloudflare 시크릿**에 있고 재조회가 불가능하다.
백업으로 users를 되살려도 **PEPPER가 없으면 아무도 로그인하지 못한다** → 비밀번호 관리자 백업을 계속 유지할 것(2026-08-10 완료).

---

## 🔴 덤프 파일 자체가 개인정보다

내보낸 `.sql`에는 **회원 이메일·비밀번호 해시·전화번호·민감정보 동의 기록·결제 내역**이 평문 구조로 들어 있다.

1. **저장소 안에 두지 말 것.** 커밋되는 순간 그 자체가 유출이고, GitHub은 이력에서 지우기도 어렵다.
   기본 위치는 저장소 **바깥**인 `../_chamroad-backups`다. `.gitignore`에 `*.dump`·`backups/`·`d1-*.sql` 등을 넣어 두었지만 그건 **2차 방어선**일 뿐이다.
   (`backup-d1.ps1`은 대상 경로가 저장소 안이면 **실행을 거부한다.**)
2. **무한정 쌓지 말 것.** 개인정보는 필요한 기간만 보관한다 — 스크립트가 최근 **14개**만 남기고 자동 삭제한다.
3. PC를 바꾸거나 폐기할 때 **백업 폴더를 먼저 지울 것.**

---

## 정기 백업

```powershell
powershell -ExecutionPolicy Bypass -File tools\backup-d1.ps1
```

- 기본 저장 위치 `../_chamroad-backups`, 최근 14개 유지. 바꾸려면 `-Dest`, `-Keep`.
- ⚠️ 내부적으로 **`wrangler@4.120.0`으로 버전을 고정**한다. 4.120.1은 D1 원격 작업이 `code 7403`으로 실패한다.
- 테이블이 0개로 나오면 스크립트가 오류를 내고 멈춘다(비정상 덤프를 정상으로 착각하지 않기 위해).

**언제 돌리나**
- 🔴 **D1 마이그레이션을 적용하기 직전 — 반드시.** 스키마 변경은 되돌리기가 가장 어렵다.
- 판매 오픈 직전, 그리고 오픈 후에는 주 1회.
- 회원·결제가 늘면 주기를 좁힐 것.

### `d1 export`가 401로 실패할 때

2026-08-16에 `Authentication error [code: 10000]`으로 한 번 실패했다가, 잠시 뒤 **같은 OAuth 토큰으로 정상 성공**했다. 같은 시각 `d1 execute --remote`는 계속 정상이었으므로 토큰이 죽은 것은 아니다. 원인은 확정하지 못했다.

⛔ **한 번 실패했다고 "권한이 없다"고 단정하지 말 것.** 순서는 이렇다:

1. **그냥 다시 실행해 본다.** 이것으로 해결된 사례가 있다.
2. 그래도 안 되면 **대체 경로**로 덤프를 뜬다. 스크립트가 자동으로 넘어가지만 직접 부를 수도 있다:
   ```powershell
   powershell -ExecutionPolicy Bypass -File tools\backup-d1.ps1 -ForceFallback
   ```
   살아 있는 `d1 execute --remote --command`로 전 테이블을 SELECT해 `tools/d1-dump-fallback.js`가 INSERT 문으로 되만든다. **`api/schema.sql`을 앞에 붙이므로 이 덤프 하나로도 빈 DB에 복원된다**(리허설로 확인함).
   ⚠️ 대신 schema.sql에 없는 표는 복원되지 않는다 — 그래서 **마이그레이션을 적용하면 `schema.sql`에도 반드시 반영할 것.** (실제로 `email_usage`가 2026-08-11 마이그레이션 이후 반영되지 않아 2026-08-16까지 빠져 있었다. 코드가 조회 실패를 삼켜서 화면엔 0으로만 보였다.)
3. 반복되면 **API 토큰**을 쓴다(아래).

### 백업 토큰 (export가 계속 실패할 때)

OAuth 갱신과 무관해져 더 안정적이다.

1. Cloudflare 대시보드 → **My Profile → API Tokens → Create Token → Create Custom Token**
2. 권한에 **`Account · D1 · Read`** 를 넣는다(export가 여전히 401이면 `Edit`으로 올려 본다). 계정 범위는 이 계정 하나로 제한할 것.
3. 토큰을 **저장소 바깥**에 둔다 — 백업 폴더 옆이 기본 위치다:
   ```
   ..\_chamroad-backups\.cf-token
   ```
   스크립트가 이 파일을 자동으로 읽는다. 환경변수 `CLOUDFLARE_API_TOKEN`이 있으면 그쪽이 우선이다.
4. 🔴 **토큰은 저장소에 넣지 말 것.** `.gitignore`에 `.cf-token` 규칙이 있지만 그건 2차 방어선일 뿐이다. 유출되면 대시보드에서 즉시 폐기(Roll)할 것.

## 백업이 실제로 복원되는지 확인 (리허설)

받아만 두고 복원해 본 적 없는 백업은 백업이 아니다.

```powershell
node tools\restore-drill.js "..\_chamroad-backups\chamroad-d1-YYYYMMDD-HHMM.sql"
```

운영 DB도 로컬 개발 상태도 건드리지 않고 **임시 SQLite에 실제로 복원**한 뒤 테이블·행 수를 세고 지운다.
개인정보 보호를 위해 **행의 내용은 출력하지 않는다.** 종료 코드 0이면 통과.

**2026-08-15 리허설 결과: 16개 테이블 97행 복원 성공, 핵심 테이블 4종 모두 확인.**

---

## 복구 — 두 가지 길

### ① Time Travel (실수 직후 최선)

D1이 기본 제공하는 시점 복구다. **별도 백업이 없어도 된다.**

```powershell
cd api
npx wrangler@4.120.0 d1 time-travel info chamroad                      # 현재 북마크 확인
npx wrangler@4.120.0 d1 time-travel restore chamroad --timestamp=<유닉스초>
npx wrangler@4.120.0 d1 time-travel restore chamroad --bookmark=<북마크>
```

- ⚠️ **파괴적이고 제자리(in-place) 복구다.** 별도 DB로 복원되지 않고 현재 DB를 덮어쓰며, 진행 중인 쿼리·트랜잭션은 취소된다.
- 복구 후 **이전 상태의 북마크가 표시되므로 되돌리기가 가능하다** — 복구 직후 그 북마크를 반드시 적어 둘 것.
- 🔴 **보존 기간이 플랜에 따라 다르다: Workers 무료 7일 / 유료 30일.** 현재 무료 한도로 운영 중이므로 **7일로 보고 움직이는 것이 안전하다.** 7일이 지난 사고는 Time Travel로 못 되돌린다 → 그래서 정기 덤프가 필요하다.

### ② 덤프에서 복원 (7일을 넘겼거나 DB 자체를 잃었을 때)

⚠️ **덤프에는 `DROP TABLE`이 없다.** 그래서 **테이블이 이미 있는 DB에 그대로 넣으면 실패한다.** 복원 대상은 **빈 DB**여야 한다.

1. 먼저 현재 상태를 백업한다(복원이 잘못될 수 있다).
2. 빈 DB를 준비한다 — 새 D1을 만들거나, 기존 DB의 테이블을 지운 뒤 넣는다.
3. 원격에 적용:
   ```powershell
   cd api
   npx wrangler@4.120.0 d1 execute chamroad --remote --file="<덤프.sql>"
   ```
   ⚠️ **원격은 `--file`이 인증오류(10000)를 내는 경우가 있다.** 그때는 덤프를 나눠 `--command`로 넣거나, Time Travel 쪽을 먼저 검토할 것.
4. 새 DB로 복원했다면 `api/wrangler.jsonc`의 `database_id`를 바꾸고 재배포해야 한다.
5. 복원 후 **로그인 1건·결제 내역 조회 1건**을 반드시 확인한다.

---

## 확인된 것 / 확인 못 한 것

**2026-08-15에 실제로 확인**
- `wrangler d1 export --remote` 정상 동작(16테이블·99 INSERT·15.8KB)
- 격리 환경 복원 리허설 통과(97행, 핵심 테이블 4종)
- `time-travel info`가 북마크를 정상 반환
- 덤프에 `DROP TABLE`이 없다는 점(→ 빈 DB 전제)

**확인 못 함**
- 원격 `d1 execute --file`로의 **실제 전량 복원**은 해 보지 않았다. 운영 DB를 덮어쓰는 파괴적 작업이라 리허설 대상이 아니었다.
  실제 사고 시에는 **새 D1을 만들어 거기에 복원한 뒤 `database_id`를 바꾸는 쪽**이 안전하다.
- 우리 계정의 Time Travel 보존 기간을 콘솔에서 직접 확인하지는 않았다(문서 기준 무료 7일). **플랜을 올리면 30일이 된다.**
